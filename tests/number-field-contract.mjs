/*
 * ══════════════════════════════════════════════════════════════════════
 *  NumberField：兩支 React 程式共用的數字輸入框，逐位元相同
 * ══════════════════════════════════════════════════════════════════════
 *
 * ⚠️ 這個元件在兩支程式裡必須**一字不差**：
 *     路口轉向     lib/number-field.tsx
 *     全日交通量   app/number-field.tsx
 *   （交通服務水準是原生 JS 的非受控輸入，本來就沒有這個毛病，也沒有這個檔案。）
 *
 * 為什麼要釘住：它修的是「欄位按空之後黏一個 0」那個 bug。
 * 兩支各自維護一份的結果，一定是其中一支哪天被「順手」改回
 * `value={數字}`，而那正是 bug 本身。與 direction-pair、chart-levels
 * 同一種做法。
 *
 * ⚠️ 雜湊值寫死在這裡。兩份一起改之後，這裡的期望值也要一起更新——
 *   **不可以改成從其中一份現算**，那樣就永遠對得上，等於沒有守門。
 */
export const NUMBER_FIELD_SHA256 =
  "93b203151bef9b5c5bd2701e6e7b6eb13991f833623ea516312925f39883b569";
