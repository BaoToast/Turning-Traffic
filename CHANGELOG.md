# 更新紀錄

> **完整的逐版紀錄在 `lib/traffic.ts` 的 `VERSION_HISTORY`**（畫面上「版本紀錄」看得到的那一份），
> 那是唯一來源。本檔只留重點版本的詳細說明；兩邊如果對不上，以 `VERSION_HISTORY` 為準。
>
> 之所以要這樣寫：本檔曾經漏掉 v2.1.5～v2.1.25 共 21 個版本而沒有人發現——同一份歷史有
> 兩個來源，就一定會漂移。`tests/release-structure.test.mjs` 現在會檢查本檔最新一則的版號
> 等於程式版號，避免它再次靜靜落後。

## v2.1.63（2026-09-10）

GPT 以 v2.1.62 為唯一基準完成風險導向獨立複查並修正下列問題。
**沒有變更交通量、PCU、尖峰挑選或流向判定**，`LAST_CALC_CHANGE_VERSION`
維持 `v2.1.30`。

- 單一路口歷季趨勢圖的畫面、右側摘要與可編輯 Excel 原生圖表，統一使用
  使用者所選指標的數值、標題、單位與格式。車種數量使用 `輛/hr`，車種占比
  使用 `%`，不再誤標 `PCU/hr`。
- IndexedDB 寫入改為等到交易真正完成才回報成功；瀏覽器封鎖儲存時，畫面
  明確說明無法判定是否已有舊資料，不再誤稱本機沒有資料。
- 長期間趨勢圖寬度設 4,800px 安全上限，避免 2 倍 PNG 匯出配置過大畫布；
  數值標籤依寬度抽樣，折線與每季資料點仍完整保留。
- 跨計畫選擇「全部」時，所有實際資料別各自分線，不再只假設平日與假日。
- README 的 AM／PM 搜尋範圍與現行程式一致；修正手冊 HTML 頁面標題版號。

## v2.1.62（2026-09-10）

側欄分成五個功能區、圖旁補上說明、並修掉四項 GPT 複查指出的問題
（其中「儲存競態」是**真的會掉資料**的一項）。
**沒有變更任何交通量或 PCU 計算**，`LAST_CALC_CHANGE_VERSION` 維持 `v2.1.30`。

### 一、側欄分成五個功能區

17 個項目平鋪、順序照當初做出來的先後排，不是照使用流程。改成：
**開始／一 資料匯入／二 參數設定／三 資料檢視／四 圖表與比較／五 產出與維護**，
每一區有小標與代表色。三支系統（交通服務水準、路口轉向、全日交通量）用
同一套分區名稱與順序，換一支不用重新學。

**只重排與換小標，View 的 id 一個都沒改。** 兩處順序調整：

- 多計畫管理 → 移到「圖表與比較」最後（它是跨計畫比較的前置）。
- 資料品質檢查 → 移到「資料匯入」區（匯完就檢查是同一個動作的兩半）。

### 二、圖旁邊的說明

講稿原本排在圖的下方、整頁寬，寬螢幕時右欄下方是空的卻要往下捲才看得到。
改成 **≥1400px 時接在右欄（＝圖的旁邊），窄螢幕回到圖下方**。

斷點取 1400 而不是 1080：講稿佔掉 320px 之後，1280～1366 的筆電上 8 季的圖
就開始需要橫向捲動，反而比放在下面難看。

### 三、尖峰的車輛數單位應該是「輛/hr」（GPT 指出）

`metricUnit()` 自己又寫了一套判斷，而且只寫對一半——PCU 有分
（FULL → PCU、其餘 → PCU/hr），車輛數卻不分，一律回「輛」。
三個尖峰的車輛數是**一小時內**的量，標成「輛」會被讀成一整天的量。

改為一律轉給 `lib/traffic.ts` 的 `scopeUnit()`，全系統只有那一支說了算。
新增 2 項守門逐一比對每個指標 × 每個統計範圍；退回舊寫法實測 2 項紅字。

### 四、缺季的可編輯 Excel（GPT 指出沒有守門，實測是壞的）

畫面上的 X 軸已經排滿起訖之間的每一季，但匯出的 Excel 只列「有資料的那
幾季」——Excel 裡那張折線圖照樣把缺季壓掉。**交出去的是 Excel，不是畫面。**

- 改為依 `axisQuarters` 產生，補出來的季整列留空（Excel 遇到空白儲存格會
  斷線），並加一欄「備註：這一季沒有調查資料」**放在最後**——不可以插在
  中間，折線圖是用欄位字母指定數列的。
- 缺季之後那一季的「較前季（%）」改為留空。舊版會寫 **0**，那會被讀成
  「持平」，而事實是前一季根本沒有資料可比。

紅字：退回只列有資料的季度 → ❌ 4 項（Excel 只有 2 列、缺季那列找不到、
「較前季」是 0）。

### 五、跨計畫趨勢的資料別拆線（GPT 指出沒有守門）

新增 `scripts/e2e-cross-trend.mjs`，驗「全部（分開顯示）」時線數＝計畫數 ×
日別數、線名帶日別，**而且平日那條線的值與單獨選平日時完全相同**。

⚠️ 這一段第一次是拿線尾標籤的 y 當值——那些標籤為了不互相重疊會被「排開」，
四條線與兩條線排開的幅度不同，於是量出一個**假紅字**。改讀講稿裡的數字
（與圖同源，而且那是會被念出去的那一份）。

### 六、淡灰字加深

實測（WCAG）站號 2.67:1、單位 2.92:1、副標 3.27:1，一般文字的 AA 標準是
4.5:1。加深到 5.19:1，色相不變。計畫代碼在淺綠底上 4.49:1，改用同色系深一階
（5.63:1）；品牌色 `--teal` 本身不動。

### 七、大量季度

版面守門的測資從 24 季提高到 **44 季**（GPT 把大量季度列為風險）。

### 八、連續存檔會把新資料蓋回舊資料（GPT 指出，查證屬實，會掉資料）

`saveState()` 每次呼叫都自己 `indexedDB.open()` 一次，而 open 是非同步的，
兩次寫入誰先開好**沒有保證**。連續改兩次時，第一次（舊資料）若比第二次
（新資料）晚開好，它的 `put` 就會**後**落地，資料庫裡最後留下舊資料。
重新整理之後使用者剛改的東西不見了，**沒有任何錯誤訊息**。

⚠️ `traffic-app.tsx` 的 `saveTokenRef` 擋不住這一種——那個序號只決定
「哪一次的結果可以更新畫面與 toast」，不會取消已經送出去的 IndexedDB 寫入。
這正是「看起來有防護、其實沒防到」的典型。

改法：`lib/state-storage.ts` 內把**所有寫入**（含 localStorage → IndexedDB
的搬遷寫入與「全部清除」）排成一條鏈，前一次寫完才開始下一次，順序由
呼叫順序決定，與 open 的回應時間無關。刻意**不做**「跳過被取代的那一次」：
那樣先呼叫的會拿到「成功」卻其實沒寫，而如果最後一次因配額失敗，
就變成兩次都沒進去。

紅字：把排隊拿掉還原成直接寫入 → ❌ 2 項
（`'第一次（舊）' !== '第三次（新）'`；清除與存檔交錯時最後留下的是被清掉前的值）。
新增 `tests/state-storage-race.test.ts`（2 項）。

## v2.1.61（2026-09-09）

修正單一路口歷季趨勢圖**資料點畫的位置與縱軸說的不一致**，並把縱軸刻度
改成好讀的整數。**沒有變更任何交通量或 PCU 計算**，
`LAST_CALC_CHANGE_VERSION` 維持 `v2.1.30`。

### 一、點畫的位置與縱軸對不上（使用者問出來的）

資料點的縱向對應寫的是 `310 - (值 / max) * 235`，格線卻畫在
y=70／130／190／250／310——跨距是 **240**。兩個對應關係差 5px，於是整片
資料點被往下壓：值等於軸頂的點畫在 y=75，而標著那個值的格線在 y=70。

誤差隨值線性放大，**照著縱軸讀一個高點會少讀約 2%**——在 6,000 PCU 的軸上
就是 120 PCU。圖畫得出來、線也很漂亮，沒有任何錯誤訊息，這是圖表最惡質的
一種錯。實測還原後守門顯示最大誤差 **99.9 PCU**。

點的縱向對應改用與格線相同的 240。

⚠️ **跨計畫趨勢圖沒有這個問題**：它的 `py` 與格線本來就都用
`(bottom - top)`，這一版沒有動它的幾何。

### 二、縱軸刻度是一排亂數

軸頂原本是「資料最大值 ×1.12」直接四等分，刻度變成
5,371.3／4,028.5／2,685.6／1,342.8／0——看圖的人得先在心裡換算。

新增 `niceAxisMax()`：**先決定每一格的高度，再回推軸頂**。先定軸頂再均分
的話，軸頂雖然是整數，每一格仍可能變成 1,750 這種數字（7,000 ÷ 4）。
每一格只允許 1／1.5／2／2.5／3／4／5／6／8／10 的 10 的次方倍，往上吸附，
所以軸頂一定 ≥ 資料最大值而且最多只高一階。同一個例子會變成
6,000／4,500／3,000／1,500／0。

單一路口圖與跨計畫圖都改。三支系統刻意用同一套規則。

### 三、縱軸名稱在無頭瀏覽器／替代字型下會糊成一團

縱軸名稱原本用 `writing-mode: vertical-rl`（真正的直排）。直排要靠字型裡的
**直排前進量**才知道下一個字往下多少；字型沒有那組度量時（無頭瀏覽器、
部分 Linux／Android 環境、以及匯出 PNG 時載入的替代字型），中文字的前進量
會變成 0.4px——實測「路口總量（」五個字全部疊在 1.6px 裡糊成一團，
只有 `PCU/hr` 排得開。畫面上看起來像一個汙點，而這張圖是要貼進簡報的。

改成「橫排的字整段轉 90 度」（`transform="… rotate(-90)"`），只用得到橫排
度量，每一種字型都有。姊妹專案本來就是這樣做的（交通服務水準用
`rotate(-90)`、全日交通量用 canvas `rotate`），這一支是唯一的例外。

⚠️ **原本的守門為什麼沒抓到**：版面檢查只比對**不同** `<text>` 之間有沒有
重疊，而 `getBBox()` 回的是整段的外框——一個框不會跟自己重疊，所以
「同一段文字內部糊成一團」完全驗不到。本版新增逐字檢查
（`getExtentOfChar`，相鄰兩字必須真的前進，門檻取字級的四分之一），
畫面與「模擬匯出」兩條路徑各驗一次。還原成 `vertical-rl` 之後實測
❌ 2 項紅字，訊息就是「『路口總量（PCU/hr）』的『路口』只前進 0.4px」。

### 守門與紅字證明

`scripts/e2e-chart-layout.mjs` 新增 8 項。其中一項是**從 SVG 直接量**：
取最上與最下兩條格線建立「像素 ↔ 值」的對應，再把每個點的 `cy` 反推回值，
與該點旁邊標的數字比對（標籤與點自 v2.1.58 起同源，所以標籤＝真值）。

⚠️ 迴避的假通過陷阱：只驗「最下面那條格線＝0 的點畫在那裡」會恆真——
兩種算法在 0 的地方本來就重合，誤差是從 0 往上長的。所以另外釘住
「測資要有一個夠高的點」這個前置條件。

| 還原的內容 | 結果 |
|---|---|
| 點的縱向對應退回 235 | ❌ 照縱軸反推最大誤差 **99.9**（容差 30.0） |
| 刻度退回「最大值 ×1.12 均分」 | ❌ 刻度印成 5,371.3／4,028.5／2,685.6／1,342.8／0 |
| 全部還原成修正後 | ✅ 全部通過 |

## v2.1.60（2026-09-09）

歷季趨勢圖的橫軸改為排滿「起訖之間的每一季」，整季沒有調查的季度留空並讓
折線斷開。**沒有變更任何交通量或 PCU 計算**，`LAST_CALC_CHANGE_VERSION`
維持 `v2.1.30`。

### 症狀

舊版橫軸只排「有資料的那幾季」。某個路口若只做了 113Q1 與 114Q1，圖上就
只有兩格、兩個點緊鄰——看圖的人（業主）會讀成「上一季到這一季」的變化，
實際上中間隔了整整一年。這張圖會被下載成 PNG 貼進簡報，讀錯就跟著交出去。

### 修法

新增 `completeQuarterRange()`（`lib/trend-metrics.ts`），單一計畫趨勢圖與
跨計畫趨勢圖的橫軸都改由它產生。補出來的季度在紀錄裡查不到，值一律 `null`，
折線在那裡斷開——**斷線代表「這幾季沒調查」，不是「這幾季是 0」**，與既有的
「算不出來」走同一條路徑。補出來的空季 N＝0，不會被算成「路口數最少的
一季」而蓋掉其他季度真正的小樣本警告。

保守處理，寧可不補也不要猜：期別不是 `<年>Q<1-4>`（例如自訂期別名稱）、
或民國三碼與西元四碼混用、或跨距超過 100 年時，一律原樣不補——補出來的
格子必須挑一種寫法，挑錯會讓 X 軸同時出現「113Q2」與「2024Q3」兩種格式。

### 守門與紅字證明

`tests/trend-metrics.test.ts` 新增 3 項、`scripts/e2e-chart-layout.mjs`
新增 5 項（以只有 113Q1 與 114Q1 的測資，驗 X 軸出現補出來的 113Q3、
標籤數為 5、兩點相距四格、折線斷成兩段）。把補齊還原、**重新建置**、
跑同一支腳本，實測單元測試 3 項紅字、E2E 3 項紅字；還原回修正後全部通過。

## v2.1.59（2026-09-09）

修正 v2.1.58 新增的跨計畫歷季趨勢圖把不同資料別混在一起的問題。
**沒有變更任何交通量或 PCU 計算**，`LAST_CALC_CHANGE_VERSION` 維持 `v2.1.30`。

### 症狀

v2.1.58 的跨計畫圖完全沒有過濾資料別，而同一個路口同一季通常有平日與假日
兩筆。後果有兩個，第二個更嚴重：

1. 平日與假日被平均在一起，得到一個**不對應任何一天**的數字。
   使用者的原話：「把平日和假日的車輛數加總起來，是沒有意義的圖表」——
   平均在一起同理。
2. **N（路口數）會變成筆數**：12 個路口被算成 24 筆，每路口平均整個錯掉，
   而畫面上還理直氣壯地標著「N=24」。

### 修法

- 跨計畫圖新增「資料別」選單，預設單一種；另有「全部（分開顯示）」會把
  一個計畫拆成平日一條、假日一條——**同時顯示，不是加總**。
- `buildCrossProjectTrend()` 內以路口鍵去重當最後一道防線（同一路口同一季
  取最後匯入的那一筆），就算呼叫端忘了過濾也不會把筆數當成路口數。
- 畫面上直接寫明「平日與假日不會被加總」。

### 單一計畫的趨勢圖沒有這個問題

它的資料別是單選；統計範圍選「整體」時是畫 AM／PM／全日尖峰**三條線**，
不是加總。本版沒有動它——這一點有回頭確認過，不是假設。

### 守門

`tests/trend-metrics.test.ts` 新增 2 項：「同一路口同一季有兩筆時只能算一筆
（N 是路口數，不是筆數）」與「兩個不同路口才算兩筆」。
把去重拿掉之後實測 **4 項紅字**——原有的「每路口平均」與「加權平均」兩項
也一起紅，那正說明去重壞掉時平均值會跟著錯。

## v2.1.58（2026-09-09）

歷季趨勢圖可選指標、圖旁的簡報講稿、跨計畫趨勢圖；並修掉匯出圖片的版面問題。
**沒有變更任何交通量或 PCU 計算**，`LAST_CALC_CHANGE_VERSION` 維持 `v2.1.30`。

### 一、趨勢圖新增可選指標

舊版只畫得出「路口總量（PCU）」一種。本版新增實際車輛數、單一車種車輛數、
單一車種佔比、單一支線量、單一轉向量，共六個；**預設仍是路口總量**，
不動這個選單時的行為與舊版完全相同。

新增 `lib/trend-metrics.ts`，所有指標一律走 `buildMetricSeries()`：
**圖、右側摘要、講稿、PNG、Excel 全部只讀同一份 series。**

這是刻意的架構限制。舊版曾經發生過「點畫在駛入的高度、旁邊標的卻是駛出的數字」
——點座標用 `totalOf`、標籤用 `recordTotal`，同一張圖上有兩個計算來源。
收斂成一份之後，那一類錯誤在結構上不可能再發生。

⚠️ **沒有新增任何交通量算法。** 每一個指標的值都是呼叫既有、已被測試釘住的
函式取得的：`recordTotal`（與畫面原本的駛出／駛入同一支）、`totalMovement`
（與路口明細同一支）、`movements[scope].vehicle`（匯入時寫入的值）。

算不出來一律回 `null` 畫成斷線，**絕不用 0 代替**——0 會被抄進報告，「－」不會。
除了原本的「不足 24 小時算不出全日尖峰」，新增兩種：所選支線這一季不存在
（改過名稱或路口幾何不同）、車種佔比沒有分母。

### 二、圖旁新增簡報講稿

四段固定格式：這張圖在說什麼／重點變化／怎麼看這張圖／要先講清楚的。
只讀上面那一份 series，不回頭重算——圖與講稿分岔時，被念出來的是講稿。

中文寫法依使用者要求**不用「個百分點」**，一律先講兩端再講變化量與倍數：
「從 113Q3 的 14.3%，到 114Q2 的 42.9%，整體上升 28.6%，大約是原來的 3 倍」。

第四段會主動講出算不出來的季度與原因、樣本太少、駛出駛入對不起來、
站號歷年變動、資料別還是「待設定」。

### 三、多計畫管理新增跨計畫歷季趨勢圖

一個計畫一條線。**一律比每路口平均，不比總量**——各計畫的路口數本來就不一樣，
比總量只會證明「路口比較多的計畫比較大」，那是已知的，不是資訊。
佔比類指標用**加權平均**（分子分母各自加總再相除），不是把各路口的百分比再
平均一次——後者會讓一個 10 輛的路口和一個 1000 輛的路口有同樣的份量。

線尾標出該季實際算得出來的路口數 `N=`；明細表預設收合、可搜尋。

### 四、修掉匯出圖片的版面問題

`svgToPng()` 把 SVG 序列化成 blob URL 再交給 `<img>` 畫進 canvas，
而那份 blob 是**獨立文件，讀不到 `globals.css`**。

這個系統的折線是用屬性（`stroke`／`fill`）畫的，所以線不會消失；
但**文字全靠樣式表**——字級從 9～10px 變成瀏覽器預設的 16px，而且
`text-anchor` 從 `middle`／`end` 退回 `start`，**每一段字整個往右移半個字寬**，
縱軸刻度從右對齊變成左對齊直接壓進繪圖區。兩件事加起來就是字互相重疊、被切掉。

**畫面上完全正常，只有下載下來那一張壞掉**——而那一張才是要貼進簡報交給業主的。

修法：把樣式**內嵌進每一張 SVG**（`CHART_SVG_STYLE`），畫面與匯出讀同一份。

同時修掉四項：

1. 縱軸名稱改為跟著指標走並**一律帶單位**。舊版寫死「尖峰小時交通量（PCU/hr）」，
   換成車種或佔比之後就是錯的。
2. 新增橫軸名稱「季度」。
3. 刻度不再每一格重複單位（單位在軸名稱上，五格重複本來就多餘；
   換成「輛」或「%」之後每一格還會是錯的）。
4. X 軸標籤依**實際字寬**計算間隔印出。

另修掉一個實測到的重疊：24 季時點上的數字每個都重複「PCU/hr」，標籤寬到 85px，
相鄰兩個直接相交（實測「5,371.3」與「4,795.8 PCU/hr」）。單位既然已經在軸上，
點上就不再重複，寬度降到 40px。

### 五、匯出改成「圖歸圖、字歸字」

依使用者要求：

- **PNG 只有圖**，說明文字不印在圖上——那些話是簡報者要口述的，
  印在圖上反而破壞版面。
- **可編輯 Excel 新增「圖表說明」工作表**放講稿；資料工作表的欄名與數值
  都跟著所選指標走（舊版欄名寫死 `scopeUnit()`，永遠是 PCU/hr）。

### 守門

| 測試 | 項數 | 紅字證明 |
|---|---|---|
| `tests/trend-metrics.test.ts`（新增） | 20 | 五種變異各自實測紅字（算不出來回 0／跨計畫比總量／佔比分母 0 回 0%／不講缺季／佔比不用加權） |
| `scripts/e2e-chart-layout.mjs`（新增） | 22 | 四種還原各自實測紅字（樣式不內嵌／縱軸名稱寫死／拿掉橫軸名稱／點標籤重複單位） |

`e2e-chart-layout.mjs` 刻意做了一件事：量完畫面上的文字外框之後，
**把 SVG 的內嵌樣式抽掉再量一次**，模擬匯出時讀不到樣式表的處境。
只量畫面的話，第四節那個最嚴重的問題一項都驗不到——畫面讀得到樣式表。

紅字都是把修正還原、**重新建置**、跑同一支腳本實測出來的，不是推論。

## v2.1.57（2026-09-08）

用**支線數**做算術核對；四岔以上的橫線不再安靜刪掉。
**沒有變更任何交通量或 PCU 計算**，`LAST_CALC_CHANGE_VERSION` 維持 v2.1.30。

### 起因

使用者問：「三岔的原始檔路口編號只有路口A、B、C，沒有路口D，
這對判讀有沒有幫助？還是現在判讀 X 個岔路口已經完全沒問題了？」

查核結果分兩半：

- **支線代碼本來就有在讀**。`sourceCode()` 直接抓表頭的
  `路口編號：路口X`，這是支線代碼的唯一權威來源；支線數也一直在用
  （`movementTargetIndex()` 的角度換算、轉向圖的等角配置、畫面上的「N 叉」）。
- **但先前沒有拿它做過任何核對。** 這一版補的就是核對。

### 核對的內容是純算術

一支支線只能去 **（支線數 − 1）** 個地方，調查表卻固定印同樣幾個轉向欄，
兩者的差就是「應該有幾個轉向不存在」：

| 支線數 | 轉向欄 | 去向 | 應該不存在的轉向 |
|---|---|---|---|
| 3 叉 | 3 | 2 | 每支剛好 **1** 個（全路口 3 個） |
| 4 叉 | 3 | 3 | **0** 個 |

這不依賴任何調查廠商的編號習慣。實測 37 份實檔完全符合：
三份三岔檔（岡山北路－育才路口、左楠路－世運大道、高楠路－1003 巷）
每支剛好 1 個純橫線；五份四岔檔（台1－路科一路口、中山路－國昌路－民強街、
台1－台28、左楠路－後昌路、後昌路－宏毅二路）一個純橫線都沒有。

新增 `auditArmMovements()` / `absentBeyondExpectation()` /
`blankExplainedByArithmetic()` 於 `lib/traffic.ts`，
示警與畫面共用同一份算術，不讓兩邊分岔。

### 帶來三件事

**一、數量對不上會示警。** 三岔卻只數到 2 個橫線，代表有一欄被留成空白、
或表頭欄位被讀錯。以前這種檔案安靜通過。這則只是提醒，不改任何數值。

**二、四岔以上出現橫線不再安靜刪掉（本版最重要的一項）。**

實測 `movementTargetIndex()` 的對應（等角配置，3～7 叉逐一算過）：

| 支線數 | 3 欄能解出的不重複 from→to | 兩個轉向撞到同一個去向 |
|---|---|---|
| **3 叉** | 6 組（用掉 9 欄） | **有，3 組** |
| 4 叉 | 12 組 | 沒有 |
| 5 叉 | 15 組 | 沒有 |
| 6 叉 | 18 組 | 沒有 |
| 7 叉 | 21 組 | 沒有 |

也就是說「幽靈重複列」是**三岔獨有的**。四岔以上的三個轉向本來就各自
對應一個真實去向，所以會畫橫線通常代表**禁止轉向或單行道**——
那是真實的路口管制資訊，不是版面上多印的空欄。

v2.1.56 的過濾一視同仁，而且是一個沒有任何提示的 `.filter()`，
碰到這種檔案會**安靜少掉一條流向**。本版改成列進裁決視窗。

> **預設選項仍是「移除」＝與 v2.1.56 結果完全相同。**
> 端對端實測兩版都是 11 條流向、總量都是 680／680。
> 本版改變的是這件事**看不看得見、改不改得回來、答案記不記得住**。

三岔那些照預期畫的橫線**維持安靜移除**，不會每季跳出來煩使用者。

**三、整欄空白時給出有依據的建議。** 橫線畫得比預期少 N 個、
而剛好有 N 個轉向整欄空白時，指認那幾個並預設「沒有這個轉向」，
視窗上寫出算術依據。數量對不上就不給建議——
寧可讓使用者自己決定，也不要把猜的裝成有依據。

### 另修兩處

**（一）「全部先保留（不記住）」的殘留標記。**
這顆按鈕只做了 `setPresenceQuestions([])`，**沒有清掉流向上的
`presence: "unknown"`**，那個標記會一路存進資料與備份。
而組題目的那段是掃**全部紀錄**找 `presence === "unknown"`、
不是只掃這次匯入的，所以按過一次之後，之後匯入**任何一個無關的路口**
都會把舊的未答項目再抓出來問一次——正是使用者抱怨過的
「每次匯入都跳」那類毛病。瀏覽器實測重現：殘留 3 條、
第二輪匯入無關路口時視窗又跳出來。

清掉殘留不影響「該問的還是要問」：重新匯入同一個路口時，
`recordFromPreview()` 會依調查表欄位重新判定成空白而再次發問
（第三輪實測有跳）。

**（二）裁決視窗的版面。** `.presence-modal` 只寫了 `max-height: 86vh`
卻沒指定列高，格線三列各自照內容撐開，內容一長就把 footer 擠出視窗外、
「套用並記住」點不到（本版說明變長後實測撞到，Playwright 重試 60 次後逾時）。
改為 `grid-template-rows: auto minmax(0, 1fr) auto`。

### 守門

| 測試 | 項數 | 對 v2.1.56 紅字 |
|---|---|---|
| `tests/arm-arithmetic.test.ts`（新增） | 7 | 函式不存在 |
| `scripts/e2e-three-arm.mjs`（26 → 36） | 36 | **7** |
| `scripts/e2e-presence-keep.mjs`（新增） | 13 | **2** |

紅字都是把 v2.1.56 的原始碼放回去、**重新建置**、跑同一支腳本實測出來的，
不是推論。單元測試裡刻意放了兩個反例：
「四岔真的整天量到 0 的轉向不可以被算成缺口」與
「空白數量對不上時不可以給建議」。

---

## v2.1.56（2026-09-07）

新增「這個轉向到底存不存在」的**裁決視窗**。
**沒有變更任何交通量或 PCU 計算。**

調查表用 `0` 表示「有這個轉向、只是整天量到 0」，用 `--` 表示「根本沒有這個轉向」。
v2.1.55 已能分辨這兩種，但**兩種都沒寫、整欄空白**時仍然分不出來，
當時只在匯入提醒裡寫一行。使用者指出「寫成提醒可能不會被注意到」——
而這個答案會直接改變流向清單、路口轉向圖與各項成果，所以本版改成跳視窗逐條詢問。

**觸發條件很窄**：只有「既沒有填數值、也沒有寫 `--`」的轉向才會列進來。
實測 73 份轉向版型的調查檔：有數字 4,488 欄、純橫線 224 欄、
**整欄空白只有 12 欄**（集中在 1 份檔），所以絕大多數匯入不會看到這個視窗。

**預設一律是「有這個轉向」（保留）**——直接關掉視窗等於保留。
選「沒有這個轉向」時被移除的流向整份調查都是 0，**任何加總都不受影響**。

**答案會記住**：存進新的 `movementPresence`（鍵值是路口名稱正規化鍵＋支線代號＋轉向，
不含站號與季度），跟著計畫存、跟著備份走，下一季匯入同一個路口直接套用、
**不再問第二次**。另提供「全部先保留（不記住）」。

守門：`scripts/e2e-three-arm.mjs` 增為 26 項，四份匿名對照檔一次批次匯入，
實測驗到視窗只列空白那 3 條、裁決前保留 9 條並標記 `unknown`、
選「沒有」後變 6 條且總量不變、答案存進 `movementPresence`、
**再匯入同一批檔案不會再跳視窗且直接套用**。對 v2.1.55 有 **5 項紅字**。
`tests/backup-completeness.test.mjs` 的必帶清單也加上 `movementPresence`。

## v2.1.55（2026-09-07）

v2.1.54 的欄位記帳有一個**我自己的錯**，本版修正，並補上「分不出來」時的提醒。
**沒有變更任何交通量或 PCU 計算。**

**(1) 空白格不可以算成「量到 0」。**
v2.1.54 新增的 `numericCells` 記帳把**空白格也算成數字**——把空字串丟給
`Number()` 會得到 0，而 0 是有限數，所以 `usable` 對空白格也是 true。
後果是「整欄空白」被記成「整欄量到 0」，「調查表沒填」與「調查表填了 0」
分不出來。這個錯讓第一次的量測完全失真：掃過 201 份檔說「一欄空白都沒有」，
其實不是沒有，是全被算成數字了。

修正後重新量（73 份轉向版型的預覽）：有數字 4,488 欄、純橫線 224 欄、
**整欄空白 12 欄**（集中在 1 份檔的一個整塊空白區塊）。
對真實檔的判斷結果**完全沒有改變**——八筆真實紀錄複驗：三岔 6 條、四岔 12 條、
無重複起訖、路線合計＝支線合計——因為真實檔沒有「同一欄既有橫線又有空白」
的情形；但記帳本身現在才是誠實的。

**(2)「整欄空白」會在匯入提醒裡講出來。**
調查表用 `0` 表示「這個轉向存在但整天量到 0」、用 `--` 表示「根本沒有這個轉向」。
如果一欄**既沒有填數值、也沒有寫 `--`**，那就是沒填，系統無從判斷。
這時候**一律保留**（當成「有這個轉向、量到 0」）——保留最多是畫面上多一列 0，
刪掉卻可能弄丟真實流向。但保留是預設值不是判斷，所以會在匯入辨識結果的提醒裡
寫出是哪幾支支線的哪個轉向，並告訴使用者若確定沒有該轉向可到
「道路與流向管理」刪除。

守門：`scripts/e2e-three-arm.mjs` 增為 17 項，新增第四份對照檔「三岔留空白」。
另修 `tests/silent-failure-guards.test.ts` 一處字串比對——它用 `/不是數字/`
找警告，新提醒原本也含這四個字而被誤抓（測試紅、程式對），
已改成比對完整片語，順便把那個比對變嚴謹。

## v2.1.54（2026-09-07）

兩項修正，**沒有變更任何交通量或 PCU 計算**。

**(1) 三岔路口的空流向改用「調查表原本寫什麼」判斷。**
v2.1.53 是間接推論（整份為 0 ＋ 起訖與一條有流量的重複），實測會誤刪：
三岔路口若有一個**真的存在、但整天量到 0** 的轉向，而它剛好與一條有流量的
轉向解到同一支目的支線，v2.1.53 會把它一起刪掉（實測 7 條變 6 條；
總量不受影響，但畫面上少一列真實流向）。真實檔裡這種形狀確實存在。

本版改為：`inspectWorkbook` 逐欄記下 `numericCells` / `placeholderCells`
（這一欄出現過幾格數字、幾格橫線佔位符）。**數值本身完全不動**——
`--` 一樣算 0——這些計數只用來讓下游分得出「沒有這個轉向」與「整天量到 0」。
只有「一格數字都沒有、而且出現過橫線」的欄位所對應的流向才會被拿掉。

實測：三份真實三岔檔各有剛好 12 欄純橫線、0 欄誤判；四岔與七岔檔 0 欄純橫線，
卻各有 6～9 欄是真的整天量到 0，全部保留。八筆真實紀錄複驗：
三岔 6 條、四岔 12 條、無重複起訖、路線合計＝支線合計。

**(2) 移除「季度批次匯入」頁的「調查檔格式範本」面板（三張範本卡片與其下的
「已記住的實際調查版型」折疊區）。**沒有變更任何交通量或 PCU 計算**，
也沒有變更任何解析行為。

使用者的原話：「程式本身有記憶調查版型的格式就可以了，使用者不需要知道程式
記憶了哪些」「不用特地為了解釋『全日路段車種表會匯入成功，但不建立路口轉向
成果』而用三張卡片佔版面」。

那個面板本來就有誤導：`formatMemories` 是**只寫不讀**的，沒有任何一處拿它去
影響解析——每次匯入都是重新讀表頭當場判斷版型（`templateId` 由工作表內容推定，
見 `inspectWorkbook`），所以「刪除格式記憶」那顆按鈕會讓人以為按了能改變
辨識結果，實際上不會。

**`formatMemories` 的資料本身刻意保留**（照樣累積、照樣進備份），
備份格式完全不變，舊備份還原不受影響；只是不再顯示。
相關 CSS 一併移除。三張卡片裡唯一有用的那句「沒有左直右的檔案會讀但不建立
路口轉向成果」改寫進手冊第 6 章。

守門：`tests/v170-features.test.mjs` 新增一項（對 v2.1.53 會紅）。

## v2.1.53（2026-09-07）

三項修正，**沒有變更任何交通量或 PCU 計算**，`LAST_CALC_CHANGE_VERSION` 維持 v2.1.30，
固定計算黃金值不變。

**(1) 三岔路口不再列出重複而且一輛都沒有的流向。**
三岔路口的調查表每一支支線一定有一欄整欄寫 `--`（A 沒有右轉、B 沒有直進、C 沒有左轉），
欄位卻照樣排出來；而三岔只有兩個去向，三個轉向硬要對到兩支目的支線，
`movementTargetIndex` 必然讓兩個轉向解到同一支。結果 9 條流向裡有 3 條
「from→to 與別人重複、整份調查一輛都沒有」的空列，畫面上看起來像同一個路口被列了兩次。
以三份真實三岔檔實測：一律 9 條、3 條四個時段全零、3 組重複，
而且每一組重複裡剛好一條全零、一條有流量；四岔一律 12 條、0 重複。
本版在建立流向後把「整份調查為零、且同一個 from→to 另有一條真的有流量」的流向濾掉，
三岔 9→6 條、四岔完全不變，**任何加總都不受影響**（實測路線合計＝支線合計，差 0）。
註：解析階段已經把 `--` 直接當成 0，程式目前分不出「沒有這個轉向」與「整天量到 0」，
所以刻意不改解析層，而是加上「另有一條同 from→to 有流量」這個條件——
四岔路口真的整天量到 0 的轉向因此不會被誤刪。

**(2) 本機資料從 localStorage 搬到 IndexedDB。**
`localStorage` 每個網站硬上限實測 4.94 MB，而路口轉向是三支裡唯一把全部資料都放在那裡的；
單一路口快照平均 14.2 KB、最大 38.7 KB，一季就可能用完。
新增 `lib/state-storage.ts`，第一次啟動時把舊資料搬進 IndexedDB，
**寫入後一定重新讀回來逐字比對相同才刪掉舊的**，比對不過就原封不動留著下次再搬。
依使用者定案**不做雙寫**。存檔改成非同步，因此加上「後發先至」的序號保護
與寫入未完成時的離開確認。儲存空間被封鎖時的搶救畫面維持 v2.1.52 的行為。

**(3) 還原點改成一次操作一個。**
舊版每一筆紀錄各建一個還原點，一次批次覆蓋 65 個路口就長出 65 個項目，
而且淘汰用逐筆 `slice(0, 300)`，會把同一次操作攔腰切斷。
本版同一次操作共用一個 `batchId`，上限改為最近 30 次操作加 20 MB 總量，整批進出；
畫面加上每筆的「刪除這個還原點」與「刪除還原點不會影響現有資料」的說明，
**不做獨立的「清除還原點」按鈕**。舊備份沒有 `batchId`，會被當成一筆一批，
照樣顯示、照樣還原得回去。

**(4) 結論草稿的「小數位數」現在真的管得到百分比。**
使用者在全日交通量實測回報，三支系統寫法相同、毛病也相同：`pct()` 的位數參數
有預設值 1，而所有呼叫端都沒有傳，於是把小數位數改成 2 位時百分比完全不動。
本支受影響四處（佔駛入、佔駛出、支線車種組成、路口車種組成），
另加跨季度變動幅度的增減百分比（原本寫死 `toFixed(1)`）。
本版把設定帶進全部五處，並拿掉 `pct()` 的預設值——日後漏傳是編譯錯誤，
不會再無聲失效。車輛數維持整數。**沒有變更任何計算**。

新增守門：`scripts/e2e-three-arm.mjs`（11 項，對未修正的 v2.1.52 有 **3 項紅字**）、
`scripts/e2e-storage-idb.mjs`（18 項，**7 項紅字**）、
`scripts/e2e-revision-batch.mjs`（15 項，**9 項紅字**）、
`tests/conclusion-digits.test.ts`（2 項，**2 項紅字**）。

## v2.1.52（2026-09-06）

瀏覽器不允許本站使用本機儲存空間時，**畫面整頁空白**——本版修正，
**沒有變更任何交通量或 PCU 計算**，`LAST_CALC_CHANGE_VERSION` 維持 v2.1.30。

- **症狀**：瀏覽器設定成「封鎖所有 Cookie／網站資料」時，連讀取
  `window.localStorage` 這個屬性本身都會丟 `SecurityError`。而讀取那兩行原本
  寫在 `try` 之外，例外直接冒到 React、整棵元件樹卸載。實測（把 localStorage
  改成存取即拋錯）：**畫面 0 個字、連導覽列都沒有**，主控台一則未捕捉例外，
  沒有任何訊息告訴使用者發生什麼事。使用者只會覺得「這個網站壞了」。
- **三支系統同情境對照**（都實測過）：全日交通量出 toast「IndexedDB 已被停用」
  且不假裝存檔成功；交通服務水準顯示搶救畫面；只有本支整頁空白。
- **修正**：讀取包進 `try`，並把「儲存空間用不了」與「資料格式壞掉」分成兩種
  畫面。原本那句「您的原始資料仍然完整保留在瀏覽器裡」在封鎖情境下並不成立
  （根本沒有資料），改為明講原因、列出三種常見設定（封鎖 Cookie／無痕模式／
  擴充套件）與處理方式、給一顆「調整設定後，重新載入」，並提醒此時不要匯入
  （畫面看起來會成功，關掉分頁就全部消失）。搶救畫面的下載鈕也補上 `try`。
- **新增守門**：`scripts/e2e-storage-blocked.mjs`（9 項），實測對未修正的
  v2.1.51 有 **5 項紅字**；同時驗「儲存空間正常時不可以誤跳這個畫面」，
  避免寫成無條件顯示也會通過。

## v2.1.51（2026-09-05）

三支系統全面徹查後的四項修正，**沒有變更任何交通量或 PCU 計算**，
`LAST_CALC_CHANGE_VERSION` 維持 v2.1.30，37 份真實工作簿的解析結果與已發布的
v2.1.45 逐位元相同。

- **選「全日時段」時三處單位寫死 PCU/hr**：批次成果包 README、轉向進階核對
  Excel 的「支線流量平衡」表、「連續 60 分鐘候選排行」面板。FULL 的值是一整天
  的累計（PCU/調查日），語意差約 24 倍；README 還把「全日時段」印成「FULL 尖峰」。
  前兩處改成跟著 `scopeUnit(peak)`、時段名稱改用 `SCOPE_LABELS`；候選排行那張表
  其實不隨 peak 變動（`peakSensitivity` 永遠是 60 分鐘視窗），改成固定標
  「PCU/hr（固定 60 分鐘視窗）」。
- **混用時間格會靜靜算錯尖峰——本版只加警告，不改計算**：格距取全表眾數，
  而尖峰視窗是數格數不是累計分鐘數；「全日整點＋尖峰拆 15 分鐘」的版型會讓
  尖峰那一小時只取到其中一格，低估 75%，且總量守恆、以總量為基礎的檢查抓不到。
  改挑選邏輯會變更計算口徑並需推進 `LAST_CALC_CHANGE_VERSION`，屬使用者拍板事項。
  判準調校過：只看「格距不一致」實測誤報 19／55 筆（休息時段跳號、旅行時間格式），
  改成「兩種以上、各佔兩成以上，且各自至少重複兩次的規律格距」；短時段只漏
  一列時不會把單次跳號誤稱為混合時間格，合成的真混用檔仍會報。
- **補上 `paint()` 的守門測試**：v2.1.50 宣稱三項修正都有測試釘住，但實測把
  `await paint()` 改回 `await breathe()` 整支測試仍全綠——只有 250ms 退路那一半
  有守到。改為計數匯入期間的 `requestAnimationFrame` 呼叫（paint 的兩層 rAF 是
  唯一可觀測的痕跡），拿掉修正實測紅字（0 次 < 2 次）。
- **移除三項恆真的假檢查**：`e2e-reimport.mjs` 的「解析中連點按鈕不會觸發」依
  HTML 規範本來就恆為 0（實測：無關的空白按鈕停用時 0 次、啟用時 5 次）。改成在
  按鈕確實停用的期間送出真正的滑鼠點擊，再看檔案對話框有沒有被叫出來；拿掉
  `disabled` 實測三項全紅。
- **發布前獨立複查再修正兩項顯示／警告問題**：一是短時段只漏一列時，單次
  30 分鐘跳號可能剛好佔 25%，原判準會誤報混合時間格；現要求候選格距至少出現
  兩次。二是進階分析頁的「守恆差值」仍寫死 PCU/hr，改為跟著 `scopeUnit(peak)`。
  兩項都新增守門測試；前者只校正警告、後者只校正單位文字，尖峰挑選與流量數值
  均未改動。

## v2.1.50（2026-09-05）

補上「按下選擇檔案之後、檔案還在讀」這段空窗期的提示，並確保第一份檔案開始解析前
畫面一定先重畫一次，**沒有變更任何交通量或 PCU 計算**，
`LAST_CALC_CHANGE_VERSION` 維持 v2.1.30，固定計算黃金值完全相同。

- **按下「選擇檔案」之後就看得到提示**：使用者回報「點了選擇檔案、選了大量資料，
  系統對話框沒有馬上關掉，但資料其實有在匯入」。對話框關閉到程式收到 `change`
  之間完全是瀏覽器在讀檔，程式插不進去，所以提示只能從按下去那一刻開始顯示。
  現在按下按鈕當下就出現「正在讀取您選擇的檔案，請稍候…」，選好檔（`change`）
  會收掉；按取消沒有 `change` 事件，改以視窗重新取得焦點後 1.2 秒當退路收掉。
- **第一份檔案的進度列不會來不及顯示**：原本 `setImportProgress` 之後直接進入
  解析迴圈，解析把主執行緒佔滿，React 還沒重畫就卡住，使用者要等全部讀完才看到
  進度。改成先等一次真正的重畫（連續兩個 `requestAnimationFrame`）再開始解析。
- **背景分頁不會永遠卡住**：分頁在背景時 `requestAnimationFrame` 不會觸發，
  所以那一次等待附 250ms 時間退路。守門測試會停掉 `requestAnimationFrame`
  再跑一次匯入，確認仍然會完成。
- 三項修正各自都以「拿掉修正」的版本實測過守門測試會紅字（提示不出現／取消後
  提示收不掉／背景分頁 20 秒沒完成）。
- **新增 `scripts/e2e-reimport.mjs`**：使用者擔心「以為沒匯入成功而重複匯入、
  系統卻沒擋」。實測同一批檔案匯兩次，紀錄數維持 3 筆不翻倍，畫面在寫入前
  就標示「已存在第 1 版／採覆蓋」；解析中連點按鈕 7 次、觸發 0 次；判讀中
  重新整理頁面不會留下半筆資料。拿掉去重（`exact` 固定 -1）實測紅字 3→6 筆。

## v2.1.49（2026-09-05）

修正全域拖放防呆會連一般文字拖曳都擋掉的問題，**沒有變更任何交通量或 PCU 計算**，
`LAST_CALC_CHANGE_VERSION` 維持 v2.1.30，固定計算黃金值完全相同。

- **拖一般文字不會再被擋掉**：v2.1.47 加入的「檔案掉在放置區外面不要把
  使用者帶離頁面」少了一層判斷——沒有先確認拖進來的是不是檔案，
  於是使用者在頁面內拖動選取文字（例如把一段字拖到輸入框）也一起被擋住。
  實測輸入框、側邊選單與內容區三處都中。改成只有 `dataTransfer.types`
  含 `Files` 時才攔截。全日交通量 v20.47 本來就是這樣寫的，本版與它對齊。
- **守門測試的取樣方式改掉**：`scripts/e2e-progress.mjs` 原本每 10ms 輪詢一次
  畫面。短命的畫面狀態用輪詢會漏掉，改成 MutationObserver 全量記錄
  （輪詢保留做保險）。實測：把 v2.1.47 的序號錯位寫法改回去、並讓那個
  狀態刻意停留 120ms，守門測試會確實紅字。
- 新增「拖一般文字不會被擋掉」的守門測試，對「拿掉修正」的版本實測會紅字。

## v2.1.48（2026-09-05）

獨立複查 v2.1.47 後修正大量檔案判讀的進度顯示，**沒有變更任何交通量或 PCU 計算**，
`LAST_CALC_CHANGE_VERSION` 維持 v2.1.30，固定計算黃金值完全相同。

- 每份檔案剛讀完、切換下一份的短暫畫面，原本可能把下一個序號配到上一個檔名；
  最後一份也可能短暫顯示超過總份數。現在序號、總份數與檔名始終一致。
- 後續預覽整理即使發生非預期例外，也一定解除「正在解析」與按鈕停用狀態，
  不會讓匯入功能卡死到必須重新整理頁面。
- `e2e-progress.mjs` 新增序號、檔名與總數一致性的守門檢查。

## v2.1.47（2026-09-04）

上傳與判讀的操作回饋，**沒有變更任何交通量或 PCU 計算**，
`LAST_CALC_CHANGE_VERSION` 維持 v2.1.30，固定計算黃金值完全相同。

- **拖曳上傳的視覺回饋**：上傳卡片本來就接得到 drop，但畫面上完全沒有變化，
  使用者拖到一半看不出來「丟這裡對不對」，只能賭一把放開。
  現在拖曳經過時整張卡片會亮起來，放開就收掉。
- **備份還原也吃拖曳**：那一張卡原本只能按按鈕選檔，現在可以直接把
  備份檔（ZIP 或 JSON）拖進去。一次拖多個會取第一個並講清楚。
- **檔案掉在放置區外面不會把使用者帶離頁面**：以前沒有全域攔截，
  檔案掉在旁邊時瀏覽器會直接開啟那個檔案，等於離開系統畫面。
- **判讀進度**：使用者回報「上傳大量檔案後以為沒成功」，原因是判讀中
  只有按鈕文字換成「正在解析…」，一動也不動，檔案一多就分不出
  還在跑還是當掉了。現在按鈕顯示「正在解析… 3／12」，上傳卡片上
  另有一行寫出正在讀哪一個檔名，並且每讀完一份就讓瀏覽器有機會重畫。
- 新增 `scripts/e2e-drop.mjs` 與 `scripts/e2e-progress.mjs`，
  兩支都對「拿掉功能」的版本實測會紅字。

## v2.1.46 — 2026-09-04（結論草稿只留一顆「產生草稿」＋保守的結果捲動，不動計算）

- **結論草稿產生器移除頁首那一顆「產生草稿」。** 舊版頁首與草稿框旁邊各一顆，
  呼叫的是同一個函式。使用者指出實際動線用不到頁首那一顆：條件與條件範本都在下方，
  「哪怕條件沒變，為了確保資料正確，正常情況下仍會往下滑動確認條件」，
  所以每一條動線最後都停在草稿框旁邊。兩顆同名按鈕反而讓人以為有差別，
  也可能讓新手在還沒勾任何條件時就按下去，拿到一份用預設條件產生的草稿。
- **草稿框旁邊那一顆由「重新產生」改名為「產生草稿」。** 行為完全不變：
  已經手改過的草稿仍會先詢問再覆蓋。提示文字與 placeholder 一併對齊。
- **保守的結果捲動。** 按下之後草稿框如果整個看得見就**完全不動**；
  只有被推到視窗外（視窗特別矮、或草稿變長）才捲到剛好看得見，
  並遵守系統的「減少動態效果」設定。新增 `scripts/e2e-reveal.mjs`，
  同時釘住「看不到時要帶過去」與「看得到時不可以捲」兩半，
  對未修正的版本實測紅字。
- **修正三處已過期的程式註解**（`lib/traffic.ts` 兩處、`tests/traffic.test.ts` 一處）：
  SheetJS 0.20.3 自 v2.1.45 起已改為包內 `vendor/` 相依並以 SHA-256 鎖定，
  註解卻仍寫「官方 CDN」。只是註解，不影響行為。
- **沒有變更任何交通量或 PCU 計算**，`LAST_CALC_CHANGE_VERSION` 維持 v2.1.30，
  固定計算黃金值完全相同。

## v2.1.45 — 2026-09-03（年度輸入判定改看年份數值、與另外兩支對齊、精簡手冊，不動計算）

- **新手手冊大幅精簡。** 手冊裡累積了 37 則歷次版本的改善紀錄，占整本超過一半，
  新手要看的教學被埋在裡面；而同一份歷史在 `VERSION_HISTORY`（唯一來源）、
  本檔與「【更新說明】請先讀我.txt」裡本來就有。依使用者指示，手冊只寫「目前的功能是
  什麼、怎麼用」，版本紀錄**全部移除**並指向本檔；混在教學段落裡的維護敘事
  （「舊版要輸入左右 X／上下 Y，v2.1.0 改成拖曳」這類）也改寫成現況描述。
  **教學內容一個字都沒有刪。** PDF 54 頁 → 30 頁。
  新增 `tests/manual-version-log.test.mjs` 守門，防止它再長回去。
- v2.1.44 已產生但從未發布，內容全部併入本版。

- **本版未變更任何交通量或 PCU 計算**，`LAST_CALC_CHANGE_VERSION` 維持 `v2.1.30`。
  37 份真實調查表的解析結果與 v2.1.43 逐字相同。
- **年度輸入的判定改看年份數值，不看位數。** v2.1.43 依位數分派（四碼一律當西元），
  於是把民國 115 打成 `0115` 會被判成「西元 115 年、超出範圍」，回一句「民國年請填
  90～200」——但 0115 就是 115，訊息與事實矛盾；而共用的 `normalizeSurveyPeriod()`
  一直都把 `0115Q1` 算成 `115Q1`，等於同一個模組裡檢查與正規化有兩種看法。
  現在以年份數值判定（民國 90～200 與西元 2001～2111 兩段不重疊），並補上 NFKC
  與去空白，`１１５Ｑ１`、`115 Q1`、`2026 Q1` 都認得。**可接受的年份範圍完全沒有改。**
- **守門測試從「比對原始碼字樣」改成「比對行為契約」。** v2.1.43 的
  `共用期別模組必須包含本輪新增的檢查` 斷言原始碼含有 `rocYear >= 90 && rocYear <= 200`
  ——那是本系統這一種實作的長相，不是行為。全日交通量的寫法不含這串，所以想把
  三支同步回同一份程式，反而會踩紅這支測試：守門測試從「防止分歧」變成「鎖住分歧」。
  現在三支各放一份逐位元相同的 `tests/period-input-contract.mjs`，SHA-256 互相釘住，
  每一支都跑完整份 54 筆契約，且不跨包引用檔案。

## v2.1.43 — 2026-09-03（年度輸入補上範圍檢查，不動計算）

- **本版未變更任何交通量或 PCU 計算**，`LAST_CALC_CHANGE_VERSION` 維持 `v2.1.30`。
- **年度輸入補上範圍檢查。** v2.1.42 開放民國與西元兩種寫法之後，年度欄位只做
  「去掉非數字、截到四碼」，沒有任何範圍檢查；而 `normalizeSurveyPeriod()` 只在
  民國 90～200（西元 2001～2111）這個窗口內換算，窗口外的四碼年份會**原樣回傳**。
  於是打成 `2112` 或 `1990` 會直接把西元字串存成季度鍵——它和對應的民國寫法
  （`201Q3`、`79Q2`）是同一季，`quarterKey()` 的排序鍵也完全相同、會相鄰出現，
  畫面上只看得出「同一季出現了兩次」。這正是 v2.1.42 要消滅的那一類問題，
  只是發生在換算窗口之外。
- **另一個方向也修掉：** 民國兩碼年份（`99Q4` ＝ 西元 2010）是合法寫法，
  排序、正規化與 Excel 排序一直都認得它，不該被輸入檢查擋掉。
- 檢查集中在共用期別模組的 `checkSurveyPeriodInput()`，匯入選檔與實際寫入都使用
  同一規則；即使完成預覽後又把年度改成不合法值，也不能寫入。
- 新增 3 項守門測試，包含民國 90～200／西元 2001～2111 的上下界，並確保
  來源包不依賴專案外的其他系統檔案即可獨立執行測試。

## v2.1.42 — 2026-08-31（季度可填民國或西元年，不動計算）

- **本版未變更任何交通量或 PCU 計算**，`LAST_CALC_CHANGE_VERSION` 維持 `v2.1.30`。
- **調查年度同時接受民國與西元。** 舊版是 `max="999"` 加上 `slice(0, 3)`，
  打「2026」會被**靜靜截成「202」**而且沒有任何提示；拿到以西元年標示的
  委託案時只能自己換算。全日交通量的季度輸入框一直是兩種都收的。
- **但寫入的季度一律正規化成民國年**（`normalizeSurveyPeriod()`，三支共用的
  `period-date` 模組）。照打的字原樣存的話，同一季會因為寫法不同而變成兩個
  不同的鍵——季度清單與歷季比較都是以這個字串分組的，115Q1 與 2026Q1 會
  並列成兩季且永遠不會合併，而兩者的排序鍵完全相同、會相鄰出現，
  看起來只像「同一季出現兩次」。
- 打西元時輸入框下方會即時顯示「將存成 115Q2（資料一律以民國年記錄）」。

## v2.1.41 — 2026-08-31（三支系統跨系統徹查後的七項修正，不動計算）

- **本版未變更任何交通量或 PCU 計算**，`LAST_CALC_CHANGE_VERSION` 維持 `v2.1.30`；
  固定計算黃金值完全相同，37 份真實檔的總車輛數與修正前逐位相同。
- 本版是「路口轉向、全日交通量、交通服務水準」三支一次跨系統徹查的結果：拿 37 份真實
  調查表分別餵給不同系統、比對彼此的判讀與數字，凡是不一致的就往下追。
- **(H1) 工作表名稱前面多一個空白時，平日與假日被合併成一筆。** 判斷日別資料頁的規則
  以前是 `/^(平日|假日)\s*$/`——只容許尾端空白。37 份真實檔裡有 11 份的分頁名稱帶空白，
  前導空白會讓 `daySheets.length < 2`，於是退回單一 `inspectWorkbook()`，而它把所有
  資料頁依時間疊加。實測 `11535T1502` 的假日量由 86,207 輛變成 194,235 輛（虛增 125%），
  總量守恆所以任何以總量為基礎的檢查都抓不到，畫面只顯示匯入成功。抽出
  `isDayTypeSheetName()`，與全日交通量的 `trafficSheetNamesForDay()` 同一套規則。
- **(H4) 匯出的轉向圖檔名不帶資料別。** 平日與假日是兩筆各自獨立的紀錄，但 ZIP 的檔名
  只有站號＋時段，JSZip 同名直接覆寫——同一站只留下一張圖，且看不出是哪一天。
  批次成果包、單張 SVG 與單張 PNG 三處都補上 `surveyType`。
- **(M9) 表頭寫非內建車種時不再依欄位位置強制歸類。** `usePositionalVehicles` 在
  「恰有 4 種表頭車種」時就依位置映射到內建四車種，於是 `11535T1503` 後兩個支線區塊的
  「大貨車／大客車」被併進「大型車／特種車」，大客車因此拿到特種車的當量 2.5，
  但大客車在工程上通常算大型車。改為表頭有非內建車種時以表頭為準，保留成自訂車種由
  使用者自行歸類（與全日交通量一致）；表頭寫的都是內建四車種、只是順序亂掉或有殘留舊值時
  （如 `11017T1501` 七叉路口 `T5:Y5` 合併範圍裡的舊「大型車」）仍用位置推定救援。
- **(M6) 全形數字「１２３」舊版計 0 輛**，全日交通量讀成 123；同一格在兩支得到不同的數字。
- **(M7)「--」「－」「—」「–」不再被誤報成壞資料**——這是「該轉向不存在」的標準記法，
  全日交通量的 `isUnusableCount()` 早就視為合法。實測 `11017T1502` 因此跳出
  「有 192 個儲存格有內容但不是數字」，192 次誤報會讓使用者學會忽略這個警告。
- **(M5) 算不出來的統計範圍寫「－」不寫 0。** 不足 24 小時的調查沒有全日尖峰小時，
  舊版的車種組成分析表仍寫 0 輛/hr 與 0.0%，會被 Excel 的加總與平均吃進去；
  同一支函式裡的另一張表早就寫「－」了。
- **(M1) 三支共用的非調查日期清單同步**：新增彙整、輸出、建檔、產製四種。
- 新增 8 項守門測試，並以 v2.1.40 的原始碼驗證其中 7 項會失敗
  （唯一通過的是刻意設計的反面對照，確保位置推定救援沒有被一併拿掉）。

## v2.1.40 — 2026-08-31（「製表日期」不再被當成調查日期，不動計算）

- **本版未變更任何交通量或 PCU 計算**，`LAST_CALC_CHANGE_VERSION` 維持 `v2.1.30`；固定計算黃金值完全相同。
- **實際紀錄日期、資料別與期別檢查改用同一支 `findSurveyDate()`。** 舊版紀錄取第一個像日期的格子，期別檢查卻優先取明確標示的調查日期，同一份檔案可能得到兩個日期。現在一律優先採用明確標示且有效的調查日期。
- **排除更多明確非調查日期。** 除製表、列印、報告日期外，亦排除彙整、輸出、建檔與產製日期；規則只留在共用 `period-date` 模組，不在匯入器另寫一份。

## v2.1.39 — 2026-08-31（發布前複查修正，不動計算）

- **撤回候選包的 SheetJS 降版**：恢復既有官方 CDN 0.20.3，不降回 npm registry 的 0.18.5；新增發布結構守門測試，防止相同退步再發生。
- **修正檔案角色與原因矛盾**：只有全日路段分向車種、沒有轉向或 OD 的純代號檔名，最終角色與原因都會明確顯示「非路口轉向」，不再錯誤要求使用者改檔名。
- **修正相同提示互相提早關閉**：通知改用獨立序號管理，前一則提示的計時器不會誤關掉內容相同的後一則；手動關閉也會讓舊計時器失效。
- 本版沒有變更尖峰選擇、交通量、PCU 當量、轉向分類或路口總量，`LAST_CALC_CHANGE_VERSION` 維持 `v2.1.30`。

## v2.1.38 — 2026-08-30（檔名規則不再是隱形的，不動計算）

- **本版未變更任何交通量或 PCU 計算**，`LAST_CALC_CHANGE_VERSION` 維持 `v2.1.30`。五份真實調查檔的逐車種車輛數與 AM／PM 尖峰 PCU 與 v2.1.37 逐位相同，並已與全日交通量系統交叉驗算。
- **讀不到站號時不再捏造一個。** `stationFromFilename()` 舊版在切不出站號時回傳 `S-<雜湊值>`——那是整支程式唯一會「安靜出錯」的地方：不報錯，只給一個看起來像真站號的值，然後一路進到報表、匯出檔名與歷季比較，使用者永遠不會知道系統其實沒讀到站號。現在回傳空字串，預覽標示「站號未判定」並提供欄位直接填寫；沒有站號的紀錄不予寫入。
- **站號來源公開。** `ImportPreview` 新增 `stationSource`（`workbook`／`filename`／`none`）。站號本來就優先讀檔案內的「站號：」欄位、讀不到才退回檔名，但畫面上看不出是哪一種；現在逐列標示，由檔名推定時明確請使用者核對。
- **匯入失敗要說得出原因。** 舊版一律只丟「沒有可寫入的原始交通量檔。」，而使用者最常撞到的是檔名被判成參考計算檔——畫面上只有一個藍色標籤，訊息又不提檔名，於是完全無從得知「改個檔名就好了」。現在逐檔說明「哪個檔、為什麼、怎麼辦」，並區分檔名造成的與內容造成的（處理方式完全不同）。`ImportPreview` 新增 `roleReason`。
- **部分檔案未寫入時要指名道姓。** 舊版成功訊息只算使用者自己選擇略過的數量，因角色或版面被濾掉的檔案不會出現在任何地方；五個檔進來、四個寫入時只說「已寫入 4 個路口」。
- **「參考計算檔」標籤由藍色改為警示色**並附上原因說明——藍色在本介面代表中性資訊，使用者不會意識到那代表「這個檔不會被寫入」。
- **匯入頁新增「檔名會決定什麼？」說明區塊**，上傳前就看得到，含正例反例與建議命名。
- **移除轉向圖摘要卡寫死的「本系統只彙整尖峰轉向流量。」**——同一頁的時段選單就提供全日尖峰小時與全日時段，且上方已有一句依資料涵蓋時數變動的正確說明，兩者並存自相矛盾；系統目前也不只彙整轉向流量。
- **手冊建置腳本改為從 `manual.html` 封面戳記讀版號與日期**，不再寫死，讓版號漂移在結構上不可能發生（而不是只靠測試事後抓）。
- 新增 7 項守門測試涵蓋角色判定、原因訊息、站號來源，以及「不得再出現 `S-` 開頭的假站號」。

### 發布前再稽核，另修三處同類的「安靜失敗」

- **資料別不再把括號裡的任何內容都當真。** 「日期：115年06月03日（晴）」原本會得到資料別「晴」。而「晴」不是「待設定」，`isSameSurvey` 不讓重新匯入接手它——同一個檔案再匯一次只會多出一筆、趨勢線被拆成兩條，而且完全繞過系統為「待設定」準備的整套補救機制（專屬 UI、批次補完）。現在只接受平日與假日。
- **推定的支線代碼要說出來。** 表頭讀不到「路口編號：」時代碼會被推定成 `A1`、`A2`…，與真實代碼在畫面與匯出上分不出來。它是跨季幾何繼承、轉向繼承與參考轉向表比對的鍵：同一路口若一季讀得到、一季讀不到，兩季的支線會靜靜對不起來。推定行為保留（全給空字串會讓多支線模型建不起來），但會列出推定的代碼並提醒確認。
- **非數值儲存格不再默默歸零。** 「-」「休」「N/A」原本一律當成 0 輛。「0」與「沒測到」在交通量統計上意義完全不同，而且這個轉換讓品質檢查的 `Number.isFinite` 規則在匯入路徑上**永遠不會觸發**——等於對使用者謊稱已經檢查過。現在逐格記錄並在預覽列出原文。
- 三項各補守門測試，並實測對未修正的版本會失敗（4 項紅字）。

### 交付前對抗性複查，再修五項（其中一項是 v2.1.37 就存在的資料損毀）

- **日期格式或布林值的儲存格會把 1.78 兆輛塞進資料。** `SAFE_XLSX_READ_OPTIONS` 帶 `cellDates: true`，日期格的 `.v` 是 `Date` 物件，而 `Number(Date)` 是**有限的** epoch 毫秒，會通過 `|| 0` 直接存進去，然後一路進尖峰視窗挑選、PCU 換算與全日累計。Excel 對「7:00」這類輸入會自動套時間格式，承辦很容易踩到。**這是 v2.1.37 就存在的問題，而且當時完全沒有任何提示。** 修法是讓判斷與寫入共用同一個運算式——否則警告會說「已計為 0」，實際卻存了 1.78 兆。
- **匯入失敗的逐檔說明使用者根本看不到。** 訊息一律 2.8 秒消失，且 `.toast` 沒有 `white-space: pre-line`，條列式的 `\n` 被摺成空白擠成一團。說明寫得再清楚也沒用。現在顯示時間依字數延長（上限 20 秒）、保留換行、可點擊關閉。
- **使用者補填的站號沒有正規化。** 訊息本身叫他「改成含 T<站號> 的格式（例如 `11017T14-02_路口名稱.xls`）」，他很自然會照樣填 `11017T14-02`；原樣存下去的話，同一路口另一季由檔名推出的是 `T14-02`，兩季對不起來——等於換一種方式重現正要修掉的問題。現在走 `stationFromFilename()` 正規化。
- **補填站號後，覆蓋／版本判斷不會重算。** 衝突模式是在選檔當下算的，那時站號還是空的；補填後才對上既有紀錄時會拿到預設的 `overwrite`，沒問過就覆蓋。現在補填造成的新對應一律改走建立新版本。
- **舊資料的「晴」會被這個修正本身害到。** v2.1.37 以前存下的 `surveyType: "晴"` 既不是平日假日也不是「待設定」，`isSameSurvey` 不讓新匯入接手，重新匯入會多出一筆——正是本版要修的症狀。現在比對時把非平日／假日一律視同「待設定」。
- 另新增 `tests/calculation-golden.test.ts`：**在此之前沒有任何測試在守「不得變更計算口徑」這條最高原則**，每次改版都靠人工比對外部腳本。現在把一份固定輸入的逐車種數量、尖峰視窗起訖與總 PCU 鎖進 repo，值取自 v2.1.37 實測。

### 修正說明文字與實際行為不符之處

- 摘要卡那句被移除文案的**孿生句**（「本系統專注尖峰轉向流量彙整」）。
- 當量「只用於換算成尖峰轉向 PCU」——實際上它同時**決定尖峰挑在哪一小時**（`rollingPeak` 以 PCU 加權），也套用於全日時段。
- 總覽儀表板、轉向進階分析、核對工作台寫死的 `PCU/hr`：這三頁的時段可以是全日時段，實際單位是 `PCU/調查日`，改用既有的 `scopeUnit()`。
- 品質檢查要使用者「以 v1.4.0 重新匯入」——v1.4.0 是 2026-08-11 的版本，計算口徑在 v2.1.30 已改，照做反而會得到舊口徑數字。
- **手冊七處**：尖峰搜尋範圍仍寫 05:00～12:00／12:00～23:00（v2.1.30 起已放寬為 00:00～12:00／12:00～24:00，使用者會用錯門檻判斷警示是否合理）、「四車種分類合計」（實際加總所有車種含自訂車種）、選單 17 項（實為 18 項）、支線上限 A～G、轉向圖只能切 AM／PM，以及手冊前面引用了本版已移除的那句文案。

## v2.1.37 — 2026-08-30（逐車種對帳，補上總數互相抵銷的漏報）

- **保留 v2.1.36 的正確修法**：單一車種車輛數與轉向圖同讀 `row.vehicle`，單一車種 PCU 仍依實際 OD 與轉向當量逐筆計算。
- **修正只核對全部車種總數的漏報。** v2.1.36 若遇到「機車多 7 輛、小型車少 7 輛」，差異會互相抵銷；全部車種總數與總 PCU 都可能相同，`reconciled` 會誤判為一致，但單一車種的輛數與 PCU 明細其實來自兩份不一致資料。
- 現在改為逐車種核對 `row.vehicle` 與 OD 車種數，每一車種各自沿用 5 輛或 5% 的既有門檻；警告會列出發生差異的車種及兩邊數值。
- PCU 與車輛數若同時不一致，兩項原因會一起顯示，不再只顯示其中一項。
- 新增「各車種差異互相抵銷」反例測試；本版不改尖峰選擇、當量係數、轉向分類或路口總量，`LAST_CALC_CHANGE_VERSION` 維持 `v2.1.30`。
- 補齊與現有 Cloudflare 外掛相符的 Worker／D1 官方型別與 `DB` 綁定宣告，乾淨 `npm ci` 後可直接完成 `tsc --noEmit`；這項開發驗證修正不會進入交通計算。

## v2.1.36 — 2026-08-30（摘要車輛數改回與圖同源，不動計算）

- **v2.1.35 的兩項修正都對，完整保留**：單一車種 PCU 改用實際 OD 逐筆加總（修掉我 v2.1.34 用支線總 PCU 比例反推的錯誤），以及日期的實際曆日驗證。
- **但 v2.1.35 連「車輛數」也一起改成從 OD 加總，這帶進一個新的不一致。** 轉向圖的車輛數讀的是 `row.vehicle[id]`（`totalMovement(approach, scope, undefined, id)` 就是它）。AM／PM／全日尖峰的 `row.vehicle` 是匯入時由尖峰視窗的原始格子算出來的，`syncRouteTotals` **刻意不重建**——重建會蓋掉使用者在核對工作台改過的值。兩者本來就可能不一樣，而既有的「車種統計異常」品質檢查**容許 5 輛或 5% 的落差不報警**。落在那個範圍裡，圖顯示一個數字、摘要顯示另一個，兩邊都不吭聲，正是 v2.1.34 要修掉的那個病。
- 實測（v2.1.35，瀏覽器）：把資料改成分歧狀態後，**圖 2,832 / 摘要 2,683**。
- **本版：車輛數一律用 `row.vehicle`（與圖同源），PCU 一律走實際 OD**（保留 v2.1.35 的正確作法）。兩者對不起來時由 `reconciled` 明講，並在摘要下方寫出實際原因，不靠任何一邊悄悄改寫另一邊。
- 車輛數的對帳門檻**沿用既有「車種統計異常」的 5 輛或 5%**，不另外發明第三套標準。
- 新增守門：`tests/pcu-breakdown.test.ts` 增 4 項（與圖同源、差很多要講、小幅落差不濫報、一致時不誤報），`scripts/e2e-diagram-summary.mjs` 增 2 項瀏覽器檢查。**已對 v2.1.35 實測紅字**（單元 4 項紅、瀏覽器 1 項紅）。
- 本版沒有變更尖峰選擇、當量係數、轉向分類或任何交通量計算；`LAST_CALC_CHANGE_VERSION` 維持 `v2.1.30`。

## v2.1.35 2026-08-30（複查修正：單一車種 PCU 與曆日驗證）

- **單一車種 PCU 改用實際 OD 的車種與轉向數量。** v2.1.34 的 `pcuBreakdown()` 用支線左／直／右「總 PCU 比例」反推每個車種的轉向分布；不同車種、不同轉向的當量不同時，會把個別車種 PCU 分錯，總量甚至可能剛好對得上而沒有警告。現在只要紀錄有 `routes`，便逐筆依「車種 × 實際轉向 × 匯入時當量」加總，與轉向圖使用相同資料路徑；僅沒有 OD 的舊備份保留比例估算與對帳警告。
- **日期檢查新增實際曆日驗證。** 不再接受民國 115 年 2 月 29 日、4 月 31 日等不存在日期；閏年 113 年 2 月 29 日可正確辨識。讀不到或無效日期仍維持 `unknown`，只提醒、不阻擋匯入。
- 新增單元測試，釘住 OD 車種 PCU 與閏年／月底邊界；瀏覽器守門新增「單一車種＋PCU」圖與摘要數值、單位一致檢查。
- 本版修正的是 v2.1.34 新增的顯示分解與日期檢查，不改既有尖峰選擇、當量係數、轉向分類或路口總量；`LAST_CALC_CHANGE_VERSION` 維持 `v2.1.30`。

## v2.1.34 — 2026-08-30（摘要與圖不再各說各話＋日期／期別檢查，不動計算）

- **轉向圖右側摘要現在跟著「顯示 × 時段 × 車種」走。** 以前那一格寫死 `recordTotal()` ＋文字「PCU/hr」，完全不看使用者選了什麼。使用者回報：顯示切成「車輛數」時，圖的抬頭寫「全路口流量 10,779 輛/調查日」，右邊摘要卻寫「10,469.5 PCU/hr」——**同一張畫面上兩個數字、兩種單位，講的是同一件事**。
- 根本原因：算「值＋單位」的正確邏輯**寫在轉向圖產生器裡面的區域變數**（`countMode` / `unit` / `total`），外面拿不到，摘要只好自己再猜一次。現在抽成 `displayValueKind()`，圖與摘要都走它。
- **新增第五種顯示模式「車輛數＋百分比」。**
- **單一車種的單位不再說謊。** 系統沒有存每個車種各自的 PCU，所以以前車種只要不是「全部車種」就一律改報輛數——行為對，但選單還寫著 PCU。現在用 `vehiclePcuFor()` 現算，用的是匯入當時存下來的當量矩陣、和 `syncRouteTotals` 建立 movements PCU 時**同一支 `pceFactor`**，不是另寫一套公式。
- **對不起來時會警告。** `pcuBreakdown()` 逐支線對帳，各車種 PCU 加總若與已存的總 PCU 差超過容差（舊紀錄沒存當量矩陣時可能發生），摘要下方出現紅字，明講這一筆的單一車種 PCU 不可靠。**不默默顯示一個可能錯的數字。**
- **摘要新增車種組成。** 百分比模式列各車種百分比；車輛數＋百分比模式連車輛數一起列。選單一車種時改報該車種的數值與它占路口總量的百分比。
- **總覽儀表板與跨計畫比較的同類問題一併修好：** 單位不再寫死 PCU/hr、標籤不再印出「最高流量路口 · `FULL`」這種內部代碼、算不出的全日尖峰不再以相容欄位的 0 參與排名；跨計畫比較的時段選單補齊為四個（以前只有 AM／PM，卻改的是全域時段）。
- **新增「調查日期 × 期別」一致性檢查。** 不一致時預覽面板紅底標示並在寫入前再確認一次；日期從表頭整塊文字判讀，不看固定欄位位置；**讀不到日期一律不阻擋匯入**。判斷邏輯集中在新檔 `lib/period-date.ts`，路口轉向、全日交通量、交通服務水準**三支同一份程式、同一份測試表**。
- **新增「期別顯示」切換（季別 ⇄ 實際調查月份）。** 只換顯示文字，資料仍以季別分組與計算，匯出內容一個字都沒變。
- `ImportPreview` 新增唯讀欄位 `dateCandidates`；既有的 `date` / `dateSource` 取法與值完全沒變。
- 新增守門腳本 `scripts/e2e-diagram-summary.mjs`（19 項）、`scripts/e2e-period-date.mjs`（13 項）、`scripts/e2e-period-month.mjs`（8 項），全部掛進 `npm run e2e`，並**已對未修正的 v2.1.33 實測紅字**（摘要那一支 19 項中 13 項紅，量的是「圖的抬頭」與「右側摘要」兩段兩版都有的畫面文字）。
- 本版沒有變更尖峰選擇、當量係數、轉向分類或任何交通量計算；`LAST_CALC_CHANGE_VERSION` 維持 `v2.1.30`。

## v2.1.33 — 2026-08-29（發布前複查：補齊 Excel 匯出，不動計算）

- v2.1.32 已讓網頁歷季趨勢在無法計算全日尖峰時顯示「－」並斷線，但兩條 Excel 匯出路徑仍把底層相容欄位的 0 寫成真正數值：批次分析報表，以及頁面上的「下載趨勢 Excel」。可編輯圖表因此仍會畫出一個掉到零的假資料點。
- 新增共用 `scopeValueOrNull()`：無法計算的 DAY 在 Excel 中寫成空白 `null`，既有原生折線圖依 `dispBlanksAs=gap` 正確斷線；真正算得出的 0、以及 AM／PM 的 0 都保留。
- 新增 3 項匯出語意守門測試。交通量、尖峰選擇、當量係數、轉向分類均未變更；`LAST_CALC_CHANGE_VERSION` 維持 `v2.1.30`。

## v2.1.32 — 2026-08-29（複查修正：顯示，不動計算）

- **歷季趨勢圖不再把「算不出全日尖峰」畫成 0。** 不足 24 小時的調查、以及還沒重新匯入的舊備份，底層 DAY 欄位是空值 0（`ensureRecordScopes` 補的）。趨勢圖直接拿 `totalOf` 畫，折線就掉到零、右側摘要寫「全日尖峰 0 PCU/hr」——看起來像那一季交通量歸零，事實是這份調查根本算不出全日尖峰。**0 會被抄進報告，「－」不會。**
- 系統其他地方（統計範圍切換、路口明細）本來就用 `hasDayPeak` 顯示「－」，只有趨勢圖漏了這一條，v2.1.30 起就存在；v2.1.31 幫第三條線補上顏色、圖例、點標籤與摘要數字之後，這個 0 變得很顯眼。
- 現在：算不出來的季度**折線斷開、不畫資料點**，右側摘要與增減率都顯示「－」。判斷統一走新的 `hasScopeValue()`（內部即 `hasDayPeak`），**不另寫第二套**。上午／下午尖峰完全不受影響。
- **品質異常明細的欄位標籤由「四車種分類合計」改為「各車種分類合計」。** v2.1.31 已把品質訊息改成「各車種合計」，卻漏了顯示同一個數字的欄位標籤，同一個數字上一行叫「各車種合計」、下一行叫「四車種分類合計」。
- 新增守門腳本 `scripts/e2e-trend-day.mjs`（已掛進 `npm run e2e`）與 `tests/trend-day-gap.test.ts`，**已對未修正的 v2.1.31 實測紅字**。
- 本版沒有變更尖峰選擇、當量係數、轉向分類或任何交通量計算；`LAST_CALC_CHANGE_VERSION` 維持 `v2.1.30`。

## v2.1.31 — 2026-08-28（發布前複查修正）

- 修正歷季趨勢「整體」模式的呈現不一致：v2.1.30 已畫入全日尖峰第三條線，卻沒有對應圖例、獨立顏色與右側季度摘要。現在 AM、PM、全日尖峰三條線均有各自的顏色、圖例、點標籤與季變化數值，下載檔名也改為 `AM_PM_DAY`。
- 多車種調查的品質訊息由「四車種合計」改為「各車種合計」，避免第五種以上自訂車種已正常列入計算，但訊息文字仍誤導使用者。
- 本版只修正圖表與文字呈現，沒有變更尖峰選擇、當量係數、轉向分類或交通量計算；`LAST_CALC_CHANGE_VERSION` 維持 `v2.1.30`。

## v2.1.30 — 2026-08-28（新增全日時段與全日尖峰小時）

- **新增「全日尖峰小時」。** 掃過完整 24 小時找出流量最大的那一個小時。**只有 24 小時的調查檔算得出來**；不足一天的調查（例如只做 07:00–09:00＋17:00–19:00 共 4 小時）一律顯示「－」。不這樣做的話，那個數字必然等於上午與下午尖峰裡較大的一個——看起來像新資訊，其實只是換個名字，而且會被當成一整天的最大值寫進報告。
- **「全日時段」納入同一套統計範圍。** 路口轉向圖、車種組成分析、駛入／駛出流量、流量核對工作台、OD 矩陣、支線平衡、Excel 匯出與批次成果包，全部改由 `SCOPE_KEYS` 產生欄位。以前是「上午、下午、全日」三組欄位各自手寫，加一個時段就要在四個地方各補一次，漏掉哪一個都不會有人發現。
- **全日時段不另外存檔。** 每次載入由 `record.survey` / `route.survey` 現算（`syncRouteTotals`）。同一個概念存兩份，改當量係數、刪支線、合併路口之後就會分岔，而且是在報告送出去之後才被發現——這個專案已經因為同類問題修過三輪。
- **⚠️ 計算口徑變更（一）：上午／下午尖峰的搜尋範圍。** 上午由 `[05:00, 12:00)` 改為 `[00:00, 12:00)`，下午由 `[12:00, 23:00)` 改為 `[12:00, 24:00)`。舊的兩段合起來**掃不到 23:00–24:00 與 00:00–05:00**，真正的尖峰若落在那六個小時，任何一個數字都抓不到。新的兩段剛好把一天鋪滿而且不重疊。實測使用者提供的 8 份調查檔，新舊口徑算出來的上午／下午尖峰**完全相同**。
- **⚠️ 計算口徑變更（二）：每一格的車輛數先四捨五入成整數。** 有些調查檔的儲存格存的是小數（0.36、5.5506…），Excel 的格式把它顯示成整數，於是報告上看到的是 0 與 6、程式拿去算的卻是 0.36 與 5.5506，全日車輛數會算出「27,988.79 輛」這種不存在的車。四捨五入放在**全系統唯一讀取原始儲存格的地方**，之後的加總、尖峰滾動、PCU 換算、全日累計全部自動吃到整數。
- 以上兩項確實變更了計算口徑，依使用者決定一併把 `LAST_CALC_CHANGE_VERSION` 推進到 v2.1.30，v2.1.30 以前鎖定的季度會亮出鎖定衝突。
- **轉向圖新增「車輛數」顯示模式。** 原本只有交通量（PCU）、百分比、PCU＋百分比三種，車種選「全部車種」時拿不到純輛數。新模式一律報實際調查到的車輛數，可搭配車種篩選看單一車種或全部車種的輛數。圖上的單位也跟著統計範圍走（尖峰是 PCU/hr、輛/hr，全日時段是 PCU/調查日、輛/調查日），不再寫死 PCU/hr。
- **「全調查時段」的標示寫出實際時數。** 它的定義是「這份檔案匯入到多少就算多少」，24 小時的檔案才等於全日；只做 4 小時的檔案以前也叫「全調查時段」，很容易被當成一整天。
- **既有資料相容。** 舊備份與舊 localStorage 只有上午／下午，載入時自動補上空的全日欄位，**既有數字一個都不動**。全日時段因為是從已存的調查總量現算，舊資料直接就有；全日尖峰小時需要重新匯入原始檔（逐時間格的資料沒有存檔，無法回推），畫面與品質檢查都會寫明是哪一種原因。

## v2.1.29 — 2026-08-27（發布後複查修正）

- **沒有變更任何計算規則，也沒有變更任何操作方式。**
- **修正 v2.1.28 新增的「被更新版本鎖定」警示。** 判斷時沒有先確認版號解析得出來——`isVersionAtLeast` 認不得格式時一律回傳 `false`（對「鎖定版本早於常數」那條是對的），用在這裡卻會反過來：只要 `VERSION` 帶了 `-final`、`-rc1` 這種尾巴或多一個空白就解析不出來，於是**每一筆 v2.1.21 以後鎖定的資料全部亮紅字**，正是 v2.1.28 要修掉的那個症狀（實測 19×19 版號矩陣會多出 344 筆紅字），而且訊息會說反。改為兩個版號都解析得出來才判斷——版號認不得是「不知道」，不是「有問題」。
- **四大車種的名稱兩張表對齊。** `app/traffic-app.tsx` 的 `VEHICLE_LABELS` 寫「大型／大客車」「特種／聯結車」，`lib/traffic.ts` 的 `CORE_VEHICLE_LABELS` 寫「大型車」「特種車」，而匯入時寫進紀錄的是後者。`vehicleLabel()` 優先讀紀錄，所以 v2.1.19 以前沒有 `vehicleLabels` 欄位的舊備份會落到前者——跨季的結論草稿因此同一個車種前後兩種寫法。以匯入時實際寫入的那一組為準；新增第 12 項發布結構檢查釘住兩張表一致（已實測會在舊寫法下紅字）。
- **重新打包，中文檔名標記 UTF-8。** v2.1.28 交付的 ZIP 沒有標記，解壓後檔名全部變成 `#Uxxxx` 亂碼，包內因此找不到程式連得到的手冊檔名——發布結構檢查第 4 項紅字（44 項中 2 項失敗）。**那是打包的問題，不是程式的問題**：檔名還原後 44/44 全過。⚠️ **線上網站不受影響**（使用者實測手冊下載正常）——受影響的只有交付出去的備存 ZIP，拿它還原 repository 會少掉手冊檔。

## v2.1.28 — 2026-08-27（測試種子去識別化）

- **沒有變更任何計算規則，也沒有變更任何操作方式。**
- `scripts/seed-state.json` 與 `seed-wide.json` 原本寫著實際的委託案名稱、計畫編號、站號與路口名稱，而這兩個檔案就在公開的 repository 裡、也能從 GitHub Pages 直接下載。（裡面的流量數字本來就是 `seed-state.mjs` 用固定種子產生的，不是實際調查值，但名稱是真的。）已全部換成「示範…」與 `A0000`／`S01-01` 這類假識別碼，並同步改掉依名稱選取的 e2e 腳本。
- `lib/` 與 `app/` 裡的路口名稱**刻意未動**：`referenceMovementForOd` 等產品行為靠路口名稱判斷要不要套用參考轉向表，改掉會影響實際匯入。要不要改是產品決策，不是清理工作。
- 新增 `.gitattributes`（`* -text`）。上一版的備份 ZIP 裡每一個文字檔都被轉成 CRLF，連建置產物也一樣，導致 `assets/index-<雜湊>.js` 的內容不再雜湊成它自己的檔名、無法與線上逐位元核對——而網站照樣跑得動，完全看不出來。成因是在 Windows 上取出（`core.autocrlf`）。
- 發布結構檢查由 9 項增為 **11 項**；兩項新增檢查都已實測會在對應的錯誤狀態下失敗。
- **修正鎖定衝突誤報**（使用者回報）。舊版 `record.resultLock.version !== VERSION` 是字串完全相等比較，所以升過一次版之後，**所有已鎖定的資料就永遠亮紅字**，下一版又全部重來；使用者有 20 幾季資料。而且它和真正該注意的「鎖定後的資料內容已變更」長得一模一樣紅，看久了連真的那一次也會被略過。
  新增 `LAST_CALC_CHANGE_VERSION`（目前 `v2.1.21`，最後一次變更尖峰小時口徑的版本）與 `lockStatus()`：只有鎖定當時的版本**早於**它才算衝突；升版但沒動計算改為一行灰字說明。**使用者不需要解除並重新鎖定**——那會蓋掉原本的鎖定時間與版本，那是稽核紀錄。
  ⚠️ 日後若真的改了計算口徑（PCU 當量、尖峰視窗、駛入推導、車種歸類…），`LAST_CALC_CHANGE_VERSION` 要跟著改。
  新增單元測試 6 項（含「版號要照數字比大小，不是照字串」——字串比較會說 `v2.1.9 > v2.1.10`）與端對端檢查 2 項。
- **修正轉向圖兩半各說各話**（複查時發現）。同一張圖卡的「駛出」半格印 `approach.name`，「駛入」半格印 `approach.sourceCode`。使用者替支線改名之後，圖上只有一半會變；用「新增支線」加進來的支線會印成 `←人工2`。PNG 與 PDF 匯出共用同一支繪圖程式，所以**交付出去的圖也是錯的**。兩半改為同一種取法（含去掉開頭的「路口」兩字）；支線代碼並未消失，圖卡標題那一行仍然寫「代碼 · 名稱」。
- **移除右上角季度選單裡的死選項「尚無季度」**（使用者回報「沒任何作用」，診斷完全正確）。它被無條件畫出來，但選了會把季度設成空字串，緊接著 `useEffect` 又把它拉回最新一季——等於點了什麼事都沒發生。系統裡其他五個季度選單也都沒有這一項。現在只有 `quarters.length === 0` 時才顯示，那時候它才真的是提示。
- 新增端對端腳本 `scripts/e2e-arm-name.mjs`（納入 `npm run e2e`）。**已實測會在未修正的舊版下紅字 4 項**：選單裡仍有「尚無季度」、選它之後季度被拉回最新一季、駛入那半沒有新名稱、駛入與駛出兩半用的標籤集合不同。修正後全部通過。
- 發布前增量複查補上兩個相容性邊界：較新版本建立的鎖定資料不再被較舊版本誤判為安全；很舊的備份若只有 `custom:` 車種識別碼，結論草稿會與車種組成頁一致顯示乾淨車種名稱。兩項都只修正警示／文字，不改任何交通量或 PCU 計算。
- `npm test` 改為先跑 `npm run lint`，CI 從此會把關程式碼檢查。

## v2.1.27 — 2026-08-26（把發布結構釘進自動檢查）

- **沒有變更任何計算規則，也沒有變更任何操作方式。**
- 新增 `tests/release-structure.test.mjs`（9 項），釘住 v2.1.26 修好的發布結構：只能有一條 GitHub Pages 發布路徑、點號開頭檔案不得遺失、不得出現第二套套件鎖定檔、根目錄不得殘留上一版的雜湊資產、手冊必須跟著版號重新產生並刪除舊版、手冊封面戳記與每一頁頁尾的版號與日期必須一致、本檔最新版號必須等於程式版號，並確認七叉路口壓力測試在未提供參數時仍能找到正式建置目錄。
- 前 8 項**已逐一實測會在對應的錯誤狀態下失敗**（共 11 種情境：刪掉 `.nojekyll`、換回會發布的 workflow、放回舊雜湊資產、放回 pnpm 鎖定檔、刪掉 `.openai/hosting.json`、升版漏改手冊檔名、頁尾日期與封面不符、手冊沒重新產生、漏寫本版更新內容、`package.json` 版號沒跟著改），第 9 項則針對本次複查實際重現的無參數啟動錯誤新增。還原後全部通過。只加會通過的檢查等於沒加檢查。
- 重寫 `DEPLOYMENT.md`：舊版仍寫著「兩種相容部署路徑」、已退役的 GPT Site 網址，以及一組 `pnpm` 指令（v2.1.26 已把 pnpm 鎖定檔移除，照做會把它裝回來）。
- 修正 `scripts/stress-drag.mjs` 寫死了某一台機器上的種子檔絕對路徑（`/home/claude/work/turning/scripts/seed-state.json`），並補上原先仍遺漏的正式建置目錄預設值；現在可直接執行，不必另外傳入路徑。實測 7 叉路口 14 張圖卡、300 步拖曳，0 次當掉、0 個頁面錯誤。
- `README.md` 與 `DEPLOYMENT.md` 補上：`npm ci` 需要連到 `cdn.sheetjs.com`，連不到時的 403 是網路問題而非程式問題，且**不可以**用 npm registry 上的替代版本產生要發布的 `assets/`。

## v2.1.26 — 2026-08-26（發布與維護健檢）

- **交通量計算規則與既有分析結果均未變更。** 本版只處理發布、依賴安全、程式品質與交付一致性。
- GitHub Pages 統一由 `main` 分支根目錄發布；Actions workflow 只建置與測試，避免兩條部署路徑互相覆蓋。
- 補齊 `.nojekyll`，清除過期雜湊資產與舊版手冊；點號開頭檔案列入人工上傳檢查表。
- 套件管理統一為 npm；SheetJS 升至官方 0.20.3 發布包，正式相依套件安全掃描為 0 項弱點。
- ESLint 由 25 個錯誤、2 個警告修正為 0 錯誤、0 警告；完整測試與端對端測試仍須全數通過才發布。
- 線上版本驗證只採用帶版號的手冊、`?v=` 與新版專屬雜湊資產，避免固定路徑的 CDN 快取造成誤判。

## v2.1.4 — 2026-08-22（匯出的 Excel 會要求「修復」）

- **修正匯出的 .xlsx 在 Excel 開啟時跳出「我們發現…部分內容有問題。您要我們盡可能嘗試復原嗎？」，按「是」之後歷季趨勢圖被整張丟掉。** 圖表 XML 有兩類不符合 ECMA-376（Excel 的檔案規格）的問題：
  1. `<c:lineChart>` 把 `<c:smooth>` 排在 `<c:ser>` 之前。規格的順序是 grouping → varyColors → ser* → … → marker → smooth → axId。
  2. `<c:valAx>` 把 `<c:majorGridlines>` 排在 `<c:numFmt>` 之後。規格要求它排在 `<c:axPos>` 之後、`<c:title>` 之前。
- Excel 開檔時會逐一驗證每個內部 part，任一處不合規就判定檔案毀損並要求修復，修復等於把整張圖表刪掉，因此使用者看到的是「只有數字、沒有圖」。LibreOffice 較寬鬆，能開起來不代表 Excel 能開。
- 新增自動檢查 `scripts/ooxml-check.mjs` 與 `scripts/e2e-xlsx-repair.mjs`：實際跑一次匯出，再依規格驗證圖表 part 的元素順序、`[Content_Types].xml` 覆蓋率與 relationship 對應。
- **舊版 Excel 提醒**：可編輯的原生圖表需要 Excel 2007 以上版本；更舊的版本請改用「下載舊版 .xls」的數值表。

## v2.1.3 — 2026-08-22（「駛入／駛出」用詞統一）

本系統採用的定義（與「全日交通量及車種組成」系統一致）：

- **駛入路口X ＝ 車輛從其他支線駛入 X**，也就是以 X 為**終點**（穿越路口中央後開進 X）。
- **駛出路口X ＝ 車輛從 X 駛出**、開進路口中央再分往各支線，也就是以 X 為**起點**。

- **修正「調查資料 → 全調查時段道路方向車種數量」表的「與路口關係」欄。** 該欄把以 X 為**起點**（`fromApproachId`）的那一列寫成「駛入路口」、以 X 為**終點**（`toApproachId`）的那一列寫成「駛離路口」，兩個標籤都是反的。現已改為「駛出路口」與「駛入路口」。
- **修正「跨計畫比較 → 各支線駛入／駛出尖峰流量」表的欄位標題與說明文字。** 表頭把 `enteringIntersection`（依起點統計）標成「駛入路口」、`leavingIntersection`（依終點統計）標成「駛出至支線」，與同一份資料匯出到 Excel 時的欄位名稱互相矛盾。現已對調並改寫下方說明。
- **歷季趨勢匯出**的兩個欄位改名為「AM／PM 駛出路口（該支線→路口中心）」與「AM／PM 駛入路口（路口中心→該支線）」。
- 轉向圖卡、OD 矩陣、支線平衡檢核、報表匯出與焦點模式本來就依上述定義計算，未做更動。**本版沒有任何數值變動，只有標籤文字。**

## v2.1.2 — 2026-08-22（轉向圖圖例修正）

- **修正路口轉向圖右下角的流向圖例**。左轉、直行、右轉三個項目原本都寫成同樣的「●」符號，而 `.legend text` 統一指定為深灰色，三個圓點完全一樣，看不出來是在對應圖上的箭頭顏色。現改為畫出與圖上箭頭同色、同樣帶箭頭的短線段：左轉 #d64ba7（桃紅）、直行 #2166d1（藍）、右轉 #e24538（紅），與流向曲線一致。
- 交通量、OD 流向、尖峰搜尋、匯入規則與報表內容皆未變更。

## v2.1.1 — 2026-08-22（全面檢查後的修正）

- **修正寬螢幕下拖曳圖卡的位移量與滑鼠不成比例**。圖面 SVG 會依視窗等比例縮放並在左右留白，舊版直接以容器寬度換算，於 2200 像素寬的視窗下誤差達 15.1%（滑鼠移 200px、圖卡移約 230px）。現改用 `getScreenCTM()` 取得實際繪圖比例換算，實測滑鼠移 200px、圖卡即移 200px。
- **修正拖到邊界後回拖有一段「死區」**。舊版放開滑鼠時記下未經邊界修正的座標，畫面卻已被夾在邊界上，導致往回拖前面一段完全沒有視覺變化。現在記錄與繪製都套用同一組邊界，回拖 120px 即實際移動 120px。
- **修正跨季度同步時同名支線可能配對錯誤**：改以「支線代碼＋序號」作為對應鍵，重複名稱不會互相覆蓋；新增的支線會取得獨立的代碼，並重設位移與版面，不再沿用其他支線的位置。
- **修正焦點支線索引在刪除支線後可能越界**，以及圖卡數量多於外圍格位時的補位計算（原本會重複套用同一個格位）。
- 三種顯示畫面（只顯示駛入／只顯示駛出／駛入＋駛出）的圖卡與路口標籤版面互不干擾，已於 1680 與 2200 像素寬度下逐項驗證。
- **修正拖曳「路口A～路口F」標籤會整個跳到畫面左上角**。標籤沒有標示自動基準點，程式把基準點誤判為 (0,0)，一拖就飛到角落；若該支線先前存過標籤位置，還會被這個錯誤位置覆蓋掉。現已補上基準點與可放置範圍，實測滑鼠移多少、標籤就移多少。
- **修正重新整理後「（平日）」「（假日）」被拆成兩個路口**。讀檔與還原備份時會再正規化一次路口名稱，而正規化會把括號整個拿掉，於是同一路口的平日與假日資料在重新整理後不再視為同一個路口，資料別選單、幾何同步與歷季比較都會跟著錯。現已保留括號內容。
- **修正每一車種的轉向量是按 PCU 比例分攤的**。有逐條 OD 流向時改為直接加總該轉向的實際車輛數；以機車左轉 100 輛、直行 100 輛為例，舊版會顯示 125／75，新版為正確的 100／100。沒有 OD 流向的舊資料仍只能按比例推估，並已於程式中註明。
- **修正批次成果 ZIP 會把「目前開著的計畫」的匯出項目套用到每一個計畫**；現在每個計畫都用自己勾選的項目。批次 PDF 也改為跟批次 PNG 一致，依目前的駛入／駛出顯示模式繪製。
- **儲存空間寫滿時不再讓整頁變空白**。存檔改為先精簡還原點再重試，最後才提示使用者下載備份，過程中不會有例外從 effect 逃出去把畫面整個卸載。
- 修正舊版備份（沒有 OD 流向）在選擇單一車種時，駛入卡每格有數字但合計顯示 0、百分比全為 0% 的問題。
- 修正整點（60 分鐘）調查被寫成「15 分鐘區間」的文字；改依實際區間長度顯示。
- 修正選定車種在某些來源區塊缺少該車種欄位時，可能讓轉向圖丟出例外而變成空白頁。
- 批次成果包產生失敗時會顯示原因，不再按了沒有任何反應。
- 交通量、OD 流向、尖峰搜尋、匯入規則與報表內容皆未變更。

## v2.1.0 — 2026-08-22（拖曳穩定性、箭頭方向與報表自選）

- **修正拖曳圖卡久了整頁崩潰（This page couldn't load）**。舊版在每一次滑鼠移動都會複製整筆紀錄、寫入 300 筆版本快照，再把整包資料 JSON 寫進 localStorage；實測 7 叉路口連續拖曳 300 步會產生 211 次存檔、把 localStorage 撐到 5.1 MB 而觸發 QuotaExceededError，畫面隨即變成空白錯誤頁。現在拖曳過程只更新該圖卡的 transform（以 requestAnimationFrame 節流），放開滑鼠才寫入一次：同樣 300 步只存檔 1 次、耗時由 45.7 秒降到 5.0 秒。
- **多岔路口的圖卡配位改用貪婪配對加兩兩交換**，取代原本會指數爆炸的遞迴窮舉；卡片多於格位時自動補位，不再在畫面繪製過程中丟出例外造成整頁空白。
- **每一張圖卡都可各自拖曳**：同一支線的「駛入」與「駛出」卡分別記住自己的位置，不再連動。
- **路口 A～G 標籤也可以拖曳**到任意位置，並跟著季度同步。
- **只看駛入、只看駛出、駛入＋駛出三種畫面各自保存版面**：同一張圖卡（或路口標籤）可以在只看駛入時擺左邊、在駛入＋駛出時擺右邊，彼此互不干擾。某個模式還沒調整過時會沿用共同起點，既有備份不會跑位；「重設所有圖卡位置」會一次清掉全部三種模式。
- **移除圖卡位移的 X／Y 數字輸入**，改為純拖曳；每條支線可單獨還原，也可一鍵重設全部。
- **圖卡標題「駛入路口A」「駛出路口A」置中**（原本缺少樣式，文字從卡片中線往右排而溢出）。
- **修正只顯示駛入／駛出時的箭頭方向**。舊版把流向曲線從中點切一半，箭頭停在路口中央；現在一律畫完整的「起點支線→目的支線」曲線：只顯示駛入＝各支線畫箭頭指向該路口，只顯示駛出＝從該路口畫箭頭指向各支線。
- **報表匯出項目可依計畫勾選**：各路口駛出／駛入尖峰流量、駛入＋駛出完整流量表、車種組成分析、歷季趨勢、跨計畫比較、OD 轉向矩陣、支線流量平衡、資料品質檢核、車種轉向當量參數，共 10 項，勾到才會出現在 Excel。勾選會記在計畫上，也可存成範本重複套用到其他計畫。
- 未勾選「歷季趨勢比較」時不再注入指向不存在工作表的折線圖，避免 Excel 開檔跳出修復提示。
- 車種轉向當量說明補述：四大類沿用教育訓練簡報第 15 頁的參考值；簡報未提供參考值的新增車種一律預設 1.0，由使用者於當量表逐一修正。
- **新手操作手冊全面改寫**（19 頁）：改以「完全沒有交通背景」的使用者為對象，先白話解釋路口轉向調查、支線與 OD、駛出／駛入、尖峰小時、PCU 與當量係數、守恆等 10 個名詞，再逐頁帶操作，並補上本版的拖曳、箭頭方向與報表勾選說明；同時提供 PDF 與可編輯的 Word 版，兩者由同一份原始檔產生。
- 交通量、OD 流向、尖峰搜尋與既有匯入規則皆未變更。

## v2.0.1 — 2026-08-21（最終版介面修正）

- 道路與流向管理恢復清楚的右側道路簡圖，不再用縮小的正式圖取代幾何預覽。
- 新增獨立全幅「交通量圖卡排版預覽」，圖卡可直接拖曳，並可切換只看駛入、只看駛出或同時顯示。
- 圖卡位置欄位改為清楚的「左右 X」「上下 Y」，修正欄位被壓縮到無法辨識的排版問題。
- 新增網站內新手操作手冊，說明基本流程、常用名詞與進階功能使用時機，並提供 PDF 下載。
- 交通量、OD 流向、車種當量及既有匯入規則皆未變更。

## v2.0.0 — 2026-08-21（最終版）

- 匯入預覽新增同季度、同路口版本差異，可選擇保留新版、覆蓋或略過；修改前建立可還原版本。
- 流量核對工作台新增原始工作表／儲存格、車種、當量與 PCU 換算追溯，並可輸出核對 Excel。
- 新增待核對、已核對、已確認、需修正審核狀態；季度鎖定仍可人工解除，鎖定衝突會明確提示。
- 新增 OD 轉向矩陣、支線駛入／駛出平衡、連續 60 分鐘尖峰候選排行與核對 Excel。
- 新增季度資料品質總表 Excel，列出 AM／PM 守恆差值、未對應流向、日期與問題明細。
- 道路與流向管理支援拖曳或輸入 X／Y 調整數據框位置；版面設定跨季度同步，匯出前提示可能重疊。
- 調查格式記憶可重新命名或刪除；車種歸類方案可保存、套用與刪除。
- 完整備份納入格式記憶、車種方案與歷次資料版本，可在另一台電腦延續核對。
- 所有既有尖峰、車種、駛入／駛出與原格式解析規則維持不變。

## v1.8.0 — 2026-08-20（正式版）

- 匯入器不再把車種寫死為四類，會依欄名辨識任意數量車種；既有機車、小型車、大型車、特種車格式維持原計算規則。
- 新車種預設各自保留分析，可分別設定左轉、直行、右轉當量；也可在匯入預覽將大貨車、大客車、聯結車等併入四個標準類別後再計算。
- 車種組成、轉向圖車種篩選、資料核對與 Excel 匯出均依實際分析車種動態產生欄位。
- 匯入時保存原始車種名稱、歸類規則與當量快照；JSON／ZIP 備份可將設定移至另一台電腦繼續使用。
- 只有全日路段分向車種、但沒有左直右或起訖 OD 欄位的檔案，會明確標示為「非路口轉向」，不會誤建轉向圖或虛構流向。
- 新增五車種與既有四車種回歸測試，並以 `14013T5-10-北林路.xlsx` 驗證五車種結構辨識。

## v1.7.2 — 2026-08-14（正式版）

- 五岔至七岔路口新增專用外圍排版，流量卡改配置於圖面上、右、下、左四側，不再沿相近道路角度重疊堆放。
- 依各卡片原始支線位置自動選擇最近的獨立槽位；同時顯示駛入與駛出時，最多 14 張卡片仍各自占用不同位置。
- 三岔與四岔路口維持原有版面與計算方式，不受多岔路排版變更影響。

## v1.7.1 — 2026-08-14（正式版）

- 新增「各路口駛入流量」分析表，以系統確認的道路方位與左／直／右目的支線計算全日、AM Peak、PM Peak 的駛入 PCU 與實際車輛數。
- 僅在匯入資料確實涵蓋完整 24 小時時顯示全日欄位；只有上午／下午尖峰或部分時段的原格式報告，全日欄位顯示「－」，不以經驗值推估。
- 分析 Excel 新增「駛入各路口流量」工作表，保留季度、站號、目的支線、尖峰時段及各項單位。
- 目的支線數值以系統匯入的原始流向為主，不採用外部計算表中未通過來源／目的總量守恆檢核的結果。
- 轉向圖摘要用語統一為「駛入該路口／支線」與「由該路口／支線駛出」，並可選擇僅顯示任一方向或同時顯示，避免與「進入中央路口」的視角混淆。
- 駛入流量表擴充為駛入／駛出雙向分析，全日、AM Peak、PM Peak 的 PCU 與實際車輛數皆分別計算並輸出 Excel。
- 道路與流向管理依標準路口識別碼合併平日、假日重複選項；日別調查資料仍分開保存。
- 轉向圖摘要簡化為「駛入路口D／駛出路口D」等文字。
- 同一標準路口包含平日、假日等多筆資料時，轉向圖、車種組成、駛入／駛出分析與道路管理新增獨立「資料別」選單，不再把日別混入正式路口名稱。
- 轉向圖上方新增醒目的「藍框流量顯示」三段按鈕，可直接切換駛入＋駛出、只顯示駛入或只顯示駛出，畫面與匯出圖同步。
- 流量卡依支線角度與卡片尺寸自動外移，避免遮住路口 A／B／C／D 等道路標示；畫面與 SVG、PNG、PDF 匯出一致。
- 每一來源支線固定保留左轉、直行、右轉三條結構流向；實際流量為 0 的路徑改以淡色虛線顯示，避免誤認為漏畫或存在車流。
- 以原格式三岔、七岔調查檔及新版平日／假日整點調查檔完成回歸驗證；原格式辨識規則與既有尖峰計算結果維持不變。

## v1.7.0 — 2026-08-13

- 新增流量核對工作台，可逐筆展開 AM／PM 尖峰總量的 OD 流向、車種數、當量係數算式與 PCU/hr 小計。
- 新增季度成果手動鎖定／解除與版本、內容衝突提示；修改或覆蓋已鎖定成果前會先詢問。
- 轉向圖新增「駛入＋駛出、僅駛入中央路口、僅駛出中央路口」摘要顯示模式，並套用至匯出圖。
- 調查檔格式範本會記住成功辨識的工作表組合與欄位特徵，並納入跨電腦備份。
- 保留多季度期間 Excel 與多計畫、多季度 Excel／PDF／PNG 批次成果 ZIP。


## v1.6.0 — 2026-08-13

- 修正道路角度調整後，流量說明卡仍停留在舊位置的問題。
- 美化可編輯 Excel 趨勢圖，補齊圖例、X／Y 軸名稱與 PCU/hr 單位。
- 分析 Excel 可選擇起迄季度，將多季資料匯出在同一活頁簿。
- 新增多計畫、多季度 Excel／PDF／PNG 批次成果 ZIP。
- 新增平日／假日全日整點轉向表格式範本，分開辨識兩種日別。

## v1.5.1 — 2026-08-11

- 歷季趨勢 Excel 的可編輯折線圖改置於數據表下方，不再覆蓋季度、路口名稱或尖峰時段欄位。
- 重整圖表標題、座標軸、圖例、網格線與留白；圖表及 Y 軸均清楚標示 PCU/hr。
- 跨計畫／多路口比較新增各支線 AM／PM 尖峰流量明細，分別列出「由支線駛入中央路口」及「由中央路口駛出至支線」。
- 跨計畫 Excel 每條支線各列一筆，保留路口尖峰轉向總量、尖峰時段及支線駛入／駛出數值，便於核對各支線加總。

## v1.5.0 — 2026-08-11

- 歷季趨勢新增「整體」模式，同一張圖同時呈現 AM Peak 與 PM Peak，避免把不同時段的尖峰量相加。
- 車種組成分析新增專用 Excel 下載，完整保留各路口、各分析時段的實際車輛數（輛/調查時段或輛/hr）與比例。
- 歷季趨勢新增 Excel 下載，包含彙整數據與以儲存格為資料來源的原生可編輯折線圖。
- 報表 Excel 精簡為三類成果：車種組成分析、歷季趨勢比較（含可編輯圖表）、跨計畫／多路口比較；移除其他不需要的工作表。
- 修正道路與流向管理頁在瀏覽器 100% 縮放下，右側幾何圖超出容器而被裁切的問題。

## v1.4.0 — 2026-08-11

- 依最新使用目的移除容量、V/C 與服務水準相關介面及報表欄位，避免用未調查條件產生誤導性估算。
- 保留四車種 × 左／直／右共 12 個可調整當量係數，新增每筆匯入資料的係數快照；Excel 報表另附目前設定與各路口歷史係數。
- 道路管理移除不參與尖峰計算的快／慢／機車等車道數輸入，只保留原始路口代碼、道路支線、方位、角度與起訖流向。
- 缺少時相圖不再提示容量欄位不足，只提示道路幾何可能需要人工校正。
- 七叉路 A～G 角度調整後會保留原始 A→B 起訖流量、自動重判左／直／右，流量卡亦依實際角度重新排列。
- 新增全調查時段、AM 尖峰、PM 尖峰三種車種組成，並輸出實際輛數與比例至新舊版 Excel。
- 修正跨計畫摘要卡季度不同步、歷季趨勢以每季站號拆分同一路口，以及標準版多叉路 SVG 邊界裁切。
- 轉向圖拆分來源與目的口徑，保留一位 PCU 小數且不做中途整數化；T 字路口依實際幾何判定目的支線。
- 分析頁統一改為先選資料季度、再選標準路口；跨季維持同一路口，不再以 T14／T15 站號拆成不同路口。
- 路口下拉選單不再把站號當成名稱前綴；站號改列為原始資料追溯欄位，名稱正規化也會排除 `15-01` 類站號與「七叉路口」等幾何註記。
- 歷季趨勢改為起始／結束季度範圍，可比較所有已匯入季度，不再限制 2～4 季；季度較多時圖表自動加寬。
- 道路支線方位改由角度自動換算為東、東南、南、西南、西、西北、北、東北，不再要求使用者重複維護角度與方位。

## v1.2.0 — 2026-08-11

- 以 T15-01～05 實際 `.xlsx` 重建匯入器：先辨識工作表版型，再處理各入口區塊與時間欄，不再把並排區塊混成 35／47 欄。
- T15-01 七岔路改為保留 7 個入口、168 個起點→終點×車種欄位，建立 42 組可追溯 OD 流向；不再硬套左／直／右三欄。
- 自動讀取檔內調查日期、站號與站名；五份 115Q2 實檔均讀到 2026-05-04。
- 加入 Excel 97–2003 `.xls` 匯入與 BIFF8 `.xls` 報表輸出回歸測試。
- 路口名稱改為計畫層級標準路口：相同名稱跨季自動併入，只有新名稱才詢問建立、併入或取消。
- 道路與號誌管理新增路口切換、V/C 用途與取值說明，以及七岔路起點→終點流向對照修正表。
- 正式圖依確認版式加寬流量卡，數值與百分比分行，文字保持正向，並以實際 OD 目的支線繪箭頭。

## v1.1.0 — 2026-08-11

- 正式環境改為空白起始，不再預載示範數值；季度資料可單筆或整季刪除。
- 匯入前強制選擇民國年度與第 1～4 季，未完成設定不得選檔或寫入。
- 修正舊版將「輛/hr」與「PCU/hr」直接相減造成的假車種異常；舊資料自動移轉後不再執行無單位基礎的比較。
- 匯入器新增表頭語意辨識：方向、左直右與四車種；信心不足時明確標示且不下車種一致性結論。
- 品質頁新增異常計算明細、單位、判定前提與重新匯入導引，不再誤跳道路幾何頁。
- 新增多計畫管理、跨計畫比較，以及包含全部計畫與參數的 A／B 電腦 ZIP／JSON 移轉。
- 新增可調整的四車種左／直／右當量表，並明確標示簡報來源未詳與 2022 手冊適用限制。
- 新增飽和流率建議範圍、現地疏解率換算、有效綠燈／週期說明及容量公式。
- 正式轉向圖改採流量表搭配跨路口箭線，另提供單一進口方向聚焦；修正文字重疊、反向字與單位。
- 趨勢圖補上由上到下的 Y 軸名稱與 PCU/hr，Excel 輸出補上單位、欄寬、篩選與可編輯趨勢資料。

## v1.0.0 — 2026-08-11

- 建立季度批次匯入、更新與匯入預覽。
- 建立模組化 60 分鐘滑動視窗尖峰計算與測試。
- 建立 Dashboard、多路口比較、歷季比較、資料品質檢查與名稱管理。
- 建立 3–7 叉路口 SVG 轉向圖、幾何管理與 PNG／SVG／PDF 批次輸出。
- 建立 Excel 報表、JSON／ZIP 備份還原與 GitHub Pages／Sites 部署設定。
- 明確排除 LOS／HCM 分析。
