# Turning Traffic v2.1.49 驗證報告

驗證日期：2026-09-05
基準：v2.1.48（GPT 發布，應用程式提交 `e790101faf3868fe65cde0f6ea1713a969340a4a`）

## 結論

**本版沒有變更尖峰挑選、OD 加總、左右直判定、車種歸類、車種當量、PCU、
駛入／駛出或報表計算。** `LAST_CALC_CHANGE_VERSION` 維持 `v2.1.30`，
固定計算黃金值通過；37 份真實調查工作簿的解析結果與已發布的 v2.1.45
**逐位元相同（37/37）**。

## 本次修正

### 一、全域拖放防呆把一般文字拖曳也擋掉了

v2.1.47 加入「檔案掉在放置區外面不要把使用者帶離頁面」時，少了一層
「拖進來的是不是檔案」的判斷。結果使用者在頁面內拖動選取文字也被擋住。

實測（v2.1.48）：拖一般文字到輸入框、側邊選單、內容區，`dragover`
全部被 `preventDefault()`，也就是放不下去。

改成只有 `dataTransfer.types` 含 `Files` 時才攔截。
全日交通量 v20.47 本來就是這樣寫的（`types.includes("Files")`），本版與它對齊。

### 二、進度守門測試的取樣方式改掉

`scripts/e2e-progress.mjs` 原本每 10ms 輪詢一次畫面。短命的畫面狀態
用輪詢會漏掉，改成 MutationObserver 全量記錄（輪詢保留做保險）。

## 突變測試

| 對程式做的破壞 | 結果 |
|---|---|
| 全域防呆改回不判斷是否為檔案 | ❌ 3 項紅字（輸入框、側邊選單、內容區） |

也重新驗證了 GPT 在 v2.1.48 加入的修正確實是有用的：

| 情境 | 結果 |
|---|---|
| 判讀後整理階段注入例外，**保留** `try/finally` | ✅ 按鈕回到「選擇檔案」可按，進度列收掉 |
| 同上，但**拿掉** `try/finally` 的保證 | ❌ 按鈕卡在「正在解析… 1／1」停用，只能重新整理頁面 |

### 關於 GPT 新增的「序號與檔名一致」守門檢查

把 v2.1.47 的序號錯位寫法改回去之後，那一項**仍然 3/3 全綠**——
用 GPT 的 10ms 輪詢是如此，改成 MutationObserver 全量記錄也是如此。

追查後確認原因：那個錯位狀態被設定之後，在同一個 `setTimeout(0)` 週期內
就被下一份檔案的狀態覆蓋，React **從來沒有把它提交到 DOM**。
在該狀態刻意停留 120ms 之後重測，守門檢查立刻紅字
（抓到 `2/12→檔1`）——證明檢查本身是對的，只是那一格畫面在正常速度下
不存在。

因此：GPT 描述的「短暫出現第 2／12 份卻顯示第 1 份檔名」在本機**重現不出來**；
該修正屬於防禦性強化（若單檔交接變慢就會顯現），保留是合理的，
但不應被記述為使用者實際看得到的缺陷。

## 驗證結果

| 項目 | 結果 |
|---|---|
| ESLint | 通過，0 錯誤 |
| 結構／發布測試 | 52/52 通過 |
| TypeScript／計算測試 | 210 通過、0 失敗、0 略過 |
| 固定計算黃金值 | 通過 |
| 瀏覽器端到端測試 | 17 支腳本全過 |
| 真實資料回歸 | 37 份工作簿與 v2.1.45 逐位元相同（37/37） |

> 關於略過數：GPT 的 v2.1.48 報告寫「207 通過、3 略過」，本報告寫
> 「210 通過、0 略過」。兩邊都正確——那三項需要外部 Excel 樣本，
> 交付原始碼包不含它們；本機備有 37 份真實工作簿，因此**實際執行並通過**：
> 「前導空白不得讓平日與假日被合併成一筆」「表頭寫非內建車種時要保留原名」
> 「表頭殘留舊值時仍要靠欄位位置救回來」。

## 獨立重建核對

以 v2.1.48 原始碼在本機重新 `npm run build:github`，產出的主資產
SHA-256 為 `7f3c944515df1f02afa9682480e52b9a42120afb72cba8fbd077c98297b7fa0e`，
與 GPT 報告的線上資產雜湊**完全相同**——建置是可重現的。

## 證據界線

本包尚未推送，因此沒有 GitHub Actions run 編號，也沒有線上網址的核對結果。

`npm audit` 仍回報 5 項建置期相依弱點，與前幾版相同，本輪未強制升級。
