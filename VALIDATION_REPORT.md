# Turning Traffic v2.1.48 驗證報告

驗證日期：2026-09-05
基準：Claude 交付的 v2.1.47 候選包
前一正式版：v2.1.45（GPT 發布，提交 `0650c2e2d8935809f6d328a9ee33a8663465010b`）

## 結論

**本版沒有變更尖峰挑選、OD 加總、左右直判定、車種歸類、車種當量、PCU、
駛入／駛出或報表計算。** `LAST_CALC_CHANGE_VERSION` 維持 `v2.1.30`，
固定計算黃金值通過。

## 本次修正

### 零、GPT 獨立複查修正

大量檔案依序判讀時，每份讀完後原本會先增加序號，卻仍保留上一份檔名，
造成短暫出現「第 2／12 份：第 1 份檔名」；最後一份亦可能短暫超過總份數。
本版讓序號、檔名與總份數一致，並補上端對端守門檢查。另以 `finally` 保證
非預期例外也會解除載入狀態，避免選檔按鈕永久停用。

### 一、拖曳時看得出來丟得中

上傳卡片自舊版起就接得到 `drop`，但畫面上**完全沒有變化**：使用者拖到一半
看不出來「丟這裡對不對」，只能賭一把放開。現在拖曳經過時整張卡片會亮起來
（邊框、底色與外光暈一起變），放開就收掉。

進出深度記在該區塊自己的 `dataset` 上，用計數而不是布林旗標：拖曳經過區塊
內部的子元素時，子元素的 `dragenter` 會比父元素的 `dragleave` 晚送達，
用布林會讓高亮一閃一閃。

### 二、備份還原也吃拖曳

那一張卡原本只能按按鈕選檔。現在可以直接把備份檔（ZIP 或 JSON）拖進去；
一次拖多個會取第一個並在通知裡講清楚用了哪一個。

### 三、檔案掉在放置區外面不會把使用者帶離頁面

以前沒有任何全域攔截：檔案掉在卡片旁邊時，瀏覽器預設會直接開啟那個檔案，
等於把使用者踢出系統畫面。現在全域擋掉並讓游標顯示「不可放置」；
放置區內部（`[data-dropzone]`）照常放行。

### 四、判讀大量檔案時看得到進度

使用者回報：「上傳了大量的檔案後，因為沒有『讀取中』等提示文字，
會誤以為沒上傳成功。」舊版判讀中只有按鈕文字換成「正在解析…」，
一動也不動，檔案一多、跑上十幾秒就分不出「還在跑」和「當掉了」。

現在按鈕顯示「正在解析… 3／12」，上傳卡片上另有一行寫出正在讀哪一個檔名，
並且每讀完一份就 `setTimeout(0)` 讓瀏覽器有機會重畫。判讀中按鈕也會停用，
避免重複觸發。

### 五、一支「鎖住實作長相」的測試，順手改成驗行為

`tests/cross-system-guards.test.ts` 有一則斷言原始碼含有
`disabled={!importPeriodReady}`——那是**某一種實作的長相**，不是行為。
本版為了避免使用者在判讀途中重複觸發匯入，把它改成
`disabled={!importPeriodReady || importing}`（條件只有變嚴格），
那則斷言就紅了。

這正是同一支測試在另一則裡已經記過的教訓（v2.1.45 的 rocYear 那次：
「守門測試從防止分歧變成鎖住分歧」）。現已改成只要求 disabled 條件裡
必須用到 `importPeriodReady`，並且仍然不可以退回只看 `importPeriod`。

## 突變測試

新增 `scripts/e2e-drop.mjs` 與 `scripts/e2e-progress.mjs`，都已接進 `npm run e2e`。

| 對程式做的破壞 | 結果 |
|---|---|
| 拿掉高亮（`setDragZone` 不做事） | ❌ 2 項紅字（匯入卡片、備份還原卡片都不會亮） |
| 拿掉全域防呆 | ❌ 「檔案掉在放置區外面…被擋掉」紅字 |
| 備份還原卡片拿掉 `onDrop` 的檔案處理 | ❌ 「拖進來的備份真的被當成備份讀進去」紅字 |
| 進度回到舊版（只有按鈕「正在解析…」） | ❌ 5 項紅字 |
| 讀完忘了收掉進度列 | ✅ 全綠 — **預期**：JSX 條件是 `importing && importProgress`，`importing` 轉 false 時整行本來就會消失，屬程式本身的冗餘 |

`e2e-drop.mjs` 撰寫過程中移除了一項「游標顯示可以放（`dropEffect === "copy"`）」：
合成的 `DragEvent` 上量不到真實的 `dropEffect`（一律回 `"none"`），
驗了只會變成假檢查。原因寫在該檔註解裡。

備份還原那一項刻意改成用**系統自己匯出的備份檔**來測，不自己捏一份 JSON——
捏出來的東西只會走到「格式不符」的錯誤路徑，那樣就只驗到「drop 有被接到」，
沒驗到「拖曳還原真的會還原」。

## 驗證結果

| 項目 | 結果 |
|---|---|
| ESLint | 通過，0 錯誤 |
| 結構／發布測試 | 52/52 通過 |
| TypeScript／計算測試 | 207 通過、0 失敗、3 略過 |
| 固定計算黃金值 | 通過 |
| 瀏覽器端到端測試 | 16 支腳本全過（新增 `e2e-drop.mjs`、`e2e-progress.mjs`） |
| 新手手冊 | PDF 30 頁 |
| 手冊副本 | 根目錄、`public/`、`github-pages-dist/` 三份雜湊一致 |

## 證據界線

本包尚未推送，因此沒有 GitHub Actions run 編號，也沒有線上網址的核對結果。

三項略過測試依賴未隨原始碼包交付的外部 Excel 樣本，分別檢查平日／假日
前導空白分頁、非內建車種表頭，以及殘留舊表頭的欄位位置救援；因此不能把
它們記為已通過。其餘測試（包含固定計算黃金值）均已實際執行並通過。

`npm audit` 仍回報 5 項相依套件弱點（4 moderate、1 high），與 v2.1.45／46 相同，
本輪同樣沒有為消除提示而強制升級核心相依。這 5 項都在建置期相依裡，
不會進到發布出去的 bundle。
