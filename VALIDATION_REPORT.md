# Turning Traffic v2.1.26 驗證報告

驗證日期：2026-08-26（Asia/Taipei）

## 驗證結論

本次維護版已完成乾淨安裝、單元測試、瀏覽器端到端測試、版面測試、程式碼檢查、正式建置及安全檢查，可作為 GitHub Pages 正式發布版本。本次未變更既有交通量計算規則，也未加入 LOS／HCM 計算。

## 已通過項目

- 使用 npm 11.6.2 執行 `npm ci`，乾淨環境安裝成功。
- 自動測試共 152 項，全數通過：32 項模組測試與 120 項 TypeScript 測試。
- 瀏覽器端到端測試全數通過：轉向圖、圖卡拖曳、駛入／駛出箭頭、七叉路口壓力測試、報表匯出、平假日切換、歷季趨勢、備份還原、季度鎖定及例外檢查。
- 640、760、820、900、1024、1100、1180、1280、1350、1440、1680、1920 像素寬度的主要頁面版面檢查均無裁切或 JavaScript 錯誤。
- `npm run lint` 通過，無錯誤與警告。
- GitHub Pages 正式建置成功；入口檔只引用本版五個雜湊資產。
- 正式環境相依套件安全檢查為 0 項弱點。
- 完整相依套件檢查有 4 項中度風險，均位於僅供開發建置使用的 drizzle-kit／esbuild 鏈，未進入公開網站成品；未採用會造成相容性風險的強制降版。
- v2.1.26 新手手冊 PDF 共 43 頁，已逐頁轉圖檢查，未見文字重疊、裁切或空白頁。

## 本次維護重點

- 修正 GitHub Pages 重複部署流程，保留單一 `main` 分支根目錄發布來源；自訂 Actions 僅負責建置與測試。
- 補上 `.nojekyll`，並清除舊版雜湊資產與舊版手冊，避免固定檔名或 CDN 快取造成版本誤判。
- 統一 npm 鎖定檔與相依版本，並將 SheetJS 升至官方 0.20.3，確認可由乾淨環境重建。
- 修正 React 狀態相依、舊備份欄位別名與程式碼檢查問題，不改變交通量計算公式。
- 更新 v2.1.26 新手操作手冊、README、更新紀錄與維護說明。

## 已知限制

- 本機未安裝 LibreOffice，因此 DOCX 未能使用 LibreOffice 轉圖進行視覺檢查；同一來源產生的 PDF 已逐頁檢查，DOCX 仍保留供使用者編輯。
- `.openai/hosting.json` 仍由 `vite.config.ts` 匯入，屬建置相依檔，必須保留；本版仍不部署 GPT Site。
- 線上版本判斷必須使用帶版號的手冊路徑、`?v=2.1.26` 或本版專屬雜湊資產；不可只依固定路徑的 `package.json`、`app.js` 或未加版本參數的首頁判斷。

## 正式發布原則

- 正式網站：`https://baotoast.github.io/Turning-Traffic/?v=2.1.26`
- 發布來源：GitHub `main` 分支根目錄。
- GPT Site 已退役，本版不部署 GPT Site。
