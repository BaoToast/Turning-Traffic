# Turning Traffic v2.1.39 複查與發布驗證報告

日期：2026-08-31

複查基底：GitHub `main` 的 v2.1.37（`7f9e3df`）

Claude 交付來源：`路口轉向_v2.1.38.zip`

## 結論

v2.1.38 對檔名、站號來源、匯入警告、非數值儲存格與說明文字的修正方向正確。複查另外找到三項發布前問題，正式發布版因此推進為 **v2.1.39**：

1. 候選包把 SheetJS 從官方 CDN 0.20.3 降回 npm registry 0.18.5。已恢復 0.20.3，並加入發布結構守門測試。
2. 全日路段分向車種表若檔名只有站號，畫面可能顯示「非路口轉向」，原因卻仍叫使用者改檔名解除「參考計算檔」判定。已讓原因依最終角色產生，並加入反例測試。
3. 連續出現相同提示時，前一則提示的舊計時器可能提早關閉後一則。已改用獨立提示序號；手動關閉亦會使舊計時器失效。

本版沒有變更尖峰選擇、交通量、PCU 當量、轉向分類或路口總量；`LAST_CALC_CHANGE_VERSION` 維持 **v2.1.30**。

## 驗證結果

| 檢查 | 結果 |
| --- | --- |
| 乾淨安裝 | 通過；實際安裝 SheetJS 0.20.3 |
| ESLint | 通過，0 error |
| TypeScript `tsc --noEmit` | 通過，0 error |
| vinext 正式建置 | 通過 |
| GitHub Pages 建置 | 通過 |
| `tests/*.test.mjs` | **46/46 通過** |
| `tests/*.test.ts` | **190/190 通過** |
| 計算黃金值 | 與固定的 v2.1.37 基準逐車種數量、尖峰起訖及 PCU 完全一致 |
| 完整瀏覽器 E2E | 全部通過，0 JavaScript 例外 |
| 多寬度排版 | 640～1920px、17 個分頁全部通過 |
| Word 手冊 | 34 頁逐頁檢查，無裁切、重疊或孤立末頁 |
| PDF 手冊 | 52 頁逐頁檢查，無裁切、重疊或頁尾超界 |

瀏覽器流程涵蓋轉向圖、多岔路圖卡拖曳、駛入／駛出切換、多車種與當量、Excel 報表、結論草稿、流量核對、資料別、備份還原、成果鎖定、歷季趨勢、日期／期別與月份顯示。

## 安全稽核

`npm audit` 回報 4 項中度提醒，均位於開發工具鏈；正式 GitHub Pages 靜態網站不載入這些開發套件。建議的強制自動修復會造成不相容降版，因此未執行 `npm audit fix --force`。目前為 **0 high、0 critical**。

## 驗證邊界

本次附件只有程式與說明，沒有另附真實交通調查 Excel。本報告因此不宣稱重新逐格核對特定實際調查檔；能確認的是固定計算黃金值、程式產生的工作簿及完整瀏覽器流程均通過，且本版沒有改動既有計算口徑。

## 發布要求

- 只發布 GitHub／GitHub Pages。
- 正式網站必須引用 v2.1.39 的內容雜湊資產。
- v2.1.39 PDF／DOCX 手冊必須可由帶版號 URL 下載。
- 線上版本只以帶版號手冊、`?v=` 查詢參數及新版雜湊資產驗證，不以可能被快取的固定檔名判斷。
