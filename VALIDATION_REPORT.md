# Turning Traffic v2.1.28 驗證報告

驗證日期：2026-08-27（Asia/Taipei）

## 驗證結論

v2.1.28 **沒有變更任何交通量、PCU、駛入／駛出、尖峰搜尋或車種組成的計算規則**，也沒有加入 LOS／HCM 計算。畫面上改變的行為都與數字無關：

1. 鎖定衝突的判斷（決定要不要顯示紅字警告）。
2. 轉向圖「駛入」半格的支線標籤，改為與「駛出」半格同一種取法。
3. 右上角季度選單不再顯示沒有作用的「尚無季度」選項。
4. 較新版本建立的鎖定資料，在較舊版本開啟時會明確提示無法確認相容性。
5. 舊備份的自訂車種在結論草稿與車種組成頁使用相同顯示名稱。

第 1～3 項是本次交付內容，第 4～5 項是增量複查補上的相容性邊界；**五項都不影響任何交通量或 PCU 數值**。

## 已通過項目

- 以 npm 10.9.4 執行乾淨安裝成功，實際安裝官方 SheetJS 0.20.3。
- `npm run lint`：0 錯誤、0 警告。
- 自動測試 **171/171** 通過：模組測試 44 項、TypeScript 測試 127 項。
- 瀏覽器端對端測試 **9 支**全數通過，涵蓋轉向圖、支線名稱與季度選單、完整流向箭頭、圖卡拖曳、報表、草稿、調查型態、版面、備份還原與季度鎖定。
- 640、760、820、900、1024、1100、1180、1280、1350、1440、1680、1920 像素寬度的全部主要頁面均無橫向溢出或 JavaScript 例外。
- 七叉路口獨立壓力測試可不帶參數直接執行；14 張圖卡連續拖曳 300 步，無頁面崩潰或 JavaScript 例外。
- GitHub Pages 建置成功，正式雜湊資產已由本版乾淨安裝環境重新產生並納入發布內容。
- 正式環境相依套件安全檢查為 0 項弱點。
- 完整相依套件檢查的 4 項中度風險均位於僅供開發建置的 drizzle-kit／esbuild 鏈，沒有進入公開網站成品；未採用可能破壞相容性的強制修復。
- v2.1.28 新手手冊 PDF 共 45 頁，未見空白頁；封面與每一頁頁尾皆為 `v2.1.28 ｜ 2026-08-27`。

## 發布結構守門

`tests/release-structure.test.mjs` 現有 12 項檢查：

1. 版號在程式、套件、手冊產生器與下載連結一致。
2. 更新紀錄的最新一則等於程式版號。
3. 手冊封面與頁尾版號、日期一致。
4. 本版手冊存在且舊版手冊已移除。
5. `.nojekyll`、`.gitignore`、`.github`、`.openai/hosting.json` 等點號檔案完整。
6. 自訂 workflow 只測試、不發布，維持單一 GitHub Pages 發布路徑。
7. 套件管理只使用 npm 與 `package-lock.json`。
8. 根目錄沒有上一版殘留的雜湊資產。
9. 七叉路口壓力測試未傳參數時會使用 `github-pages-dist`，不再因未定義路徑中止。
10. 測試種子與依名稱選取的腳本裡不得出現實際的計畫、站號或路口名稱。
11. 必須有 `.gitattributes` 且關閉換行轉換。
12. 結論草稿必須共用完整車種名稱 fallback，不可另寫不完整版本。

## 已知限制

- 本機未安裝 LibreOffice，因此 DOCX 未使用 LibreOffice 轉圖檢查；DOCX 與已逐頁檢查的 PDF 由同一份 `manual.html` 產生。
- `.openai/hosting.json` 仍由 `vite.config.ts` 匯入，是建置相依檔，必須保留；GPT Site 已退役，本版不部署 GPT Site。
- 線上版本判斷必須使用 `?v=2.1.28`、帶版號的手冊路徑或只存在於本版的雜湊資產，不可只依固定路徑判斷。

## 正式發布原則

- 正式網站：`https://baotoast.github.io/Turning-Traffic/?v=2.1.28`
- 發布來源：GitHub `main` 分支根目錄。
- 自訂 GitHub Actions workflow 僅負責建置與測試，不執行 Pages 發布。
- GPT Site 已退役，本版不部署 GPT Site。

## 守門檢查的實測（v2.1.28）

只加會通過的檢查等於沒加檢查，所以每一項都在**未修正的狀態**下實際跑過：

| 新增的檢查 | 在舊版下的結果 |
| --- | --- |
| 發布結構第 10 項（種子不得有實際名稱） | 把實際計畫名稱貼回種子產生器 → 紅字 |
| 發布結構第 11 項（`.gitattributes`） | 刪掉該檔 → 紅字 |
| 鎖定狀態單元測試 7 項 | 舊的字串相等比較與未檢查未來版本 → 6 項紅字 |
| `e2e-lock-review.mjs` 新增 2 項 | 舊版剛鎖定完就出現鎖定衝突 → 紅字 |
| `e2e-arm-name.mjs`（本版新增） | 未修正的 v2.1.27 建置 → **4 項紅字**：選單裡仍有「尚無季度」、選它之後季度被拉回最新一季（這正是使用者說的「沒任何作用」）、駛入那半沒有新名稱、駛入與駛出兩半用的標籤集合不同（駛入＝A｜B｜C｜D，駛出＝B｜C｜D｜示範北路北側入口） |

還原後全部通過。
