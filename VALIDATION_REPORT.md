# Turning Traffic v2.1.35 複查與驗證報告

日期：2026-08-30
複查來源：Claude 交付的 `TurningTraffic_Source_v2.1.34.zip`
來源 SHA-256：`7B73B2536B3CE38D073E1AB8B07AE2D8E2F180DF6B2E51573309DC0D25EE93AA`（與交付清單一致）
比較基底：GitHub `main` 的 v2.1.33（commit `e2d8804`）

## 一、複查結論

Claude 的 v2.1.34 已完成「顯示 × 時段 × 車種」摘要同步、第五種顯示模式、單一車種 PCU、調查日期與期別檢查，以及期別月份顯示。乾淨安裝後，原交付內容的既有測試均可通過；但進一步以不同比例的車種與轉向當量建構反例，發現兩項新功能缺陷，因此修正並推進為 **v2.1.35**。

本次沒有改動已確認的尖峰選擇、轉向分類、當量係數、路口總量或四捨五入規則；`LAST_CALC_CHANGE_VERSION` 維持 `v2.1.30`。

## 二、發現並修正的問題

### 1. 單一車種 PCU 曾以整條支線比例反推

v2.1.34 的 `pcuBreakdown()` 先取得整條支線左／直／右的總 PCU 比例，再用比例分配各車種數量。當不同車種走不同轉向，且轉向當量不同時，個別車種 PCU 會被分錯；所有車種合計甚至可能剛好等於已存總 PCU，使 `reconciled` 對帳無法察覺。

守門反例：機車 10 輛左轉（當量 0.5）與小型車 10 輛直行（當量 1.0）。正確機車 PCU 應為 5.0，原邏輯得到 3.7。

修正後：

- 有 `routes` 的紀錄逐筆依「實際車種數 × 該 OD 的實際轉向當量」加總。
- 圖與右側摘要使用同一組 OD、同一份 `record.pceUsed`、同一支 `pceFactor()`。
- 沒有 OD 明細的舊備份才保留比例估算，並繼續顯示對帳警告。
- 新增 `tests/pcu-breakdown.test.ts`，釘住上述反例。
- `scripts/e2e-diagram-summary.mjs` 新增單一車種 PCU 圖面與摘要的數值及單位一致性檢查。

### 2. 日期格式正確但曆日不存在時仍會通過

v2.1.34 可讀民國與西元日期格式，但只檢查年月日字樣，未驗證該日期是否真的存在，因此「民國 115 年 2 月 29 日」與「115 年 4 月 31 日」會被接受。

修正後：

- `lib/period-date.ts` 新增實際曆日驗證。
- 民國 115 年 2 月 29 日與 115 年 4 月 31 日會被拒絕。
- 民國 113 年 2 月 29 日可正確辨識為 2024-02-29。
- 無效或讀不到日期仍只提醒、不阻擋匯入，維持既定產品規則。

## 三、未改動的計算與資料邊界

- 尖峰小時仍由連續 60 分鐘資料依既定範圍選取。
- 左轉、直行、右轉分類與人工確認結果未改。
- 車種轉向當量矩陣與匯入時保存方式未改。
- 路口總量、各支線駛入／駛出與守恆關係未改。
- 全日尖峰與全日時段的可用性判斷未改。
- Excel、PDF、PNG、備份與成果鎖定格式未改。
- 調查日期仍不參與資料覆蓋鍵值；讀不到日期不阻擋匯入。

## 四、驗證結果

### 依賴與來源

- 於乾淨目錄執行 `npm ci` 成功。
- 實際載入 `xlsx 0.20.3`，未以替代版套件跳過試驗。
- v2.1.34 原始 ZIP 的 SHA-256 與使用者提供的清單一致。

### 自動測試

| 項目 | 結果 |
| --- | --- |
| `tests/*.test.mjs` | 45/45 通過 |
| `tests/*.test.ts` | 167/167 通過 |
| `npm run lint` | 通過 |
| `npm run build` | 通過 |
| `npm run e2e` | 全部端對端腳本通過 |
| 單一車種 PCU 圖／摘要 | 9,841.3 = 9,841.3，單位一致 |
| 日期符合／不符／未知流程 | 全部通過；未知日期不阻擋 |
| 版面、備份、鎖定、報表與匯出 | 全部既有守門檢查通過 |

### 文件視覺檢查

- v2.1.35 DOCX：33 頁，逐頁檢查完成。
- v2.1.35 PDF：49 頁，逐頁檢查完成。
- 未發現文字截斷、重疊、缺字、表格超界或頁首頁尾錯版。

## 五、版號說明

因複查後實際修改了程式邏輯，不能仍沿用 Claude 的 v2.1.34；正式版推進為 **v2.1.35**。以下項目均已同步：

- `package.json` 與 `package-lock.json`
- `lib/traffic.ts` 的 `VERSION` 與版本紀錄
- `CHANGELOG.md`
- 網站顯示與手冊下載連結
- PDF／DOCX 檔名、封面與頁尾
- 建置與 GitHub Pages 發布內容

## 六、建議 Claude 複查重點

1. `pcuBreakdown()` 有 `routes` 時是否完全依實際 OD 車種與轉向加總，而非再以支線比例反推。
2. 沒有 `routes` 的舊備份是否仍可讀，且估算值對不起來時會顯示警告。
3. `parseSurveyDateText()` 與 `periodOfDate()` 是否拒絕不存在的曆日、接受正確閏日。
4. 日期未知時是否仍不阻擋匯入。
5. v2.1.34 的摘要同步、第五種顯示、期別月份切換與既有 v2.1.33 Excel 修正是否完整保留。
6. `LAST_CALC_CHANGE_VERSION` 維持 v2.1.30 是否符合版本鎖定與衝突提示的既定規則。
