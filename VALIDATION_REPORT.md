# Turning Traffic v2.1.29 驗證報告

驗證日期：2026-08-27（Asia/Taipei）

## 驗證結論

v2.1.29 **沒有變更任何交通量、PCU、駛入／駛出、尖峰搜尋或車種組成的計算規則**，也沒有加入 LOS／HCM 計算，也沒有變更任何操作方式。本版只有兩處程式變更：

1. **修正 v2.1.28 新增的「被更新版本鎖定」分支。** `isVersionAtLeast()` 在版號解析不出來時回傳 `false`，用在這條新分支會反過來——`VERSION` 只要帶了 `-final`、`-rc1` 這種尾巴或多一個空白就解析不出來，於是**每一筆 v2.1.21 以後鎖定的資料全部亮紅字**（實測 19×19 版號矩陣多出 344 筆），正是 v2.1.28 要修掉的症狀，而且訊息會說反。改為兩個版號都解析得出來才判斷。
2. **四大車種名稱兩張表對齊。** `app/traffic-app.tsx` 的 `VEHICLE_LABELS` 與 `lib/traffic.ts` 的 `CORE_VEHICLE_LABELS` 對 heavy／special 寫法不同，而匯入時寫進紀錄的是後者，所以 v2.1.19 以前的舊備份會落到前者，跨季出現兩種寫法。以匯入時實際寫入的那一組為準。（`PCE_LABELS` 是不同畫面，刻意未動。）

以下是 v2.1.28 的變更，一併沿用：鎖定衝突的判斷、轉向圖「駛入」半格的支線標籤、右上角季度選單移除「尚無季度」死選項、舊備份自訂車種的顯示名稱一致。

## 已通過項目

- `npm run lint`：0 錯誤、0 警告。
- 自動測試 **172/172** 通過：模組測試 **45 項**（需先 `npm run build`，因為其中一項要讀 `dist/server/index.js`）、TypeScript 測試 **127 項**。
- 瀏覽器端對端測試 **11 支**腳本、**308 項**檢查全數通過，0 個 JavaScript 例外；涵蓋轉向圖、支線名稱與季度選單、完整流向箭頭、圖卡拖曳、報表、草稿、調查型態、版面、備份還原與季度鎖定。

> ⚠️ **v2.1.28 驗證報告寫「171/171」，那個數字對交付出去的那一包不成立。**
> 該包的中文檔名沒有標記 UTF-8，解壓後變成 `#Uxxxx`，發布結構檢查第 4 項因此紅字（44 項中 2 項失敗）；
> 而 `npm test` 是 `lint && build && …mjs && …ts` 用 `&&` 串的，前面的 `.mjs` 一失敗就整條中斷，
> **127 項 TypeScript 測試根本沒有跑到**。那是打包的問題，不是程式的問題：檔名還原後 44/44 全過。
> **線上網站不受影響**（使用者實測手冊下載正常），受影響的只有交付出去的備存 ZIP。
> 本版已改用會標記 UTF-8 的打包方式，並且是看最後的 `# fail 0` 才記錄結果。
- 640、760、820、900、1024、1100、1180、1280、1350、1440、1680、1920 像素寬度的全部主要頁面均無橫向溢出或 JavaScript 例外。
- 七叉路口獨立壓力測試可不帶參數直接執行；14 張圖卡連續拖曳 300 步，無頁面崩潰或 JavaScript 例外。
- ⚠️ **本包不含 `index.html`、`assets/`、`dist/`、`github-pages-dist/`，需要發布者自行建置。** 本版由 Claude 在一個連不到 `cdn.sheetjs.com` 的環境完成（403），裝到的是 npm 上的 0.18.5；在那裡建置出來的網站會把 SheetJS 悄悄降回舊版而且看不出來，因此不交建置產物。上述測試與端對端檢查是在該環境以 0.18.5 跑的。
- 正式環境相依套件安全檢查為 0 項弱點。
- 完整相依套件檢查的 4 項中度風險均位於僅供開發建置的 drizzle-kit／esbuild 鏈，沒有進入公開網站成品；未採用可能破壞相容性的強制修復。
- v2.1.29 新手手冊 PDF 共 45 頁，未見空白頁；封面與每一頁頁尾皆為 `v2.1.29 ｜ 2026-08-27`。

## 發布結構守門

`tests/release-structure.test.mjs` 現有 **13** 項檢查：

1. 版號在程式、套件、手冊產生器與下載連結一致。
2. 更新紀錄的最新一則等於程式版號。
3. 手冊封面與頁尾版號、日期一致。
4. 本版手冊存在且舊版手冊已移除。
5. `.nojekyll`、`.gitignore`、`.github`、`.openai/hosting.json` 等點號檔案完整。
6. 自訂 workflow 只測試、不發布，維持單一 GitHub Pages 發布路徑。
7. 套件管理只使用 npm 與 `package-lock.json`。
8. 根目錄沒有上一版殘留的雜湊資產。
9. 七叉路口壓力測試未傳參數時會使用 `github-pages-dist`，不再因未定義路徑中止。
10. 測試種子與依名稱選取的腳本裡不得出現實際的計畫、站號或路口名稱。
11. 必須有 `.gitattributes` 且關閉換行轉換。
12. 結論草稿必須共用完整車種名稱 fallback，不可另寫不完整版本。
13. 四大車種的名稱在 `app/traffic-app.tsx` 與 `lib/traffic.ts` 兩邊一致。

## 已知限制

- 本機未安裝 LibreOffice，因此 DOCX 未使用 LibreOffice 轉圖檢查；DOCX 與已逐頁檢查的 PDF 由同一份 `manual.html` 產生。
- `.openai/hosting.json` 仍由 `vite.config.ts` 匯入，是建置相依檔，必須保留；GPT Site 已退役，本版不部署 GPT Site。
- 線上版本判斷必須使用 `?v=2.1.29`、帶版號的手冊路徑或只存在於本版的雜湊資產，不可只依固定路徑判斷。

## 正式發布原則

- 正式網站：`https://baotoast.github.io/Turning-Traffic/?v=2.1.29`
- 發布來源：GitHub `main` 分支根目錄。
- 自訂 GitHub Actions workflow 僅負責建置與測試，不執行 Pages 發布。
- GPT Site 已退役，本版不部署 GPT Site。

## 守門檢查的實測（v2.1.29）

只加會通過的檢查等於沒加檢查，所以每一項都在**未修正的狀態**下實際跑過：

| 新增的檢查 | 在舊版下的結果 |
| --- | --- |
| 發布結構第 10 項（種子不得有實際名稱） | 把實際計畫名稱貼回種子產生器 → 紅字 |
| 發布結構第 11 項（`.gitattributes`） | 刪掉該檔 → 紅字 |
| 鎖定狀態單元測試 7 項 | 舊的字串相等比較與未檢查未來版本 → 6 項紅字 |
| `e2e-lock-review.mjs` 新增 2 項 | 舊版剛鎖定完就出現鎖定衝突 → 紅字 |
| **發布結構第 13 項（車種名稱兩表一致）** | v2.1.28 的兩張表 → **紅字** |
| **鎖定狀態版號解析** | 把 `VERSION` 設成 `v2.1.28-final` 跑 19×19 版號矩陣 → v2.1.28 多出 **344 筆**誤報紅字，v2.1.29 為 0 |
| `e2e-arm-name.mjs`（v2.1.28 新增） | 未修正的 v2.1.27 建置 → **4 項紅字**：選單裡仍有「尚無季度」、選它之後季度被拉回最新一季（這正是使用者說的「沒任何作用」）、駛入那半沒有新名稱、駛入與駛出兩半用的標籤集合不同（駛入＝A｜B｜C｜D，駛出＝B｜C｜D｜示範北路北側入口） |

還原後全部通過。
