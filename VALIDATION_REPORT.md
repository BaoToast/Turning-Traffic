# Turning Traffic v2.1.63 風險導向複查報告

驗證日期：2026-09-10
基準：Claude 提供的 `路口轉向_完整專案_v2.1.62.zip`（SHA-256：`181FE9ACE578E327808705D228846485FEA53037DF52F74343E6A8BCB9C30A74`）。

## 結論

本次涉及共用瀏覽器儲存、歷季趨勢顯示與 Excel 匯出，判定為中高風險並採完整資料流與全套回歸複查。GPT 獨立發現並修正九項缺陷後，版本推進至 v2.1.63；交通計算公式及既有確認口徑未變，`LAST_CALC_CHANGE_VERSION` 維持 `v2.1.30`。

## GPT 額外修正

1. 可編輯趨勢 Excel 的圖表標題、Y 軸與數字格式改為跟隨所選指標，不再固定顯示 PCU/hr。
2. 趨勢圖卡狀態與右側季度摘要改用所選指標的數值及單位。
3. IndexedDB 寫入改為等待交易真正完成後才回報成功，避免請求成功但交易尚未提交的競態。
4. 儲存權限被封鎖時，不再武斷宣稱電腦沒有資料。
5. README 上午／下午尖峰搜尋範圍改為與程式一致的 00:00–12:00、12:00–24:00。
6. 長期趨勢圖寬度上限設為 4,800 px，避免超大型 canvas；120 季時抽樣文字標籤但保留全部資料點及折線。
7. 跨計畫「全部」模式依實際資料別分線，不再只假設平日／假日。
8. 手冊 HTML 標題及成品版號更新為 v2.1.63。
9. 新增百分比 Excel 圖表、120 季圖寬／標籤與 IndexedDB 交易提交等回歸測試。

## 驗證結果

| 項目 | 結果 |
|---|---|
| SHA-256 與 ZIP 路徑安全 | Claude v2.1.62 收件檔全部相符；未發現絕對路徑或上層跳脫 |
| 乾淨環境安裝 | `npm ci` 成功，518 packages |
| ESLint | 通過，0 錯誤 |
| TypeScript | `tsc --noEmit` 通過 |
| Production build | vinext build 與 GitHub Pages build 均通過 |
| 結構／發布測試 | 53/53 通過 |
| 運算／解析／資料契約測試 | 250 通過、0 失敗、3 條件式略過 |
| 固定計算黃金值 | 通過，既有逐車種車輛數與尖峰 PCU 未變 |
| 瀏覽器 E2E | 27 支腳本全部通過 |
| Excel OOXML | 18 個 part、1 張圖表，結構合規 |
| 多寬度版面 | 18 個功能頁於 640–1920 px 全部無橫向溢出 |
| 新手手冊 | PDF 31 頁逐頁視覺檢查通過；DOCX 結構與副本一致性通過 |
| Production dependency audit | 0 個已知漏洞 |

三項略過測試需要交付包外的真實附件：`11535T1502...xls`、`11535T1503...xls` 與 `11017T1501...xlsx`。因此本報告明確列為略過，不列作通過；對應的合成測資與其餘解析回歸均已通過。

完整 audit 另列 10 項開發／建置工具鏈警示（4 moderate、6 high），來自 drizzle-kit、Wrangler/Cloudflare、fast-uri、js-yaml、sharp/esbuild 等鏈；正式執行依賴為 0。未使用會引入破壞性升級的 `audit fix --force`。

## 資料流一致性

- 輸入／解析：原始與新格式、多車種、平假日、日期與期別、15/60 分混合格警告、全日／不足 24 小時均有回歸。
- 計算：OD 守恆、逐車種當量、連續 60 分鐘尖峰、駛入／駛出、全日範圍與黃金值均通過。
- 顯示：轉向箭頭、3–7 岔路口、長期趨勢、資料別分線、選定指標數值與單位均通過。
- 匯出：Excel、圖表、批次成果、結論草稿、缺值斷線與所選指標單位均通過。
- 儲存：IndexedDB 遷移、大型資料、交易提交、連續存檔、備份還原、版本鎖定均通過。

## 已知界線

- 混合時間格仍依既定決策「只警告、不改計算」。
- 長期趨勢超過可讀密度時只抽樣文字標籤，全部季度資料點、折線及 Excel 原始資料仍完整保留。
- 本機缺少文件技能指定的獨立 LibreOffice 執行環境，因此 DOCX 未另做點陣化視覺渲染；PDF 31 頁已逐頁檢視，DOCX 已通過 OOXML、版號及副本一致性測試。

## 發布證據

發布提交、GitHub Actions、Pages 新版雜湊資產及帶版號手冊的線上 SHA-256，於正式發布完成後記錄於交付資料夾的 Claude 交接說明及 SHA-256 清單。
