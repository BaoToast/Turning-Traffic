# Turning Traffic v2.1.42 獨立複查與發布驗證報告

- 複查日期：2026-09-01
- 前一正式版：v2.1.40（commit `b77ed8f33cf232cd23b8b71d1d040a269d22fdcf`）
- 發布目標：`BaoToast/Turning-Traffic` 的 GitHub Pages（`main`／根目錄）
- 本版涵蓋：未單獨發布的 v2.1.41，以及 v2.1.42

## 結論

本次獨立複查未發現需要改寫 v2.1.42 程式邏輯的新缺陷。Claude 提供的候選包有兩項預期中的發布前缺口：根目錄仍是 v2.1.40 網站建置檔，且 v2.1.42 手冊尚未放入正式發布位置；兩項均已在本次複查環境完成重建與同步。

固定計算黃金值通過，`LAST_CALC_CHANGE_VERSION` 維持 **v2.1.30**。v2.1.41／v2.1.42 沒有改變既有尖峰挑選、轉向分類、車種當量或 PCU 計算口徑。

## 本版功能與修正邊界

1. 平日／假日工作表名稱前後有空白時，仍能分開匯入，不再落入整份合併解析。
2. 匯出轉向圖檔名帶資料別，避免平日與假日圖互相覆寫。
3. 非內建車種保留成自訂車種，由使用者決定當量或歸類；只有表頭確為內建四車種時才允許位置救援。
4. 全形數字可正確讀取；合法橫線佔位符按 0 處理但不誤報為壞資料。
5. 無法計算的統計範圍顯示與匯出為「－」／空白，不偽裝成 0。
6. 季度輸入接受民國或西元年，但資料鍵一律正規化為民國年。
7. 頁首可切換民國／西元顯示；畫面、結論草稿、Excel、SVG／PNG／PDF 圖說跟著切換，儲存值、排序與計算不變。

## 本次實際執行的驗證

| 項目 | 結果 |
|---|---|
| 附件 ZIP SHA-256 | `b4bb92dac6862804cd22fa4b2a8e0081b6322023d6ca75d7ef60c89b35fdcae6`，與附件 checksum 相符 |
| 乾淨安裝 | 使用 lock 檔與官方 SheetJS 0.20.3 完成 |
| ESLint | 0 錯誤 |
| 一般建置 | 完成 |
| `.mjs` 結構／發布測試 | **48/48 通過** |
| TypeScript／計算測試 | **202 通過、0 失敗、3 跳過**；跳過項目需要本包未附的特定真實工作簿 |
| TypeScript `tsc --noEmit` | 0 錯誤 |
| 固定計算黃金值 | 通過；逐車種車輛數與尖峰 PCU 基準未變 |
| 瀏覽器端對端測試 | 全部腳本通過，0 JavaScript 例外 |
| 多寬度排版 | 640、760、820、900、1024、1100、1180、1280、1350、1440、1680、1920 px 全分頁通過 |
| 年份切換全分頁掃描 | 西元模式沒有殘留民國季度；切換前後數值完全相同；切回後畫面還原 |
| Word 手冊 | 由 `manual.html` 重建，共 **36 頁**；LibreOffice 轉圖後逐頁巡查，未見裁切、重疊或缺頁 |
| PDF 手冊 | 由 `manual.html` 重建，共 **53 頁**；Poppler 逐頁轉圖後巡查，未見裁切、重疊或缺頁 |
| GitHub Pages 建置 | 使用官方 SheetJS 0.20.3 重建，主資產為 `assets/index-CoBGwvYb.js` |

## 證據界線

Claude 的原說明記載曾以 37 份真實調查表、跨系統比對與手寫七叉路口驗算進行驗證；那些原始工作簿沒有包含在本次 v2.1.42 交付附件中，因此本報告**不把該段當成本次獨立重跑的結果**。本次能獨立確認的是：候選包內測試、合成工作簿、固定黃金值、完整瀏覽器操作、建置檔與手冊皆通過上述檢查。

三個依賴真實工作簿的測試保持跳過，不以合成資料冒充真實檔驗證，也不據此宣稱已重新核對 37 份檔案。

## 發布檔案

- 根目錄 `index.html` 已指向 `assets/index-CoBGwvYb.js`。
- 舊版 `assets/index-YKetotIB.js` 與 v2.1.40 手冊已自發布根目錄與 `public/` 移除。
- v2.1.42 Word／PDF 手冊已放入根目錄與 `public/`。
- `.nojekyll`、`.github`、`.openai` 等隱藏項目維持原狀。

發布後仍須以帶版號手冊、首頁 `?v=v2.1.42` 與本版雜湊資產交叉確認線上內容；固定檔名不作為唯一版本依據。
