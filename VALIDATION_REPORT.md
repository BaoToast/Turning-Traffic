# Turning Traffic v2.1.31 驗證報告

日期：2026-08-28

## 一、本版做什麼

本版承接 v2.1.30 新增的「全日時段」與「全日尖峰小時」，發布前再次人工複查圖表與多車種文字。

v2.1.31 修正四項發布前複查問題：

1. 歷季趨勢「整體」模式已畫入全日尖峰第三條線，但 v2.1.30 缺少它的獨立顏色、圖例、右側摘要與正確檔名。現在 AM、PM、DAY 三個數列一致呈現。
2. 多車種品質訊息的「四車種合計」改為「各車種合計」，避免對第五種以上自訂車種產生誤解。
3. 單一計畫備份篩選紀錄時，補上舊資料可能沒有 `projectId` 的型別防護；既有篩選結果不變。
4. 原始列追溯欄位正式納入共用 `IntervalRow` 型別，避免解析器與型別定義不同步；數值計算不變。

**v2.1.31 沒有變更任何計算口徑**；下列口徑變更仍是 v2.1.30 的紀錄：

**⚠️ 本版有變更計算口徑**（前幾版都沒有）：

1. **上午／下午尖峰的搜尋範圍**：上午 `[05:00, 12:00)` → `[00:00, 12:00)`，
   下午 `[12:00, 23:00)` → `[12:00, 24:00)`。舊的兩段合起來掃不到
   23:00–24:00 與 00:00–05:00。實測使用者 8 份實際調查檔，
   **新舊口徑算出來的上午／下午尖峰完全相同**。
2. **每一格的車輛數先四捨五入成整數再進入所有計算**。部分調查檔的儲存格
   存的是小數，Excel 只是顯示成整數。實測只影響儲存格本來就有小數的那一份
   （06525T2503：24,463.3 → 24,451.2 PCU、27,988.8 → 27,985 輛）。

因此 `LAST_CALC_CHANGE_VERSION` 推進到 v2.1.30，v2.1.30 以前鎖定的季度會亮出
鎖定衝突。使用者已確認接受。

## 二、與全日交通量系統的交叉驗算

兩套系統的轉向 PCU 當量係數逐格相同。用**全日交通量自己的尖峰演算法**
（`app/partial-day.ts` 的 `rollingPeak`）與它的係數，對三份 24 小時調查檔
重新算一次，與路口轉向 v2.1.30 比對：

| 檔案 | 全日時段 PCU | 全日時段 輛 | 全日尖峰 時段 | 全日尖峰 PCU | 全日尖峰 輛 |
| --- | --- | --- | --- | --- | --- |
| 06525T2501 | 10,469.5 ✅ | 10,779 ✅ | 07:00-08:00 ✅ | 1,133.8 ✅ | 1,310 ✅ |
| 06525T2502 | 33,174.7 ✅ | 28,166 ✅ | 07:00-08:00 ✅ | 3,881.7 ✅ | 3,436 ✅ |
| 06525T2503 | 24,451.2 ✅ | 27,985 ✅ | 17:00-18:00 ✅ | 2,494.7 ✅ | 3,026 ✅ |

**15 項全部一致。**

⚠️ 一項尚未一致：全日交通量沒有「每一格先四捨五入」這一條
（`app/traffic-parser.ts` 是 `Number(cell) || 0`）。要不要一併補上，
是產品決策，留給使用者。

## 三、守門檢查的實測（對未修正的 v2.1.29）

新測試用的函式在 v2.1.29 根本不存在，直接跑只會「模組沒有這個匯出」——
那不算證明。另外寫了一支**只用兩版都讀得到的欄位**的行為檢查：

| 檢查 | v2.1.29 | v2.1.30 |
| --- | --- | --- |
| 24 小時調查算得出全日尖峰小時 | ❌ `DAY=null` | ✅ |
| 全日尖峰挑到 03:00 | ❌ | ✅ |
| 上午尖峰掃得到 03:00 | ❌ 挑到 05:00 | ✅ |
| 下午尖峰掃得到 23:00 | ❌ 挑到 12:00 | ✅ |
| 只有 4 小時時不報全日尖峰 | ⚪ 空的通過（舊版沒有這個概念） | ✅ |
| 每一格先四捨五入：全日車輛數是整數 | ❌ `13.830599999999992` | ✅ |
| 逐格資料也是整數 | ❌ | ✅ |
| `0.36→0`、`5.5506→6` | ❌ `13.8306` | ✅ `6` |

**8 項裡 7 項在未修正版紅字。**

## 四、測試結果

| 項目 | 結果 |
| --- | --- |
| 模組與 TypeScript 測試 | **190 項全過**（模組 45 ＋ TypeScript 145） |
| 其中 `tests/full-day-scope.test.ts` | 18 項 |
| `npm run lint` | 0 錯誤、0 警告 |
| `npm run e2e` | 11 支腳本、**308 項全過**，0 個未處理例外 |
| `tsc --noEmit` | 應用程式新增的 2 項型別錯誤已修正；仍有 3 項既有 Cloudflare Worker 環境型別缺漏（`cloudflare:workers`、`Fetcher`、`D1Database`），不影響靜態 GitHub Pages 建置 |
| `npm audit --omit=dev` | **0 項正式環境弱點**；完整開發依賴仍有 4 項 moderate，來源為 `drizzle-kit` 的舊版開發鏈，官方建議修法會造成破壞性升版，本版未強制套用 |
| 手冊 | v2.1.31 PDF／DOCX 已重新產生，舊版已刪除 |
| 使用者 8 份實際調查檔 | 只留在測試環境，**沒有進交付包** |

## 五、既有資料相容

| | 既有的舊資料 | 重新匯入後 |
| --- | --- | --- |
| 全日時段 | 直接就有（從已存的調查總量現算） | 有 |
| 全日尖峰小時 | 「－（舊版匯入，請重新匯入原始檔）」 | 有 |
| 上午／下午尖峰 | 維持匯入當時的數字，**一個都不動** | 依新口徑重算 |

載入舊備份時 `ensureRecordScopes()` 自動補上空的 DAY／FULL。
補出來的是**空值不是 0**——畫面靠 `peaks.DAY.start` 是不是空字串，
分辨「調查不足 24 小時」與「舊資料要重新匯入」兩種不同的「－」。

## 六、發布注意

- Claude 提供的原始來源包沒有夾帶舊的建置網站；本次已在乾淨依賴環境重新執行
  `npm ci && npm run build:github`，正式 GitHub Pages 根目錄與交付包均放入
  v2.1.31 建置成品。產出的主 bundle 已確認帶有 **SheetJS 0.20.3**。
- 線上版本判斷請使用 `?v=2.1.31`、帶版號的手冊路徑，或只存在於本版的雜湊資產。
- 正式網站：`https://baotoast.github.io/Turning-Traffic/?v=2.1.31`
- `.openai/hosting.json` 不可刪除（`vite.config.ts` 會匯入它）。
