# Turning Traffic v2.1.40 複查與發布驗證報告

日期：2026-08-31

複查基底：GitHub `main` 的 v2.1.39（`2a223957982e40b8a588dad039caec0e17356c0e`）

Claude 交付來源：`路口轉向_v2.1.40.zip`

## 結論

Claude 對「製表日期被誤認為調查日期」的修正方向正確。複查發現候選實作仍有兩條日期選取路徑：匯入紀錄先取最前面的日期字樣，期別檢查則另用 `findSurveyDate()`。因此，較前方的普通日期或無效日期仍可能成為紀錄日期，與期別檢查選到的日期不一致。

正式 v2.1.40 已統一成同一套日期選擇規則：

1. 匯入紀錄日期、資料別與日期／期別檢查全部共用 `findSurveyDate()`。
2. 明確標示且有效的調查日期優先；沒有明確標示時才退回第一個有效日期。
3. 排除製表、列印、報告、出圖、填表、核定、審查、校核、繪製、修正、更新、彙整、輸出、建檔、產製等非調查日期文字。
4. 無效日曆日期不會被選為調查日期。

本版沒有變更尖峰選擇、交通量、PCU 當量、轉向分類或路口總量；`LAST_CALC_CHANGE_VERSION` 維持 **v2.1.30**。

## 驗證結果

| 檢查 | 結果 |
| --- | --- |
| 乾淨安裝 | 通過；518 packages |
| ESLint | 通過，0 error |
| TypeScript `tsc --noEmit` | 通過，0 error |
| vinext 正式建置 | 通過 |
| GitHub Pages 建置 | 通過 |
| `tests/*.test.mjs` | **46/46 通過** |
| `tests/*.test.ts` | **191/191 通過** |
| 日期回歸測試 | 非調查日期、普通日期、無效日期在前時，仍選到後方明確標示的有效調查日期 |
| 計算黃金值 | 固定的 v2.1.37 逐車種數量、尖峰起訖及 PCU 基準完全一致 |
| 完整瀏覽器 E2E | 全部通過，0 JavaScript 例外 |
| 多寬度排版 | 640～1920px、17 個分頁全部通過 |
| Word 手冊 | 35 頁逐頁檢查，無裁切、重疊或內容超界 |
| PDF 手冊 | 52 頁逐頁檢查，無裁切、重疊或頁尾超界 |

瀏覽器流程另實測日期在期別內、日期與期別不一致、取消／確認寫入、讀不到日期但不阻擋、月份顯示等情境，全部通過。

## 安全稽核

依賴安裝回報 4 項中度提醒，均位於開發工具鏈。未執行可能造成不相容版本變動的強制自動修復；正式 GitHub Pages 為靜態建置，不載入開發伺服器套件。

## 驗證邊界

本次附件只有程式與說明，沒有另附真實交通調查 Excel。本報告不宣稱重新逐格核對 Claude 說明中提到的實際檔案；能確認的是固定計算黃金值、合成工作簿、完整瀏覽器流程均通過，且本版沒有改動既有計算口徑。

## 發布驗證要求

- 正式網站必須引用 v2.1.40 的內容雜湊資產。
- v2.1.40 PDF／DOCX 手冊必須可由帶版號 URL 下載。
- 線上版本只以帶版號手冊、`?v=` 查詢參數及新版雜湊資產驗證，不以可能被快取的固定檔名判斷。
