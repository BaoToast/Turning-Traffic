# Turning Traffic v2.1.37 複查與發布驗證報告

日期：2026-08-30

複查基底：GitHub `main` 的 v2.1.35（`7b252a7`）

Claude 交付來源：`TurningTraffic_Source_v2.1.36.zip`

來源 ZIP SHA-256：`6A75FAF0E297734371D69DE214F6670359918F43FF07BE44022B611A6F39D5DA`

## 結論

Claude v2.1.36 的主要修正方向正確：

- 摘要的單一車種「車輛數」改回使用 `row.vehicle`，與轉向圖同源。
- 單一車種 PCU 保持依實際 OD、車種及左／直／右當量逐筆計算。
- 資料來源不一致時，以 `reconciled` 與原因文字提示，不默默改寫任一來源。
- v2.1.35 的實際 OD 拆解與曆日驗證完整保留。

複查另外找到兩項問題並修正，因此正式發布版推進為 **v2.1.37**：

1. **不同車種的差異可能互相抵銷。** v2.1.36 只比較全部車種總數；例如各車種欄的機車比 OD 多 7 輛、小型車比 OD 少 7 輛，兩邊總數仍相同，甚至總 PCU 也可能相同，系統會誤判為一致。本版改為逐車種比較，每一車種各自沿用「5 輛或 5%」容差，並新增反例守門測試。
2. **乾淨原始碼無法完成 TypeScript 驗證。** 原始碼使用 Cloudflare Worker／D1 型別，但套件清單與 TypeScript 設定未載入對應型別。本版加入與現有 Cloudflare 外掛相符的官方 `@cloudflare/workers-types@5.20260825.1`，並以 `worker-configuration.d.ts` 宣告實際使用的 `DB` 綁定。這是開發／備存完整性修正，不改公開網站的交通計算。

`LAST_CALC_CHANGE_VERSION` 維持 **v2.1.30**；本版沒有更改已確認的尖峰、OD、PCU 或流向分類規則。

## 逐項技術核對

### 車輛數與 PCU 來源

| 項目 | 正式規則 | 結果 |
| --- | --- | --- |
| 摘要車輛數 | `row.vehicle`，與轉向圖 `totalMovement()` 同源 | 通過 |
| 單一車種 PCU | 實際 OD 車種數 × 該車種左／直／右當量 | 通過 |
| 舊備份沒有 `row.vehicle` | 才退回 OD 車種數，避免整張摘要為 0 | 通過 |
| PCU 對帳 | 已存路口 PCU 與 OD 推導 PCU，比較 1% 或 0.5 PCU 容差 | 通過 |
| 車輛數對帳 | 每一車種各自比較 `row.vehicle` 與 OD，使用 5 輛或 5% 容差 | 通過 |
| 多項不一致 | PCU 與車輛數原因完整並列 | 通過 |

### v2.1.37 新增反例

測試刻意建立以下狀態：

- `row.vehicle`：機車 17、小型車 3，共 20 輛。
- OD：機車 10、小型車 10，共 20 輛。
- OD 推導 PCU 與已存總 PCU 也相同。

若只核對總車數與總 PCU，會錯誤得到「一致」。v2.1.37 會逐車種找出兩項差異，`reconciled` 正確為 `false`，並在畫面提示使用者先至「流量核對工作台」確認。

## 完整測試結果

| 檢查 | 結果 |
| --- | --- |
| ESLint | 通過，0 error |
| TypeScript `tsc --noEmit` | 通過，0 error |
| vinext 正式建置 | 通過 |
| GitHub Pages 建置 | 通過 |
| `tests/*.test.mjs` | **45/45 通過** |
| `tests/*.test.ts` | **173/173 通過** |
| 瀏覽器 E2E（16 段流程） | **364 項通過**，0 JS 例外 |
| 多寬度排版 | 640、760、820、900、1024、1100、1180、1280、1350、1440、1680、1920px 全分頁通過 |
| Word 手冊 | 34 頁逐頁檢查，無裁切、重疊或頁尾超界 |
| PDF 手冊 | 50 頁逐頁檢查，無裁切、重疊或頁尾超界 |
| SheetJS | 安裝與建置使用 **0.20.3** |

瀏覽器流程包含：多岔路圖卡與箭頭、駛入／駛出模式、車種當量與多車種、Excel 匯出、結論草稿、流量核對、平假日資料別、各頁排版、備份還原、成果鎖定、歷季趨勢、全日範圍、日期與期別檢查、月份顯示等。

## 套件安全稽核

`npm audit` 回報 4 項中度提醒，來源均為 `drizzle-kit` 開發工具鏈內的舊版 `esbuild`；公開 GitHub Pages 靜態網站不會載入這些套件。稽核建議的自動修復會把 `drizzle-kit` 降到不相容的舊主版，因此沒有執行 `npm audit fix --force`。目前為 **0 項 high、0 項 critical**。

## 驗證邊界

本次附件只有程式原始碼與說明檔，沒有另附實際交通調查 Excel，因此本報告沒有宣稱重新核對任何特定實際調查檔的數值。Excel 解析與交通計算已由既有測試資料、程式產生的工作簿及瀏覽器流程驗證；若未來要對某一份實際調查檔逐格對帳，仍應另附原始 Excel 與人工基準值。

## 發布放行條件

- 只發布 GitHub／GitHub Pages，不建立或回報 GPT Site。
- 正式網站必須引用 v2.1.37 的雜湊資產。
- v2.1.37 PDF／DOCX 手冊必須可由帶版號 URL 下載。
- 舊版 v2.1.36 手冊 URL 必須失效，避免固定檔快取造成版本誤判。
- 發布後以帶 `?v=2.1.37` 的頁面、版本手冊與實際資產 SHA-256 複核。
