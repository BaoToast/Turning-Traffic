# Turning Traffic v2.1.51 驗證報告

驗證日期：2026-09-06
來源：Claude 提供的 v2.1.51 完整原始碼與 GitHub Pages 更新包，經 GPT 獨立複查及發布前補正。

## 結論

v2.1.51 可發布。複查發現並修正兩項非計算問題：

1. 短時段資料只漏一列時，單次較長間隔可能剛好占兩成以上，原判準會誤報「混合時間格」。現要求候選格距至少出現兩次。
2. 轉向進階分析頁的「守恆差值」仍寫死 `PCU/hr`；選全日時段時改為 `PCU/調查日`。

兩項修正只影響警告判定與單位文字。尖峰挑選、連續 60 分鐘視窗、OD 加總、左／直／右分類、車種辨識與歸類、轉向當量、PCU、駛入／駛出及全日量均未變更；`LAST_CALC_CHANGE_VERSION` 維持 `v2.1.30`。

混合時間格仍依使用者決定維持「只警告、不改計算」。

## 收件完整性

- `SHA256SUMS.txt` 所列三個 v2.1.51 檔案均與實際 SHA-256 相符。
- ZIP 路徑檢查未發現絕對路徑或上層跳脫項目。
- 內附 SheetJS `vendor/xlsx-0.20.3.tgz` 維持固定來源與既有雜湊守門。

## 自動驗證

| 項目 | 結果 |
|---|---|
| ESLint | 通過，0 錯誤 |
| 正式建置 | 通過 |
| 結構／發布測試 | 52/52 通過，0 失敗，0 略過 |
| TypeScript／計算／資料契約測試 | 210 通過，0 失敗，3 項條件式略過 |
| 固定計算黃金值 | 通過，既有數值未變 |
| 瀏覽器 E2E | 18 支腳本全部通過 |
| Excel OOXML 結構 | 18 個 part、1 張圖表，0 項不合規 |

三項略過測試需要交付包之外的實際 Excel 樣本；因此本報告不把它們寫成通過。其餘測試皆有本次執行證據。

## 重點實測

- 匯入期間按鈕確實停用；停用時送出真實滑鼠點擊，不會再開第二個檔案對話框。
- 同一批 12 份檔案重複匯入後，仍為 12 筆紀錄、12 個路口，不會翻倍。
- 第一份檔案解析前確實經過兩次 `requestAnimationFrame`；背景分頁停用 rAF 時，250ms 退路仍可完成匯入。
- 批次 Excel 的全日時段欄位為 `PCU/調查日`／`輛/調查日`。
- 全站 18 個功能頁在 640～1920 px 多種寬度均無橫向溢出。
- 備份合併保留計畫隔離與審核狀態；季度鎖定會阻擋未確認成果，修改鎖定資料前會詢問。
- 日期與所選期別不一致時會列出來源儲存格並二次確認；無法辨識日期時只提醒、不阻擋。
- 短時段只有一次漏列形成的 30 分鐘跳號不再誤報；真正重複的 15／60 分鐘混合格距仍會警告。

## 已知界線

- 解析 Excel 仍在瀏覽器主執行緒進行；本版改善進度可見性與重複操作防護，沒有改成 Web Worker。
- 混合時間格只提出人工核對警告，尖峰挑選邏輯刻意不改。
- `npm audit` 仍有 5 項建置期相依警示（4 moderate、1 high）；本輪未做破壞性升級。

## 發布證據

本節於 GitHub 推送、Actions 完成及公開網址驗證後補入最終提交、工作流程與線上主資產雜湊。
