# Turning Traffic 驗證報告（累積，最新一段在最前面）

## 2026-09-26 GPT 高風險獨立複查與修正（v2.1.83）

本輪以 Claude 提供的 v2.1.83 完整專案 ZIP（SHA-256
`62BBE34914B3D5EF0D59C818960A1DEF65B289A6FA7517BDFEF1382B3F4B0E06`）與說明檔
（SHA-256 `3F4D18E9BC4E5BB977DFBE1833526C5DFC431C2CC05A98A2DA6CF7C72EE41B48`）為輸入，
依高風險規則檢查完整資料流。Claude 的「全綠」聲明不是本輪證據；原候選在本機實際出現
lint 錯誤與字面 `npm test` 記憶體不足，均在本輪修正後重新走完整門檻。

### GPT 獨立發現並修正

- `lengthMinutes` 在匯入預覽轉成持久化 `sourceIntervals.rows` 時遺失；舊紀錄格長回推雖已寫好，
  新匯入資料反而沒有保存。現已完整保留並加儲存契約測試。
- `survey.minutes` 仍以「列數 × 眾數格長」計算，混合 60＋15 分鐘格會錯；現改為逐格使用
  `lengthMinutes`，沒有時依序從 label 回推、最後才退回眾數，與 `rollingPeak()` 同一口徑。
- `ImportPreview.intervalRows` 與 `approachPeakBreakdown` 的列型別漏掉 `lengthMinutes`；已補齊，
  避免型別層再次丟失資料。
- 結論草稿同時含完整／部分涵蓋紀錄時，標題只列一個保守單位；現列出實際存在的全部
  FULL 單位，並說明每筆依自己的涵蓋決定。
- 車種組成、報告草稿、Excel 與兩處 UI 仍有寫死「調查時段」的漏網；已統一改走
  `compositionScopeUnit()`，並新增呼叫點守門。
- 候選的兩個 lint 錯誤（控制字元正規式、未使用 import）已修正。
- `tests/perf-scaling.test.ts` 為驗證測試框架，建立七份各 30 萬筆完整交通紀錄，造成標準
  Node heap 下字面 `npm test` OOM；改用輕量鍵值保留相同迴圈、資料規模與線性／平方反證，
  不放寬門檻。
- 同步更正與實作相反的註解；未變更 `IntervalRow.lengthMinutes` 的既定註解，也未放寬
  「湊不滿 60 分鐘即資料不足」規則。`LAST_CALC_CHANGE_VERSION` 維持 v2.1.83。

### 本輪可重現驗證

| 項目 | 本輪結果 |
| --- | --- |
| 乾淨依賴 | bundled Node v24.19.0 ＋ npm 11.6.2；`npm ci` 成功，added 509、audited 510 |
| 字面 `npm test` | 全部通過：lint、glyph guard、TypeScript、production build |
| `.mjs` tests | **255／255 通過，0 失敗，0 略過** |
| TypeScript tests | **371 項：367 通過、0 失敗、4 條件式略過** |
| `npm run e2e` | **83 支瀏覽器測試＋2 支種子資料產生器＝85 個循序步驟**，退出碼 0；不得稱為 85 支瀏覽器 E2E |
| `npm run e2e:tryout` | 通過；v2.1.83 單檔 HTML 以 `file://` 開啟，核心流程及資源載入正常 |
| 手冊 PDF | 20 頁以 Poppler 點陣化逐頁檢視，未見截字、重疊、表格溢出或空白頁；pypdf 可擷取 20 頁文字且無替代字元 |
| 依賴 audit | 10 項，4 moderate、6 high、0 critical；均在 dev/build 工具鏈，未執行破壞性的 `npm audit fix` |

根目錄 GitHub Pages 資產由最終原始碼重新建置並同步：主程式
`assets/index-CaAh6Y_H.js` SHA-256
`AB62AF30F2FF97235C2135BBEE06851F2777BAD309172CE259D5B9521FF9E88C`；
根目錄 `index.html` SHA-256
`8655912B24EFBD53FFBED45A935752E27EFD28A59EEC01B22F36F2E1C8E031A8`；
手冊 SHA-256
`E51EE7731BD15974E37CF2F9F1AE921C87D57B0B244C80046C0A3B6E8AEC041C`。

### 證據界線

- 4 項略過是需要特定真實附件的條件式測試；本候選包未附那些原檔，因此本輪沒有把它們
  描述成通過。合成資料與 E2E 已驗證混合格長、只寫起點的 fallback、判定方式切換與可逆性，
  但不能取代未提供的真實附件。
- 本輪已檢查 Excel 的 10 張工作表、OOXML、原生圖表結構與匯出流程，但沒有另以桌面版
  Microsoft Excel 開啟；不得把 v2.1.79 的 Office 證據冒充本輪結果。
- 本輪手冊 PDF 已獨立點陣化逐頁檢視；E2E 產生的正式分析 PDF 已驗證產生與流程，未另以
  桌面 PDF 閱讀器逐頁人工開啟。
- Claude 報告中的 15,152 字元是 `pdftotext` 算法；本輪 pypdf NFKC 為 14,846 字元。
  兩者抽取器不同，頁數與視覺內容均正常，故只記錄差異，不據此改寫歷史數字。

---

## 2026-09-25 第六輪獨立複查（版號維持 v2.1.83）

**沒有變更任何尖峰、PCU 或轉向計算。** 會變的是**畫面與匯出上的單位字樣**——
滿 24 小時的資料，原本寫「調查時段」的地方現在正確寫「調查日」；**數值一格都不變**。
本輪共 **31 件**（單位 **11** ＋ 測試把缺陷釘住 **3** ＋ 守門缺口 **6** ＋ 使用者看得到的文字 **7** ＋ 過期註解 **4**），逐件明細見隨交付附上的「待修正事項總表」P 區。

### 一、主線：一個預設參數養出 20 幾處單位不一致

`lib/traffic.ts` 的 `scopeUnit(scope, kind = "pcu", coverage = "unknown")`
有兩個預設值。第五輪抓到「轉向圖抬頭與下拉選單對同一批資料印出兩種單位」，
當時只修了下拉選單那一處——**因為那是當時看得到的那一處**。
第六輪把每一個呼叫點盤過，發現**其餘 20 幾處全都沒傳涵蓋**。

| 哪裡 | 原本印出 | 現在 |
| --- | --- | --- |
| 流量核對工作台（13 處 PCU ＋ 4 處車輛數） | 永遠「PCU/調查時段」 | 整頁算一次 `coverageOf(record)`，存成變數共用 |
| 三張匯出工作表的欄名 | 永遠「調查時段」 | 帶**整批**的涵蓋 |
| 轉向進階分析（頁首／守恆差值／OD 矩陣／各支線流量平衡） | 四處都是「調查時段」 | 算一次 `advancedCoverage` 共用 |
| 進階分析 Excel（OD 矩陣 ＋ 支線平衡） | 與畫面不一致 | 另算 `advancedExportCoverage`（來源是真正要匯出的那一筆） |
| 批次成果包 README | 「調查時段」 | 帶**整包**的涵蓋 |
| 各路口駛入／駛出流量的表頭 | 「調查時段」 | 帶那一筆的涵蓋 |
| 各路口尖峰彙總的表頭 | 「調查時段」 | 帶整張表的涵蓋（順手發現同一頁的篩選條件**抄了兩份**，已收斂成一份） |
| 「各支線駛入／駛出尖峰流量」的說明 | **寫死**「單位均為 PCU/hr」 | 走 `scopeUnit()`，而且問的是**這一區實際顯示的時段**（見下方⚠️） |
| `compositionScopeUnit()` | `SURVEY` **寫死**「輛/調查時段」 | 映到 `FULL` 去問同一支函式 |
| 異常訊息「總數不一致」 | 「調查時段」 | 帶那一筆的涵蓋 |
| 結論草稿 15 個勾選標籤 | 單位**寫死在標籤字面上** | 標籤只寫「是什麼量」，單位由草稿本文逐句寫出 |

⚠️ **匯出的欄名刻意帶「整批」的涵蓋，不是逐列算。**
Excel 的欄名就是物件的鍵：某幾列算出「調查日」、另幾列算出「調查時段」時，
`json_to_sheet` 會生出**兩組欄位**，同一種數字散在兩欄、加總全錯。
整批混合時 `coverageOf()` 回 `mixed`，單位落在「調查時段」——
那正是使用者 2026-09-10 定的「混合的表欄名統一用調查時段，表下方註明清楚」。

**處置**：`scopeUnit()`／`metricUnit()`／`buildMetricSeries()` 三支函式的參數
**全部改成必填**，由 TypeScript 一次列出 42 個呼叫點。
真的拿不到涵蓋的地方明寫 `"unknown"` 並寫一行為什麼。

⚠️ **補上涵蓋的時候，第一個參數（時段）也要問對。** 「各支線駛入／駛出尖峰流量」
那一句的第一版改成跟著主工具列的時段走——**那是錯的**：那一區的卡片**只列
上午尖峰與下午尖峰兩欄**，兩者都是「某一小時的流率」，與主工具列選到哪一個
時段無關。選「全調查時段」時那句話會寫「PCU/調查日」，而下面的卡片仍然是
每小時流率。已改成明確問「上午尖峰的單位」——既不寫死、也不會跟錯。
**這一件是我自己在複查修正結果時抓到的**，不是複查者提的。
其餘每一個呼叫點都逐一確認過「它問的時段就是那一塊實際顯示的時段」。

**守門**：
- `scopeUnit()` 的簽章**不可以再有預設值**（比對原始碼，不比對行為——行為上「有預設值」與「每個呼叫端明寫 unknown」完全一樣，看不出差別）
- `lib/conclusion.ts` 自己那一份單位規則（它刻意沒有任何 import）必須與 `scopeUnit()` **逐格相同**：4 範圍 × 4 涵蓋 × 2 量＝**32 格**
- 勾選標籤裡不可以寫死單位（半形與全形斜線都抓）

**反證（已實測）**：加回預設值 → 紅；改壞 32 格中的一格 → 紅；把單位寫回標籤 → 紅。

### 二、測試把缺陷釘住（三處）

| 哪一支 | 釘住了什麼 |
| --- | --- |
| `tests/full-day-scope.test.ts` | `assert.equal(scopeUnit("FULL"), "PCU/調查時段")` ——用正面斷言宣告「不傳涵蓋是合法用法」 |
| `tests/scope-source.test.ts` | `assert.match(page, /\{scopeUnit\(advancedPeak\)\}/)` |
| `tests/v170-features.test.mjs` | `assert.match(appSource, /const pcu = scopeUnit\(scope\);/)` |

三處都不只是「沒抓到缺陷」——它們讓**補上涵蓋這個正確的修正會變紅**，
而紅的時候看起來像是修正弄壞了東西。三處已改成釘正確形
（要求帶涵蓋，並禁止不帶涵蓋的寫法）。

⚠️ **請把「如果有人把這裡修對了，這一條會不會紅？」當成每一條正面斷言的檢查表。**

### 三、守門本身的六個缺口

| 守門 | 缺口 | 現在 | 反證 |
| --- | --- | --- | --- |
| 手冊按鈕名 | 整檔 `includes` 吃到 `VERSION_HISTORY` 與註解——**凡是曾經存在過的按鈕名都算存在** | 比對前切掉 `VERSION_HISTORY` 與全部註解，並斷言「真的切掉了、沒切光」 | 手冊寫「請按『跨計畫比較』」（2026-09-16 已整組移除）→ 紅 |
| 分頁名豁免清單 | ①留著 v2.1.64 改掉的舊時段名「全日」；②幽靈檢查同樣整檔 `includes`；③只掃 `traffic-app.tsx`，而時段名與異常類別名住在 `lib/traffic.ts` | ①移除；②切註解與 `VERSION_HISTORY`；③範圍擴到三個檔；④**短名字改整串比對** | 把「全日」放回清單 → 紅。⚠️ **第一版沒紅**：另一句正常的說明寫著「全日**整點**」，`includes` 照樣過 |
| 混用格長紅線 | 上一輪說是「概念級」，實際仍綁死**「只警告」三個字**；禁止清單用的是「不動計算、只給提醒」 | 「混合／混用時間格」＋**任一種「不改計算」的說法**，正反語序都抓 | 三種寫法各丟一次 → 各自紅 |
| 手冊章號交叉引用 | ①只掃手冊，而驗證報告與更新說明同樣寫「手冊第 N 章」；②只驗「章號存在」，抓不到「號碼存在但指錯章」；③樣式抓不到 `第 **18** 章` 這種粗體寫法 | ①範圍擴到四份文件；②加「指名主題必須落在對的章」；③樣式允許粗體 | 塞一個手冊裡不存在的章號 → 紅；把「成果交付」指到「認識畫面」那一章 → 紅；用粗體寫法把「備份」指到前一章 → 紅 |
| e2e 例外表 | 理由寫「由 `npm run e2e:tryout` 另外跑」，而 `package.json` **沒有那個 script**——那支腳本**從來沒有任何方式會跑到** | 補上 `e2e:tryout`；新增守門：理由裡指名的每一個 `npm run xxx` 必須存在，**而且那個命令真的會執行到那支腳本** | 拿掉 script → 紅 |
| 步驟數與「支」 | `PROJECT_HANDOFF` 自己寫著「不得把那 85 個步驟全部稱為瀏覽器 E2E」，而本報告的數字表寫的就是「e2e｜85 支」 | 改成「83 支瀏覽器測試 ＋ 2 支種子產生器＝85 個執行步驟」；新增守門（數字從實際數出來，禁止句本身不算違規） | 寫回「85 支」→ 紅 |

### 四、使用者看得到的文字（七件）

| 哪裡 | 問題 | 處置 |
| --- | --- | --- |
| 手冊第 20 章 | **兩個錯疊在同一句**：①叫使用者去「匯入紀錄」查匯入時間——那是**姊妹系統**的頁面，本支沒有，而且本支沒有任何畫面顯示 `importedAt`；②把版號分界換算成「2026 年 9 月 17 日」，而 v2.1.65 是 **2026-09-11** 發布的（差六天，那六天之間匯入的資料會被誤導成要重新匯入原始檔）。⚠️ **這一句是上一版新寫的** | 整句改成不依賴日期也不依賴版號的做法：先按「重新套用計算」看補不補得上 |
| 手冊第 14 章 | 「顯示」寫「PCU、車輛數、百分比，或兩者並列」——實際**五種**，第一種叫「交通量」不叫「PCU」，「兩者並列」其實是兩種不同模式 | 列出五種，並說明哪兩種是「輛」 |
| 手冊第 15 章 | 寫按鈕名叫「⧉ 複製說明文字」，而按鈕上**沒有那個圖示字元**（2026-09-15 已刻意移除，它不在 Big5） | 改寫並註明刻意不放圖示 |
| 全站的轉向名稱 | 標籤表有**三份**，`through` 一份寫「直進」、兩份寫「直行」，**兩種都印在畫面上**（OD 矩陣與匯出欄名「直行」，歷季趨勢指標名稱與匯入盤點訊息「直進」），手冊六處全寫「直行」 | 統一「直行」，`traffic-app` 那一份改成別名。⚠️ 解析原始檔的樣式**仍然同時接受**兩種寫法——那是別人填的調查表 |
| `pceIssues()` 的訊息 | 把**內部鍵**印給使用者：「機車／through＝abc（不是數字）」。車種那半早就查表了，轉向這半漏了 | 查 `MOVEMENT_LABELS`；查不到的鍵才退回原字 |
| 手冊、**畫面上的提示**、`lib/traffic.ts` 註解（三處） | 都叫使用者「按一下『重新套用計算』」，而**全站沒有那一顆按鈕**：實際是「車種轉向當量」頁的「用目前的設定重算（N 筆）」與當量套用範圍那一列的「用這一組重算」。使用者照著找會找不到，然後以為自己漏了一步 | 三處都改成寫出頁面與真正的按鈕名 |
| 4.5.1.3 的引用 | 把被引用的那句話講成**要求**，而它自己用的是「宜」——那是**建議**（2026-09-25 更正） | 改成「建議」 |

**新增守門**：
- 手冊不可以叫使用者去一個不存在的畫面（拿 `NAV` 的 `label` 當合法清單，掃「去／到／從／點開／切到『X』」的導引句型；只掃導引句型，含斜線的名字排掉——那是單位）
- ⚠️ 「手冊叫使用者按的按鈕，程式裡必須真的有」**再補強一次**：原本只驗「這串字出現在原始碼裡」，而「重新套用計算」之所以「找得到」，是因為它出現在一段**說明文字**裡——說明文字提到一個不存在的按鈕，正是這一支要抓的東西。現在另外抽出**真正的按鈕文字**（`<button>` 的內容，含三元式裡的字串字面值，加上側欄 `NAV` 的 label），要求被引用的名字是其中之一；只允許「後面接括號補述」的前綴相符（「重設所有圖卡位置（全部模式）」），不允許任意前綴。
  ⚠️ 抽取方式有一個坑：**不可以**用 `/<button[^>]*>/` 找開始標籤——標籤裡有箭頭函式（`onClick={() => …}`），`[^>]*` 會在 `=>` 的那個 `>` 就停下來，於是抓到的「內容」是程式碼；「產生草稿」那一顆就是這樣漏掉的。改成從 `</button>` 往回找，並跳過成對的大括號與字串。
  反證：把手冊改回那個不存在的按鈕名 → 紅並列出它。
- 手冊寫的「顯示」模式數量＝`DisplayMode` 聯集的成員數
- 每一份轉向標籤表都與 `lib` 那一份一致；`traffic-app` 不可以再自己寫一份
- `pceIssues()` 不可以印內部鍵（正面驗中文名，**反面**禁 `left`／`through`／`right`）
- 引用 4.5.1.3 時力度必須是「建議／鼓勵」

**反證（已實測）**：五支各改回去一次 → 各自紅並指名。

⚠️ **那一支守門的第一版是逐行掃的，抓不到跨行的句子**——
被引用的章節編號與那個過強的動詞在註解裡分在兩行（2026-09-25 更正），
逐行掃一個字都抓不到，是**反證沒有變紅**才發現的。
已改成先正規化換行再依句掃，並加負向後查（正解會寫「那是建議，**不是規定**」）。

### 五、過期註解（四處）

四處寫著「不傳的話會走安全預設（調查時段）」——三個參數已改成必填，
「不傳」在型別上就過不了。註解留著舊的可能性，下一個人會以為不傳是合法的省事寫法。

### 六、驗證過程本身抓到的兩件（都是假訊號）

| 什麼 | 怎麼發現的 | 處置 |
| --- | --- | --- |
| **e2e 的離開碼是假的綠** | 驗證腳本寫成 `npm run e2e 2>&1 \| grep … \| tail -30` 然後 `echo "EXIT=$?"`——`$?` 抓到的是管線**最後那個 `tail`**，永遠是 0。83 支瀏覽器測試在 **5 分鐘**內「跑完」並回報 `EXIT=0`，光看時間就不可能 | 改成 `npm run e2e > log 2>&1; echo "EXIT=$?"`（不接管線）後重跑，真的紅了 |
| **紅的原因是本輪的修正自己造成的** | `scripts/e2e-conclusion.mjs` 把結論草稿勾選框的字**寫死**成「各支線駛入流量（PCU/hr）」，而本輪正是把那個寫死的單位從標籤上拿掉；Playwright 找不到 label，等到 30 秒超時、整包中斷在第 2 支 | 那一支改成用**不含單位**的子字串找（「各支線駛入流量」），並斷言「剛好找到一個」——日後新增名字相近的指標會當場指名是哪一個字串。⚠️ 中間試過 `import lib/conclusion.ts` 用 key 取標籤（跑得起來，Node 22 預設剝型別），**已改回去**：交付包裡用 `node scripts/…` 直跑的腳本不應該押在新版 Node 才有的行為上 |

⚠️ **「太快通過」和「反證沒有變紅」是同一類的假的綠**，請一起放進你的檢查表。

### ⚠️ F1（在解開的交付包上重跑）抓到的一件

封關規則 F1：交付前的最後一個動作是把打包好的 zip 解開，在那一份上重跑全部測試。

`tests/rendered-html.test.mjs` 在**剛解開的包**上會拋 `ERR_MODULE_NOT_FOUND`
（3 支紅）。它驗的是**建置出來的 HTML**，需要 `dist/server/index.js`——
那是建置產物，`pack.sh` 刻意排除。`npm test` 本來就會先 `npm run build`，
所以正式流程沒問題；但單獨跑 `node --test tests/*.test.mjs` 的人會看到一個
看不懂的模組找不到錯誤，然後**以為交付包壞了**。

**處置**：改成先檢查檔案存在，不存在時丟一句**講得出該做什麼**的錯誤
（「請先跑 `npm run build`，或直接 `npm test`；`dist/` 是建置產物，刻意不在交付包裡」）。
⚠️ 刻意**不改成 skip**：這一支要驗的東西很重要，靜靜跳過就等於沒人守。
三支同一條。

**F1 在解開的包上實測的數字**（先 `npm run build`，這是文件寫的流程）：

| 項目 | 結果 |
| --- | --- |
| `glyph-guard` | 通過 |
| `node --test tests/*.test.mjs` | **252 支：252 通過／0 失敗／0 略過** |
| `node --test tests/*.test.ts` | **369 支：365 通過／0 失敗／4 略過**（略過的是需要真實路口檔的條件式測試，會自己印出原因） |
| `npm run e2e` | **離開碼 0、0 個紅字**（83 支瀏覽器測試 ＋ 2 支種子產生器＝85 個執行步驟） |

⚠️ F1 另外確認：解開的那一份與工作目錄**逐位元相同**（278 個檔案、0 個不同），
兩份解壓樹（驗證用／複查用）也完全相同。


### 七、驗證數字

| 項目 | 結果 |
| --- | --- |
| `npm run lint` | 0 error 0 warning |
| `npm run glyph-guard` | 通過（畫面符號全部在 Big5 安全名單內） |
| `npm run typecheck` | 通過 |
| `npm run build` | 通過 |
| `node --test tests/*.test.mjs` | **252 支 0 失敗** |
| `node --test tests/*.test.ts` | **369 支 0 失敗**（3 支需要真實檔的條件式測試會自己印出原因） |
| `npm run e2e` | **83 支瀏覽器測試 ＋ 2 支種子資料產生器＝85 個執行步驟**，全綠，離開碼 0，沒有並行 |
| 網站資產 | 重新 `npm run build:github` 並同步到發布根目錄；五個資產雜湊已換新 |
| 手冊 | v2.1.83 重新產生，**20 頁 / 15,152 字元**，包裡四個位置逐位元相同 |

⚠️ 上面那兩個數字請照這兩行自己重算，不要相信報告寫的字
（2026-09-25 第六輪新增：姊妹系統交通服務水準的報告曾經寫過一個
**用任何算法都重現不出來**的字數，而讀的人會拿它當「我手上這一本是同一本」的依據）：

```
pdfinfo 路口轉向程式手冊_v2.1.83.pdf | grep Pages
pdftotext -enc UTF-8 路口轉向程式手冊_v2.1.83.pdf - \
  | python3 -c "import sys,unicodedata; print(len(unicodedata.normalize('NFKC', sys.stdin.read())))"
```


⚠️ 依 `PROJECT_HANDOFF.md` 的要求：那 85 個步驟**不可以**全部稱為瀏覽器 E2E。

---

## 2026-09-25 第五輪獨立複查（版號維持 v2.1.83）

**沒有變更任何尖峰、PCU 或轉向計算。** 本輪改的是使用者看得到的文字、
一處單位不一致、交接文件的兩條過期紅線，以及六支守門的缺口。
逐件明細見 `【更新說明】請先讀我.txt` 最前面那一節。

### 一、會害使用者做錯事的四件

| # | 哪裡 | 問題 | 怎麼確認的 |
| --- | --- | --- | --- |
| 1 | 手冊第 6 章 | 叫使用者按一顆不存在的按鈕。那六個字**全包只有手冊那一行有**；實際按鈕是「選擇檔案」 | `grep` 全包命中數 1；`app/traffic-app.tsx` 的按鈕文字三態 |
| 2 | 手冊第 18 章 | 備份卡片名用的是程式**明文禁止**的講法（卡片抬頭是「備份本計畫」） | `assets/index-*.js` 裡那個字串 0 次；程式註解就寫著禁止 |
| 3 | 手冊導讀表與第 1 章 | 四處「第 N 章」指錯章（交報表、換電腦、備份、看數字） | 章號由 `<h2>` 現場算，逐列核對 |
| 4 | 手冊第 3 章 | 主工具列三個下拉各漏一個選項，**「資料別」漏掉的正是預設值**（平日＋假日並列） | `DAY_ORDER`／`PEAK_ORDER`／`DISPLAY_ORDER` 與 `DEFAULT_MAIN_FILTERS` |

### 二、單位：同一頁對同一批資料印出兩種

路口轉向圖「顯示」下拉的五個選項呼叫 `scopeUnit()` 時**沒有傳 coverage**，
於是 `FULL` 時恆寫「PCU/調查時段」，而同一頁的圖面走
`scopeUnit(diagramPeak, kind, coverageOf(diagramRecord))`，
滿 24 小時的檔寫「PCU/調查日」。

`PROJECT_HANDOFF` 的規則是「單位的唯一來源是 `scopeUnit()`；不得另寫一套判斷」。
三處都呼叫了同一支，但**傳的參數不同**——規則字面過關、畫面仍然不一致。
所以守門要守的是「呼叫端傳了什麼」。

**守門**：`tests/full-day-scope.test.ts` 新增一支，只看那個 `<select>` 的區塊，
要求每一個 `scopeUnit(…)` 都含 `coverageOf(`。
**反證（已實測）**：把其中一個改回不傳 → 紅並印出是哪一個呼叫。

同一類的文件問題：手冊名詞表只列「PCU/調查日」，
而不足 24 小時、混著不同涵蓋、或判斷不出來時寫的是「調查時段」
（預設刻意是後者：寫少不寫多，才不會把 4 小時的累計量誤讀成全日量）。
照手冊抄就會犯程式註解裡在防的那個錯。已補齊。

### 三、交接文件的兩條過期紅線

| 節 | 原本要求的 | 程式實際 | 為什麼特別貴 |
| --- | --- | --- | --- |
| 11（已放棄／禁止清單） | 要求這件事不必動到計算、加個提醒就好 | v2.1.83 起逐格採用自己的長度並列入異常清單（使用者 2026-09-24 拍板） | 它是寫給下一個人的**紅線**：照它做會把核心修正當成未經授權的口徑變更而要求還原。同一件事 6.2 節已經更正，**第 11 節漏了** |
| 6.3（統計範圍） | `FULL` 要有完整調查日，不足 24 小時只給破折號 | `fullDayUnavailableReason()` 只在「沒有記錄調查時數」時才回原因；涵蓋時數由 `scopeWindowLabel()` 寫出來 | 與同一節第一條（單位依涵蓋二選一）**自相矛盾**；這是 `DAY` 那件事的同類第二例，上一輪只修了 `DAY` |

**守門**：`tests/manual-matches-system.test.mjs` 的交接文件那一支——
混用格長那一條改成**概念級**（不再只擋一種字面寫法）；新增 `FULL` 兩條的禁止樣式與**正面斷言**
（只寫「不是舊規則」不算，要寫出正解）。
⚠️ 禁止樣式必須帶負向後查 `(?<!不)`，否則會把正解自己抓成違規——本輪實際發生過。
**反證（已實測）**：兩條各寫回去一次 → 各自紅並指名。

⚠️ 這一支守門**自己的說明也寫錯了**：它原本寫「只有 FULL 要求完整調查日，
DAY 不要求」——FULL 也不要求。守門的說明寫錯會讓下一個人照著改回去。已更正。

### 四、守門的六個缺口

| 守門 | 缺口 | 現在 | 反證 |
| --- | --- | --- | --- |
| 分頁名白名單 | 只掃 `<strong>「…」</strong>` 與「到／進／去「…」」兩種寫法，**按鈕（「按「…」」）完全不在射程內** | **新增**一支：手冊裡「按／按一下／點／點一下」＋「…」的字串，必須在 `app/`、`lib/` 的原始碼裡逐字出現（機械可算，不需要白名單） | 手冊寫回那顆不存在的按鈕 → 紅並列出 |
| 章節交叉引用 | **完全沒有守門**——章號與章名都是機械可算的 | **新增**一支：①「第 N 章」必須存在；②導讀表每一列指到的章，章名必須含具名關鍵詞 | 導讀表改回錯的章號 → 紅並印出「那幾章是什麼」 |
| 混用格長紅線 | 只擋一種字面寫法 | 概念級樣式 | 已實測 |
| 混用格長紅線（第六輪再補） | 說是概念級，樣式仍綁死「只警告」三個字；禁止清單用的是另一種說法，一個字都抓不到 | 「混合／混用時間格」＋**任一種「不改計算」說法**，語序正反都抓 | 三種寫法各丟一次 → 各自紅；連帶抓到交接文件的更正註記原句引用舊紅線，已改成轉述 |
| `FULL` 兩條 | 一條都沒守 | 禁止樣式＋正面斷言 | 已實測 |
| 分頁名白名單的豁免清單 | **反向不成立** | 豁免清單裡不可以有程式裡找不到的名字（那種名字會替手冊的舊名打掩護）。⚠️ 刻意**不**改成「每一個都必須被手冊引用」——手冊本來就不一定每個分區名都用引號提，那樣會產生假的紅（實測 18 個裡 13 個目前沒被引用） | 豁免清單塞一個不存在的名字 → 紅 |
| 一行空操作的 `replace` | 它剝的那種括號**手冊裡從來沒有過**（0 次命中），卻讓人以為手冊允許帶版號的歷史括號，而 `manual-version-log` 的規則是不允許——兩支守門對同一件事給了不同答案 | 整行拿掉 | — |

### 五、三處過期註解與一處指向包外檔案

- `app/traffic-app.tsx`：一段註解用舊時段名寫著「要有 24 小時才算得出來」，
  而它就貼在呼叫 `fullDayUnavailableReason()` 的那幾行上面——照它改會把對的改回錯的。
- `app/layout.tsx` 的 `description` 還寫著舊分頁名。
- `lib/traffic.ts`：`pceIssues()` 的註解還用**現在式**寫「它一個呼叫點都沒有、
  被 tree-shake 掉」——它已經接上線了（`pceMatrixIssue()` ＋ 兩個呼叫點，
  字串也在 `assets/index-*.js` 裡）。讀者會以為缺陷還在。
- `【更新說明】` 指向一份「給 GPT 的說明」，而那份檔案**不在這個 zip 裡**
  （與三個 zip 並列交付）。已註明它在哪裡、以及本包自己的數字寫在哪裡。

### 六、本輪重新產生的東西

`app/traffic-app.tsx`、`app/layout.tsx`、`lib/traffic.ts` 與手冊都改過，所以：

- `npm run build:github` 重新建置，並把 `github-pages-dist/` 的 `index.html`
  與 `assets/` **同步到發布根目錄**（兩邊逐位元相同，有測試釘住）；
- 手冊重新產生，**四個位置**（根目錄／`public/`／`github-pages-dist/`／
  跑過 build 的 `dist/client/`）一起換；
- `【更新說明】` 的五個資產雜湊與手冊雜湊全部換新。

⚠️ 發布時記得**刪掉舊雜湊的 `assets/` 檔案**，不要只是覆蓋。

### 七、驗證數字

| 項目 | 結果 |
| --- | --- |
| `tests/*.test.mjs` ＋ `tests/*.test.ts` | 在**解開的交付包**上重跑：**0 失敗**（略過的是需要真實調查檔的條件式測試，會自己印出原因） |
| `lint` / `typecheck` / `glyph-guard` / `build` | 全部通過 |
| e2e | **83 支瀏覽器測試 ＋ 2 支種子資料產生器＝85 個執行步驟**，全綠（⚠️ `PROJECT_HANDOFF.md` 明文要求不得把那 85 個步驟全部稱為瀏覽器 E2E——本報告上一版自己違反了這一條，2026-09-25 第六輪抓到） |
| 手冊 | v2.1.83，**20 頁**，重新產生 |

---

## v2.1.83 候選（2026-09-24，Asia/Taipei）調查格距與尖峰認定 ＋ 徹底檢查收尾


### 2026-09-24 第三輪獨立複查（F6）補記：兩處紀錄修正（程式未動）

**一、「本版網站資產」只列了一半的雜湊。**
`【更新說明】請先讀我.txt` 原本只寫主程式 `index-*.js` 與樣式表 `index-*.css`
兩個 SHA-256，而 `assets/` 裡實際有五個檔案（另三個：`html2canvas-*.js`、
`index.es-*.js`、`purify.es-*.js`）。列一半的清單，使用者沒有列到的那幾個
就無從核對。手冊也完全沒有雜湊，而它是使用者最常單獨下載的一個檔案。

已補齊五個資產 ＋ 手冊，並把 `tests/release-structure.test.mjs` 那一支守門改成：

- `assets/` 底下的**每一個**檔案都必須出現在清單裡（漏一個就紅並指名）；
- 必須有一組是 `.pdf`；
- 每一組都**實際 `createHash("sha256")` 重算**比對（原本也有重算，但範圍只有
  它自己抓到的那兩行）；
- 含「抓到 ≥ 3 組」的前置檢查，格式一改就要有人知道。
- 格式與姊妹系統全日交通量統一成「檔名一行、`SHA-256 = ` 一行」。

**反證（已實測）**：① 從清單裡拔掉 `purify.es-ChwZkWde.js` → 紅，訊息指名它；
② 把手冊雜湊改一位 → 紅，訊息指名手冊。

⚠️ 順帶說明我改格式時發生的事：舊守門是靠 `assets/<檔名>` 與雜湊**同一行附近**
的寫法抓的，我一改格式它立刻紅——那是它該有的反應，也順便證明它原本不是恆真的。

**二、「請以本次交付的 SHA256SUMS.txt 為準」會誤導。**
`SHA256SUMS.txt` 是**與三個 zip 並列交付**的（不在任何一個 zip 內），
算的是三個 zip 檔本身的雜湊。照原句去找，會在包裡找一個不在包裡的檔案。
已改寫成明確區分兩者用途。

**本輪未變更任何程式碼**，因此版號維持 v2.1.83（它仍是未發布的候選）。
⚠️ **2026-09-24 正名**：這一段原本標「v2.1.82 候選」，而下面 2026-09-23 那一段
**也叫 v2.1.82**——同一個版號兩份不同內容，正是本版在別處修掉的那個毛病，
只有這份報告沒跟上（2026-09-24 的 F6 獨立複查抓到）。09-24 這一批正名為 **v2.1.83**；
09-23 那一段維持原樣，它記錄的是當時實際發生的事。

⚠️ **這一版變更了計算口徑**，`LAST_CALC_CHANGE_VERSION` 由 `v2.1.64`
推進為 **`v2.1.83`**（使用者 2026-09-24 拍板，選項②）。
格長一致的資料**一個數字都不會變**；會變的只有格長混用、或格長組不成整小時
的資料——而那些原本都是錯的。

### 一、尖峰小時的認定：從「數格數」改成「累計分鐘數」

⚠️ **更正一句 Claude 先前講太滿的話。** 2026-09-23 的報告寫著
「路口轉向那一支已經寫對了」——**那句話只對一半**。實測結果：

- **2 小時一格、45 分鐘一格** → 本支確實**拒絕給數字**（回 `null`，畫面寫
  「資料不足」）。這一半是對的。
- **混用 60＋15 分鐘** → **本支也會挑錯**。它的逐格檢查比對的是**起點間距**
  （`r.start - slice[i-1].start !== intervalMinutes`），不是**格子長度**——
  所以 08:45 的 15 分鐘格接 09:00 的整點格、間距剛好 15 分鐘、檢查會通過，
  於是 50+50+50+100 ＝ 250 被當成 08:15–09:15 這一小時的量。

| 情境 | 舊版（眾數 60 ／眾數 15） | 新版 | 真值 |
| --- | --- | --- | --- |
| 全日整點 100，07–09 拆 15 分鐘每格 50 | 100 ／ 250 | **200**（不論傳哪個眾數） | 200 |
| 07–10 每 15 分 50，17–21 每小時 300 | — ／ 200 | **300** | 300 |
| 全部 15／20／30／60（四種對照組） | ✅ | ✅ **逐格相同** | — |
| 2 小時、45 分鐘一格 | 資料不足 ✅ | 資料不足 ✅ | — |
| 部分時段（中間有空檔） | ✅ | ✅ 相同 | — |
| 舊紀錄（時間欄只寫起點、格長未知） | ✅ | ✅ **退回眾數，行為相同** | — |

**根因**：`IntervalRow` 只存 `start`，**沒有存每一格自己的長度**，格長只有一個
整份檔案的眾數。本版新增 `lengthMinutes`（取自時間欄原始文字寫出的起訖），
並把視窗改成逐格累加它自己的長度、只接受剛好 60 分鐘；相鄰判準也跟著改成
「下一格的起點要等於上一格的起點＋上一格的長度」。

⚠️ `lengthMinutes` 未知時（v2.1.67 以前匯入、或時間欄只寫起點）**退回眾數**，
行為與改版前完全相同，不會誤殺。有一支測試釘住這件事。

⚠️ 修這一支時中途犯過一個錯並修掉：先加了一道「每格長度必須等於眾數」的檢查，
結果把**正確的視窗也擋掉了**（眾數 60 時那四格 15 分鐘的視窗被拒）。
累計分鐘數本身已經涵蓋這件事，那道檢查是多餘且有害的，已移除。

### 二、調查格距列進資料異常清單（使用者 2026-09-24 指定）

新增兩個 `QualityIssue` 類別：

- **調查格距異常**（有格子超過 60 分鐘）：敘述寫出實際格長、格數、總格數與
  1～3 格實例，解決方式明講「多半是時間欄位誤植」並引用手冊 4.5.1.3
  （15 分鐘是手冊鼓勵的做法，不是異常）。這同時解決了使用者提的第四點——
  以前只顯示「資料不足」，使用者不知道為什麼、也不知道該去看什麼。
- **調查格距混用**（兩種以上規律格長）：明講「不一定是錯」，不逼使用者去改
  一份沒有錯的檔。

匯入時也會提示，**不阻擋匯入**。判準逐格看它自己的長度，**不取眾數**——
48 格裡有 1 格誤植成 2 小時的話眾數仍是 60，那一格會被蓋掉。

### 三、順手修掉的兩件

1. **`sourceIntervals.rows` 的型別是另外抄的一份行內型別**，於是 `IntervalRow`
   新增欄位時它沒有跟上（讀得到值卻讀不出型別）。改成直接用 `IntervalRow`。
2. **`tests/survey-date-pick.test.mjs` 的守門寫成「這一類必須是聯集的最後一項」**
   （正規式結尾有分號），於是新增任何類別都會紅，而紅的理由是「你做對了」。
   同一個專案的 maintenance 守門註解早就寫過這個教訓，這裡漏了。已放寬成
   只驗「有登記」。
3. **`PROJECT_HANDOFF.md` 寫死「`LAST_CALC_CHANGE_VERSION` 必須維持 v2.1.30」**
   （GPT 指出），而程式早在 v2.1.64 就合法推進過——文件反過來要求把一個正確的
   值改回舊值。已改成不寫死數值、以原始碼為準。

### 四、發布資產

⚠️ 資產檔名與 SHA-256 是在**最後一次建置之後**重新算過才寫進【更新說明】的。
上一輪的說明檔寫的是舊值（GPT 指出這一點）；本輪改成由實際檔案產生，不手抄。

### 驗證結果（v2.1.82 候選當時的數字，本版數字見本節末）

> ⚠️ 2026-09-25 更正標題：這一段**在 v2.1.83 那一節裡面**，卻只標著 v2.1.82，
> 讀起來像是本版的驗證結果。本輪正名版號時漏了這一處（它是 `###`，
> 而那支守門只看 `^## `）。數字本身沒有改，它就是 v2.1.82 候選當時跑出來的。

- `npm test`：**578 項，575 通過、0 失敗、3 跳過**
  （`tests/*.test.mjs` 233／233；`tests/*.test.ts` 345 中 342 通過、3 跳過）。
  lint 0 warnings、TypeScript、Big5 字形守門、`vinext build` 全部通過。

---

## v2.1.82 候選（2026-09-23，Asia/Taipei）Claude 三支同步大檢查

**沒有變更任何交通量或 PCU 計算**；`LAST_CALC_CHANGE_VERSION` 維持 v2.1.64，
固定計算黃金值不變。本版改動集中在結論／報表草稿、備份還原、版面與守門。

### 反向對帳：表格算得出來、草稿卻寫不出來的（使用者 2026-09-23 逐件裁示）

使用者原話：

> 「不要讓使用者出了題卻無法抓出答案來，但明明表格中卻能查到答案」

> 「我所謂表格有 草稿卻沒有，是指表格分析出來的結果……而針對那種原始資料來源追溯這類
>   並不是報告會用的分析結果，草稿就不需要這類結論了」

三支程式共找出 32 件缺口，剔除追溯／設定／資料品質類之後由使用者逐件裁示。
**本支核准 4 件，其餘不做**。

| # | 補了什麼 | 為什麼 |
| --- | --- | --- |
| 1 | 結論草稿新增條件「尖峰時段判定方式」（整個調查點同一時段／各方向各自認定自己的尖峰） | 見下面那一段的更正說明 |
| 2 | 選「各方向各自認定」時，每一條支線寫出**自己最忙的那一小時** | 表格的「各支線駛入／駛出尖峰流量」在那個判定方式下就是逐支線換時段的（真實檔實測 1,049.7→1,071.7），草稿只寫得出整個路口一個時段 |
| 3 | 報表草稿的各支線流量、路口轉向總量、駛出／駛入合計補齊**全調查時段尖峰**與**全調查時段** | 「這 4 個名詞是我們交通調查的 4 個核心」（使用者 2026-09-21）。各支線流量表四個範圍都列得出來，報表文字草稿只寫 AM 與 PM |
| 4 | 支線佔比新增**車輛數版本**；分析範圍句補「涵蓋 N 個調查日」 | 表格在「顯示數值＝車輛數＋百分比」時，百分比是拿車輛數算的；草稿永遠拿 PCU 算，同一個支線兩種分母是不同的數字。調查日數則是儀表板那張卡上有、草稿沒有 |

#### ⚠️ 更正一個先前寫在原始碼註解裡的錯誤判斷

`lib/conclusion.ts` 與 `app/traffic-app.tsx` 原本都寫著：結論草稿**做不到**
「各方向各自認定」，理由是

> 草稿的每一筆紀錄要同時提供上午／下午／全調查時段三個時段的數字，
> 而「各方向各自認定」是逐時段各自重新挑尖峰、每個時段得到一份不同的紀錄——
> 一筆紀錄裝不下三份。

**那個理由是錯的。** `ConclusionRecord.peaks` 本來就是**按時段分開存**的，
所以 `peaks.AM` 可以吃 AM 那一份重挑過的紀錄、`peaks.PM` 吃 PM 那一份，互不干擾。
裝不下的是「整筆紀錄」，不是 `peaks`。本版照這個事實改掉，
並把三處說明（草稿正文、「套用主工具列」的回報、`e2e-draft-conditions.mjs` 第 ⑥ 條）
一併更新。

⚠️ **預設仍是「整個調查點同一時段」＝改版前的唯一行為**，
舊範本與舊備份沒有 `peakRule` 欄位時一律回到它（`normalizeCondition`），
所以升級當天草稿輸出**逐字不變**，有一支測試把這件事釘住。

⚠️ 選「各方向各自認定」時草稿會印出**不可以相加**的警告：各支線的尖峰不在同一小時，
加起來不是任何一個時刻的量。畫面與報告文字草稿本來就印著同一句，三處現在一致。
反證：把那句警告改成兩種判定方式都印同一句、或把支線視窗那一段拿掉，都會變紅（實跑過）。

⚠️ **效能**：重挑那一份是 `useMemo` 快取的，而且只有在主工具列**或**結論草稿
其中一處真的選了「各方向各自認定」時才算；兩處都沒選時 `recordWithApproachPeaks`
一次都不會跑，與改版前相同。傳給面板的取用函式是 `useMemo` 出來的穩定參考——
寫成 JSX 行內箭頭函式的話，面板裡的 `useMemo` 會**每一次 render 都重算整份草稿資料**。

**使用者裁示不做的**（本支相關）：OD 矩陣指標、尖峰小時內 15 分鐘分布、
尖峰穩定度（第一名比第二名多幾 %）、連續 60 分鐘候選排行、趨勢指標維度、
左直右佔該支線的百分比、車種組成逐時段輛數、季度逐季變化與「哪一季跳最多」、
圖旁的分級判讀進草稿。使用者的理由是「圖上有就好」。

### 驗證結果

- `npm test`：**566 項，563 通過、0 失敗、3 跳過**
  （`tests/*.test.mjs` 233／233；`tests/*.test.ts` 333 中 330 通過、3 跳過）。
  lint 0 warnings、TypeScript、Big5 字形守門、`vinext build` 全部通過。
- `npm run e2e`：見下方「本輪 e2e」。
- `tests/release-structure.test.mjs`：17／17。

### 交付前 F6（自己先跑一次 GPT 那一關）抓到的事，本版一併修掉

封關清單 F6：交付前開一個**獨立**的複查，只給它打包好的那一包與規則，
不告訴它本來想做什麼。這一輪抓到 5 件，全部已修並補守門：

| # | 問題 | 嚴重度 |
| --- | --- | --- |
| 1 | **`ackedIssues`（按過的「已人工確認」）完全沒進備份**，兩條還原路徑也都不讀（`setAckedIssues` 在還原區塊出現 0 次）。它與 `movementPresence`／`intersectionAliases` 是同一類東西，那兩個的註解都寫著「換一台電腦要一起帶走」，只有它漏了。症狀：`chooseSurveyDate()` 是**同時**寫 `surveyDateOverrides` 與 `ackedIssues` 的，換電腦還原之後覆寫回來了、確認沒回來，「調查日期不只一個」整批重新變成未處理，側欄紅字／品質分數／待人工確認／需處理錯誤四個數字一起回跳，而畫面只說「還原完成」 | 高 |
| 2 | **結論草稿的「全日流量（輛／調查日）」實際讀的是「全調查時段」**。資料來源是 `row.inbound.FULL`，而且**沒有任何 24 小時判斷**（那個門檻 2026-09-11 就拿掉了，`traffic-app.tsx` 的註解自己寫著）。於是一份 4 小時的調查，它的 4 小時累計會被寫成「全日駛入 N 輛/調查日」貼進報告——同一份草稿裡 `branchCompositionIn` 標的是「輛/調查時段」，兩個勾選框互相打臉。系統本來就有 `scopeUnit()`／`scopeVehicleUnit()` 在管這件事，註解還特別寫著「錯的方向要選會少講，不要選會多講」，這裡正好反過來多講 | 高 |
| 3 | **報表文字草稿宣告的單位清單是寫死的**（「PCU/hr、輛、%」），與它自己下一段印出來的 `PCU/調查時段` 直接衝突。v2.1.82 把 `siteSummaries` 由 3 個尖峰擴成 4 個統計範圍之後就對不起來了；**結論草稿那一支（`unitsInUse`）2026-09-21 已經改對，報表草稿漏了** | 中 |
| 4 | **`PROJECT_HANDOFF.md` 的 e2e 支數停在 83／82／84**，實際是 **84／83／85**（v2.1.82 新增的 `e2e-trend-order.mjs` 已掛上）。同一份文件又要求複查的人照這個數字核對，過期的數字會變成拿舊數字糾正新事實 | 中 |
| 5 | **README 宣稱提供 PDF／DOCX 手冊**（v2.1.64 起只出 PDF，Word 產生器與檔案都已移除，還有一條反面守門擋它回來）、**列出的 10 張 Excel 工作表與 `REPORT_ITEMS` 不符**（把「駛出／駛入」併成一個、又把不屬於匯出中心的「原始儲存格追溯」算進來，總數剛好也是 10 所以數字對、清單錯）、**「僅接受時間連續的 4 列」**（只有 15 分鐘格距成立，與同一份 README 第 8 行自己寫的「15／20／30／60 分鐘都支援」矛盾）。【更新說明】另指向一份不存在的證據（「14 頁逐字相同，見 VALIDATION_REPORT.md」——該檔全文 0 次提到 2.1.82） | 中 |

### 新增與修正的守門

| 守門 | 守什麼 |
| --- | --- |
| `tests/backup-completeness.test.mjs`（改寫） | **清單由程式反推**：存檔收的每一個鍵，不是也進備份，就是要列在例外表裡並寫明理由。原本比對的是**手寫清單**，而 `ackedIssues` 不在裡面——「忘了加進備份的人，也會忘了加進清單」。另加兩條：兩條還原路徑都要讀得回來；顯示偏好刻意只在「完整取代」那一條還原，**不可以**被加進「併入」 |
| `tests/e2e-inventory.test.mjs`（新） | e2e 腳本的三個數字要等於實際數量；每一支腳本不是掛進 `npm run e2e`，就是列在例外表裡並寫明理由（姊妹專案全日交通量在 v20.63 交接時就有兩支腳本寫了卻沒掛，而所有人以為「E2E 全部通過」） |
| `tests/shared-contract-pins.test.mjs`（新，三支同步） | 三支共用的**契約案例表**（`direction-pair-contract.mjs`、`chart-levels-contract.mjs`）以 SHA-256 釘住。三支的 README 一直寫著「由 `tests/*-contract.mjs` 以 SHA-256 釘住」，實際上**沒有任何測試在比對它們** |
| `tests/no-hardcoded-paths.test.mjs`（新，三支同步） | 原始碼不可以寫死某一台機器上的絕對路徑。這個缺陷在三支上已經發生過三次，其中一次寫死的是「真實調查檔放在哪裡」——那是**安靜的**，不會有任何東西提醒你 |
| `tests/conclusion-coverage.test.ts`（擴充） | 新增**對照組**：一個指標都不勾時，任何一個判定字樣命中就是紅的。姊妹專案交通服務水準的 12 格矩陣就有 5 格因此恆真 |
| `tests/report-draft.test.ts`（擴充） | 宣告的單位必須就是這一份草稿真的印出來的單位；`sites` 段的抬頭不可以把「全調查時段」的單位寫死 |
| `tests/nav-button-rule.test.mjs`（收緊） | 反面門檻由 `>= 6` 改為 `>= 7`（本支實際 7 顆）。原本訊息寫「其餘 24 顆」而斷言是 6，兩者不是同一件事——訊息與實際守的範圍對不起來，會讓人以為守得比實際嚴 |

### 證據界線（照實寫）

- 本容器沒有 LibreOffice／Microsoft Office，**不能宣稱完成 Excel 的人工開啟檢查**。
- 連不到 `baotoast.github.io`，沒有做線上比對。
- 依賴 audit 維持原狀，不做破壞性升級。
- 手冊內容沒有改寫；本版沒有動到 `manual-src`。

---

## Turning Traffic v2.1.80 驗證報告

## GPT 風險導向獨立複查（2026-09-20，Asia/Taipei）

### 風險與輸入身分

- 完整專案 ZIP SHA-256：`36446FFBDE4DAEAE3A7C3F76E0B3F5337DE5F0F64F5253D75221CA2D3DB1F467`，與 Claude 說明相符。
- 因修改涵蓋 parser 日期候選、IndexedDB／跨電腦備份、UI、圖說、轉向圖版面與匯出資料流，GPT 將 Claude 所稱「中風險」上調為**高風險**，依全專案範圍複查。
- `LAST_CALC_CHANGE_VERSION` 維持 v2.1.64；本版沒有改動交通量、PCU、尖峰挑選或流向判定。

### GPT 額外找到並修正

1. **指定調查日期與日期欄顯示開關沒有進入跨電腦備份。** Claude 已把 `surveyDateOverrides`／`showSurveyDate` 存入 IndexedDB，但 JSON／ZIP 備份 payload 與兩條還原路徑漏掉。現已補入單一計畫 pick、全部計畫、併入及完整取代還原，並以 `backup-completeness.test.mjs` 與 `e2e-backup.mjs` 同時守門。
2. **八叉 E2E 在乾淨 Windows 字型環境會誤報。** 卡片標題依設計位於卡片矩形上方；舊測試用「文字矩形是否完全落在卡片矩形內」判定擁有者，會把自己的標題誤認成別張卡。現改用 DOM `data-card-id`／`data-card-section` 判斷文字所屬。這是測試缺陷，不是正式 UI 缺陷。
3. **多日期資料流缺少瀏覽器端到端證據。** 擴充 `e2e-period-date.mjs`，確認匯入、異常檢查、使用者指定、尖峰彙總、重新整理持久化、轉向圖與下載 SVG 全部使用同一有效日期。測試尊重既有「同幾何路口合併」規則，不再錯誤要求下拉顯示原始檔站名。
4. **全新工作區無法建立單檔試用版。** `scripts/build-tryout.mjs` 直接寫入上層 `out/`，卻沒有建立資料夾；Claude 的暫存環境因 `out/` 已存在而未暴露。現已在輸出前遞迴建立資料夾，並新增發布結構守門；正式 Repository 刪除既有 `out/` 的前提下重跑 `build:tryout` 與 `e2e-tryout-smoke.mjs` 全綠。

### 最終驗證結果

| 項目 | 結果 |
| --- | --- |
| 乾淨安裝 | `npm ci` 成功；509 packages added、510 audited |
| 一般驗證 | lint、glyph guard、TypeScript、vinext production build 全部通過 |
| `.mjs` tests | 200 項：199 通過、1 條件式略過、0 失敗 |
| TypeScript tests | 295 項：292 通過、3 條件式略過、0 失敗 |
| 完整 E2E | 82 支瀏覽器測試＋2 支種子資料產生器，共 84 個執行步驟，全數成功 |
| 變更後目標重跑 | `e2e-backup.mjs`、`e2e-period-date.mjs` 均全數成功 |
| 版面 | sticky 1120–1600px／1420–1421 邊界 131 點通過；八叉 16 卡、文字、裁切、碰撞與 20° 壓力案例通過 |
| 手冊 | v2.1.80 PDF 19 頁完整點陣化檢視，未見截字、重疊、黑塊或表格溢出 |
| 依賴警示 | 完整 audit 10 項（4 moderate、6 high），均在開發／建置工具鏈；未執行破壞性的 `audit fix --force` |

精確計數：Repository 共有 83 支 `e2e-*.mjs`，`npm run e2e` 內有 82 支瀏覽器測試；另有 2 支種子資料產生器。不得把 84 個執行步驟稱為「84 支瀏覽器 E2E」。

### 證據界線

- 3 個需要特定真實附件的自動測試依既定條件略過，未描述為通過。
- 本輪新增的是合成多日期活頁簿的完整資料流驗證；不是三個特定真實附件的替代品。
- v2.1.80 手冊 PDF 已完成點陣化目視檢查；本輪未另以 Microsoft Excel／桌面 PDF 閱讀器重做 Excel／匯出 PDF 的人工開啟。v2.1.79 的 Microsoft Excel／Foxit 結果只保留為歷史證據，不描述成這次重新驗證。
- 功能／發布 commit：`937c306baa572ced640d6885ae553ed5a5ab86c9`。GitHub「建置與測試」run `35516856347` 與 `pages build and deployment` run `35516855416` 均成功。
- 帶版號首頁回應 HTTP 200；線上主 JS `assets/index-Dimc0N1p.js` 與本機 SHA-256 均為 `1BD37BED7B3E4A8A46F8FDFD2F43E4FE599ED61849E7207FE8CF567FD2A04D24`；線上／本機 v2.1.80 手冊均為 `AAB15A9AD22114F104A29347088C33BE2B866E824015EE0C6C120BAA888B6E44`。舊 v2.1.79 主資產 `assets/index-D2lY7Qv1.js` 與 `路口轉向程式手冊_v2.1.79.pdf` 均為 HTTP 404。

## v2.1.80（2026-09-20）Claude 二次複查 ＋ 大檢查

### 一、對 GPT 交回的 v2.1.79 的核對（全部實跑，不是讀報告）

- **身分**：7 個上傳檔 ＋ 驗證證據 xlsx 的 SHA-256 與 `SHA256SUMS.txt` **逐項相符**。
- **逐檔 diff（我的 v2.1.79 ↔ GPT 交回的 v2.1.79）**：
  `app/`、`lib/` **一行都沒動**。實際差異只有 16 支 e2e 腳本 ＋ 1 支單元測試的
  Windows 路徑可攜性（`pathToFileURL`／`fileURLToPath`／共用 `chrome-path.mjs`），
  與 GPT 的說明相符。CHANGELOG／VALIDATION／新增 PROJECT_HANDOFF 為文件變更。
- **GPT 未申報的兩項異動**（我另外查出來的，兩件都合理但報告沒寫）：
  - 刪除 `scripts/_m.mjs`（我留下的臨時探針，無人引用，而且寫死 Linux Chromium 路徑）
  - 新增 `examples/import-sample.csv`（`examples/README.md` 本來就提到它、但檔案不存在）
- **試用版**：GPT 交回的試用版 HTML 與我交付的那一份**逐位元相同**（`cmp` 通過）。
- **`npm ci`**：成功（added 516 packages）。GPT 報 509，差異來自 npm 版本與平台
  optional dependency，不是缺陷。

### 二、本次找到並修正的兩項缺陷

#### (1) 路口幾何示意圖被「用顏色檢視轉向」整片蓋住

使用者 2026-09-20 回報：「路口幾何示意圖我看不到，似乎和顏色檢視轉向重疊」。

`app/globals.css` 兩個斷點各寫各的：

| 規則 | 生效條件 |
| --- | --- |
| `.geometry-layout` 收成單欄 | `@media (max-width: 1420px)` |
| `.geometry-turn-preview` 取消 `position: sticky` | `@media (max-width: 1100px)` ← 錯 |

於是 **1101～1420px** 這一段「已經是單欄、卻還釘著」。單欄時它的正下方就是
「路口幾何示意圖」，往下捲它釘在畫面上緣不動；sticky 是定位元素、下面那塊是
一般元素，依 CSS 繪製順序定位元素在上，整塊幾何示意圖被不透明的面板底色
**整片蓋掉**——不是沒畫出來，是被蓋住。側欄跳轉與 `.is-focused` 外框都正常，
所以看起來像「圖沒畫出來」。

**改法不是把 1100 改成 1420**：那等於把正確性押在兩個數字永遠手動同步上，
而這次就是這樣壞的。改成**預設不釘、只有兩欄版面（`min-width: 1421px`）才釘**，
漏掉的那一邊是安全的那一邊。

守門 `scripts/e2e-sticky-cover.mjs`：掃寬度 1120～1600（每 60px，外加斷點兩側
1420／1421），逐一點側欄項目、等它捲到定位後，對被點名那一塊的
12%／35%／60%／85% 高度做 `document.elementFromPoint`，看到的必須就是那一塊。

刻意迴避的三種假通過：**只測 1366／1536 不算數**（這個坑就是兩個 media query
中間的空窗）、**停在頁首截圖不算數**（sticky 要捲過它上緣才會釘起來）、
**用矩形相交判斷不算數**（兩欄版面裡同列相鄰的兩塊本來就重疊，而且相交不等於
被蓋住，要看的是誰畫在上面）。`position: fixed` 的浮動層另外計數略過。

**反證（實跑）**：改回無條件 sticky →

```
❌ 1120px ❌ 1180px ❌ 1240px ❌ 1300px ❌ 1360px ❌ 1420px（共 7 項紅）
   被 #geometry-turn-preview（position:sticky）蓋住
✅ 1421 / 1480 / 1540 / 1600 維持綠；取樣點數維持 131 點
```

⚠️ **GPT 本輪「1536×864、1366×768 各 18 頁逐頁檢查未見重疊」這句不成立。**
1366 就在出事區間內；漏掉的原因是只在捲到頁首時截圖。

#### (2) 交付包「更新說明」寫了一個不存在的資產檔名

v2.1.79 交付包的 `【更新說明】請先讀我.txt` 寫著
`assets/index-C0pvj8lC.js`（SHA-256 `3478…`），但那一包的 `assets/` 裡**沒有
這個檔案**，實際是 `index-D2lY7Qv1.js`（SHA-256 `6084…`）。
升版重建之後忘了回頭改，而且**沒有任何檢查在看它**。

這一段不是裝飾：GPT 與使用者就是照它去核對「線上那一份是不是這一包」。

守門加進 `tests/release-structure.test.mjs`：照檔名**真的算一次 SHA-256** 再比。

**反證（實跑，三個都紅）**：檔名寫錯 → 紅並列出實際有哪些檔案；
雜湊只錯一個字元 → 紅；整段被改寫掉 → 紅（不可以安靜變成恆真）。

### 二-b、使用者 2026-09-20 授權的四件事（本支承接的部分）

#### 1. 手動新增支線上限 7 → 8：抓到兩個真缺陷

常數改一行之後寫了 `scripts/e2e-eight-arm.mjs` 實測（**不是讀程式碼推論**）：

| 缺陷 | 實測證據 |
|---|---|
| 外圍格位不夠時補出來的位置會疊在既有格位上 | 兩張卡實測座標 (16,254–232,370) 與 (10,282–226,398) |
| 卡片上方那一行標題畫在矩形外面，分離時漏算 | 「來源 C · 示範東路三段」等 3 處壓到隔壁卡 |

- 外圍原本 14 個格位（上 4＋右 3＋下 4＋左 3），八叉在「駛入＋駛出並列」時是
  **16 張卡**，多出來的兩張走舊的「往下再長一圈」規則就會撞上左側那一排。
- 修法：**只在格位真的不夠時**加寬成 5＋4＋5＋4；四叉～七叉一個像素都不動
  （`e2e-diagram`／`e2e-diagram-scale`／`e2e-diagram-bounds`／`e2e-diagram-detach`／
  `e2e-chart-layout`／`e2e-three-arm` 六支重跑全綠，證明沒有動到既有版面）。
- ⚠️ **中途走過一條錯路**：試過用 `textLength` 把標題壓進卡寬，結果更糟——
  它會把**短標題也撐成滿寬**，重疊從 2 處變成 3 處。已還原。這一條記在這裡，
  是因為它是個反直覺的坑。
- ⚠️ **這一支測試第一版是假的綠**：一條支線畫兩張卡，四叉剛好也是 8 張卡，
  用「圖卡數＝8」認八叉會驗到四叉而全綠。改成用路口名稱認，
  並另外把「圖卡數＝支線數×2」釘住。

#### 2. 調查日期

- 候選一律**全表掃描**，只看交通量工作表；系統**實際採用**的那一個仍以標題區優先
  （`findSurveyDate(scopedDateCells) || findSurveyDate(cells)` 那一行**沒有動**）。
- 候選清單的**第一個一定是系統實際採用的那一個**——直接用 `findAllSurveyDates`
  的順序會出問題（那是對全表排的，實際採用的是標題區優先挑出來的）。
- 異常檢查新增「調查日期不只一個」，那一列多一顆「指定調查日期」下拉。
- ⚠️ 訊息裡**不可以**寫「目前採用的是 X」：這一支的確認指紋是 `[id, message]`，
  使用者一指定 X 就變了，他剛按下去的確認會當場失效。會變的字全部留在畫面層。
- ⚠️ 轉向圖上那一行「調查日期 …」也換成使用者指定的那一個——
  不換的話畫面寫他指定的日期、**匯出的圖卻寫系統判讀的那一個**。

#### 3. 圖說第 3、4 級

`lib/chart-levels.ts` 與全日交通量 `app/chart-levels.ts` **逐位元相同**
（SHA-256 `0f9a62f5…`，由 `tests/chart-levels.test.ts` 釘住）；
契約 `tests/chart-levels-contract.mjs` 三支逐位元相同（30 列案例）。

#### 4. 「已人工確認」不再只進不出

`pruneOrphanAcks()`：只在按下「執行資料異常檢查」之後清，只清孤兒，
白名單用 `currentIssues`（這一次檢查真的跑出來的全部項目）而**不是畫面上篩過的那一份**——
用篩過的會把「只是被篩掉」的確認當成孤兒清掉。

#### 本次新增守門與反證（全部實跑）

| 守門 | 反證方式 | 結果 |
|---|---|---|
| `tests/survey-date-pick.test.mjs`（15 項） | 候選只掃標題區 | 14 pass / 1 fail |
| 同上 | message 寫「目前採用的是 X」 | 14 pass / 1 fail |
| 同上 | 覆寫不檢查是否在候選內 | 14 pass / 1 fail |
| 同上 | 圖上不換日期 | 14 pass / 1 fail |
| 同上 | 存檔不收覆寫 | 14 pass / 1 fail |
| `tests/orphan-ack-prune.test.mjs`（5 項） | — | 釘住「只清孤兒、沒有孤兒不寫存檔」 |
| `tests/chart-levels.test.ts`（2 項） | — | 契約 30 列全過 ＋ SHA-256 釘住共用模組 |
| `scripts/e2e-eight-arm.mjs`（14 項） | 上限改回 7／拿掉加寬規則 | 轉紅 |
| `scripts/e2e-chart-layout.mjs` 末段 | — | 量實際座標：第 3 級在、不是空話、沒撐出框、圖仍然 sticky |

還原之後全部回到全綠。

### 三、本版測試結果

- `npm test`：`.mjs` **173 pass／0 fail／1 skip**；`.ts` **290 pass／0 fail／3 skip**
  （新增 1 支守門）。lint、glyph guard、TypeScript、production build 全過。
- `npm run e2e`：**83 個流程步驟全綠**（新增 `e2e-sticky-cover.mjs`）。
  ⚠️ 第一次跑到第 40 支左右掛掉（`e2e-factor-scope` 點擊逾時）——查證是**環境問題**：
  這個容器只有 2 顆 CPU，當時同時在跑另外兩支程式的 Chromium 測試，
  違反「`npm run e2e` 不可以並行」。單獨重跑才是上面這個結果，
  第一次那個紅字**不採計、也不隱瞞**。
- 手冊 PDF 已重新產生（v2.1.80 ｜ 2026-09-20），根目錄資產已重新建置並同步。

### 四、三支聯合驗算（本支相關部分）

- **跨系統總車輛數**：五份真實路口檔餵給本支與全日交通量，兩支各自獨立的 parser
  得到 **19,428／16,553／13,488／15,801／18,038**，完全相同，
  且與 2026-09-18 那一輪記錄的數字一模一樣。
- **調查日期**：兩支對五份檔都讀到 2026-05-04（平日），來源都是表頭
  「日期：115年05月04日(平日)」。
- **共用檔逐位元相同**：`vendor/xlsx-0.20.3.tgz`（`8dc73fc3…`，與規則檔記錄一致）、
  `tests/period-input-contract.mjs`、`lib/period-date.ts`（與全日交通量的
  `app/period-date.ts` 同為 `572d663a…`）。

### 五、證據界線（沒驗到的要講）

- 這個容器連不到 `baotoast.github.io`，**無法逐位元比對線上原始檔**；
  線上狀態只能引用 GPT 的報告與使用者的畫面截圖（截圖顯示 v2.1.79 正式版）。
- 沒有 Microsoft Excel／Foxit，**無法複驗** GPT 的「10 張工作表由真 Excel 開啟」
  與「5 頁匯出 PDF ＋ 19 頁手冊逐頁檢視」。這兩項只能採信其報告。
- 3 條需要特定真實附件的自動測試仍為條件式略過。

---

# Turning Traffic v2.1.79 風險導向複查報告

驗證日期：2026-09-20（Asia/Taipei）

輸入：Claude 提供的 `路口轉向_完整專案_v2.1.79.zip`、`路口轉向_試用版_v2.1.79.html`、`路口轉向程式手冊_v2.1.79.pdf` 與「給 GPT 的說明」。所有輸入檔均先依交付 `SHA256SUMS.txt` 核對成功。

## 風險判定與複查範圍

本版從正式 v2.1.63 跨越多輪 Claude／使用者試用修正，涉及 parser 重複資料阻擋、尖峰判定說明、主工具列與多頁篩選、跨頁報表、IndexedDB、Excel/PDF 匯出、手冊與發布結構，依固定規則判定為高風險，採全專案與完整資料流複查。

Claude 說明所稱 v2.1.78 → v2.1.79 沒有新增計算變更是正確的；但正式 Repository 是由 v2.1.63 升級，因此整體差異包含 v2.1.64 的「全調查時段尖峰不再要求 24 小時」計算口徑變更。`LAST_CALC_CHANGE_VERSION` 正確維持 v2.1.64，不能把整段正式升級誤述為完全沒有計算變更。

## GPT 獨立修正

正式應用程式的計算與 UI 程式碼沒有因複查而另行改寫。GPT 修正的是 Windows 上會讓測試／E2E 無法正確啟動的 17 支測試腳本：

- 動態載入本機 `.mjs` 時改用 `pathToFileURL(...).href`，避免 Windows 磁碟機代號被當成不支援的 ESM URL scheme。
- `tests/wording.test.mjs` 以真實檔案路徑排除 `lib/traffic.ts` 的歷史版本敘述，避免反斜線路徑造成誤判。
- `scripts/e2e-text-wrap.mjs` 改用共用 `chrome-path.mjs` 探測瀏覽器，不再寫死 Linux Chromium 路徑。

上述修正只改善測試可攜性，不變更正式頁面、parser、計算、儲存或匯出結果。

## 自動化驗證

| 項目 | 結果 |
|---|---|
| 乾淨安裝 | `npm ci` 成功；509 packages added、510 audited |
| ESLint | 0 errors、0 warnings |
| 字形守門 | 通過 |
| TypeScript | `tsc --noEmit` 通過 |
| Production build | 通過 |
| `.mjs` tests | 172 通過、0 失敗、1 條件式略過 |
| TypeScript tests | 290 通過、0 失敗、3 條件式略過 |
| GitHub Pages build | 通過；根目錄發布資產已由新建置同步 |
| E2E | 82 個流程步驟全部成功：80 支瀏覽器測試＋2 支種子資料產生器 |
| 單檔試用版 smoke | `file://` 開啟並通過；與 Claude 試用版 SHA-256 相同 |
| Production dependency audit | 0 已知漏洞 |
| 完整 dependency audit | 10 項開發／建置工具鏈警示：4 moderate、6 high；未強制升級 |

`npm run e2e` 不得描述成「82 支瀏覽器 E2E」。Repository 共有 81 支 `e2e-*.mjs`；其中 80 支列在 `npm run e2e`，`e2e-tryout-smoke.mjs` 是另外執行的單檔試用版 smoke。

第一次在正式 Repository 執行 `npm test` 時，發布結構守門正確抓到忽略目錄 `github-pages-dist` 的舊 v2.1.63 DOCX 與根目錄舊資產。重新執行 `build:github` 清空輸出並同步新版資產後，完整 `npm test` 全綠。這是建置產物更新，不是放寬測試。

## 真實資料與人工驗證

### 真實 T15-01

- 使用者提供的 `11017T15-01-中山北路-岡山路口七叉路口.xlsx` 有 168 個逐格來源欄。
- 真實附件別名測試完整通過。
- 在主工具列切換「尖峰時段判定方式」後，5 個相關頁面的數字均改變；切回後全部恢復。
- 「各路口尖峰彙總」的數字會跟著判定方式改變，頁面沒有誤寫「不適用判定方式」。

自動測試仍有 3 條需要特定真實附件的條件式略過。T15-01 的額外實測不能被擴張描述為另外兩個附件也已通過。

### 1536×864 與 1366×768

- 兩種尺寸各檢查 18 個功能頁，共 36 張畫面。
- 程式化檢查根層橫向溢出、未保護表格溢出、轉向圖裁切與工具列／面板碰撞：0 異常。
- 36 張畫面整理為 contact sheet 後逐頁肉眼檢查：未見文字重疊、表格溢出或轉向圖被截斷。

### Microsoft Excel 與 PDF 閱讀器

- 從正式應用程式勾選全部 10 個可選成果並匯出 Excel；以真正的 Microsoft Excel 開啟，無修復／毀損提示。
- 10 個工作表名稱完整可見；第一張工作表的資料表、原生折線圖、圖例、座標軸與資料點可辨識，第二張工作表亦可正常切換。
- 匯出的 PDF 由 Foxit PDF Editor 正常開啟並辨識為 5 頁，無修復／毀損提示。
- 5 頁匯出 PDF 全部點陣化檢視，表格、道路圖、轉向箭頭與文字未見截斷。
- v2.1.79 使用手冊 19 頁全部點陣化檢視，版號、頁尾、表格與章節版面正常。

## 已知警示與界線

- Vite 提示未來 native config loader 對 JSON import attributes 與 extensionless import 的相容性警告；目前建置成功，列為後續受控依賴維護，不在本次順手改動。
- 主 bundle 超過 500 kB，建置會提出 code-splitting 建議；目前不影響正確性或離線運作。
- 10 項 audit 警示都屬開發／建置工具鏈；production dependencies 為 0。不得直接執行可能造成破壞性升級的 `npm audit fix --force`。
- v2.1.64 起正式手冊只提供 PDF，v2.1.79 沒有 DOCX，因此不存在本版 LibreOffice DOCX 視覺驗證項目。

## 發布證據

- 功能／發布 commit：`3a401d03d5e6bc8832733bbb5a3d744087269fad`（`Release Turning Traffic v2.1.79`）。
- GitHub「建置與測試」run `35477494692`：success。
- GitHub `pages build and deployment` run `35477493555`：success。
- 帶版號首頁 <https://baotoast.github.io/Turning-Traffic/?v=2.1.79> 回應 200，引用 `assets/index-D2lY7Qv1.js` 與 `assets/index-ChnT-NVq.css`。
- 線上／本機主 JS SHA-256 均為 `6084FFC16B7F55AF7326A6B8D7041C9B60DBCD235C5708CA22F2A172B4EBE422`。
- 線上／本機 v2.1.79 PDF SHA-256 均為 `5D614A00F00A39E57C36D903093524B86AD0593F58697FFCCC1999B6B6C02343`。
- 舊主資產 `assets/index-DSVt1yFb.js`、舊 v2.1.63 PDF 與 DOCX 線上均為 HTTP 404。
