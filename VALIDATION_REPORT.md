# Turning Traffic v2.1.43 獨立複查與發布驗證報告

- 複查日期：2026-09-01
- 前一正式版：v2.1.42（commit `8fe3222c449861bd82b67e40d46ef051795fb940`）
- 發布目標：`BaoToast/Turning-Traffic` 的 GitHub Pages（`main`／根目錄）
- 附件原始 ZIP SHA-256：`de17cf3661c8b23fd354dcaf048ebf7af521dec01a2e799b19afa82166f5ccaa`

## 結論

Claude 的 v2.1.43 候選版方向正確，但獨立複查另發現兩個會讓不合法季度寫入的缺口，以及一個讓來源包無法獨立執行測試的問題，均已修正：

1. 原檢查只看 2～3 位數外形，仍會放行民國 `89Q1` 與 `201Q3`；現在明確限制民國 90～200（西元 2001～2111）。
2. 選檔預覽完成後若再把年度改成不合法值，「確認寫入」仍可按；現在按鈕與實際寫入函式都會重新把關。
3. 候選測試直接引用來源包外的另一套系統檔案，解壓後無法獨立執行；已改為只檢查本專案內的共用期別模組，並補齊上下界測試。

固定計算黃金值通過，`LAST_CALC_CHANGE_VERSION` 維持 **v2.1.30**。本版沒有變更既有尖峰挑選、轉向分類、車種當量或 PCU 計算口徑。

## 本次實際執行的驗證

| 項目 | 結果 |
|---|---|
| 附件 ZIP SHA-256 | 與附件 checksum 相符 |
| 乾淨安裝 | 使用 lock 檔與官方 SheetJS 0.20.3 完成 |
| ESLint | 0 錯誤 |
| 一般建置 | 完成 |
| `.mjs` 結構／發布測試 | **48/48 通過** |
| TypeScript／計算測試 | **205 通過、0 失敗、3 跳過**；跳過項目需要本包未附的特定真實工作簿 |
| TypeScript `tsc --noEmit` | 0 錯誤 |
| 固定計算黃金值 | 通過；逐車種車輛數與尖峰 PCU 基準未變 |
| 年度範圍測試 | 民國 90、200 與西元 2001、2111 放行；民國 89、201 與西元 2000、2112 擋下 |
| 瀏覽器端對端測試 | 全部流程通過，0 JavaScript 例外 |
| 多寬度排版 | 640、760、820、900、1024、1100、1180、1280、1350、1440、1680、1920 px 全分頁通過 |
| 年份／期別切換 | 全分頁掃描通過；切換只改文字、不改數值；切回後畫面逐字還原 |
| Word 手冊 | 由 `manual.html` 重建，共 **36 頁**；逐頁巡查未見裁切、重疊、缺字或缺頁 |
| PDF 手冊 | 由 `manual.html` 重建，共 **53 頁**；逐頁巡查未見裁切、重疊、缺字或缺頁 |
| GitHub Pages 建置 | 使用官方 SheetJS 0.20.3 重建，主資產為 `assets/index-CPgrteSt.js` |

## 證據界線

Claude 的說明記載曾以 37 份真實調查表、跨系統比對與手寫七叉路口驗算進行驗證；那些原始工作簿沒有包含在本次 v2.1.43 交付附件中，因此本報告**不把該段當成本次獨立重跑的結果**。本次能獨立確認的是：候選包內測試、合成工作簿、固定黃金值、完整瀏覽器操作、建置檔與手冊皆通過上述檢查。

三個依賴真實工作簿的測試保持跳過，不以合成資料冒充真實檔驗證，也不據此宣稱已重新核對 37 份檔案。

## 發布檔案

- 根目錄 `index.html` 已指向 `assets/index-CPgrteSt.js`。
- 舊版主資產與 v2.1.42 手冊已自發布根目錄與 `public/` 移除。
- v2.1.43 Word／PDF 手冊已放入根目錄與 `public/`。
- `.nojekyll`、`.github`、`.openai` 等隱藏項目維持原狀。

發布後仍須以帶版號手冊、首頁 `?v=v2.1.43` 與本版雜湊資產交叉確認線上內容；固定檔名不作為唯一版本依據。
