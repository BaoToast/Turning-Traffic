# Turning Traffic v2.1.33 驗證報告

日期：2026-08-29

## 一、複查結論

Claude 提供的 v2.1.32 已正確修正：

1. 歷季趨勢網頁不再把「無法計算全日尖峰」顯示成 0，資料點會斷開並以「－」表示。
2. 品質異常明細的「四車種分類合計」已改為「各車種分類合計」。

複查後另發現三個同源遺漏，因此修正並推進為 **v2.1.33**：

1. 批次分析報表的歷季趨勢 Excel 仍把無法計算的 DAY 寫成數字 0。
2. 頁面「下載趨勢 Excel」仍把無法計算的 DAY 寫成數字 0，並可能產生不應存在的增減率。
3. 報告文字草稿的「各路口分項結果」仍把無法計算的全日尖峰寫成 `0.0 PCU/hr`。

這三項都是輸出語意與呈現修正，沒有改變交通量、尖峰選擇、PCU 當量、車種歸類、
OD、駛入／駛出或轉向分類。`LAST_CALC_CHANGE_VERSION` 維持 `v2.1.30`。

## 二、修正方式

- `lib/traffic.ts` 新增共用 `scopeValueOrNull(record, scope, value)`：
  - 無法計算的 DAY 回傳 `null`，Excel 儲存格保持空白，原生折線圖依既有
    `dispBlanksAs=gap` 正確斷線。
  - 真正算得出的 0 仍保留為 0。
  - AM／PM 的 0 也完整保留。
- `app/traffic-app.tsx` 的兩條 Excel 匯出路徑統一使用上述函式；無資料時不計算百分比。
- 報告草稿建立各路口摘要時一併傳入 `available`；`lib/report-draft.ts` 在不可用時輸出
  `全日尖峰小時（－）：無法計算。`，不再列出假的總量或支線數值。
- 新增單元與瀏覽器守門檢查，防止網頁、Excel 與文字報告再次出現三套不同判斷。

## 三、測試與人工檢查

| 項目 | 結果 |
| --- | --- |
| `npm ci` | 成功，使用專案鎖定的 SheetJS 0.20.3 |
| `npm test` | **201/201 通過**（45 項 `.mjs`＋156 項 `.ts`），含 lint 與正式建置 |
| `npm run e2e` | 本次候選版全部既有瀏覽器腳本通過，無未處理頁面例外 |
| 最後增量修正後 `scripts/e2e-report-draft.mjs` | 通過；不可用 DAY 顯示「無法計算」，不再出現假 0 |
| 最後增量修正後 `scripts/e2e-trend-day.mjs` | 通過；網頁摘要與折線維持正確 |
| GitHub Pages 建置 | 成功 |
| PDF 手冊 | v2.1.33，共 **48 頁**，逐頁視覺檢查完成，無裁切、重疊或缺字 |
| DOCX 手冊 | v2.1.33，共 **32 頁**，以 LibreOffice 轉頁後逐頁檢查，無裁切、重疊或缺字 |
| 版號一致性 | `package.json`、`VERSION`、CHANGELOG、下載連結、手冊檔名／封面／頁尾均為 v2.1.33 |

完整開發依賴稽核仍有 4 項 moderate，來自既有開發鏈；依維護規則未為此自行替換
SheetJS 或進行破壞性升版。

## 四、輸入交付檔完整性

Claude 提供的 `TurningTraffic_Source_v2.1.32.zip` 實際 SHA-256 為：

`E00C329655CF2371651AAF4241C532FA58BA311D88CBFDDD63BFE5F28EA08F19`

但隨附 `SHA256SUMS_路口轉向_v2_1_32.txt` 記載：

`CA34EE0230D5E56CC6B54773057DB54302BB03825DE99241D8F61E2B796E5D39`

兩者不一致。說明檔 ZIP 與其中 MD 的雜湊則一致。本次已直接檢查實際解壓內容，
並以 v2.1.31 已發布的 `main` 為乾淨基底套入候選變更；v2.1.33 交付包會重新產生
新的 SHA-256 清單，該清單才是本次發布的權威值。

## 五、既有計算口徑（未變更）

最後一次計算口徑變更仍是 v2.1.30：

1. AM 搜尋 `[00:00, 12:00)`，PM 搜尋 `[12:00, 24:00)`。
2. 原始每格車輛數先四捨五入成整數，再進入加總、尖峰與 PCU 換算。

本版沒有修改上述規則，也沒有修改既有備份、季度鎖定或匯入解析格式。

## 六、發布驗證規則

- GitHub Pages 由 `main` 分支根目錄發布；Actions 僅負責測試與建置。
- 線上版本只能用帶版號的手冊、`?v=2.1.33`，或本版專屬的雜湊資產判斷；
  不以固定檔名的快取回應作為版本結論。
- 發布後需逐位元核對帶版號手冊與新版雜湊資產。
