# 給 Claude 的複查說明：Turning Traffic v2.1.40

日期：2026-08-31

## 本輪處理結果

本輪以 GitHub `main` 的 v2.1.39（`2a223957982e40b8a588dad039caec0e17356c0e`）為乾淨基底，選擇性套入 Claude v2.1.40 候選包的來源變更；未把候選包內的暫存腳本、舊建置成品、`tsconfig.tsbuildinfo` 或舊說明檔帶入正式版。

Claude 修正「製表日期被當成調查日期」的方向正確。複查後另補一項一致性修正：原候選版仍讓匯入紀錄與期別檢查分別走不同的日期選取路徑，可能選到不同日期。正式版已讓兩者共用同一個 `findSurveyDate()`。

## 請優先複查的修正

1. `lib/period-date.ts`
   - `isNonSurveyDateText()` 維持共用匯出。
   - 排除詞除製表、列印、報告等既有項目外，增加彙整、輸出、建檔、產製。
   - 明確標示的有效調查日期優先；無標示時才退回第一個有效日期。
2. `lib/traffic.ts`
   - 匯入紀錄日期、資料別與提供給期別檢查的候選日期全部取自 `findSurveyDate()` 的同一結果。
   - 移除另一套較寬鬆、未驗證日曆有效性的本地日期解析函式。
3. `tests/silent-failure-guards.test.ts`
   - 回歸案例同時放入彙整日期、輸出日期、普通日期、製表日期、無效日期，且都排在真正的調查日期前面。
   - 預期仍選到後方明確標示且有效的 `2026-01-26（平日）`。

## 計算邊界

- 未修改 OD、車種數量、尖峰搜尋、轉向分類、當量或 PCU 加總。
- `LAST_CALC_CHANGE_VERSION` 維持 v2.1.30。
- 固定 v2.1.37 計算黃金值測試通過。

## 驗證摘要

- ESLint、TypeScript、vinext 正式建置、GitHub Pages 建置：通過。
- MJS：46/46；TS：191/191。
- 完整瀏覽器 E2E：全部通過，0 JavaScript 例外。
- 640～1920px、17 個分頁排版：全部通過。
- 日期 E2E：期別內、不一致、取消、確認、無日期不阻擋、月份顯示均通過。
- Word 35 頁、PDF 52 頁：逐頁視覺檢查通過。

## 複查邊界

本輪沒有收到真實交通調查 Excel，因此沒有獨立重現或逐格核對 Claude 說明中提到的實際檔案。請以固定黃金值與新增日期回歸案例判斷本輪行為；若要核對特定真實檔，仍需附原始檔與人工基準值。

## 建議 Claude 複查方式

1. 執行 `npm ci`、`npm test`、`npm run e2e` 與 `tsc --noEmit`。
2. 建立同一工作表含「彙整日期、輸出日期、普通日期、製表日期、無效日期、明確調查日期」的測資，確認紀錄日期、資料別與期別檢查使用同一日期。
3. 確認 `LAST_CALC_CHANGE_VERSION` 仍為 v2.1.30，固定計算黃金值不變。
4. 驗證線上版本時，只信 v2.1.40 帶版號手冊、帶 cache-busting query 的首頁，以及本版內容雜湊資產；不要用固定檔名判斷。

若以上均無問題，可將本輪維護標記為完成；後續再依真實使用者回饋另開新版。
