# 給 Claude 的複查說明：Turning Traffic v2.1.35

日期：2026-08-30

## 交接結論

本次以 Claude 交付的 v2.1.34 為來源，先核對 SHA-256，再與 GitHub `main` 的 v2.1.33 做乾淨差異比較。v2.1.34 原有功能與測試可通過，但額外構造反例後發現兩項缺陷，因此由 GPT 修正並依維護規則推進為 v2.1.35。

本版未變更既有尖峰選擇、左／直／右分類、當量係數、路口總量或四捨五入規則；`LAST_CALC_CHANGE_VERSION` 保持 `v2.1.30`。

## GPT 額外發現並修正的兩項問題

### 1. 單一車種 PCU 的 OD 拆解

v2.1.34 的 `pcuBreakdown()` 以整條支線左／直／右的總 PCU 比例反推個別車種分布。當不同車種走不同轉向且當量不同時，單一車種 PCU 會錯，即使所有車種 PCU 合計可能仍碰巧對上。

修正後，有 `record.routes` 時逐筆依「實際 OD 車種數 × 該 OD 的轉向當量」加總；只有缺少 OD 明細的舊備份才保留比例估算與對帳警告。

守門反例：機車 10 輛左轉、左轉當量 0.5，正確值為 5.0 PCU；原邏輯得到 3.7 PCU。新測試已鎖定此案例。

請重點複查：

- `lib/traffic.ts` 的 `MOVEMENT_KEYS`、`vehiclePcuFor()`、`pcuBreakdown()`。
- `tests/pcu-breakdown.test.ts`。
- `scripts/e2e-diagram-summary.mjs` 中單一車種 PCU 的圖面與摘要一致性。
- 無 `routes` 的舊備份相容路徑及 `reconciled` 警告是否仍合理。

### 2. 不存在的日曆日期

v2.1.34 只驗證日期字樣，會接受 115/02/29、115/04/31。v2.1.35 在 `lib/period-date.ts` 增加真實曆日檢查：拒絕上述無效日期，接受合法閏日 113/02/29（2024-02-29）。讀不到或無效日期仍為 `unknown`，只提醒、不阻擋匯入。

請重點複查：

- `isValidCalendarDate()` 是否同時覆蓋民國與西元日期。
- `parseSurveyDateText()` 與 `periodOfDate()` 是否共用相同有效性規則。
- `tests/period-date.test.ts` 的平年、閏年與月底邊界。
- 日期未知時不阻擋匯入的既定行為。

## 保留的 v2.1.34 功能

- 轉向圖右側摘要與「顯示 × 時段 × 車種」同步。
- 新增「車輛數＋百分比」模式。
- 單一車種 PCU 與圖／摘要一致顯示。
- 調查日期與期別不一致時預覽提示及寫入前二次確認。
- 讀不到日期只提醒，不阻擋匯入。
- 期別顯示可切換季別與實際調查月份，且只改文字、不改資料分組或計算。

## 驗證結果

- 乾淨安裝：`npm ci` 成功，實際使用 `xlsx 0.20.3`。
- ESLint：通過。
- TypeScript 單元測試：167/167 通過。
- MJS 結構與發布測試：45/45 通過。
- GitHub 建置：通過。
- 全套瀏覽器端到端測試：全部通過，含轉向圖、報表、日期／期別、備份、鎖定、趨勢、資料別與 640–1920px 排版。
- DOCX 33 頁及 PDF 49 頁逐頁視覺檢查完成，未見截斷、重疊或缺字。

完整證據與邊界請見 `VALIDATION_REPORT.md` 及 `CHANGELOG.md`。
