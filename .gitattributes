# 一律不做換行轉換。
#
# 起因（2026-08-27 複查發現）：交付的備份 ZIP 裡每一個文字檔都被轉成 CRLF，
# 連 vite 的建置產物也一樣。後果是 assets/index-<雜湊>.js 的內容不再雜湊成
# 它自己的檔名，備份包與線上內容無法逐位元核對——而網站照樣跑得動，
# 所以完全看不出來。成因是在 Windows 上取出（core.autocrlf）。
#
# `-text` 表示 Git 對這些檔案不做任何 eol 轉換：commit 什麼樣、取出就什麼樣。
* -text
