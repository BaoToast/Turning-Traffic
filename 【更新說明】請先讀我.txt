路口轉向 — 完整專案 v2.1.83
=================================================

■ 本版（v2.1.83）已於 2026-09-26 完成 GPT 高風險獨立複查、必要修正與完整門檻；
  正式 GitHub Pages 發布證據以 PROJECT_HANDOFF.md 第 12.6 節及 GitHub 當下狀態為準。
  上一個正式版是 v2.1.80；v2.1.81、v2.1.82 均未發布。
  ⚠️ v2.1.81、v2.1.82 兩個候選都沒有發布（使用者 2026-09-24 把檔案從複查端撤回，
    要求先徹底檢查、徹底修正），所以本包含的是 v2.1.80 之後的全部修正。
  ⚠️ 另修一個既存的紀錄錯誤：更新說明裡原本有**兩則都叫 v2.1.82**
    （09-23 與 09-24，內容不同）。09-24 那一批已正名為 v2.1.83。
  正式功能／發布 commit 以本檔所在的 Git commit 為準（不在同一提交內寫死自己的雜湊）。
  GPT 本輪另修正混合格長保存與總分鐘數、完整／部分涵蓋單位、四類單位呼叫點、
  候選 lint 錯誤及標準 Node heap 下字面 npm test OOM；完整明細見 VALIDATION_REPORT.md 最新段落。

■ ⚠️ 本版**變更了計算口徑**（尖峰小時改為累計到剛好 60 分鐘，見下方 v2.1.83 那一節）。
  格長一致的資料一個數字都不會變（5 份真實路口檔是 15 分鐘格，實測完全相同）；
  會變的只有格長混用、或格長組不成整小時的資料。
  由 `tests/` 底下的單元與契約測試、以及 `npm run e2e` 的瀏覽器端流程測試
  共同確認。⚠️ 這一句原本寫「本輪數字見『給 GPT 的說明』」——那份檔案
  **不在這個 zip 裡**（它與三個 zip 並列交付，和 `SHA256SUMS.txt` 一樣），
  照字面在包裡找一定找不到。本包自己的驗證數字寫在 `VALIDATION_REPORT.md`。
  ⚠️ 這一段原本寫著「14 頁全部逐字相同（比對工具與紅字實證見
  VALIDATION_REPORT.md）」，但當時那份報告裡**沒有**這項證據。
  指向一份不存在的證據比不寫還糟，所以改成寫實際做過的那幾件事。
  ⚠️ 2026-09-25 更正這一段本身：它原本還寫著「（它最新一段是 v2.1.80／2026-09-20，
  全檔 0 次提到 2.1.82）」——那句話在本包裡已經是假的
  （報告最新一段是 v2.1.83，也有 v2.1.82 那一段）。
  報告已於本輪補上 v2.1.83 段落；當時缺的那項「逐字比對」證據仍然不存在，
  所以這一段維持寫實際做過的事。

■ 2026-09-24／25 第三輪獨立複查（F6）：紀錄修正 ＋ 兩處程式接線

  ⚠️ **這一則涵蓋兩批**：09-24 那一批**只改紀錄與守門**（下面前兩點），
    09-25 那一批**有改程式**（下面第三點起）。
    這一則原本把「只改紀錄與守門」寫成整則的前提，那只對 09-24 那一批成立；
    寫成整則的話會讓人以為這一包只有紀錄變動——而畫面文字、草稿文字
    與 PCU 都可能不同。2026-09-25 更正。

  ・**「本版網站資產」原本只列了主程式與樣式表兩個雜湊**，
    另外三個資產（html2canvas、index.es、purify.es）沒有列。
    列一半的清單，使用者沒有列到的那幾個就無從核對。已補齊五個，
    並**首次列出手冊的 SHA-256**（它是使用者最常單獨下載的一個檔案）。
    守門（`tests/release-structure.test.mjs`）跟著改成：
    `assets/` 底下的**每一個**檔案都必須被列出來、必須有一組 `.pdf`、
    而且每一組都**實際重算**雜湊比對。格式與姊妹系統全日交通量統一成
    「檔名一行、`SHA-256 = ` 一行」。

  ・**「主程式資產與 SHA-256 請以本次交付的 SHA256SUMS.txt 為準」這句話會誤導。**
    `SHA256SUMS.txt` 是**與三個 zip 放在一起**交付的（在同一個交付資料夾裡，
    不在任何一個 zip 內），它算的是**三個 zip 檔本身**的雜湊，
    用途是確認「你手上這三包就是我交出去的那三包」。
    包內各個檔案的雜湊是上面那一段。兩者用途不同，不可以互相代替。
    照原句去找，會在包裡找一個不在包裡的檔案。

  ・**（09-25，改程式）`pceIssues()` 真的接起來了。** 它 v2.1.83 就寫好、
    版本紀錄也對外宣告「逐格指名哪一格壞掉」，但**一個呼叫點都沒有**——
    整個函式被編譯器拿掉，`assets/index-*.js` 裡連它的訊息字串都沒有。
    淨效果只是「壞的當量格從靜靜變成 0 改成靜靜變成算不出來」，你照樣沒被告知。
    現在它會**列進資料異常清單**（新類別「當量係數不是數字」，計畫預設與
    每一條「季別 × 路口」覆寫都檢查），逐格指名並附解決方式。
    守門也補了一條：那個函式**必須真的被 app 用到**，只有測試在呼叫不算。

  ・**（09-25，改文件）手冊的角度慣例與分頁名更正**（見下方 v2.1.83 那一節），
    並各補一支守門：角度那一句**從程式的方位對照表反推**該講哪一個方位
    （對照表改了、手冊沒跟上照樣會紅）；手冊提到的分頁名必須是側欄
    真的有的那幾個（用**白名單**方向，不是打地鼠式地擋已知舊名）。

  ・**（09-25，改文件）交接文件兩句與程式相反的敘述**（`DAY` 要不要滿 24 小時、
    混用格長是不是只警告）已更正，並補一支守門——手冊那幾句本來就有人守，
    交接文件卻沒有，而它是「工程唯一正式基準」。

  ・**（09-25，改文件）其他過期紀錄**：`DEPLOYMENT.md` 的手冊檔名仍是舊格式
    （照它去抓會 404 而得到「線上不是最新版」的錯誤結論）；
    `PROJECT_HANDOFF.md` 的 e2e 支數一處寫 82／84、另一處寫 84／83／85，
    自己前後矛盾（實際是 84／83／85）；驗證報告在 v2.1.83 那一節裡留著
    一個標成 v2.1.82 的子標題；`README.md` 少寫「八叉」（v2.1.80 就放寬了）
    且仍在描述被 v2.1.83 取代掉的「固定列數」尖峰組成機制。全部更正。

■ 發布建置已完成

  這個 repository 發布的是**根目錄**的 index.html 與 assets/。
  本包已使用內附且雜湊驗證通過的官方 SheetJS 0.20.3 重新建置，並把
  github-pages-dist/ 的 index.html 與 assets/ 同步到發布根目錄。
本版網站資產：
  html2canvas-CHFEJU_U.js
    SHA-256 = 00a64436c470e91b21e9a4563d9418631f9cc4dd82614d380f8cdde8a88a7afc
  index-CaAh6Y_H.js
    SHA-256 = ab62af30f2ff97235c2135bbee06851f2777bad309172ce259d5b9521ff9e88c
  index-CqyyYgkL.css
    SHA-256 = cadfdecf6fffe9e1d57af32274d2a6c0e3bed453a010dd7dfc335842bbf7c235
  index.es-Mo9abkzh.js
    SHA-256 = 694aeab08b867bf264403d121fef6ffe58ad06da131b30a3df6a18d360d819d5
  purify.es-ChwZkWde.js
    SHA-256 = f6935c363d84a53b19c60e4f6801a653b5734c1e02f45c44547ef4804505e95a
本版手冊：
  路口轉向程式手冊_v2.1.83.pdf
    SHA-256 = e51ee7731bd15974e37cf2f9f1ae921c87d57b0b244c80046c0a3b6e8aec041c
  ⚠️ 手冊在包裡有**三份**（根目錄／`public/`／`github-pages-dist/`），
    三份**逐位元相同**（就是上面那一個雜湊），有測試釘住。
    ⚠️ 如果你在包裡跑過 `npm run build`，`dist/client/` 底下會**再多出一份**
    （那是建置產物，不是交付內容）。守門是**遞迴掃全包**的，所以那一份也必須
    是同一個雜湊——重新產生手冊之後，記得四個位置一起換，不是只換三個。
  舊版雜湊資產（含前一版的舊 assets/index-*.js）已移除。

  ⚠️ 上一版這裡只列了主程式與樣式表兩個，另外三個資產
    （html2canvas、index.es、purify.es）沒有列出雜湊——列一半的清單，
    使用者沒有列到的那幾個就無從核對。已補齊。

  SheetJS 0.20.3 已收進 vendor/xlsx-0.20.3.tgz，日後在受限網路或
  離線環境也可使用同一份官方套件重現建置。發布時執行：

      npm ci
      npm test
      npm run build:github
      cp github-pages-dist/index.html .
      rm -rf assets && cp -r github-pages-dist/assets .

  然後把 repository 裡舊雜湊的 assets/index-*.js 刪掉。
  漏掉這一步的話，線上網站不會更新，而其他版號字串仍可能顯示新版。
  tests/release-structure.test.mjs 會檢查套件版本、來源與官方檔雜湊，
  並直接擋下錯誤版本或過期網站資產，不會略過。

■ 2026-09-25 第六輪獨立複查（版號維持 v2.1.83，未發布候選）

  ⚠️ **沒有變更任何尖峰、PCU 或轉向計算。** 會變的是**畫面與匯出上的單位字樣**：
    滿 24 小時的資料，原本寫「調查時段」的地方現在正確寫「調查日」。
    **數值一格都不變。** 共 31 件。

  ・**同一份資料，畫面上四個地方印出兩種單位。**
    ── 什麼情況會踩到 ──────────────────────────────
      調查滿 24 小時、而且時段選「全調查時段」時：
        ・轉向圖抬頭寫「PCU/調查日」（對的）
        ・流量核對工作台寫「PCU/調查時段」
        ・匯出的 Excel 欄名寫「PCU/調查時段」
        ・轉向進階分析四處都寫「PCU/調查時段」
      數字完全相同，單位不同——看報表的人會以為那是兩種量。
    ── 為什麼上一版沒修掉 ───────────────────────────
      上一版修了下拉選單那一處，因為那是當時「看得到」的那一處。
      根因是決定單位的那支函式有**預設參數**，於是二十幾個呼叫點
      都沒有把「這批資料涵蓋幾小時」傳進去，而它們全部靜靜通過。
    ── 現在的行為 ────────────────────────────────
      三支函式的參數**全部改成必填**，每一個呼叫點都必須講出涵蓋是哪來的。
      ⚠️ 匯出的 Excel 刻意用**整批**的涵蓋，不是逐列算：逐列算會讓同一張表
        長出兩組欄位、加總全錯。整批混用時統一寫「調查時段」，
        那正是您 2026-09-10 定的規則。

  ・**結論草稿的勾選框不再把單位寫死在字面上。**
    原本寫「各支線駛入流量（PCU/hr）」，而您可以勾「全調查時段」、
    也可以一次勾多個時段——那時 /hr 是錯的，而草稿本文寫的是對的。
    現在標籤只寫「是什麼量」，單位由草稿本文逐句寫出，勾選區底下另有一句
    說明單位怎麼決定。

  ・**轉向名稱統一為「直行」。**
    程式裡有三份轉向標籤表，其中一份把「直行」寫成「直進」，而**兩種都印在
    畫面上**：OD 矩陣與匯出欄名寫「直行」，歷季趨勢的指標名稱與匯入盤點訊息
    寫「直進」，手冊六處全寫「直行」。
    ⚠️ 讀原始調查檔時**仍然同時接受**「直行」與「直進」——那是別人填的表。

  ・**當量矩陣有壞格時的訊息不再印程式內部的英文代號。**
    原本寫「機車／through＝abc（不是數字）」，而 through 不是畫面上任何一個字，
    您對不到「直行」那一欄。現在寫「機車／直行」。

  ・**「各支線駛入／駛出尖峰流量」那一段說明原本寫死「單位均為 PCU/hr」**，
    選「全調查時段」時整句是錯的。

  ・手冊三處更正：
      ① 第 20 章原本叫您去「匯入紀錄」查匯入時間——**本系統沒有那個畫面**
         （那是交通服務水準的頁面），而且本系統從頭到尾沒有任何地方顯示匯入時間。
         同一句還把版號分界換算成「2026 年 9 月 17 日」，而那個版本是 9 月 11 日
         發布的——差六天，那六天之間匯入的資料會被誤導成「要重新匯入原始檔」。
         現在改成不依賴日期也不依賴版號的做法：先按一下「重新套用計算」，
         補得上就不必重新匯入。
      ② 第 14 章「顯示」寫「PCU、車輛數、百分比，或兩者並列」——實際**五種**，
         而且第一種在畫面上叫「交通量」不叫「PCU」。
      ③ 第 15 章寫按鈕名叫「⧉ 複製說明文字」，而按鈕上沒有那個圖示字元
         （2026-09-15 已刻意移除，它在某些電腦上會變成空白）。

  ・**手冊、畫面上的提示與程式註解三處都叫您「按一下『重新套用計算』」，
    而全站沒有那一顆按鈕。** 實際是「車種轉向當量」頁的
    「用目前的設定重算（N 筆）」，以及當量套用範圍那一列的「用這一組重算」。
    照著找會找不到，然後以為自己漏了一步。三處都改成寫出頁面與真正的按鈕名。

  ・引用《2022 年臺灣公路容量手冊》4.5.1.3 時，力度改成「建議」——
    被引用的那句話自己用的是「宜」。

  ・文件與守門的更正（不影響程式行為）：
      ・交接文件第 11 節那條作廢紅線的更正註記**原句引用**了那條紅線，
        搜尋或掃描的人會把它當成現行規則，已改成轉述。
      ・交接文件的 e2e 數字寫得太寬鬆（實際是 84 支檔案／83 支掛上／85 個步驟）。
      ・例外表寫著某支腳本「由 npm run e2e:tryout 另外跑」，而 package.json
        **沒有那個命令**——那支腳本從來沒有任何方式會跑到。已補上那個命令。
      ・驗證報告把整批執行步驟的數量直接當成瀏覽器 E2E 的支數來寫
        （2026-09-25 更正），而交接文件自己寫著不得那樣描述。
        實際是 83 支瀏覽器測試 ＋ 2 支種子資料產生器。
      ・四處註解寫著「不傳的話會走安全預設」——參數已改必填，寫法過期。

  ・守門（每一支都有反證，明細見 VALIDATION_REPORT.md 本輪那一段）：
      新增 5 支、補強 6 支、修正 3 處「把缺陷釘住」的正面斷言。
      ⚠️ 其中 3 支的**第一版反證沒有變紅**（整檔比對被註解與版本紀錄蒙混、
        短字串被別的詞包住、跨行的句子逐行掃抓不到），已各自重寫並重新反證。

  ・驗證過程本身抓到兩件（都是假訊號，與程式行為無關）：
      ① 我的驗證腳本用管線接了 grep，`$?` 抓到的是最後那個 tail，**永遠是 0**，
         於是 83 支 e2e 在 5 分鐘內「跑完」並回報通過。改掉之後真的紅了。
      ② 紅的原因是本輪的修正自己造成的：`scripts/e2e-conclusion.mjs`
         把結論草稿勾選框的字寫死成「各支線駛入流量（PCU/hr）」，
         而本輪正是把那個寫死的單位從標籤上拿掉。那一支已改成用
         不含單位的子字串找，並斷言「剛好找到一個」。

  ・上一版起的全部內容照樣都在（見下方 v2.1.83 與第五輪各則）。

■ 2026-09-25 第五輪獨立複查（版號維持 v2.1.83，未發布候選）

  ⚠️ **沒有變更任何尖峰、PCU 或轉向計算。** 改的是使用者看得到的文字、
    一處單位不一致、交接文件的兩條過期紅線，以及六支守門的缺口。

  ・**手冊叫您按一顆不存在的按鈕。** 原本寫「按『讀取並預覽』之後…」，
    而那六個字全包只有手冊那一行有——實際的按鈕是「**選擇檔案**」
    （還沒選年度季度時寫「請先選年度與季度」，解析中寫「正在解析…」），
    選完檔就自動顯示「辨識結果」。手冊自己第 4 章的步驟寫的才是對的流程。

  ・**備份卡片的名稱用的是程式明文禁止的講法。** 手冊「兩種備份」表第一列寫
    「只備份目前這個計畫」，而畫面上那張卡片叫「**備份本計畫**」——
    程式裡就寫著「名稱要與卡片抬頭同一個詞，不可以一個叫 A、卡片上寫 B」。

  ・**手冊有四處「第 N 章」指錯章**：
      ・要交報表、要寫報告文字 → 原本指 14～15 章（那是轉向圖與其他分析頁面），
        正確是 **16～17 章**（成果交付、結論草稿產生器）
      ・換電腦、卡住了 → 原本指 17～18 章（17 是結論草稿產生器），
        正確是 **18、20 章**（備份、常見問題）
      ・正文一處把「要帶著走的備份」指到結論草稿產生器那一章，
        正確是第 **18** 章（備份、換電腦與資料保存）
        ⚠️ 這一行刻意**轉述**、而且刻意**不寫出原本那個錯的章號**：
          原句一字不改留在文件裡，搜尋或掃描的人（包含守門）會把它當成
          現行說法；只要把「備份」和錯的號碼寫在同一句，守門就會紅——
          而它紅得有道理。
      ・想看懂畫面上那些數字 → 原本指 9～13 章，正確是 **11～15 章**

  ・**主工具列三個下拉，手冊各漏一個選項**，而「資料別」漏掉的**正是預設值**：
      ・尖峰時段：手冊寫四個，實際**五個**（漏「上午＋下午並列」）
      ・資料別：手冊寫「平日／假日」，實際**三個**，
        而預設是「**平日＋假日並列**」——所以剛打開時兩種日別都在，
        照手冊看會不懂為什麼
      ・顯示數值：手冊寫「PCU、車輛數、百分比或並列」，
        實際五個，而且畫面上叫「**交通流量**」（路口轉向圖上寫「交通量（PCU/hr）」），
        **選單上沒有「PCU」這個選項名**
      ・路口流量視角：手冊寫「駛出／駛入／兩者並列」，
        畫面上是「駛出路口（以該支線為起點）／駛入路口（以該支線為終點）／駛出＋駛入並列」

  ・**「全調查時段」的單位，手冊只寫了兩種裡的一種。** 手冊名詞表只列
    「PCU/調查日」，而程式只有**整批都是 24 小時**時才寫「調查日」；
    不足 24 小時、混著不同涵蓋範圍、或判斷不出來時寫的是「**調查時段**」。
    照手冊抄，4 小時的案子會把 4 小時的累計量宣告成全日量——
    那正是程式註解裡在防的那個錯。

  ・**同一頁對同一批資料印出兩種單位。** 路口轉向圖「顯示」下拉的五個選項
    沒有把「這一筆的調查涵蓋」傳進單位函式，於是「全調查時段」時選單恆寫
    「PCU/調查時段」，而同一頁的圖面對滿 24 小時的檔寫「PCU/調查日」。
    規則是「單位的唯一來源是 scopeUnit()」——三處都呼叫了同一支，
    但傳的參數不同，**規則字面過關、畫面仍然不一致**。已補上。

  ・**手冊「資料維護會抓什麼」那張表，一列是假的、四種真的沒列。**
    表列五種，其中「資料別待設定」**不是**異常類別
    （補資料別的地方在「流量核對工作台」），而真正的八種裡少了
    「調查日期不只一個」「調查格距異常」「調查格距混用」「當量係數不是數字」
    ——最後一種正是本版新接上線、使用者一定會看到的那一個。
    另外兩種類別名寫的不是畫面上的字（「總數對不起來」「尖峰時段可疑」，
    畫面上是「總數不一致」「尖峰時段異常」）。

  ・**交接文件第 11 節還留著一條與程式相反的紅線。** 同一件事在 6.2 節已經
    更正，第 11 節「已放棄、暫緩或禁止」清單裡卻還要求這件事不必動到計算、
    加個提醒就好——而 v2.1.83 的核心修正就是改掉它。紅線比一般敘述危險：
    照它做會把正確的修正當成未經授權的口徑變更而要求還原。

  ・**交接文件 6.3 節的 `FULL` 兩條也與程式相反**，而且與同一節上面第一條
    自相矛盾。這是 6.2 節那件事的**同類第二例**：09-25 那一輪只修了 `DAY`。

  ・手冊裡唯一一處版號拿掉，改成可操作的判斷方式（先按「重新套用計算」看補不補得上）。
    ⚠️ 2026-09-25 更正：上一版把版號換成「2026 年 9 月 17 日」，而 v2.1.65 是
    2026-09-11 發布的——差六天，而且那六天之間匯入的資料會被誤導成要重新匯入
    原始檔。同一句還叫使用者去「匯入紀錄」查匯入時間，而本系統**沒有**那個畫面。
    兩個問題都出在同一句，現在整句改成不依賴日期也不依賴版號的做法。
    三處過期註解更正（舊時段名與已廢的 24 小時規則、`app/layout.tsx` 的
    舊分頁名、以及「pceIssues 一個呼叫點都沒有」——它**已經接上線了**，
    註解卻還用現在式寫）。

  ・更新說明自己一處：本文件前面指向一份「給 GPT 的說明」，
    而那份檔案**不在這個 zip 裡**（它與三個 zip 並列交付）。
    指向一份在包裡找不到的證據，比不寫更糟——已註明它在哪裡。

  ・守門補強六支（每一支都有反證，見 VALIDATION_REPORT.md 本版那一節）：
      ① **新增**「手冊叫使用者按的按鈕，程式裡必須真的有」——
         既有的分頁名白名單只掃 `「…」` 被 strong 包起來、與「到／進／去「…」」
         兩種寫法，**按鈕是「按「…」」，完全不在它的射程內**。
      ② **新增**「手冊裡的第 N 章必須存在，導讀表也必須指到對的章」。
      ③ 混用格長那一條原本只擋**一種字面寫法**，改成概念級。
         ⚠️ 2026-09-25 第六輪再補一次：上一輪雖然叫「概念級」，樣式還是綁死
         「只警告」三個字，而禁止清單裡用的是另一種說法（不必動到計算、
         加個提醒就好），一個字都抓不到。現在同一句裡出現「混合／混用時間格」
         加上**任何一種「不改計算」的說法**都會紅，語序相反也會紅。
         連帶抓到交接文件自己的更正註記原句引用了那條舊紅線，已改成轉述。
      ④ 補上 `FULL` 兩條的樣式與正面斷言。
      ⑤ 分頁名白名單補上**反向檢查**：豁免清單裡不可以有程式裡找不到的名字
         （那種名字會替手冊的舊名打掩護）。
      ⑥ 拿掉一行從第一天就是空操作的 `replace`
         （它讓人以為手冊允許帶版號的歷史括號，而另一支守門的規則是不允許）。

  ・上一則（v2.1.83）的全部內容照樣都在，見下方。

■ v2.1.83（2026-09-24）：調查格距與尖峰認定 ＋ 徹底檢查收尾

  ⚠️ 這一版**變更了計算口徑**（你 2026-09-24 拍板的選項②），
    LAST_CALC_CHANGE_VERSION 由 v2.1.64 推進為 v2.1.83。

  ⚠️⚠️ **相對於上一個正式發布版 v2.1.80，會讓數字或文字不同的不只格長那一項。**
    下面這幾種情形**原本漏寫了**（2026-09-25 補上）：每一則各自寫
    「格長一致的資料一個數字都不會變」，那對「格長那一項」是對的，
    但整包還有另外幾項也會變，而它們與格長完全無關。

    ① **當量矩陣裡有格子不是數字時，PCU 從「靜靜變成 0」改成「算不出來」。**
       舊版把空白字串、空格、空陣列這類值轉成數字都得到 **0**，
       於是那個車種那個轉向的 PCU 全部變 0，而畫面、Excel、草稿全部照印，
       **沒有任何警告**。來源很實際：手改過的備份、或舊版數字框把欄位清空後存下的值。
       ⚠️ 新版回「算不出來」而不是悄悄套預設 1——套一個看起來合理的預設
       會讓錯誤永遠不被發現。
       ⚠️ 而且它現在會**列進資料異常清單並逐格指名哪一格壞掉**，附解決方式
       （這一項的接線是 2026-09-25 才補上的，見上面 F6 那一則）。
       真正「這一格沒填」（留空）仍然沿用 1，行為與以前相同。

    ② **歷季變化的說明句改寫。** 讀不到數值時，舊版寫「起始季為 0，
       變動幅度無法以百分比表示」——那是把「讀不到」當成「量到 0」。
       新版改成寫明有幾季讀不到、可能的原因，以及沒有做變動幅度比較。

    ③ **「全調查時段」＋「各方向各自認定」時的草稿與畫面文字反轉。**
       舊版印「本數值不適用相加」的警告；新版改成印
       「這個時段不套用各方向各自認定，各支線量仍然可以相加，合計等於路口總量」。
       ⚠️ 這是**更正**，不是改語氣：全調查時段是一段累計量，不是一個尖峰小時，
       沒有「各方向各自挑視窗」這件事，所以本來就可以相加。

    ④ **按過「併入既有路口」之後，轉向存在性的裁決答案開始真的生效。**
       舊版那個答案的鍵用的是併入前的名字，所以永遠查不到、每次都重問；
       修好之後它會生效，而那會改變「哪些轉向存在」，進而改變合計數字。

    ⑤ 另外還有：位數夾限改用型別守衛、三個複製按鈕不再靜默失敗
       （舊寫法的樂觀鏈**then 與 catch 都不會執行**，已實測）、
       備份只挑前綴相符的別名鍵、清掉兩處死分支。這幾項不改數字。

    ⚠️ 以上 ①～⑤ 就是版本紀錄裡那句「K9～K13、K19～K22、K30、K35 共 11 項」，
    `CHANGELOG.md` 與程式裡的版本紀錄都有列，**只有這一份使用者說明漏了**。

  ・**J2：舊紀錄的格長改為先從時間欄原始文字回推。**
    匯入時「原始文字」與「這一格幾分鐘」取自**同一個字串**，所以 v2.1.67 以前的
    舊紀錄雖然沒存長度，還是回推得出來——在此之前那條路徑一路退回眾數，
    下面那條修正在舊紀錄上其實還活著（實測會把 50+50+50+100＝250 當成一小時）。
    時間欄真的只寫起點時仍然退回眾數，**不猜**。

  ・**與全日交通量共用一張逐位元相同的尖峰認定對照表**（10 個情境，
    兩支各自跑自己的程式、結果 10/10 一致，表格內容以 SHA-256 釘住）。

  ・**車種組成的占比補上四捨五入說明**：逐項各自四捨五入到小數第 1 位，
    加起來可能不等於 100%（三項各 1/3 會印成 33.3＋33.3＋33.3＝99.9）。
    刻意不把差額塞給任何一項，否則那一項的占比就對不上它自己的數值÷總量。
    手冊同步補上同一句——這張卡會被抄進正式報告，審查時一定有人把它加起來。

  ・**手冊兩處錯誤更正（2026-09-25 抓到，都是照著做就會錯）**：
    「角度」那一列原本把 0 度說成北方——實際上**0 度是正東**
    （90 度＝南、180 度＝西、270 度／-90 度＝北）。照原本那樣填，
    每一條支線都會偏 90 度，而系統是用兩條支線之間的夾角判左／直／右，
    所以**左轉會被判成右轉**，而且手冊自己下面就寫著「不會有錯誤訊息」。
    另外手冊有五處指到一個**不存在的分頁「資料品質檢查」**——那一頁已經
    改名為「資料維護」並從第一區搬到第五區，而手冊同一張分區表裡
    同時列著舊名（在錯的分區）與新名（在對的分區），自己前後矛盾。

  ・**手冊兩處更正**：「各方向各自認定不可以相加」補上「全調查時段是例外」
    （它是累計量不是尖峰小時，各方向仍可相加、合計等於路口總量）；
    「沒有逐時間格資料可以回推」更正為 v2.1.65 起只需重新套用計算。
    格長一致的資料**一個數字都不會變**（你的 5 份真實路口檔是 15 分鐘格，
    實測完全相同）；會變的只有格長混用、或格長組不成整小時的資料。

  ・**尖峰小時改成「累計到剛好 60 分鐘」認定。**
    舊版只比「起點間距」，所以 08:45 的 15 分鐘格接 09:00 的整點格時，
    間距剛好 15 分鐘就會過關，於是 50+50+50+100 被當成一小時的量。
    根因是每一格自己的長度**根本沒存下來**，只有一個整份檔案的眾數。
    現在每一格都記下自己的長度（取自時間欄原始文字的起訖），逐格累加。
    ⚠️ v2.1.67 以前匯入的舊紀錄沒有起訖，那時退回眾數，行為與以前相同。

  ・**2 小時以上一格會被列為異常**（匯入時提示 ＋ 資料異常清單，不阻擋匯入）。
    以前這種資料只會讓尖峰欄位顯示「資料不足」，你不知道為什麼、
    也不知道該去看什麼。現在清單裡會寫「多半是時間欄位誤植，請核對原始檔」，
    並舉出是哪幾格。

  ・**同一份檔案混用兩種以上格長也會列進異常清單**，並明講「不一定是錯」
    （很多調查就是全日整點、尖峰另外拆細）。要確認的是那是刻意的，
    還是某一格的結束時間打錯了。

■ v2.1.82（2026-09-23）：使用者試用回報 ＋ 三支同步大檢查

  沒有變更任何交通量或 PCU 計算，黃金值不變。

  ── 〇、2026-09-23 反向對帳新增的四件（使用者逐件裁示核准）──────

  這一輪把畫面上每一個表格與圖說的輸出，對照兩支草稿產生器的全部選項，
  三支共找出 32 件「表格算得出來、草稿寫不出來」的缺口，由使用者逐件裁示。
  本支核准四件：

  ・結論草稿新增條件「**尖峰時段判定方式**」：整個調查點同一時段（預設）／
    各方向各自認定自己的尖峰。
    ⚠️ 這一項原本被判定為「草稿做不到」，理由寫在原始碼註解裡：
      「一筆紀錄裝不下三份」。**那個理由是錯的**——ConclusionRecord.peaks
      本來就是按時段分開存的，每一個時段可以各自吃自己那一份重挑過的紀錄。
    ⚠️ 預設仍是「整個調查點同一時段」＝改版前的唯一行為，舊範本與舊備份
      沒有這個欄位時一律回到它，**升級當天草稿輸出逐字不變**。
  ・選「各方向各自認定」時，每一條支線寫出**自己最忙的那一小時**，
    並印出「不可以相加」的警告（各支線的尖峰不在同一小時，
    加起來不是任何一個時刻的量）。算不出自己視窗的支線會**明講算不出來**，
    不會偷偷沿用整個路口的視窗。
  ・報表草稿的各支線流量、路口轉向總量、駛出／駛入合計補齊
    **全調查時段尖峰**與**全調查時段**（原本只寫上午與下午）。
    單位一律走 scopeUnit()——全調查時段是累計量，不是 /hr。
  ・支線佔比新增**車輛數版本**（原本永遠用 PCU 當分母，而表格在
    「顯示數值＝車輛數＋百分比」時是拿車輛數算的）；
    分析範圍句補「涵蓋 N 個調查日」。

  ⚠️ 效能：重挑那一份是快取的，而且只有在主工具列**或**結論草稿其中一處
    真的選了「各方向各自認定」時才算；兩處都沒選時一次都不會跑。

  ── 一、結論草稿產生器（使用者實際踩到的三件事）─────────────
  ・「時段」由兩顆勾選框補成**四顆**：上午尖峰／下午尖峰／全調查時段／
    全調查時段尖峰。以前「全調查時段」是靠**四顆都不勾**這個看不見的狀態
    表達的，所以沒辦法同時要「上午尖峰」和「全調查時段」。
  ・「全調查時段尖峰」那一顆以前**接在空的地方**，勾了永遠得到
    「這一筆沒有資料」，24 小時的調查檔也一樣。現在四個時段都建得出數字。
  ・支線篩選以前對「車種組成」**完全無效**（勾路口A、路口B、全選寫出一樣的字），
    抬頭卻寫著「只敘述指定支線：路口 B」。現在真的會篩；
    這一筆若是雙向合計呈現、篩不了時會**明講篩不了與為什麼**，
    不再靜靜印出整個路口的合計。
  ・⚠️「全調查時段」的單位是 **PCU／調查時段**，不是 PCU/hr。
    兩種單位同時出現時，草稿會自動加一句「不可以相加、也不可以直接比大小」。
  ・草稿同時寫「駛入」與「駛出」時會加一句**不可相加**的提醒——
    四條支線的駛出合計＝駛入合計＝路口總量，相加會得到剛好兩倍的假總量。

  ── 二、報表文字草稿 ─────────────────────────────────────
  ・各路口分項結果補上「全調查時段」（以前只寫三個尖峰），單位各自正確。

  ── 三、資料維護 ─────────────────────────────────────────
  ・按過「已人工確認」之後，側欄「資料維護」的紅字、品質分數、
    待人工確認、需處理錯誤**四個數字現在都會跟著減**（以前只有一個會）。
    已確認的另外列一行「已人工確認 N」，不會看起來像資料憑空消失。

  ── 四、輸入欄位 ─────────────────────────────────────────
  ・角度等數字欄位**按空之後不再黏一個 0**（以前會變成 090、045、1809）。
  ・角度超出 -180～360 會提醒，但**不會擅自改你的值**。
  ・**兩條支線角度相同時就地提醒**（轉向圖上那兩條會完全疊在一起）。

  ── 五、備份 ─────────────────────────────────────────────
  ・併入備份時，同一個計畫的調查日期指定改成**逐筆合併**
    （以前會被整批取代，本機原有的指定會靜默消失）。
  ・「顯示調查日期」這個開關**不再跟著單一計畫備份走**——
    它是這台電腦這個人的顯示偏好，併入別人的備份不會再翻掉它。

  ── 六、版面 ─────────────────────────────────────────────
  ・視窗寬度不足 1400px（例如瀏覽器放大到 110%）收成單欄時，
    歷季趨勢的順序改成 **圖 → 圖說 → 季度變化**（以前圖說被季度變化隔開）。
    兩欄版面**一個像素都沒有動**。

  ── 七、沒有意義的按鈕 ───────────────────────────────────
  ・「調查日期不只一個」那一列的「前往流量核對工作台」按鈕已移除
    （動作完全在那一列完成，那一頁連日期都不顯示）。
    其餘 24 顆「前往…」按鈕全部盤過，**一顆都沒有動**。

■ v2.1.80（2026-09-20）：使用者 09-20 授權的四件事 ＋ 路口幾何示意圖被蓋住

  ── 一、手動新增支線上限 7 → 8 ─────────────────────────────
  ・「支線與流向設定」頁的手動新增上限由 7 改為 8。
    （匯入不受這個上限限制，調查表有幾支就讀幾支。）
  ・⚠️ 改上限之後實測八叉版面，抓到**兩個原本就存在、但七叉時碰不到**的排版問題：
    外圍格位只有 14 個，八叉在「駛入＋駛出並列」時是 16 張卡，多出來的兩張
    會疊到左側那一排；另外卡片上方那一行標題畫在卡片外面，分離時沒有算進去。
    兩個都已修好，而且**四叉～七叉的版面一個像素都沒有動**。

  ── 二、逐筆調查日期 ─────────────────────────────────────
  ・「各路口尖峰彙總」表的「調查日」欄多一顆「顯示調查日期」開關，**預設是開**。
    關掉時那一欄**仍然在**、只寫「－」（整欄拿掉會讓表格欄位位移）。
  ・日期改用**全文搜索**判讀，不再只掃標題區前 13 列，所以日期寫在哪一格都找得到。
    只看交通量工作表——監測照片、時相圖上的日期不算調查日期。
    ⚠️ 系統**實際採用**的那一個仍以標題區優先，正常檔案的結果與改版前完全相同。
  ・同一份檔案讀到**兩個以上不同的日期**時，「資料維護 → 執行資料異常檢查」
    會列出「調查日期不只一個」，那一列右邊有一顆**「指定調查日期」**下拉。
    挑一個按下去就會記為已確認，紀錄卡、明細、「期別顯示調查月份」
    以及**匯出的轉向圖**都會改用您指定的那一個。
    ⚠️ 系統不會自己挑，也不會取平均。您指定的是一份**覆寫**，
    原始判讀結果一直留著，把下拉選回「請指定…」就會復原。

  ── 三、圖旁說明升到第 3、4 級 ───────────────────────────
  ・講稿原本是四段（這張圖在說什麼／重點變化／怎麼看這張圖／要先講清楚的）。
    本版在「重點變化」之後再加兩段：
        「代表什麼狀況」——這個數字落在哪一個區間、意思是什麼
        「要怎麼處理」——接下來該做什麼，或還缺什麼資料才能下判斷
  ・**「要怎麼處理」寫不出具體的就整段不出現**，不會有「建議注意」這種空話。
  ・三支程式用的是**同一份判定規則**，同一個數字不會在三支被說成不同的狀況。

  ── 四、「已人工確認」不再只進不出 ───────────────────────
  ・按過的確認記在一份看不見的清單裡；指紋一變，舊紀錄就永遠對不上任何一筆異常。
    本版在**按下「執行資料異常檢查」之後**清掉那些孤兒紀錄，只清孤兒。

  ── 五、方向成對檢查：依您的裁示**不做** ─────────────────
  ・您 2026-09-20 指出「路口轉向程式本就不適用……那個角度也是使用者自己亂打，
    不可能實際測出角度，所以不是方位詞」。已移除，並在原處留了理由，
    避免日後又被當成「三支同步漏做」而加回來。

  ── 六、路口幾何示意圖被「用顏色檢視轉向」整片蓋住（使用者回報）───

  本版**沒有變更任何交通量或 PCU 計算**（LAST_CALC_CHANGE_VERSION 維持 v2.1.64）。

  使用者 2026-09-20：「路口幾何示意圖我看不到，似乎和顏色檢視轉向重疊」。

  原因是 app/globals.css 兩個斷點各寫各的：
    .geometry-layout       1420px 以下收成單欄
    .geometry-turn-preview 1100px 以下才取消 position: sticky   ← 錯
  於是 1101～1420px 這一段「已經是單欄、卻還釘著」。單欄時它的正下方就是
  「路口幾何示意圖」，往下捲它釘在畫面上緣不動；sticky 是定位元素、下面那塊
  是一般元素，依 CSS 繪製順序定位元素在上，整塊幾何示意圖被不透明的面板底色
  整片蓋掉——不是沒畫出來，是被蓋住。側欄跳轉與外框提示都正常，所以更難發現。

  ・改法不是把 1100 改成 1420（那等於把正確性押在兩個數字永遠手動同步上，
    而這次就是這樣壞的），而是改成「預設不釘、只有兩欄版面才釘」。
  ・新增守門 scripts/e2e-sticky-cover.mjs：掃寬度 1120～1600（每 60px 一次，
    外加斷點兩側 1420／1421），逐一點側欄項目、捲到定位後對該區塊的
    12%／35%／60%／85% 高度做 elementFromPoint，看到的必須就是那一塊。
    反證實跑：改回無條件 sticky 時 7 項紅並指名 #geometry-turn-preview。

■ v2.1.79（2026-09-18）：大檢查（Fable 5.1）—— 說謊的說明、重複資料擋下、星號與位置代稱清理

  本版**沒有變更任何交通量或 PCU 計算**（LAST_CALC_CHANGE_VERSION 維持 v2.1.64）。

  ・「各路口尖峰彙總」原本寫「不適用尖峰時段判定方式／資料別／轉向別」，但表格
    每一格都會套判定方式與轉向別（轉向別＝左轉時 T15-01 的 AM 轉向總量從 3,976.6
    變 1,422.8）。改成只保留真的不跟的「資料別」。
  ・「各支線駛入／駛出尖峰流量」卡片改依站號排序，不再隨主工具列尖峰時段換位置。
  ・「車種組成分析」跨路口表原本寫「不適用主工具列的尖峰時段…資料別」，實際會跟著變；
    改成只列真的不跟的四項，並在「上午＋下午並列」時說明本表顯示上午。
  ・匯入：同一張表兩個欄群同一路口編號、同一欄群兩欄同名、同一支線時距重複，
    一律擋下不寫入（原本警告後仍相加寫入），訊息寫到哪一張表、第幾欄～第幾欄。
  ・純文字裡的 Markdown 星號 7 處拿掉；位置代稱 2 句改寫；守門各自擴大。
  ・npm test 不再引用交付包裡不存在的 ../harness。
  ・驗證：tsc／lint／unit／e2e 全綠；同一組守門在 v2.1.78 建置上：e2e-filter-coverage 3 紅、
    duplicate-labels 3 紅、plaintext-markup（注入 **x**）紅。

■ v2.1.78：兩個「代表紀錄悄悄換成別筆」的問題

  加總／並列稽核清單的最後兩項，兩個都是**靜默**的：

  ・**報告文字草稿**：報表有自己的季度區間，而「目前選定的路口」跟著主工具列走。
    兩者對不上時（報表 115Q2～115Q4、但選定的路口只在 115Q1 調查過），
    舊版會退回**匯入順序上的第一筆**——別的路口、別的季、別的日別——
    然後照樣把那一筆的支線流量與車種組成寫成正文，
    畫面上只寫「以 ○○ 為代表」，沒有一句說你選的那個不在裡面。
    現在會直接寫出來，並告訴你怎麼修正（調整季度區間）。

  ・**結論草稿的「整體結果」**：代表紀錄取的是陣列的第一筆，
    而排序是「季度由小到大」——所以一直是**最舊**的那一季，
    而報告通常要引用最新一季，挑法連方向都相反。
    改成取最新一季（同季時取站號較小者），並把「怎麼挑的」
    與「其餘 N 筆沒有寫進這一段」都寫出來。

■ v2.1.76：匯入時「重複」的東西一律出聲

  使用者 2026-09-17 問：「三支程式是否都是讀取標籤，然後欄位如果有換位，
  都能讀到，如果有重複，也能指出異常讓使用者去確認的功能嗎?」
  查下去發現本支有三種重複是**完全安靜**的，已全部補上警告：

  ・兩個欄群都讀到同一個「路口編號：A」→ 兩支支線的流量被加成一支，
    畫面上只看得到一支。
  ・同一個欄群裡兩欄完全同名 → 同一個「車種×流向」被重複計入。
  ・同一支支線裡同一個時距重複 → 後一列**蓋掉**前一列，
    而且「這個數字出自第幾列」的追溯記錄一起被蓋，逐格追溯會指到錯的列。

  三者都只**指出**，不替使用者挑、不平均、不相加。
  ⚠️ 時距那一條的判斷範圍是「這一個欄群」而不是整張工作表——
  同一張表的各支線本來就共用時距，用工作表當範圍的話每一份多支線檔案都會誤報。

■ v2.1.75：版面重組（成果交付／批次輸出）＋三個「本頁被主工具列鎖住」的真錯
  ・「報表與批次輸出」拆成「成果交付」與「批次輸出」兩個大分頁；
    那一排匯出勾選同時決定報告文字草稿、分析數據 Excel 與多計畫批次成果包，
    所以兩張吃它的卡片上把相依關係寫在畫面上，並加一顆跳過去的鈕。
  ・X-60 歷季趨勢比較：自己的路口下拉選了會被扳回主工具列那一個，已修。
  ・X-64 道路與流向管理：勾了舊季的路口再匯入新一季，「切換路口」整個空掉，
    而幾何卡還停在上一季。這一頁是設定頁，改用它自己的一份資料。
  ・X-57／X-58／X-59 資料維護：那一塊貼著左邊框、摘要沒有標題、
    「執行資料異常檢查」沒進側欄，三件一起修；摘要卡的說明從「這一季」
    更正為「全部季度」（X-48 改了程式、字沒跟著改）。
  ・異常項目新增「已確認，不再提醒」，指紋帶數值，數字變了會重新提醒。
  ・守門：新增 e2e-page-isolation／e2e-trend-intersection／e2e-geometry-scope，
    三支都做過反證；並換掉一支恆真的假守門。

■ v2.1.74：主工具列上線（三態：鏡子／脫離／回歸）

  使用者 2026-09-14 指名：把散在各頁的篩選條件彙整到每一頁最上方的同一列，
  並且「圖可以自己改，但只影響那一張」「圖表自身的工具列仍舊要與主工具列同步」
  「主工具列也多一個全部回歸鈕」「有圖表不適用的條件，該圖表就單獨顯示不適用」。

  ・主工具列（每一頁都看得到）：季度起訖區間（預設起＝迄＝單季）、路口多選、
    尖峰時段、尖峰時段判定方式、路口流量視角、資料別、車種、轉向別、顯示數值。
  ・三態：鏡子／脫離（寫明「目前用本區塊自己的條件（主工具列：…）」）／
    回歸（每塊一顆「回到主工具列條件」，主工具列一顆「回歸全部（N 張圖）」）。
  ・不適用的條件單獨顯示不適用並說明理由，而且只在真的篩了那個條件時才出現。
  ・結論草稿與報表輸出維持獨立，另加一顆「套用主工具列目前的條件」。
  ・本版沒有變更任何計算口徑；升級過程中反而抓出並修好兩個既有的計算錯誤
    （只看某一轉向時 approach.movements 沒有跟著清、尖峰時窗只算了一個時段），
    兩者都補了手算黃金值的單元測試。
  ・逐頁肉眼檢查抓到兩件事：開機的告知訊息不會自動消失而蓋住每一頁右下角；
    歷季趨勢比較同時有兩組季度卻沒有一個字解釋。兩件都已修。

■ v2.1.73：點大分頁就自動展開它的小分頁（三支同步，不動計算）

  使用者 2026-09-14 在交通服務水準回報（附圖，「路段管理」被選中卻是收合的）：
    「當我點選大分頁標題(路段管理)時，它並沒有展開而是保持收合，
      請修正成自動展開下面的各項小分頁，如同另外兩個程式一樣的做法」
  接著要求「請同步確認三份程式是否都能自動展開」。

  本支是同一個毛病：小分頁的收合狀態寫在 localStorage，只要按過一次那顆 ▸，
  那一頁就**永遠**是收的，再怎麼點大分頁都不會展開。
  而那份記錄其實用不到——重新整理會回到預設分頁，要回某一頁就得點它一下。

  已改成：大分頁的點擊在換頁前先把自己展開；小分頁的收合只留在記憶體。
  分類（整區）收合**仍然記得住**，因為分類標題永遠在畫面上。
  另外把分類標題字級由 13px 改成 16px（和交通服務水準同一顆雷，本支一併修）。


■ 前一版：依使用者指名做「零警告」（不動計算）

  使用者 2026-09-14：「好的請幫我做路口轉向的 零警告」。

  ── 一、那兩條警告是什麼、為什麼不能照規則的建議改 ──────────
  來自兩個**刻意不給相依陣列**的 effect：一個量「側欄列出來的區塊，
  畫面上是不是真的畫得出來」，一個量「轉向圖有沒有超出面板被裁掉」。
  規則的警告文字建議「改成 []」——**那在這兩處是錯的修法**：
  改成 [] 等於只量一次，而這兩段存在的目的正是每次 render 後重新量一次。
  所以是就地豁免，理由寫在程式碼旁邊；lint 同時改成一條警告都不准有
  （另外兩支本來就是，三支一致）。

  ── 二、豁免不可以變成貼紙 ─────────────────────────────
  規則擔心的無限重繪是真的會發生的。這兩處之所以安全，唯一的理由是
  它們的 setState 寫成「算出來的結果和上一次一樣就原封不動回傳 previous」。
  新增 tests/render-loop-guard.test.mjs 把那道防護釘住。
  ⚠️ 第一版寫成「找得到一個 previous 寫法就算過」，反面測試照樣全綠
    （同一個 effect 裡還有另一個守規矩的頂著），已改成逐一檢查。
  ⚠️ 前置檢查也抓到我自己一個錯：同一個規則的豁免其實有兩種
    （有相依陣列但故意不完整的 3 處、完全沒有相依陣列的 2 處），
    第一版混為一談，已分開只管後者。
  ⚠️ 豁免那一行不可以加 `-- 理由` 尾巴——實測會讓 eslint 多報一條
    假的「Unused eslint-disable directive」，零警告下整包會紅。

  ── 三、短說明不可以被 max-width 逼到換行（三支同步的守門）──────
  新增 scripts/e2e-text-wrap.mjs。**本支原本就沒有中招**，只補守門
  （是在「全日交通量」上被使用者實測抓到的：`ch` 對中文是錯的單位，
    中文字約 2ch，70ch 只裝得下 35 個字）。
  順帶掃到本支有兩處說明也用 ch 當上限，但它們是**長段落**、
  折行是正確的排版，**本版沒有更動**，已在回報裡列出等使用者決定。


■ v2.1.71：使用者 2026-09-14 實測回報的三件事（不動計算）

  ── 一、「清除原始資料」那顆鈕的名字在誘導人按 ──────────────
  使用者：「這個清除重算用的原始資料是什麼意思?原始資料被清掉?表示之後
  如果我改轉向當量參數，就無法套用重算了?因為寫原始資料感覺按下去後果蠻嚴重的」

  他說對了。那顆鈕**不刪任何一個已經算出來的數字**，但丟掉的是每一筆的
  原始逐格快照，而那份東西唯一的用途就是日後重算；清掉之後要改車種歸類
  或當量係數，只能重新匯入原始檔。舊名字只寫好處（「省約 0.2 MB」），
  代價卻藏在確認框裡——那是在誘導人按。

  依使用者指定的分工改成三段：
    ・按鈕短寫「清除原始資料」
    ・旁邊小字備註「目前只佔 X MB，建議本機資料累積到 1 GB 以上、
      或瀏覽器提示空間不足時再清除」（兩個數字並排，一眼看得出差多遠）
    ・確認視窗寫明「未來要重算必須重新匯入原始檔案」，可確定／取消
  ⚠️ 我第一版把整句代價塞進按鈕名字，做成一顆很長的鈕，是矯枉過正，
    已依使用者回饋改回。

  ── 二、「這張圖怎麼講（簡報用）」不再列成左側小分頁 ────────────
  使用者：「說明文字類型的不用在左側欄位中做成小分頁。」

  它裡面有一顆「複製全部說明」，依既有判準會被算成「這一塊有可操作的
  控制項」而必須列成分頁。判準因此補一條例外：
  **「複製／下載這一塊自己的說明文字」不算可操作的控制項**。
  ⚠️ 這個例外不可以變成萬用貼紙——守門要求純說明區塊裡的按鈕**每一顆**
    都要掛 data-copy-own-text，漏一顆就紅。

  ── 三、側欄小分頁可以收合（三支同步）────────────────────
  大分頁那一列最右側一顆獨立的箭頭鈕，**只**負責收合；
  那一列其餘區域的換頁行為一個字都沒改。狀態記在本機。
  ⚠️ 收合鈕沒有另外包一層 DOM——包一層的話 `nav > div > button` 就打不到
    大分頁按鈕，而八支既有守門都是那樣寫的。

  ── 四、試用版的手冊按鈕原本是死連結 ─────────────────────
  把產出的單檔試用版真的用 file:// 打開、走到手冊那一頁去量那顆按鈕的 href
  （不是讀原始碼猜的），量到的是 ./路口轉向程式手冊_v2.1.71.pdf。
  單檔旁邊沒有那個檔，按下去**連錯誤訊息都沒有**，畫面毫無反應。
  已改為把手冊嵌成 data URI，並補兩層守門（內嵌沒命中就讓建置失敗；
  「殘留外部參照」檢查補上 JS 字面值裡的相對路徑）。三支用同一道關卡。
  ⚠️ 這一項**只影響單檔試用版**，正式網站的手冊連結本來就是對的。

  ── 五、守門與兩條過期守門 ───────────────────────────
  新增 e2e-modal-actions-visible（本支原本就符合，只補守門）、e2e-nav-collapse。
  另修掉兩條**自己過期**的守門：showQuarter 的相依斷言（v2.1.68 就過期）、
  側欄要有「計畫管理」（v2.1.68 已改名「建立與管理計畫」）——
  這兩條讓 v2.1.68、v2.1.69 的試用版交出去時測試其實是紅的。

■ v2.1.70：視窗的「取消／確認／關閉」始終可見（三支同步・不動計算）

  查證結果：**本支原本就符合**。裁決視窗是「表頭／可捲清單／動作列」三列格線，
  只有中間的清單會捲，動作列永遠在最後一列。本版沒有為此改任何樣式。
  但補了守門（e2e-modal-actions-visible.mjs），把清單撐到可捲 1951px 後
  逐顆量按鈕位置；反面測試改掉格線會紅（按鈕底緣 2582 > 畫面 700）。

  另外補齊 v2.1.68／v2.1.69 只出試用版而漏掉的發布中繼資料，
  並刪掉根目錄殘留的 v2.1.67 手冊 PDF（畫面連結指向 v2.1.69，點下去會 404）。

■ v2.1.67：使用者指名要查證的三件事（不動計算）

  ── 一、車種當量與「併入／獨立分析」有沒有確實重算 ──────────
  使用者：「請再次確認…並且套用重新計算後，有確實計算，
  因為我手上沒有錯誤的檔案可以驗證了」。

  作法：造一份**每一格都是固定常數**的調查表，讓每一個 PCU 都可以手算，
  然後逐項對答案（不是「有反應就算過」）：
    ・匯入當下（聯結車獨立分析）左/直/右＝930.8/649.6/816.4，手算相同
    ・只改當量不按重算 → 既有數字一個都沒動（刻意的：已交出去的數字
      不該在背後被改），但設定確實存下去了
    ・按下「用目前的設定重算」→ 直行 649.6→700，剛好等於
      「4 格 × 聯結車輛數 × 當量差」＝50.4；左轉右轉完全沒跟著變
      （排除「三個轉向套同一個當量」的錯）；版本號 1→2 可還原
    ・改成併入特種車再重算 → 車種明細裡聯結車消失、
      特種車輛數＝原特種車＋原聯結車＝204、
      左/直/右＝914/616/788.4 全等於用**特種車**當量的手算值；
      另外算出「若仍用聯結車自己的當量會是 700」，確認程式不等於它
      （排除兩種算法剛好同值的巧合）
  結論：確實重算，而且算對。守門 scripts/e2e-pce-recompute.mjs。

  ── 二、路名別名 ──────────────────────────────────────────
  使用者：「針對路名有別名的設定嗎? …這點我驗證不到」。

  別名功能**本來就有**、匯入時真的在用，但畫面上完全看不到——
  那一頁的說明甚至明寫「別名會留在系統內部協助辨識，不在此頁逐筆展開」。
  已補上「檔名別名清冊」：列出目前計畫記住的每一筆、可逐筆刪除，
  顯示的是使用者看得懂的名字而不是內部正規化鍵。

  ⚠️ 同時修掉兩個真的 bug：
    ① 別名**匯出備份有帶走、還原卻沒有讀回來**。換一台電腦還原之後
       別名全部消失，而畫面只說「還原完成」。諷刺的是匯出那一段的註解
       自己寫著「沒帶走的話那台電腦每一季匯入都會重新問」。
    ② 「全部清除」漏清別名，上一個委託案的對應會殘留到下一個案子，
       而按鈕上方明寫「會清除所有計畫、資料與設定」。
  守門 scripts/e2e-alias-visible.mjs（備份→清空→還原，別名要還在）。

  ── 三、清除本季資料三支是否同步 ────────────────────────────
  三支都有，但名稱、位置與定稿鎖各不相同（本支「刪除整季」在匯入資料頁，
  會攔截已鎖定成果）。本支順手修掉一個：刪季度只刪 records、
  **不刪對應的還原點**——那些孤兒還原點指向已經不存在的紀錄，
  按下去會還原出使用者以為已經刪掉的資料，而且會把真正有用的舊版
  擠出 30 筆的保留上限。

  ── 四、趨勢圖數值標籤離縱軸刻度太近 ────────────────────────
  姊妹專案被使用者回報「有和Y軸重疊的疑慮」，本支結構相同，
  一併把內縮從 3px 加到 6px；守門改成量「與刻度數字的距離」，
  不是「與繪圖區邊界的距離」——後者量不到使用者看到的那件事。

■ v2.1.66：使用者實機回報的版面與操作問題（不動計算）

  ── 一、側欄跳轉會把標題蓋住 ──────────────────────────────
  使用者：「點了流量核對工作台下面4個分頁，全部跳轉畫面都是把標題遮住，
  看不出我已經跳轉到什麼畫面，只有成果審核狀態正確跳轉」。
  scrollIntoView({block:"start"}) 把目標對到視窗上緣，正好被 sticky 的
  72px 功能列蓋掉；「成果審核狀態」看起來正常只是因為它是最後一塊、
  捲到底捲不動。改用 scroll-margin-top 扣掉功能列高度（寫成 CSS 變數）。

  ── 二、側欄文字被裁切 ────────────────────────────────────
  使用者：「原始處存格與換算來....後面的文字看不到了」。
  成因不是寬度不夠：.nav-section 的樣式被 .sidebar nav button 整組壓過去
  （具體度 0,1,2 對 0,1,0），實測字級是 16px 而不是設定的 13px、
  左右內距多 10px，格軌 190px 塞進 180px 容器，橫向溢出 21px。
  提高選擇器具體度並允許換行；守門在 100%/125%/150% 三種縮放各量一次。

  ── 三、區塊間距不一致、版本清單貼右緣 ──────────────────────
  同一頁有的區塊自己帶 margin、有的沒帶 → 改由父層一個 gap 統一決定。
  面板內距寫在特定子元素上（.panel > .panel-head、.panel > p），
  .revision-list 是 div 套不到 → 補上與抬頭對齊的左右內距。

  ── 四、轉向圖的中央路口會隨支線數縮小 ──────────────────────
  使用者：「路口示意圖的各路口不要分這麼開嗎…能保持原中間路段原本的
  樣子就好，畫布增大只是增加四周空白處」。
  圖上的幾何本來就沒有跟著畫布放大（支線半徑是固定值），是
  .diagram-canvas svg{width:100%} 把變大的 viewBox 等比縮小塞進同寬面板。
  改成顯示比例跟著畫布走，四支線時剛好是 1，十字路口一個像素都沒變。
  ⚠️ 兩個坑：檔案後面還有一條 max-height:760px 會蓋掉前面的設定；
  寬與高要分開算比例（四叉 1200×900、七叉 1620×1080 形狀不同）。

  ── 五、計畫資訊可以修改（本支原本完全沒有）──────────────────
  舊版只有建立與刪除。名稱打錯時唯一的辦法是刪掉重建，
  而刪除會連同該計畫所有季度路口資料一起消失。
  補上清單每一列的「修改」，共用同一張表單；只動 projects，不碰 records。

  ── 六、備份與還原補上側欄子項目 ────────────────────────────
  三個名稱（備份本計畫／備份全部計畫／還原計畫）各指向一張卡片，
  卡片抬頭改成與側欄同一個詞。

  ── 七、數值標籤不可以壓到縱軸刻度（預防性）────────────────────
  姊妹專案實機被抓到；本支結構相同但還沒有人點到那張圖，先行修正。

  ── 八、守門 ──────────────────────────────────────────────
  新增 e2e-nav-layout（跳轉落點、區塊間距、面板內距、側欄三種縮放）、
  e2e-diagram-scale（量畫面 CSS 像素而非 SVG 座標）、
  e2e-project-edit（改名之後每個計畫的資料筆數一筆都沒有變）。
  點名外框的 CSS 從綁元素改成純 .is-focused。

■ v2.1.64：移除跨計畫比較、新增尖峰形狀圖與彙總分頁，並更新全調查時段尖峰口徑

  ⚠️ 計算口徑變更：全調查時段尖峰不再要求資料必須涵蓋完整 24 小時；
  只要逐格資料能組成連續 60 分鐘就可計算，且不可跨越資料空隙。
  LAST_CALC_CHANGE_VERSION 因此推進為 v2.1.64。24 小時完整測資的既有黃金值不變。

  ── 一、移除「跨計畫比較」整條線 ──────────────────────────

  使用者的評估是「跨計畫比較似乎沒什麼意義」：不同計畫的路口、車道數、
  調查時間都不一樣，把原始總量並排本來就不可比。

  移除「跨計畫／多路口比較」分頁，以及 lib/trend-metrics.ts 的
  buildCrossProjectTrend 與 crossProjectScript。

  ⚠️ 有三樣東西**名字像**跨計畫、實際上不是，全部保留：

  ・「多計畫管理」是唯一能新增與刪除計畫的地方（畫面上每一顆
    「建立計畫」都指向它）。只拿掉頁內的跨計畫趨勢圖，
    頁面改名為「計畫管理」。
  ・Excel 的「跨計畫多路口比較」工作表，資料來源其實是**目前這個計畫**。
    改名為「各路口支線尖峰流量」；工作表的 key 維持 compare 不動——
    使用者存下來的匯出範本記的是 key，改了範本會失效。
  ・被移除分頁裡的「歷季各路口尖峰比較」與「各支線駛入／駛出尖峰流量」
    兩組表格，其他分頁沒有同樣的東西（「各路口駛入／駛出流量」一次
    只看一個路口，那兩張是所有路口並排）。另立新分頁
    「各路口尖峰彙總」保留。

  ── 二、畫面精簡 ──────────────────────────────────────────

  ・總覽儀表板移除「路口尖峰小時排名」。量體不同的路口把原始總量
    排大小沒有判斷意義；彙總表改為依站號排序。
  ・版本更新歷程不再顯示在畫面上。VERSION_HISTORY 陣列保留
    （發布流程與測試都要讀），只是不再渲染成畫面上的區塊。

  ── 三、轉向進階分析新增兩張尖峰形狀圖 ────────────────────

  「尖峰小時內四格 15 分鐘分布」與「連續 60 分鐘流率（依時間）」，
  都預設收合。折線圖的橫軸是時間（不是排名），最高的那個窗以橘圈
  標出，旁邊附判讀說明與「複製說明文字」。

  兩張圖只讀 sourceTrace.intervals，**不新增任何計算邏輯**。
  資料不是 15 分鐘格距、或沒有逐格資料時，圖會寫明為什麼不適用，
  不畫空圖。

  ── 四、手冊整份重寫，只出 PDF ────────────────────────────

  22 章，寫給完全沒有交通概念的人，不寫版本沿革。
  依使用者決定只提供 PDF，Word 版與 scripts/manual/build-docx.mjs
  已移除，docx 相依一併移出 package.json。


■ v2.1.63：GPT 風險導向複查修正（不動計算）

  ・趨勢圖、右側摘要與可編輯 Excel 原生圖表統一採用所選指標的數值與單位。
  ・IndexedDB 寫入等交易真正完成才回報成功。
  ・長期間趨勢圖限制安全寬度並抽樣數值標籤，折線與資料點完整保留。
  ・跨計畫「全部」依所有實際資料別分線；修正 README 與手冊標題版號。

■ v2.1.62：側欄分成五個功能區、圖旁補說明、修掉四項複查意見（不動計算）

  ── 一、側欄分成五個功能區 ────────────────────────────────

  17 個項目平鋪、順序照當初做出來的先後排，不是照使用流程。改成：
  開始／一 資料匯入／二 參數設定／三 資料檢視／四 圖表與比較／
  五 產出與維護，每一區有小標與代表色。三支系統用同一套分區。

  只重排與換小標，沒有動任何功能。兩處順序調整：
  「多計畫管理」移到圖表與比較最後、「資料品質檢查」移到匯入後面。

  ── 二、圖旁邊的說明 ──────────────────────────────────────

  講稿原本在圖的下方，寬螢幕（1400px 以上）改成接在圖的右邊，
  窄螢幕自動回到圖的下方。

  ── 三、尖峰的車輛數單位應該是「輛/hr」 ──────────────────

  三個尖峰的車輛數是一小時內的量，舊版一律標成「輛」，會被讀成
  一整天的量。改為與全系統同一支單位函式，尖峰是「輛/hr」、
  全日時段是「輛/調查日」。

  ── 四、下載的 Excel 也會排滿缺季 ────────────────────────

  畫面上的橫軸已經會排滿每一季、缺季斷線，但下載的 Excel 只列
  「有資料的那幾季」，Excel 裡的折線圖照樣把缺季壓掉——交出去的是
  Excel，不是畫面。現在 Excel 也會排滿，缺季那幾列留空並標註
  「這一季沒有調查資料」。缺季之後那一季的「較前季（%）」改為留空，
  舊版會寫 0，那會被讀成「持平」。

  ── 五、淡灰字加深 ────────────────────────────────────────

  站號、單位、副標等小字實測對比只有 2.67～3.27:1（標準是 4.5:1），
  已加深到 5.19:1，色相不變。

  ── 六、連續存檔可能把新資料蓋回舊資料（會掉資料，已修）──────

  這一條是 GPT 複查提到的「儲存競態」，查下去是**真的**，而且是三項
  複查意見裡唯一會讓使用者掉資料的一項。

  每一次存檔都自己開一次 IndexedDB，而「開啟」是非同步的，
  兩次存檔誰先開好**沒有保證**。使用者連續改兩次時：
    ・第一次存的是舊資料、第二次存的是新資料
    ・第一次若比第二次晚開好，它的寫入就會**後**落地
    ・資料庫裡最後留下的是**舊資料**
  重新整理之後，剛剛改的東西不見了，而且沒有任何錯誤訊息。

  ⚠️ 程式裡原本那個「只有最新一次的結果算數」的序號**擋不住這一種**：
  它只決定哪一次可以更新畫面與提示，不會取消已經送出去的寫入。

  改法是把所有寫入排成一條鏈，前一次寫完才開始下一次，順序由呼叫
  順序決定。實測方式是用假的儲存層讓「開啟」的回應順序完全顛倒
  （第一次等 40ms、第二次 20ms、第三次 0ms）：修好之前資料庫裡剩下
  第一次的舊值，修好之後三次的落地順序等於呼叫順序。
  由 tests/state-storage-race.test.ts 釘住。

  本版沒有變更任何交通量或 PCU 計算。

■ v2.1.61：點畫的位置與縱軸對不上，刻度改成好讀的整數（不動計算）

  ── 一、點畫的位置與縱軸說的不一致（使用者問出來的）──────

  單一路口趨勢圖的資料點，縱向對應寫的是「310 −（值 ÷ 軸頂）× 235」，
  格線卻畫在 70、130、190、250、310——跨距是 **240**。

  兩個對應關係差 5px，整片資料點被往下壓：值等於軸頂的點畫在 75，
  而標著那個值的格線在 70。誤差隨值線性放大，**照著縱軸讀一個高點
  會少讀約 2%**——在 6,000 PCU 的軸上就是 120 PCU。

  圖畫得出來、線也很漂亮，沒有任何錯誤訊息。實測還原後守門顯示
  最大誤差 99.9 PCU。已改成與格線相同的 240。

  ⚠️ 跨計畫趨勢圖沒有這個問題，本版沒有動它的幾何。

  ── 二、縱軸刻度是一排亂數 ──────────────────────────────

  軸頂原本是「資料最大值 ×1.12」直接四等分，刻度變成
  5,371.3／4,028.5／2,685.6／1,342.8／0。

  改成「先決定每一格的高度，再回推軸頂」：每一格只允許
  1／1.5／2／2.5／3／4／5／6／8／10 的 10 的次方倍，往上吸附。
  同一個例子會變成 6,000／4,500／3,000／1,500／0，
  而且軸頂一定高於資料最大值。單一路口圖與跨計畫圖都改。

  ── 三、縱軸名稱會糊成一團 ────────────────────────────────

  縱軸名稱原本用「直排」排版，而直排要靠字型裡的直排前進量才知道
  下一個字往下多少。字型沒有那組度量時（部分環境、以及匯出 PNG 時
  載入的替代字型），中文字的前進量會變成 0.4px——「路口總量（」
  五個字全部疊在一起糊成一團，只有 PCU/hr 排得開。

  改成「橫排的字整段轉 90 度」，每一種字型都排得開。
  另外兩支系統本來就是這樣做的，這一支是唯一的例外。

  本版沒有變更任何交通量或 PCU 計算。

■ v2.1.60：整季沒調查的季度不再被「壓扁」（不動計算）

  歷季趨勢圖的橫軸原本只排「有資料的那幾季」。某個路口若只做了 113Q1
  與 114Q1，圖上就只有兩格、兩個點緊鄰——看圖的人（業主）會讀成
  「上一季到這一季」的變化，實際上中間隔了整整一年。這張圖會被下載成
  PNG 貼進簡報，讀錯就跟著交出去。

  本版把橫軸改成排滿「起訖之間的每一季」：

  一、單一路口歷季趨勢圖：缺的整季留空、折線在那裡斷開。
      斷線代表「這幾季沒調查」，不是「這幾季是 0」。
  二、跨計畫歷季趨勢圖：同樣補齊，各計畫在缺季一律沒有值。
  三、補出來的空季 N＝0，不會被算成「路口數最少的一季」而蓋掉其他
      季度真正的小樣本警告。

  保守處理，寧可不補也不要猜：期別不是「<年>Q<1-4>」的格式（例如
  自訂期別名稱）、或民國三碼與西元四碼混用時，一律原樣不動。

■ v2.1.59：跨計畫趨勢圖不再把平日與假日混在一起（不動計算）

  v2.1.58 新增的跨計畫歷季趨勢圖沒有過濾資料別，而同一個路口同一季
  通常有平日與假日兩筆。後果有兩個，第二個更嚴重：

  一、平日與假日被平均在一起，得到一個不對應任何一天的數字。
  二、N（路口數）會變成筆數——12 個路口被算成 24 筆，每路口平均整個
      錯掉，而畫面上還標著「N=24」。

  本版新增「資料別」選單（預設單一種；另有「全部（分開顯示）」會把一個
  計畫拆成平日一條、假日一條，是同時顯示、不是加總），並在計算內部以
  路口鍵去重當最後一道防線。畫面上直接寫明「平日與假日不會被加總」。

  ⚠️ 單一計畫的趨勢圖從來沒有這個問題：它的資料別是單選，統計範圍選
  「整體」時是畫 AM／PM／全日尖峰三條線，不是加總。本版沒有動它。

■ v2.1.58：歷季趨勢圖可選指標、圖旁講稿、跨計畫趨勢圖；修掉匯出圖片的版面（不動計算）

  一、歷季趨勢圖新增可選指標
      舊版只畫得出「路口總量（PCU）」。本版新增：實際車輛數、單一車種車輛數、
      單一車種佔比、單一支線量、單一轉向量，共六個。預設仍是路口總量，
      不動這個選單時的行為與舊版完全相同。
      圖、右側摘要、講稿、PNG、Excel **全部只讀同一份計算結果**，
      不可能出現「圖上一個數字、旁邊寫另一個數字」。
      算不出來的季度一律畫成斷線並顯示「－」，**絕不用 0 代替**。

  二、圖旁新增「這張圖怎麼講（簡報用）」
      四段：這張圖在說什麼／重點變化／怎麼看這張圖／要先講清楚的。
      第四段會主動講出算不出來的季度與原因、樣本太少、駛出駛入對不起來、
      站號歷年變動、資料別還是「待設定」——那些都是業主可能當場問的。
      有一顆「複製全部說明」可以直接貼進簡報備忘稿。

  三、多計畫管理新增「跨計畫歷季趨勢」
      一個計畫一條線。**一律比每路口平均，不比總量**——各計畫的路口數本來
      就不一樣，比總量只會證明「路口比較多的計畫比較大」。
      佔比類指標用加權平均。線尾標出該季實際算得出來的路口數 N。
      明細表預設收合，可搜尋。

  四、修掉匯出圖片的版面問題
      下載下來的圖與畫面上不一樣：匯出是把圖轉成獨立文件再畫成點陣圖，
      那份文件讀不到網站的樣式表，而圖表文字的大小與對齊都寫在樣式表裡，
      所以字會放大成 16px、而且每一段字整個往右移半個字寬，整片重疊。
      **畫面上完全正常，只有交出去的那一張壞掉。**
      本版把樣式帶進圖裡，畫面與下載讀同一份。
      同時：縱軸名稱跟著指標走並一律帶單位（舊版寫死「尖峰小時交通量
      （PCU/hr）」，換成車種或佔比之後就是錯的）、新增橫軸名稱「季度」、
      刻度不再每一格重複單位、X 軸標籤依實際字寬計算間隔。
      另修掉一個實測到的重疊：24 季時點上的數字每個都重複「PCU/hr」，
      相鄰兩個直接疊在一起；單位既然已經在軸上，點上就不再重複。

  五、匯出改成「圖歸圖、字歸字」
      ・下載的 PNG **只有圖**，說明文字不印在圖上——那些話是簡報者要口述的。
      ・可編輯 Excel 新增「圖表說明」工作表放講稿；資料欄名與數值跟著所選指標走。

■ v2.1.57：用「支線數」做算術核對；四岔的橫線不再安靜刪掉（不動計算）

  起因是您問「三岔的原始檔只有路口A、B、C，這對判讀有沒有幫助？」

  查核結果先說清楚：支線代碼本來就有在讀（讀表頭的「路口編號：」），
  支線數也一直在用（轉向的角度換算、轉向圖的等角配置都靠它）。
  但先前**沒有拿它做過任何核對**。本版補上的就是核對。

  ── 核對的內容是純算術 ──────────────────────────────

  一支支線只能去（支線數 − 1）個地方，調查表卻固定印三個轉向欄，
  兩者的差就是「應該有幾個轉向不存在」：

      三岔：3 個轉向欄 − 2 個去向 ＝ 每支剛好 1 個（全路口共 3 個）
      四岔：3 個轉向欄 − 3 個去向 ＝ 0 個

  這不依賴任何調查廠商的編號習慣。實測您提供的 37 份實檔完全符合：
  三份三岔檔（岡山北路－育才路口、左楠路－世運大道、高楠路－1003 巷）
  每支剛好 1 個純橫線；五份四岔檔一個純橫線都沒有。

  ── 這個核對帶來三件事 ──────────────────────────────

  一、數量對不上時會示警。三岔卻只數到 2 個橫線，代表有一欄被留成空白、
      或表頭欄位被讀錯。以前這種檔案會安靜通過，您只會在畫面上看到
      一列莫名其妙的流向，卻不知道原因。

  二、四岔以上出現橫線不再安靜刪掉（本版最重要的一項）。
      實測四到七岔的轉向與去向是一對一、不會兩個轉向撞在一起，
      「幽靈重複列」是三岔獨有的。所以四岔會畫橫線通常代表
      **禁止轉向或單行道**，那是真實的路口管制資訊。
      v2.1.56 的過濾一視同仁，而且完全不出聲，碰到這種檔案會
      **安靜少掉一條流向**。本版改成列進確認視窗。

      ⚠️ 預設選項仍然是「移除」，也就是**結果與 v2.1.56 完全相同**
      （端對端實測：兩版都是 11 條流向、總量都是 680／680）。
      本版改變的是「這件事看不看得見、改不改得回來、答案記不記得住」。

      三岔那些照預期畫的橫線**維持安靜移除**，不會每季跳出來煩您。

  三、整欄空白時給出有依據的建議。橫線畫得比預期少 N 個、
      而剛好有 N 個轉向整欄空白時，系統會指認那幾個並預設「沒有這個轉向」，
      視窗上直接寫出算術依據。數量對不上就不給建議——
      寧可讓您自己決定，也不要把猜的裝成有依據。

■ v2.1.57 另修兩處

  一、「全部先保留（不記住）」按下去之後會留下殘留標記。
      這顆按鈕只清掉視窗，沒有清掉流向上的「待裁決」標記，而那個標記
      會一路存進資料與備份；組題目時又是掃**全部**紀錄找待裁決項目、
      不是只掃這次匯入的，所以按過一次之後，之後匯入**任何一個無關的
      路口**都會把舊的未答項目再抓出來問一次。瀏覽器實測重現：
      殘留 3 條、第二輪匯入無關路口時視窗又跳出來。
      清掉殘留不影響「該問的還是要問」：重新匯入同一個路口時會依
      調查表欄位重新判定再問（第三輪實測有跳）。

  二、確認視窗的版面。視窗只設了最大高度卻沒指定各列高度，內容一長
      就把底下的按鈕擠出畫面外、「套用並記住」點不到。本版說明變長後
      實測撞到，已改成清單自己捲動、按鈕永遠留在畫面上。

■ v2.1.56：「分不出來」時改成跳視窗問您（不動計算）

  v2.1.55 是把「分不出來」寫成匯入提醒。您指出提醒可能不會被注意到——
  而這個答案會直接改變流向清單、路口轉向圖與各項成果，所以本版改成
  匯入完成後跳視窗，逐條請您決定。

  什麼時候會跳：只有某個轉向欄**既沒有填數值、也沒有寫「--」**時才跳。
  寫了數字（含 0）代表這個轉向存在，寫了「--」代表不存在，兩種都不會問。
  實測 73 份轉向版型的調查檔：有數字 4,488 欄、純橫線 224 欄、
  整欄空白只有 12 欄（集中在 1 份檔）——絕大多數匯入不會看到這個視窗。

  視窗裡每一條可以選：
      「有這個轉向，只是量到 0」→ 保留（預設）
      「沒有這個轉向，請移除」  → 移除那條流向

  被移除的流向整份調查都是 0，**任何加總都不會改變**。
  直接關掉視窗等於全部保留，因為保留最多是多一列 0，
  刪掉卻可能弄丟真實流向。

  **答案會記住。** 按「套用並記住」之後，同一個路口的同一個轉向
  下一季匯入直接套用，不會再問第二次；答案也會跟著備份走，
  換一台電腦還原之後一樣不會重問。
  如果這次還不想決定，可以按「全部先保留（不記住）」。


■ v2.1.55：修正 v2.1.54 的一個記帳錯誤，並補上「分不出來」時的提醒（不動計算）

  【一】空白格不可以算成「量到 0」

    v2.1.54 為了分辨「沒有這個轉向」與「整天量到 0」，在解析時逐欄
    記了「出現過幾格數字、幾格橫線」。那個記帳有錯：**空白格也被算成數字**
    （把空字串丟給 Number() 會得到 0，而 0 是有限數）。

    後果是「整欄空白」被記成「整欄量到 0」，於是「調查表沒填」與
    「調查表填了 0」分不出來。這個錯也讓我第一次的量測完全失真——
    掃過 201 份檔說「一欄空白都沒有」，其實不是沒有，是全被算成數字了。

    修正後重新量（73 份轉向版型的預覽）：
        有數字 4,488 欄｜純橫線 224 欄｜**整欄空白 12 欄**
    那 12 欄集中在 1 份檔的一個整塊空白區塊。

    **對真實檔的判斷結果完全沒有改變**（八筆真實紀錄複驗：三岔 6 條、
    四岔 12 條、沒有重複的起訖、路線流量合計＝支線流量合計），
    因為真實檔沒有「同一欄既有橫線又有空白」的情形。但記帳本身
    現在才是誠實的，也才量得出下面那件事。


  【二】「整欄空白」會在匯入提醒裡講出來

    調查表的兩種寫法對應兩件不同的事：

        `0`  ＝ 這個轉向存在，只是整天量到 0
        `--` ＝ 這個路口根本沒有這個轉向

    如果一欄**既沒有填數值、也沒有寫「--」**，那就是沒填，
    系統沒有辦法判斷是哪一種。

    這時候**一律保留**，當成「有這個轉向、整天量到 0」——
    保留最多是畫面上多一列 0，刪掉卻可能弄丟真實的流向。
    但保留是預設值、不是判斷，所以匯入辨識結果的「檢查」欄會寫出
    是哪幾支支線的哪個轉向整欄空白，並提醒您：若確定該路口沒有這個轉向，
    可以到「道路與流向管理」把那條流向刪掉。


■ v2.1.54：兩項修正（不動計算）

  【一】三岔路口的空流向，改用「調查表原本寫什麼」判斷

    v2.1.53 是用推論的：整份調查為 0，而且起訖與另一條有流量的重複，
    就當成不存在。實測發現這樣會誤刪——三岔路口如果有一個
    **真的存在、但整天量到 0** 的轉向，而它剛好與一條有流量的轉向
    指到同一支路口，v2.1.53 會把它一起刪掉（實測 7 條變成 6 條）。
    總量不受影響，但畫面上會少一列真實的流向。

    真實檔裡這種形狀是有的：四岔與七岔的調查檔各有 6～9 欄是
    **真的整天量到 0**（不是不存在），左楠路－世運大道假日那一份
    更是同時有 12 欄純橫線與 1 欄真的量到 0。

    本版改成看調查表原本怎麼寫：

        `0`  ＝ 這個轉向存在，只是整天量到 0   → 保留
        `--` ＝ 這個路口根本沒有這個轉向        → 不建立流向

    做法是解析時逐欄多記兩個數字（這一欄出現過幾格數字、幾格橫線）。
    **解析出來的數值一格都沒有改**（`--` 一樣算 0），這兩個計數只是
    用來分辨上面那兩件事。

    複驗（八筆真實紀錄）：三岔 6 條、四岔 12 條、沒有重複的起訖、
    路線流量合計＝支線流量合計。


  【二】拿掉匯入頁的「調查檔格式範本」面板

  拿掉的是「季度批次匯入」頁上方那一整塊：三張範本卡片
  （平／假日全日整點轉向表、一般語意轉向表、全日路段車種表），
  以及它下面的「已記住的實際調查版型（N 種）」折疊區與「刪除格式記憶」按鈕。

  為什麼可以拿掉：
  ・版型從來不是使用者選的。每次匯入都是重新讀表頭當場判斷
    （見 inspectWorkbook），三張卡片只是在描述「系統看得懂哪些」。
  ・「已記住的實際調查版型」更是**只寫不讀**——程式把它記下來、
    顯示出來、可以刪掉，但**沒有任何一處拿它去影響解析**。
    所以「刪除格式記憶」那顆按鈕會讓人以為按了能改變辨識結果，
    實際上按不按都一樣。這是誤導，不是功能。

  刻意保留的部分：
  ・formatMemories 的**資料**照樣累積、照樣進備份，備份格式完全不變，
    舊備份還原不受影響。只是不再顯示。

  三張卡片裡唯一有用的那句——「只有路段車種、沒有左直右的檔案，
  系統會讀但不會誤建路口轉向成果」——改寫進手冊第 6 章，
  並點明它就是預覽表「角色」欄標成「非路口轉向」的原因。

  **沒有變更任何交通量或 PCU 計算，也沒有變更任何解析行為。**


■ v2.1.53：四項修正（不動計算）

  【一】三岔路口不再列出重複而且一輛都沒有的流向

    三岔路口的調查表，每一支支線一定有一欄整欄寫「--」
    （路口A 沒有右轉、路口B 沒有直進、路口C 沒有左轉），
    但欄位還是排出來。而三岔只有兩個去向，三個轉向硬要對到
    兩支目的支線，一定會有兩個轉向解到同一支。
    結果 9 條流向裡有 3 條是「路口重複、而且整份調查一輛都沒有」的空列，
    看起來就像同一個路口被列了兩次。

    **數字從來沒有被重複計算**：三份真實三岔檔實測，路線流量合計
    等於支線流量合計，差 0。壞的只有畫面。

    本版把那 3 條空列濾掉：三岔從 9 條變 6 條，四岔完全不變
    （12 條，一條都沒少）。被濾掉的整份調查都是 0，任何加總都不變。

    附帶說明一個已知限制：解析階段已經把「--」直接當成 0，
    程式目前分不出「這個路口沒有這個轉向」和「有這個轉向但整天量到 0」。
    所以判斷條件加上「同一組起訖另有一條真的有流量」——
    四岔路口真的整天量到 0 的轉向不會被誤刪。

  【二】資料改存在容量大得多的位置（IndexedDB）

    舊版把全部資料存在 localStorage，那個位置每個網站的硬上限
    實測是 4.94 MB，而且整個網站共用。單一個路口的快照平均 14.2 KB、
    最大 38.7 KB（七叉路口），加上還原點，一季就可能用完——
    這就是「儲存空間快滿了，這次存檔只保留 20 筆還原點」的來源。

    本版改存 IndexedDB。第一次開啟時會自動把舊資料搬過去，
    **搬完一定重新讀回來逐字比對相同，才會刪掉舊的**；
    比對不過就把舊的原封不動留著，下次開啟再搬一次。
    搬家成功時畫面右下角會說一聲。

    刻意不做「兩邊都存」：那會有「哪一份才是最新」的問題。
    保險仍然靠匯出備份。

    因為新的寫入是非同步的，本版另外加了兩道保護：
    連續修改時只有最後一次的結果算數；還在存檔時關掉分頁會先問一次。

  【三】還原點改成「一次操作一個」

    舊版是**每一筆紀錄各一個還原點**。一次批次重新匯入覆蓋 65 個路口，
    就一口氣長出 65 個看不出差別的項目。而且舊版的淘汰是逐筆切的，
    會把同一次操作攔腰切斷——還原回去只還原一半，比沒有還原點更危險。

    本版同一次操作只建一個還原點，清單上寫「…，涵蓋 65 個路口」，
    淘汰也整批進出。上限改成最近 30 次操作、總量 20 MB。

    畫面上每一個還原點旁邊多了「刪除這個還原點」，並且明講
    **刪除還原點不會影響現在畫面上的任何資料**。
    沒有做獨立的「清除還原點」按鈕——那顆按鈕使用者看不懂、也不敢按。

    舊備份與舊資料沒有批次資訊，會被當成「一筆一批」，照樣顯示、
    照樣還原得回去。


  【四】結論草稿的「小數位數」現在真的管得到百分比

    使用者在全日交通量實測回報，三支系統寫法相同、毛病也相同：
    產生器裡的 pct() 位數參數有預設值 1，而所有呼叫端都沒有傳，
    於是把小數位數改成 2 位時，百分比完全不動。

    本系統受影響五處：佔駛入、佔駛出、支線車種組成、路口車種組成，
    以及跨季度變動幅度的增減百分比（那一處原本直接寫死 toFixed(1)）。

    本版把使用者選的位數帶進這五處，並把 pct() 的預設值拿掉——
    日後漏傳會是編譯錯誤，不會再無聲失效。車輛數維持整數。

    **沒有變更任何計算**，只是顯示的位數會照您選的走。


■ v2.1.51：全面徹查後的四項修正（不動計算）

  【一】選「全日時段」時，三個地方的單位寫死成 PCU/hr

    批次成果包的 README、轉向進階核對 Excel 的「支線流量平衡」表、
    以及「連續 60 分鐘候選排行」面板，單位都寫死 PCU/hr；但「全日時段」
    的值是一整天的累計（PCU/調查日），語意差了約 24 倍。
    README 還把「全日時段」印成「FULL 尖峰」。

    前兩處改成跟著實際時段走；候選排行那張表其實不隨時段變動
    （永遠是 60 分鐘視窗），所以改成固定標「PCU/hr（固定 60 分鐘視窗）」。

  【二】混用時間格會靜靜算錯尖峰——現在會出警告

    如果同一份檔案裡有些時段是 15 分鐘一格、有些是整點一格
    （例如「全日整點、尖峰時段再拆細」），系統推算尖峰小時時
    會只取到其中一格，數值可能低估。而總量是對的，所以任何
    以總量為基礎的檢查都看不出來。

    **本版只加提醒，沒有改計算。** 改挑選邏輯會變更計算口徑，
    要連帶讓您既有的鎖定資料全部重新確認，那是您要決定的事，
    我不會自作主張。您目前的 37 份調查檔沒有一份是這種格式。

    判準做過調校：第一版只看「格距不完全一致」就報，實測會誤報
    19 筆（調查中間有休息時段的跳號、旅行時間格式的檔案都會中）。
    現在要「兩種以上、各佔兩成以上、而且各自至少重複兩次的規律格距」
    才報。獨立複查另確認：短時段只漏一列時，單次跳號即使剛好超過兩成，
    也不會被誤稱為混合時間格。

  【發布前獨立複查補正】

    轉向進階分析頁的「守恆差值」原本仍寫死 PCU/hr，選全日時段時會和
    同頁矩陣、平衡表的 PCU/調查日矛盾；現已改為跟著所選時段顯示。
    此處及上述混合時間格誤報均已新增守門測試。兩者只改單位文字或警告，
    不改尖峰挑選、OD、PCU 或任何交通量數值。

  【三】補上一項守門測試（v2.1.50 的說明與事實不符）

    v2.1.50 說「三項修正都有守門測試釘住」，但實測把「先重畫一次再解析」
    那項整個拿掉，測試仍然全綠——只有時間退路那一半有守到。已補上。

  【四】移除三項無效的檢查

    「解析中連點按鈕不會觸發第二次匯入」那三項是恆真的——停用中的按鈕
    本來就不會回應點擊，跟程式寫得對不對無關。改成在按鈕確實停用的期間
    送出真正的滑鼠點擊，再看檔案對話框有沒有被叫出來。

  **四項都沒有變更任何交通量或 PCU 計算**，LAST_CALC_CHANGE_VERSION 維持
  v2.1.30，37 份真實工作簿的解析結果與已發布的 v2.1.45 逐位元相同。


■ v2.1.50：按下「選擇檔案」之後馬上就看得到提示（不動計算）

  您回報過兩件事：

  (1) 點了「選擇檔案」、在系統對話框選了大量資料之後，對話框看起來沒有
      馬上關掉，畫面也沒有任何反應，但資料其實有在匯入。
  (2) 匯入的時候畫面沒有「讀取中」的提示，要等到全部讀完才跳出
      「已選取 X 份檔案」。

  這兩件其實是同一段空窗期。對話框關閉到程式收到檔案之間，完全是瀏覽器
  在讀您選的檔，程式插不進去；程式收到之後又立刻開始解析，把主執行緒
  佔滿，畫面來不及重畫。所以本版分兩段補：

  ・按下「選擇檔案」的當下就顯示「正在讀取您選擇的檔案，請稍候…」。
    選好檔會自己收掉；按「取消」時瀏覽器不會通知程式，改以視窗重新
    取得焦點後 1.2 秒當退路收掉。
  ・第一份檔案開始解析之前，先等一次真正的重畫，進度列在第一份就看得到，
    不必等到最後。分頁在背景時瀏覽器不會重畫，所以那次等待附 250ms
    時間退路，不會永遠卡住。

  三項修正都以「拿掉修正」的版本實測過守門測試會紅字：提示不出現、
  取消後提示收不掉、背景分頁 20 秒沒完成。

  另外針對「以為沒匯入成功、又匯了一次」這件事實測過：
  ・解析期間按鈕會停用，連點 7 次一次都不會觸發第二次匯入。
  ・同一批檔案匯第二次，紀錄數維持不變（不會變成兩份）；
    而且在按下確認之前，畫面就會標示「已存在第 N 版」與「採覆蓋」。
  ・判讀到一半重新整理頁面，不會留下半筆資料。

  **沒有變更任何交通量或 PCU 計算**，LAST_CALC_CHANGE_VERSION 維持 v2.1.30，
  固定計算黃金值完全相同。


■ v2.1.49：拖一般文字不會再被擋掉（不動計算）

  v2.1.47 加的「檔案掉在放置區外面不要把您帶離頁面」少了一層判斷：
  沒有先確認拖進來的是不是檔案，於是您在頁面內拖動選取文字
  ——例如把一段字拖到輸入框——也一起被擋住。實測輸入框、側邊選單
  與內容區三處都中。

  現在只有拖「檔案」才攔截，拖文字完全不受影響。
  全日交通量 v20.47 本來就是這樣寫的，本版與它對齊。

  另外把判讀進度的守門測試由「每 10ms 取樣」改成 MutationObserver
  全量記錄——短命的畫面狀態用輪詢會漏掉。

  **沒有變更任何交通量或 PCU 計算**，LAST_CALC_CHANGE_VERSION 維持 v2.1.30，
  固定計算黃金值完全相同。


■ v2.1.48：GPT 獨立複查修正匯入進度顯示與例外清理（不動計算）

  ・大量檔案依序判讀時，進度的序號、總份數與檔名現在始終一致，
    不會短暫把下一個序號配到上一份檔名，也不會顯示超過總份數。
  ・後續整理預覽若發生非預期例外，也一定解除「正在解析」與按鈕停用狀態。
  ・新增端對端守門檢查，逐筆核對畫面序號與檔名。
  ・沒有變更交通量、OD、尖峰、車種、當量或 PCU 計算；
    LAST_CALC_CHANGE_VERSION 維持 v2.1.30。

■ v2.1.45：年度輸入判定改看年份數值、與另外兩支系統對齊，並精簡新手手冊

  【三】新手手冊大幅精簡

    手冊裡累積了 37 則歷次版本的改善紀錄，占整本超過一半，新手要看的教學
    反而被埋在裡面；而同一份歷史在程式的「版本紀錄」頁（VERSION_HISTORY，
    文件明訂的唯一來源）、CHANGELOG.md 與本檔裡本來就有。
    使用者定的規則是：手冊只寫「目前的功能是什麼、怎麼用」——程式曾經出過
    什麼問題、哪一版怎麼修的，是維護者才需要的資訊。本版把手冊裡的版本紀錄
    全部移除（剩 0 則）並指向 CHANGELOG，混在教學段落裡的維護敘事
    （「舊版要輸入左右 X／上下 Y，v2.1.0 改成拖曳」這類）也改寫成現況描述。
    新增 tests/manual-version-log.test.mjs 守門，防止它再長回去。
    教學內容一個字都沒有刪，PDF 從 54 頁降到 30 頁。

  ※ 關於版號：v2.1.44 已產生但從未發布，內容全部併入本版。


  【一】把民國 115 打成 0115 不再被退回

    上一版依「位數」判定，四碼一律當成西元年，於是 0115 被判成「西元 115 年、
    超出範圍」，回一句「民國年請填 90～200」——但 0115 就是 115，訊息與事實
    矛盾；而共用的 normalizeSurveyPeriod() 一直都把 0115Q1 算成 115Q1，
    等於同一個模組裡，檢查與正規化對同一個字串有兩種看法。

    現在改看年份的數值（民國 90～200 與西元 2001～2111 兩段不重疊，數值本身
    就分得出來），並補上 NFKC 與去空白：從 Excel、Word 或 PDF 複製貼上很常
    帶進全形數字或全形 Ｑ，手打時也常多按一個空白；１１５Ｑ１、115 Q1、
    2026 Q1 現在都認得。

    可以填的年份範圍完全沒有改：89Q1、201Q3、2000Q1、2112Q3 一樣擋下。

  【二】三支系統的季度把關改由同一份「行為契約」鎖住

    上一版三支的實作各自被改成不同寫法，而三支的守門測試都只驗自己那一份，
    所以沒有任何一支看得到分歧。本系統那一支甚至斷言原始碼要含某一種實作
    的字樣，反而把分歧鎖住——想把三支同步回同一份程式會踩紅它。

    現在三支各放一份逐位元相同的契約檔（tests/period-input-contract.mjs，
    用 SHA-256 互相釘住），每一支都要跑完整份契約。任何一支改了行為或改了
    契約，就是它自己的測試紅字。

  ※ 本版未變更任何交通量或 PCU 計算；37 份真實調查表的解析結果與上一版
    逐字相同。LAST_CALC_CHANGE_VERSION 維持 v2.1.30。

■ v2.1.43：年度輸入補上範圍檢查

  上一版開放年度可以填民國或西元，但輸入框只做「去掉非數字、截到四碼」，
  沒有任何範圍檢查。而系統的換算只在民國 90～200（西元 2001～2111）之內，
  範圍外的四碼年份會**原樣存成季度鍵**——打成 2112 或 1990 就會這樣。

  它和對應的民國寫法（201Q3、79Q2）是同一季，排序位置也完全相同，
  會相鄰出現，畫面上只看得出「同一季出現了兩次」，很難想到是年份造成的。
  這正是上一版要消滅的那一類問題，只是發生在換算範圍之外。

  現在超出範圍會擋下，輸入框下方當場說明該填什麼，選檔按鈕也要檢查通過
  才會啟用；真的走到讀檔時再擋一次，避免按鈕被繞過。

  同時修掉另一個方向：民國兩碼年份（例如 99，即西元 2010）是合法寫法，
  排序與換算一直都認得它，不該被輸入檢查擋掉。

  ※ 檢查集中在共用期別模組的 checkSurveyPeriodInput()。選檔、確認寫入都會
    使用同一規則；即使預覽後再把年度改錯，也不會把錯誤季度寫進資料庫。
  ※ 本版未變更任何交通量或 PCU 計算。

■ v2.1.42：季度可以填民國年或西元年

  調查年度現在民國與西元都能填。舊版的年度欄位只收三碼，打「2026」會被
  靜靜截成「202」而且沒有任何提示；拿到以西元年標示的委託案時只能自己換算。

  但**資料一律以民國年記錄**。您填 2026Q2，系統會存成 115Q2，輸入框下方
  會即時顯示「將存成 115Q2」。這是必要的——如果照您打的字原樣存，同一季會
  因為寫法不同而變成兩個分開的季度，歷季比較被拆成兩段而且永遠不會合併；
  更麻煩的是它們的排序位置完全相同，畫面上看起來只像「同一季出現了兩次」，
  很難想到是寫法造成的。

■ v2.1.42（續）：畫面與匯出可以切換民國年／西元年

  頁首多了一顆「年份顯示」按鈕，按一下在民國年（115Q1）與西元年（2026Q1）
  之間切換。畫面上的表格、圖表、下拉選單、結論草稿，以及匯出的 Excel、
  轉向圖 SVG／PNG 與 PDF，全部跟著同一個設定走——不會出現畫面寫 2026Q1、
  交出去的報表卻寫 115Q1 的情形。

  【這顆按鈕只換看到的字，不動任何一個數字】

    資料一律以民國年儲存（見上一節）。分組、排序、識別鍵、季度篩選與所有
    計算，全部走儲存的那個值；切換年份寫法不會挑到不同的資料，也不會讓
    任何數字改變一位。下拉選單的「值」也仍然是民國年，只有顯示的文字換掉
    ——這一點很重要，否則切成西元年之後，結論草稿的單季條件會挑不到資料。

    端對端測試會在切換前後把整個畫面逐字比對一次：除了年份那幾個字，
    每一格數字都必須完全相同；並且掃過每一個分頁，確認切成西元年之後
    畫面上不會再殘留民國年寫法。

  【兩個顯示開關可以各自獨立使用】

    ・期別顯示：季別（115Q1）⇄ 實際調查月份（115年2、3月）
    ・年份顯示：民國年（115Q1）⇄ 西元年（2026Q1）
    兩個一起開就是「2026年2、3月」。

  【有三處刻意維持民國年，不跟著切換】

    ・匯入時的「將存成 115Q2」「本批次將寫入 115Q1」「確認寫入 115Q1」
      ——這幾句講的就是「會存成什麼」，那個值本來就是民國年。
    ・備份與專案包等資料檔（會被再匯入的），一律維持民國年儲存值。
    ・更新說明與操作手冊裡舉例用的季度字樣。

■ v2.1.41：三支系統跨系統徹查後的七項修正

  本版是「路口轉向、全日交通量、交通服務水準」三支一次跨系統徹查的結果。
  作法是拿 37 份真實調查表分別餵給不同系統、比對彼此的判讀與數字，
  凡是不一致的就往下追。

  【一】工作表名稱前面多一個空白時，平日與假日會被合併成一筆（最嚴重）

    判斷「平日」「假日」資料頁的規則以前只容許名稱後面有空白。
    您提供的 37 份真實檔裡有 11 份的分頁名稱帶空白，只要空白出現在前面，
    系統就數不到兩張日別分頁，於是退回「整份合併讀取」——
    不是少讀一天，而是平日的量被加進假日那一筆。

    實測 11535T1502 一份真實檔（只改分頁名稱、資料完全不動）：
      分頁「假日」「平日」        → 2 筆：假日 86,207 輛、平日 108,028 輛
      分頁「 假日」「 平日」      → 1 筆：假日 194,235 輛  ← 虛增 125%
      分頁「假日 」「平日 」      → 2 筆：假日 86,207 輛、平日 108,028 輛

    總量守恆（194,235 = 86,207 + 108,028），所以任何以總量為基礎的檢查
    都抓不到，畫面只顯示匯入成功。本版前後空白都容許，與全日交通量一致。

  【二】匯出的轉向圖檔名會帶資料別

    平日與假日是兩筆各自獨立的紀錄，但匯出 ZIP 的檔名以前只有站號加時段，
    同名檔會直接覆寫——同一站只會留下一張圖，而且從檔名看不出留下的是
    平日還是假日，過程沒有任何提示。單張 SVG／PNG 下載也一樣。

  【三】表頭寫「大貨車」「大客車」時會保留原名，不再依欄位位置歸類

    舊版看到「第 3、4 個車種欄位」就把它們當成大型車與特種車，
    於是大客車拿到特種車的當量 2.5——但大客車在工程上通常算大型車（1.5），
    系統沒有立場替您決定。本版比照全日交通量，把不認得的車種保留成
    自訂車種，由您在車種分類中自行歸類。

    ※ 總車輛數不變。若您照舊版的方式歸類（大貨車→大型車、大客車→特種車），
      PCU 也一位都不會變。

    ※ 表頭寫的都是內建四車種、只是順序亂掉或有殘留舊值時（例如 11017T1501
      七叉路口的合併儲存格裡藏著沒清乾淨的「大型車」），仍會用欄位位置救援。

  【四】全形數字會正確讀成數字

    儲存格若是全形的「１２３」，舊版計 0 輛，全日交通量卻讀成 123——
    同一格資料在兩支系統得到不同的數字。

  【五】「--」「－」「—」「–」不再被誤報成壞資料

    這是調查表裡「該轉向不存在」的標準記法，全日交通量早就視為合法。
    實測 11017T1502 一份真實檔因此跳出「有 192 個儲存格有內容但不是數字」——
    192 次誤報會讓人學會忽略這個警告，而它本來是要抓 N/A、休 這類真正的壞資料。

  【六】算不出來的統計範圍寫「－」，不再寫 0

    不足 24 小時的調查沒有「全日尖峰小時」，舊版的車種組成分析表照樣產生
    那幾列、數量寫 0、組成比例寫 0.0%，那些 0 會被 Excel 的加總與平均吃進去。

  【七】三支共用的非調查日期清單同步

    「彙整日期」「輸出日期」「建檔日期」「產製日期」現在三支都會排除。

=================================================
以下為 v2.1.30 當時的原始說明，保留供對照
=================================================

路口轉向 — 原始碼修正包 v2.1.30
=================================================

⚠️ 這一包不是可以直接整包上傳的網站包。請務必先看最後一節。

⚠️ 本版**有變更計算口徑**（兩項，見 v2.1.30-3 與 v2.1.30-4），
   而且 v2.1.30 以前鎖定的季度會亮出「鎖定衝突」。
   這是使用者確認過的決定，不是意外。
-----------------------------------------------------------------

■ v2.1.30-1：新增「全日時段」與「全日尖峰小時」兩大類統計

  使用者回報：系統只有上午尖峰與下午尖峰，所以路口轉向圖只有兩種圖；
  車種組成分析與各路口駛入／駛出流量也缺全日尖峰。

  現在四個統計範圍都有：
    上午尖峰、下午尖峰、全日尖峰小時、全日時段

  「全日尖峰小時」＝不分早晚，從一整天 24 小時裡找出交通量最大的那一小時。
  「全日時段」＝一整天的累計量，單位是 PCU/調查日、輛/調查日
  （和尖峰的 PCU/hr **不是同一種東西**，不可以相加或比較，
   所以畫面與 Excel 的單位會跟著時段自動改變）。

  ⚠️ 全日尖峰小時**需要完整 24 小時的調查資料**。
     只做了幾個小時的調查（例如常見的 07:00–09:00＋17:00–19:00，合計 4 小時）
     一律顯示「－」並寫明原因。不這樣做的話，那個數字必然等於上午與下午
     尖峰裡較大的那一個——看起來像新資訊，其實只是換個名字，
     還會被當成一整天的最大值寫進報告。

  ⚠️ 舊資料要重新匯入才會有全日尖峰小時。
     尖峰小時要從逐時間格的原始資料滾動搜尋，而系統為了節省儲存空間，
     匯入完成後只保留算好的結果，沒有留下逐格資料，所以無法回推。
     「全日時段」不受影響（它是從已存的調查總量現算的，舊資料直接就有）。
     已匯入的上午／下午尖峰數字**一個都不會變**。

■ v2.1.30-2：「全調查時段」不等於「全日」，現在會寫出實際時數

  車種組成分析裡的「全調查時段」，定義是「這份檔案匯入到多少就算多少」。
  24 小時的檔案它才等於全日；只做 4 小時的檔案以前也叫「全調查時段」，
  很容易被當成一整天。現在一律寫成「全調查時段（4 小時）」這種形式。

  這是舊版就存在的問題，不是本版新增的。

■ v2.1.30-3：⚠️ 計算口徑變更（一）——上午／下午尖峰的搜尋範圍

    舊：上午 [05:00, 12:00)   下午 [12:00, 23:00)
    新：上午 [00:00, 12:00)   下午 [12:00, 24:00)

  舊的兩段**合起來掃不到 23:00–24:00 與 00:00–05:00**。真正的尖峰若落在
  那六個小時，系統任何一個數字都抓不到。新的兩段剛好把一天鋪滿而且不重疊。
  「視窗頭尾都要落在範圍內」那一條沒有動（v2.1.22 的修正），
  所以上午與下午仍然不會重疊。

  實測使用者提供的 8 份實際調查檔，**新舊口徑算出來的上午／下午尖峰完全相同**。

■ v2.1.30-4：⚠️ 計算口徑變更（二）——每一格的車輛數先四捨五入成整數

  使用者的某些調查檔，儲存格裡存的是小數（0.36、5.5506…），Excel 的格式
  把它顯示成整數。於是報告上看到 0 與 6，程式拿去算的卻是 0.36 與 5.5506，
  全日車輛數會算出「27,988.79 輛」這種不存在的車，PCU 也跟著帶小數。

  依使用者決定，以畫面上看到的整數為準。四捨五入放在**全系統唯一讀取原始
  儲存格的那一行**，之後的加總、尖峰滾動、PCU 換算、全日累計全部自動吃到整數。

  實測影響（三份 24 小時檔）：
    06525T2503  24,463.3 PCU／27,988.8 輛 → 24,451.2 PCU／27,985 輛（差 −12.1／−3.8）
    06525T2502  無變化
    06525T2501  無變化

■ v2.1.30-5：⚠️ LAST_CALC_CHANGE_VERSION 推進到 v2.1.30

  因為上面兩項確實變更了計算口徑，**v2.1.30 以前鎖定的季度會亮出
  「鎖定衝突」紅字**。使用者已明確接受（可解除鎖定、確認後重新鎖定）。

■ v2.1.30-6：轉向圖新增「車輛數」顯示方式

  「顯示」原本只有交通量、百分比、PCU/hr＋百分比三種，車種選「全部車種」時
  只拿得到 PCU（各車種乘上當量後的總和）。新增的「車輛數」一律報實際調查到
  的輛數，搭配右邊的車種篩選，就能看單一車種或全部車種的輛數。

■ v2.1.30-7：與全日交通量系統交叉驗算，15 項全部一致

  兩套系統的轉向 PCU 當量係數完全相同，所以同一份 24 小時調查檔應該算出
  同一組數字。實測三份 24 小時檔的「全日時段 PCU／輛」與「全日尖峰
  時段／PCU／輛」共 15 項，兩邊完全一致。

  ⚠️ 但有一項兩套系統目前不一致：全日交通量**沒有**「每一格先四捨五入」
     這一條。同一份帶小數的檔案餵給兩套系統，全日交通量那邊仍會算出
     帶小數的那一組。要不要一併補上，請使用者決定。

■ v2.1.30-8：車種完全沒有動

  使用者特別交代「範例檔只有 4 種車，但系統要保留可以匯入多車種」。
  新增的計算一律走 recordVehicleIds()（＝資料裡實際出現過的所有車種，
  含自訂車種），沒有任何一處寫死那四種。守門測試釘住這一點。

■ v2.1.30-9：新增守門測試 17 項，其中 8 項行為檢查對未修正版實測 7 項紅字

  因為新的函式在 v2.1.29 根本不存在，直接拿新測試去跑只會「模組沒有這個
  匯出」——那不算證明。另外寫了一支只用兩版都讀得到的欄位的行為檢查，
  對 v2.1.29 與 v2.1.30 各跑一次：v2.1.29 是 1 通過／7 紅字，v2.1.30 是 8 通過。
  唯一「通過」的那一項是空的通過（舊版根本沒有全日尖峰這個概念），已標註。

-----------------------------------------------------------------
以下是前幾版的說明
-----------------------------------------------------------------

■ v2.1.29-1：修好 v2.1.28 新增的「被更新版本鎖定」警示（我自己複查抓到的）

  v2.1.28 新增了一條分支，用來提醒「這筆成果是被比目前更新的版本鎖定的」。
  它少了一件事：沒有先確認兩個版號都解析得出來。

  isVersionAtLeast() 認不得格式時一律回傳 false。用在「鎖定版本早不早於
  某個常數」是對的（不知道就當作不早於，不報警），但用在這條新分支會反過來
  ——只要 VERSION 帶了 -final、-rc1 這種尾巴，或前後多一個空白，
  就解析不出來，於是**每一筆 v2.1.21 以後鎖定的資料全部亮紅字**，
  正好是 v2.1.28 要修掉的那個症狀。實測 19×19 的版號矩陣會多出 344 筆紅字，
  而且訊息還說反了（「由較新的版本鎖定」）。

  改成兩個版號都解析得出來才判斷。版號認不得是「不知道」，不是「有問題」。

■ v2.1.29-2：四大車種的名稱，兩張表對齊

  app/traffic-app.tsx 的 VEHICLE_LABELS 寫「大型／大客車」「特種／聯結車」，
  lib/traffic.ts 的 CORE_VEHICLE_LABELS 寫「大型車」「特種車」，
  而匯入時寫進紀錄的是後者。vehicleLabel() 優先讀紀錄裡的名稱，
  所以 v2.1.19 以前沒有 vehicleLabels 欄位的舊備份會落到前者——
  跨季比較與結論草稿因此同一個車種前後兩種寫法。

  以匯入時實際寫入的那一組為準（也就是「大型車」「特種車」）。
  「車種轉向當量」那一頁用的是另一組較長的名稱，那是不同的畫面，刻意不動。

  新增第 12 項發布結構檢查釘住兩張表一致，已實測會在舊寫法下紅字。

■ v2.1.29-3：重新打包，中文檔名標記 UTF-8

  v2.1.28 交付的 ZIP 沒有標記 UTF-8，解壓後中文檔名全部變成 #Uxxxx 亂碼，
  包內因此找不到程式連得到的手冊檔名——發布結構檢查第 4 項紅字
  （44 項中 2 項失敗）。**那是打包的問題，不是程式的問題**：
  檔名還原後 44/44 全過。

  ⚠️ 線上網站不受影響（已由使用者實測手冊下載正常）。
     受影響的只有交付出去的備存 ZIP：拿它還原 repository 會少掉手冊檔。

■ v2.1.28-1：修正「鎖定衝突」誤報（使用者回報）

  畫面上出現紅字「偵測到鎖定衝突：鎖定版本與目前系統版本不同」。
  舊版是 record.resultLock.version !== VERSION——字串完全相等比較，
  所以**升過一次版，所有已鎖定的資料就永遠亮紅字**，下一版又全部重來。
  使用者有 20 幾季資料，那就是 20 幾筆永遠消不掉的警告。

  更糟的是它會淹掉真正該看的那一個：「鎖定後的資料內容已變更」。
  兩種訊息長得一模一樣紅，看久了連真的那一次也會被略過。

  新增 LAST_CALC_CHANGE_VERSION（目前 v2.1.21，最後一次變更尖峰小時口徑
  的版本）與 lockStatus()：只有鎖定當時的版本**早於**它才算衝突；
  升版但沒動計算改為一行灰字說明。真正的衝突（資料被動過）維持紅字。

  ⚠️ 使用者不需要、也不應該為了那行紅字去解除並重新鎖定——
     重新鎖定會蓋掉原本的鎖定時間與版本，那是稽核紀錄。

  ⚠️ 日後若真的改了計算口徑（PCU 當量、尖峰視窗、駛入推導、車種歸類…），
     LAST_CALC_CHANGE_VERSION 要跟著改成新版號，否則舊鎖定會被誤判為仍然有效。
     只改介面、文件、測試或發布流程則不要動它。常數的註解裡也寫了這一點。

  新增單元測試 6 項（含「版號要照數字比大小，不是照字串」——
  字串比較會說 v2.1.9 > v2.1.10）與端對端檢查 2 項。

■ v2.1.28-2：把測試示範資料裡的實際名稱換掉

  scripts/seed-state.json 與 seed-wide.json 是自動測試用的示範資料，
  裡面寫著實際的委託案名稱、計畫編號、站號與路口名稱，而這兩個檔案就跟
  程式一起放在公開的 repository 裡，也能從 GitHub Pages 直接下載。
  （裡面的流量數字本來就是 seed-state.mjs 用固定種子產生的假資料，
   不是實際調查值；但名稱是真的。）

  已全部換成「示範捷運延伸線示範標」「示範1－示範交流道路口」與
  A0000／S01-01 這類假識別碼，依名稱選取的 e2e 腳本也同步改掉。

  ⚠️ lib/ 與 app/ 裡的路口名稱**刻意沒有動**：referenceMovementForOd 是靠
     路口名稱同時含「中山北路」與「岡山路」來決定要不要套用七叉參考轉向表，
     那是產品行為，改掉會影響實際匯入。要不要改是產品決策，不是清理工作。

■ v2.1.28-3：新增 .gitattributes，換行字元不再被自動改寫

  上一版的備份 ZIP 裡每一個文字檔都被轉成 CRLF，連建置產物也一樣，
  結果 assets/index-<雜湊>.js 的內容不再雜湊成它自己的檔名，
  備份包與線上內容無法逐位元核對——而網站照樣跑得動，完全看不出來。
  成因是在 Windows 上取出（core.autocrlf）。
  本版新增 .gitattributes（`* -text`），包內所有文字檔也已正規化為 LF。

  ★ 打包時請用 git archive 從 commit 直接產生，不要用會改寫換行的工具。

■ v2.1.28-4：npm test 會先跑 lint；發布結構檢查 9 → 11 項

  程式碼檢查本來沒有被任何自動流程執行，壞了也不會有人知道
  （姊妹系統「全日交通量」就這樣紅了很久沒被發現）。
  現在 npm test 改成先跑 npm run lint，CI 從此會把關。

  第 10 項：測試種子與選取腳本不得出現實際的計畫、站號或路口名稱。
  第 11 項：必須有 .gitattributes 且關閉換行轉換。
  兩項都已實測會在對應的錯誤狀態下失敗，還原後全部通過。

■ 沒有變更任何計算規則

  交通量、PCU、駛入／駛出、尖峰搜尋、車種組成全部未動。
  鎖定衝突的判斷屬於「要不要顯示警告」，不影響任何數字。

■ v2.1.27：把上一版修好的發布結構釘進自動檢查

  v2.1.26 修好了四件發布層面的問題（單一部署路徑、補 .nojekyll、
  清掉殘留資產與舊手冊、統一套件管理），但**沒有任何東西擋著它們再壞一次**
  ——這四件事壞掉的時候畫面完全正常，版號、數字、按鈕全部看起來都對，
  只有實際打開網站的人會拿到舊版本。

  新增 tests/release-structure.test.mjs（當時 8 項，現為 19 項），釘住：
    1. 版號在每一個寫著它的檔案裡都一致（漏改就是線上手冊 404）
    2. CHANGELOG 最新一則等於程式版號
    3. 手冊封面戳記與每一頁頁尾的版號、日期一致
    4. 手冊有本版更新說明，且沒有留著舊版手冊檔
    5. .nojekyll／.gitignore／.github／.openai/hosting.json 都在
    6. workflow 不含任何發布步驟（只能有一條部署路徑）
    7. 套件管理只有 npm 一套
    8. 根目錄沒有殘留上一版的雜湊資產

  這 8 項已逐一實測會在對應的錯誤狀態下失敗，共 11 種情境，
  還原後全部通過。只加會通過的檢查等於沒加檢查。

  另外四件：
  ・重寫 DEPLOYMENT.md。舊版仍寫著「兩種相容部署路徑」、已退役的
    GPT Site 網址，以及一組 pnpm 指令——照著做會把剛移除的 pnpm 裝回來。
  ・README 與 DEPLOYMENT 補上：npm ci 會連 cdn.sheetjs.com，
    連不到時的 403 是網路問題不是程式問題。
  ・修正 scripts/stress-drag.mjs 寫死了某一台機器上的絕對路徑，
    在別台電腦上一執行就丟 ENOENT。
  ・CHANGELOG.md 改為指向 lib/traffic.ts 的 VERSION_HISTORY 這個唯一來源
    （本檔曾漏掉 v2.1.5～v2.1.25 共 21 個版本而沒有人發現）。

■ v2.1.26：發布、依賴安全與交付一致性

  1. GitHub Pages 固定由 main 分支根目錄發布；Actions 只負責建置與測試，
     不再由第二條 workflow 重複發布。
  2. 補入 .nojekyll，並清除舊雜湊資產與 v2.1.21～v2.1.25 舊手冊。
  3. 套件管理統一使用 npm；正式網站相依套件安全掃描為 0 項弱點。
  4. 程式檢查已修正為 0 錯誤、0 警告。
  5. 交通量計算、車種當量、駛入／駛出、尖峰搜尋及報表公式均未改動。
  6. 人工上傳 GitHub 時，務必確認 .github、.nojekyll、.gitignore 等
     點號開頭項目沒有被檔案選擇器略過。
  7. 驗證線上版本時，只信帶版號手冊、?v= 參數與新版專屬雜湊資產；
     不以固定檔名判斷，以免 CDN 快取造成誤判。

■ v2.1.25：佔比拆成兩個方向

  使用者問：「駛入 422.4／駛出 417.8／佔駛入 10.6% —— 這個 10.6%
  是方向無關的值，還是只是駛入的？」

  是駛入的，而且**舊版永遠只寫得出駛入那一組**。兩個百分比其實都算了，
  但輸出是「if (inShare) … else if (outShare) …」，只有整筆完全沒有駛入
  資料時才輪得到駛出；而 inflowPcu 由 destinationFlowTotal() 產生、
  那支函式永遠回傳數字，所以「佔駛出」是**執行不到的死碼**，
  一次都沒有被寫出來過。選項名稱又只寫「佔路口總量百分比」，
  沒說是哪個方向，標籤還會隨資料在兩者之間悄悄變動。

  兩個方向的分母相同（都是路口總量，因為駛入合計必然等於駛出合計），
  但分子不同，數字差很多——實測某一筆：
    路口B 駛入 10.4%／駛出 27.6%
    路口F 駛入 42.8%／駛出 14.7%

  拆成「各支線佔駛入路口總量百分比」與「各支線佔駛出路口總量百分比」，
  各勾各的；兩個都勾時駛入排在駛出前面。

  **沒有動任何計算**：舊的 share 鍵只換成 shareIn，**不是**展開成兩項
  ——展開會讓既有範本多出一段從來沒有過的「佔駛出」，那就不是輸出不變了。
  已用整份字串比對的迴歸測試釘住。

  新增單元測試 5 項（含「兩個方向用同一個分母、各自加總都是 100%」的
  防線——日後有人把駛出的分母換掉，每一格看起來仍是合理的百分比，
  畫面上看不出來）與端對端檢查 3 項。

■ v2.1.24-1：範本現在專屬於各自的計畫

  結論草稿的「條件範本」與報表的「匯出項目範本」，舊版都存成一份
  扁平清單、所有計畫共用——在甲計畫存的範本，切到乙計畫照樣列出來。

  這不只是看了礙眼：結論條件裡存著 intersectionKeys 與 branchNames，
  那是該計畫專屬的識別字，套到別的計畫會篩出 0 筆而找不出原因。
  這支程式其實已經知道這件事（換計畫時本來就會重設「正在編的條件」
  與草稿，理由就寫在那段註解裡），漏掉的只有「已存起來的範本清單」。

  改法與 pceByProject／catalogByProject／mappingsByProject 完全一致，
  連 setter 都沿用同一個 scopedSetter。

  **既有範本不會不見**：升版時舊的扁平清單會分給每一個計畫各一份，
  請使用者到各計畫把用不到的刪掉一次，之後新存的就只屬於當下那個計畫。
  （全部歸給某一個計畫的話，其他計畫就再也找不到過去存的範本。）
  備份與還原同樣依計畫分開帶，舊版備份的扁平清單也讀得懂。

■ v2.1.24-2：「各支線各車種駛入／駛出車輛數」拆成兩個勾選項

  舊版一勾就是兩個方向都寫，只需要其中一個方向時只能產生完再自己刪。
  拆成「各支線各車種駛入車輛數」與「各支線各車種駛出車輛數」兩項。

  只勾一個方向時「呈現方式」那一區會收起來——「雙向合計」是把兩個
  方向加起來，對單一方向來說那是**錯的數字**（會把另一個方向的車也
  算進去），而且它一樣會印出一行看起來合理的文字，畫面上看不出來。

  **沒有動任何計算**：舊的 branchComposition 鍵在 normalizeCondition
  裡原地展開成新的兩項，既有範本與備份產生的草稿與 v2.1.23 逐字相同，
  已用「整份字串比對」的迴歸測試釘住（只比幾個 match 驗不出多寫或
  少寫一段）。

■ 新增測試

  單元測試 6 項、備份完整性測試 6 項（新檔 tests/backup-completeness.test.mjs）、
  端對端檢查 6 項（切計畫看不到別的計畫的範本、單一方向的輸出）。
  備份完整性那一支已驗證在 v2.1.23 的程式上會失敗 5 項、在本版全數通過。

■ 上一版（v2.1.23）：結論草稿的「時段」可以兩個都不勾

  需求

  只想要某個路口「全調查時段」的車種組成那一行，但舊版強制
  「時段至少要留一個」——取消最後一個尖峰時會被自動加回去，
  草稿一定夾帶不要的尖峰段落，只能產生完再自己刪。

  兩個都不勾 ＝ 不敘述尖峰時段，只寫全調查時段的數值。
  這時記得在「要寫哪些數字」勾起「車種組成」。

  配套三件事：統計範圍那一行會寫明「不敘述尖峰時段」；
  沒有尖峰時不再印「PCU/hr 是該尖峰一小時的流率」那句說明；
  兩個都不勾卻只選了寫在尖峰底下的項目時，草稿會直接說明產生不出內容，
  而不是交出一份只有標題的空草稿。

  **沒有動任何計算**：有勾時段時的輸出與 v2.1.22 完全相同，已用測試釘住。

  另外修正一個相關的小陷阱：舊範本沒有 peaks 欄位時仍要補上預設的
  上午＋下午，但「有欄位、是空陣列」是使用者刻意的選擇，不可以被補回預設。
  這兩種情況以前被當成同一件事。


■ v2.1.45 正式發布驗證

  發布成品已使用內附且雜湊驗證通過的 SheetJS 0.20.3 重建。
  程式測試與瀏覽器端到端流程全部通過；主資產為
  assets/index-C0pvj8lC.js。


■ v2.1.47：拖曳上傳的視覺回饋，以及判讀大量檔案時的進度（不動計算）

  【一】拖曳時看得出來丟得中

    上傳卡片本來就接得到拖曳，但畫面上完全沒有變化——拖到一半
    看不出來「丟這裡對不對」，只能賭一把放開。
    現在拖曳經過時整張卡片會亮起來，放開就收掉。

  【二】備份還原也可以用拖的

    那一張卡原本只能按按鈕選檔，現在可以直接把備份檔（ZIP 或 JSON）
    拖進去。一次拖多個會取第一個，並且會講清楚用了哪一個。

  【三】檔案掉在放置區外面，不會再把您帶離頁面

    以前檔案不小心掉在卡片旁邊時，瀏覽器會直接開啟那個檔案，
    等於離開系統畫面。現在會擋住，游標也會顯示「不可放置」。

  【四】判讀大量檔案時看得到進度

    使用者回報「上傳了大量的檔案後，因為沒有『讀取中』等提示文字，
    會誤以為沒上傳成功」。舊版判讀中只有按鈕文字換成「正在解析…」，
    一動也不動，檔案一多就分不出還在跑還是當掉了。

    現在按鈕會顯示「正在解析… 3／12」，上傳卡片上另有一行寫出
    正在讀哪一個檔名。看到數字在跳就是還在跑，不必重按。

  **沒有變更任何交通量或 PCU 計算**，LAST_CALC_CHANGE_VERSION 維持 v2.1.30，
  固定計算黃金值完全相同。


■ v2.1.46：結論草稿只留一顆「產生草稿」，並補上保守的結果捲動（不動計算）

  舊版在頁首與草稿框旁邊各有一顆按鈕，呼叫的是同一個函式。
  使用者指出實際動線用不到頁首那一顆：條件與條件範本都在下方，
  「哪怕條件沒變，為了確保資料正確，正常情況下仍會往下滑動確認條件」，
  所以每一條動線最後都停在草稿框旁邊。兩顆同名按鈕反而讓人以為有差別，
  也可能讓新手在還沒勾任何條件時就按下去，拿到一份用預設條件產生的草稿。

  現在頁首那一顆移除，草稿框旁邊那一顆由「重新產生」改名為「產生草稿」。
  行為完全不變：已經手改過的草稿仍會先詢問再覆蓋。

  另外補上保守的結果捲動：按下之後草稿框如果整個看得見就完全不動，
  只有被推到視窗外（視窗特別矮、或草稿變長）才捲到剛好看得見，
  並遵守系統的「減少動態效果」設定。
  scripts/e2e-reveal.mjs 同時釘住「看不到時要帶過去」與
  「看得到時不可以捲」兩半。

  同時修正三處已過期的程式註解（lib/traffic.ts 兩處、tests/traffic.test.ts
  一處）：SheetJS 0.20.3 自 v2.1.45 起已改為包內 vendor/ 相依並以 SHA-256
  鎖定，註解卻仍寫「官方 CDN」。只是註解，不影響行為。

  **沒有變更任何交通量或 PCU 計算**，LAST_CALC_CHANGE_VERSION 維持 v2.1.30，
  固定計算黃金值完全相同。


部署完成後請確認：
  ・左側欄下方顯示「v2.1.83」
  ・https://baotoast.github.io/Turning-Traffic/?v=2.1.83 開得起來
  ・包內每一個網站資產與手冊的 SHA-256，列在這份說明上面的
    「本版網站資產」那一段（五個資產 ＋ 手冊，全部列出）。
    ⚠️ 另外交付的 `SHA256SUMS.txt` 是**與三個 zip 放在一起**的
    （在同一個交付資料夾裡，不在任何一個 zip 內），它算的是
    **三個 zip 檔本身**的雜湊，用來確認「你手上這三包就是我交出去的那三包」。
    兩者的用途不同，不要互相代替。
  ・路口轉向程式手冊_v2.1.83.pdf 可下載（不是 404）
  ・手冊封面戳記與頁尾都是 v2.1.83 ｜ 2026-09-24
  ・把視窗寬度拉到 1200～1400px（或 Windows 顯示縮放 125%／150%），
    點側欄的「路口幾何示意圖」，那張圖要**看得到**，不可以被上面那塊
    「用顏色檢視轉向」蓋住（那一項是前一版修的，本版一併回歸確認）
  ・把視窗寬度縮到 1400px 以下（或瀏覽器放大到 110%），到「歷季趨勢比較」，
    順序要是 圖 → 這張圖怎麼講 → 季度變化（本版修的那一項）；
    再拉回 1400px 以上，版面要回到兩欄、摘要在圖說上方
  ・結論草稿產生器的「時段」有**四顆**勾選框，而且勾「全調查時段尖峰」
    真的寫得出數字；勾「全調查時段」時單位是 PCU／調查時段，不是 PCU/hr
  ・在支線角度欄位按 Backspace 清空，欄位是**空的**（不會黏一個 0），
    接著打 45 得到 45（不是 045）
  ・每一頁最上方都有一列主工具列（季度起訖、路口、尖峰時段、尖峰時段判定方式、
    路口流量視角、資料別、車種、轉向別、顯示數值）
  ・在某一張圖上改條件，只有那一張改，而且那一塊出現
    「目前用本區塊自己的條件（主工具列：…）」與一顆「回到主工具列條件」；
    主工具列上出現「回歸全部（N 張圖）」
  ・主工具列改一個條件，沒有脫離的圖上那一顆**看得到跟著變**，數字也真的重算
  ・某個條件對某一張圖不適用時，那一張圖底下寫得出不適用的理由；
    條件回到預設之後那句話會收掉
  ・結論草稿與報表輸出**不會**自動跟著主工具列跑，但各有一顆
    「套用主工具列目前的條件」，按下去條件真的換成主工具列那一組
  ・開機時右下角的「資料已搬到 IndexedDB」訊息會**自己消失**（不必手動點掉）
  ・「轉向進階分析」的「這張圖怎麼講（簡報用）」**不在**左側小分頁清單裡，
    但區塊本身還在頁面上（說明文字不做分頁，不是把說明刪掉）
  ・左側任一個大分頁那一列最右邊有一顆箭頭鈕，點它只收合小分頁、不換頁；
    重新整理後收合狀態還在
  ・「清除原始資料」那顆鈕旁邊看得到「目前只佔 X MB，建議…1 GB 以上…」的備註；
    按下去會先跳確認視窗，而且視窗裡寫明「未來要重算必須重新匯入原始檔案」
  ・上一行那個確認視窗裡**看不到任何星號**（不可以印出 Markdown 記號）
  ・側欄沒有「跨計畫／多路口比較」，但有「計畫管理」與「各路口尖峰彙總」
  ・「各路口尖峰彙總」的路口下拉選單，選項數等於表格列數
  ・總覽儀表板沒有「路口尖峰小時排名」，也沒有版本更新歷程區塊
  ・「轉向進階分析」最下方有兩個收合的圖表區塊，展開後畫得出圖
    （或明確寫出為什麼不適用）
  ・「新手操作手冊」分頁只有一顆「下載完整 PDF 手冊」，沒有「Word 版」
  ・「各路口尖峰彙總」表頭第二欄是「調查日」，每一列寫得出日期
    （原始檔讀不到日期的那幾筆會寫「原始檔讀不到日期」，不是空白）
  ・「轉向進階分析」的 OD 矩陣最底下有一列「駛入合計」，
    右下角的總計同時等於「駛出合計」欄的和與「駛入合計」列的和
  ・「結論草稿產生器 → 四、要寫哪些支線」：把某一個路口的支線改名成
    「路口 A」（中間加一個空格），清單**不會**因此多出一項
  ・歷季趨勢圖：季數少時每個點旁邊直接標數值；季數多時靜止沒有標籤，
    滑鼠移到某一個點才顯示那一點的數值；下載的 PNG 一律沒有數值標籤
  ・連續做兩個會存檔的動作（例如改路口名稱後馬上改當量），
    立刻重新整理，兩個變更都還在（這一版修掉的存檔競態，見上方第六項）
  ・按下「選擇檔案」之後，還沒選完檔之前，卡片上就看得到
    「正在讀取您選擇的檔案，請稍候…」；按取消之後它會自己收掉
  ・匯入第一份檔案時就看得到進度列，不必等到全部讀完
  ・「季度批次匯入」把 Excel 拖到上傳卡片上，卡片會亮起來；
    放開之後按鈕顯示「正在解析… N／M」，卡片上另有一行寫出正在讀的檔名
  ・把檔案丟在卡片**外面**時，畫面不會跳走、不會變成直接開啟那個檔案
  ・「備份、還原與版本」的備份卡片也可以直接把備份檔拖進去
  ・「結論草稿」分頁只有一顆「產生草稿」，就在草稿框右上角；
    頁首已經沒有同名按鈕，也沒有叫「重新產生」的按鈕
  ・路口轉向圖的「時段」下拉有四個選項；只做幾小時的路口，
    後兩個會變灰並寫出原因
  ・「顯示」下拉有「車輛數」這一個選項
  ・頁首有「期別顯示」與「年份顯示」兩顆切換鈕；
    按「年份顯示」後全站季度改成 2026Q1 這種寫法，數字一個都不變
