路口轉向 — 完整專案 v2.1.23
=================================================

本版：結論草稿的「時段」可以兩個都不勾。
-----------------------------------------------------------------

■ 需求

  只想要某個路口「全調查時段」的車種組成那一行，但舊版強制
  「時段至少要留一個」——取消最後一個尖峰時會被自動加回去，
  草稿一定夾帶不要的尖峰段落，只能產生完再自己刪。

■ 本版

  兩個都不勾 ＝ 不敘述尖峰時段，只寫全調查時段的數值。
  這時記得在「要寫哪些數字」勾起「車種組成」。

  配套三件事：統計範圍那一行會寫明「不敘述尖峰時段」；
  沒有尖峰時不再印「PCU/hr 是該尖峰一小時的流率」那句說明；
  兩個都不勾卻只選了寫在尖峰底下的項目時，草稿會直接說明產生不出內容，
  而不是交出一份只有標題的空草稿。

  **沒有動任何計算**：有勾時段時的輸出與 v2.1.22 完全相同，已用測試釘住。

  另外修正一個相關的小陷阱：舊範本沒有 peaks 欄位時仍要補上預設的
  上午＋下午，但「有欄位、是空陣列」是使用者刻意的選擇，不可以被補回預設。
  這兩種情況以前被當成同一件事。

■ 新增測試

  單元測試 5 項（含「有勾時段時行為完全相同」的回歸測試），
  以及一項防止手冊漏寫更新說明的檢查。


如何更新
-----------------------------------------------------------------
把本壓縮檔內的所有檔案，覆蓋到 GitHub repository
（https://github.com/baotoast/Turning-Traffic）的根目錄，然後 commit + push。

**請先手動刪除舊版手冊**：根目錄的
Turning-Traffic-v2.1.22-新手操作手冊.pdf 與 .docx
（檔名帶版號，上傳不會自動刪掉舊的）。

部署完成後請確認：左側欄下方顯示「v2.1.23」，
結論草稿產生器把「上午尖峰」「下午尖峰」兩個都取消時，
取消得掉（不會自己跳回來），且草稿裡只剩車種組成那一行。
