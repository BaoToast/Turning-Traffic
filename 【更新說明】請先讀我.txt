路口轉向 — 完整專案 v2.1.43
=================================================

■ 本版已完成獨立複查、正式建置與 GitHub Pages 發布準備。
  複查結果與證據界線請見「VALIDATION_REPORT.md」。

■ 本版沒有變更任何計算口徑。既有數字一個都不會變。

■ 發布建置已完成

  這個 repository 發布的是**根目錄**的 index.html 與 assets/。
  本包已在能安裝官方 SheetJS 0.20.3 的環境重新建置，並把
  github-pages-dist/ 的 index.html 與 assets/ 同步到發布根目錄。
  本版主資產為 assets/index-CoBGwvYb.js，舊的 v2.1.40 雜湊資產已移除。

  日後由原始碼重新發布時，仍須在裝得到 cdn.sheetjs.com 的環境執行：

      npm ci
      npm test
      npm run build:github
      cp github-pages-dist/index.html .
      rm -rf assets && cp -r github-pages-dist/assets .

  然後把 repository 裡舊雜湊的 assets/index-*.js 刪掉。
  漏掉這一步的話，線上網站不會更新，而其他版號字串仍可能顯示新版。
  現在 tests/release-structure.test.mjs 會擋；在建不出可發布產物的環境
  它會印出「略過」與原因，**看到「略過」就代表 assets/ 還沒重建**。

■ v2.1.43：年度輸入補上範圍檢查

  上一版開放年度可以填民國或西元，但輸入框只做「去掉非數字、截到四碼」，
  沒有任何範圍檢查。而系統的換算只在民國 90～200（西元 2001～2111）之內，
  範圍外的四碼年份會**原樣存成季度鍵**——打成 2112 或 1990 就會這樣。

  它和對應的民國寫法（201Q3、79Q2）是同一季，排序位置也完全相同，
  會相鄰出現，畫面上只看得出「同一季出現了兩次」，很難想到是年份造成的。
  這正是上一版要消滅的那一類問題，只是發生在換算範圍之外。

  現在超出範圍會擋下，輸入框下方當場說明該填什麼，選檔按鈕也要檢查通過
  才會啟用；真的走到讀檔時再擋一次，避免按鈕被繞過。

  同時修掉另一個方向：民國兩碼年份（例如 99，即西元 2010）是合法寫法，
  排序與換算一直都認得它，不該被輸入檢查擋掉。

  ※ 檢查集中在共用期別模組的 checkSurveyPeriodInput()。選檔、確認寫入都會
    使用同一規則；即使預覽後再把年度改錯，也不會把錯誤季度寫進資料庫。
  ※ 本版未變更任何交通量或 PCU 計算。

■ v2.1.42：季度可以填民國年或西元年

  調查年度現在民國與西元都能填。舊版的年度欄位只收三碼，打「2026」會被
  靜靜截成「202」而且沒有任何提示；拿到以西元年標示的委託案時只能自己換算。

  但**資料一律以民國年記錄**。您填 2026Q2，系統會存成 115Q2，輸入框下方
  會即時顯示「將存成 115Q2」。這是必要的——如果照您打的字原樣存，同一季會
  因為寫法不同而變成兩個分開的季度，歷季比較被拆成兩段而且永遠不會合併；
  更麻煩的是它們的排序位置完全相同，畫面上看起來只像「同一季出現了兩次」，
  很難想到是寫法造成的。

■ v2.1.42（續）：畫面與匯出可以切換民國年／西元年

  頁首多了一顆「年份顯示」按鈕，按一下在民國年（115Q1）與西元年（2026Q1）
  之間切換。畫面上的表格、圖表、下拉選單、結論草稿，以及匯出的 Excel、
  轉向圖 SVG／PNG 與 PDF，全部跟著同一個設定走——不會出現畫面寫 2026Q1、
  交出去的報表卻寫 115Q1 的情形。

  【這顆按鈕只換看到的字，不動任何一個數字】

    資料一律以民國年儲存（見上一節）。分組、排序、識別鍵、季度篩選與所有
    計算，全部走儲存的那個值；切換年份寫法不會挑到不同的資料，也不會讓
    任何數字改變一位。下拉選單的「值」也仍然是民國年，只有顯示的文字換掉
    ——這一點很重要，否則切成西元年之後，結論草稿的單季條件會挑不到資料。

    端對端測試會在切換前後把整個畫面逐字比對一次：除了年份那幾個字，
    每一格數字都必須完全相同；並且掃過每一個分頁，確認切成西元年之後
    畫面上不會再殘留民國年寫法。

  【兩個顯示開關可以各自獨立使用】

    ・期別顯示：季別（115Q1）⇄ 實際調查月份（115年2、3月）
    ・年份顯示：民國年（115Q1）⇄ 西元年（2026Q1）
    兩個一起開就是「2026年2、3月」。

  【有三處刻意維持民國年，不跟著切換】

    ・匯入時的「將存成 115Q2」「本批次將寫入 115Q1」「確認寫入 115Q1」
      ——這幾句講的就是「會存成什麼」，那個值本來就是民國年。
    ・備份與專案包等資料檔（會被再匯入的），一律維持民國年儲存值。
    ・更新說明與操作手冊裡舉例用的季度字樣。

■ v2.1.41：三支系統跨系統徹查後的七項修正

  本版是「路口轉向、全日交通量、交通服務水準」三支一次跨系統徹查的結果。
  作法是拿 37 份真實調查表分別餵給不同系統、比對彼此的判讀與數字，
  凡是不一致的就往下追。

  【一】工作表名稱前面多一個空白時，平日與假日會被合併成一筆（最嚴重）

    判斷「平日」「假日」資料頁的規則以前只容許名稱後面有空白。
    您提供的 37 份真實檔裡有 11 份的分頁名稱帶空白，只要空白出現在前面，
    系統就數不到兩張日別分頁，於是退回「整份合併讀取」——
    不是少讀一天，而是平日的量被加進假日那一筆。

    實測 11535T1502 一份真實檔（只改分頁名稱、資料完全不動）：
      分頁「假日」「平日」        → 2 筆：假日 86,207 輛、平日 108,028 輛
      分頁「 假日」「 平日」      → 1 筆：假日 194,235 輛  ← 虛增 125%
      分頁「假日 」「平日 」      → 2 筆：假日 86,207 輛、平日 108,028 輛

    總量守恆（194,235 = 86,207 + 108,028），所以任何以總量為基礎的檢查
    都抓不到，畫面只顯示匯入成功。本版前後空白都容許，與全日交通量一致。

  【二】匯出的轉向圖檔名會帶資料別

    平日與假日是兩筆各自獨立的紀錄，但匯出 ZIP 的檔名以前只有站號加時段，
    同名檔會直接覆寫——同一站只會留下一張圖，而且從檔名看不出留下的是
    平日還是假日，過程沒有任何提示。單張 SVG／PNG 下載也一樣。

  【三】表頭寫「大貨車」「大客車」時會保留原名，不再依欄位位置歸類

    舊版看到「第 3、4 個車種欄位」就把它們當成大型車與特種車，
    於是大客車拿到特種車的當量 2.5——但大客車在工程上通常算大型車（1.5），
    系統沒有立場替您決定。本版比照全日交通量，把不認得的車種保留成
    自訂車種，由您在車種分類中自行歸類。

    ※ 總車輛數不變。若您照舊版的方式歸類（大貨車→大型車、大客車→特種車），
      PCU 也一位都不會變。

    ※ 表頭寫的都是內建四車種、只是順序亂掉或有殘留舊值時（例如 11017T1501
      七叉路口的合併儲存格裡藏著沒清乾淨的「大型車」），仍會用欄位位置救援。

  【四】全形數字會正確讀成數字

    儲存格若是全形的「１２３」，舊版計 0 輛，全日交通量卻讀成 123——
    同一格資料在兩支系統得到不同的數字。

  【五】「--」「－」「—」「–」不再被誤報成壞資料

    這是調查表裡「該轉向不存在」的標準記法，全日交通量早就視為合法。
    實測 11017T1502 一份真實檔因此跳出「有 192 個儲存格有內容但不是數字」——
    192 次誤報會讓人學會忽略這個警告，而它本來是要抓 N/A、休 這類真正的壞資料。

  【六】算不出來的統計範圍寫「－」，不再寫 0

    不足 24 小時的調查沒有「全日尖峰小時」，舊版的車種組成分析表照樣產生
    那幾列、數量寫 0、組成比例寫 0.0%，那些 0 會被 Excel 的加總與平均吃進去。

  【七】三支共用的非調查日期清單同步

    「彙整日期」「輸出日期」「建檔日期」「產製日期」現在三支都會排除。

=================================================
以下為 v2.1.30 當時的原始說明，保留供對照
=================================================

路口轉向 — 原始碼修正包 v2.1.30
=================================================

⚠️ 這一包不是可以直接整包上傳的網站包。請務必先看最後一節。

⚠️ 本版**有變更計算口徑**（兩項，見 v2.1.30-3 與 v2.1.30-4），
   而且 v2.1.30 以前鎖定的季度會亮出「鎖定衝突」。
   這是使用者確認過的決定，不是意外。
-----------------------------------------------------------------

■ v2.1.30-1：新增「全日時段」與「全日尖峰小時」兩大類統計

  使用者回報：系統只有上午尖峰與下午尖峰，所以路口轉向圖只有兩種圖；
  車種組成分析與各路口駛入／駛出流量也缺全日尖峰。

  現在四個統計範圍都有：
    上午尖峰、下午尖峰、全日尖峰小時、全日時段

  「全日尖峰小時」＝不分早晚，從一整天 24 小時裡找出交通量最大的那一小時。
  「全日時段」＝一整天的累計量，單位是 PCU/調查日、輛/調查日
  （和尖峰的 PCU/hr **不是同一種東西**，不可以相加或比較，
   所以畫面與 Excel 的單位會跟著時段自動改變）。

  ⚠️ 全日尖峰小時**需要完整 24 小時的調查資料**。
     只做了幾個小時的調查（例如常見的 07:00–09:00＋17:00–19:00，合計 4 小時）
     一律顯示「－」並寫明原因。不這樣做的話，那個數字必然等於上午與下午
     尖峰裡較大的那一個——看起來像新資訊，其實只是換個名字，
     還會被當成一整天的最大值寫進報告。

  ⚠️ 舊資料要重新匯入才會有全日尖峰小時。
     尖峰小時要從逐時間格的原始資料滾動搜尋，而系統為了節省儲存空間，
     匯入完成後只保留算好的結果，沒有留下逐格資料，所以無法回推。
     「全日時段」不受影響（它是從已存的調查總量現算的，舊資料直接就有）。
     已匯入的上午／下午尖峰數字**一個都不會變**。

■ v2.1.30-2：「全調查時段」不等於「全日」，現在會寫出實際時數

  車種組成分析裡的「全調查時段」，定義是「這份檔案匯入到多少就算多少」。
  24 小時的檔案它才等於全日；只做 4 小時的檔案以前也叫「全調查時段」，
  很容易被當成一整天。現在一律寫成「全調查時段（4 小時）」這種形式。

  這是舊版就存在的問題，不是本版新增的。

■ v2.1.30-3：⚠️ 計算口徑變更（一）——上午／下午尖峰的搜尋範圍

    舊：上午 [05:00, 12:00)   下午 [12:00, 23:00)
    新：上午 [00:00, 12:00)   下午 [12:00, 24:00)

  舊的兩段**合起來掃不到 23:00–24:00 與 00:00–05:00**。真正的尖峰若落在
  那六個小時，系統任何一個數字都抓不到。新的兩段剛好把一天鋪滿而且不重疊。
  「視窗頭尾都要落在範圍內」那一條沒有動（v2.1.22 的修正），
  所以上午與下午仍然不會重疊。

  實測使用者提供的 8 份實際調查檔，**新舊口徑算出來的上午／下午尖峰完全相同**。

■ v2.1.30-4：⚠️ 計算口徑變更（二）——每一格的車輛數先四捨五入成整數

  使用者的某些調查檔，儲存格裡存的是小數（0.36、5.5506…），Excel 的格式
  把它顯示成整數。於是報告上看到 0 與 6，程式拿去算的卻是 0.36 與 5.5506，
  全日車輛數會算出「27,988.79 輛」這種不存在的車，PCU 也跟著帶小數。

  依使用者決定，以畫面上看到的整數為準。四捨五入放在**全系統唯一讀取原始
  儲存格的那一行**，之後的加總、尖峰滾動、PCU 換算、全日累計全部自動吃到整數。

  實測影響（三份 24 小時檔）：
    06525T2503  24,463.3 PCU／27,988.8 輛 → 24,451.2 PCU／27,985 輛（差 −12.1／−3.8）
    06525T2502  無變化
    06525T2501  無變化

■ v2.1.30-5：⚠️ LAST_CALC_CHANGE_VERSION 推進到 v2.1.30

  因為上面兩項確實變更了計算口徑，**v2.1.30 以前鎖定的季度會亮出
  「鎖定衝突」紅字**。使用者已明確接受（可解除鎖定、確認後重新鎖定）。

■ v2.1.30-6：轉向圖新增「車輛數」顯示方式

  「顯示」原本只有交通量、百分比、PCU/hr＋百分比三種，車種選「全部車種」時
  只拿得到 PCU（各車種乘上當量後的總和）。新增的「車輛數」一律報實際調查到
  的輛數，搭配右邊的車種篩選，就能看單一車種或全部車種的輛數。

■ v2.1.30-7：與全日交通量系統交叉驗算，15 項全部一致

  兩套系統的轉向 PCU 當量係數完全相同，所以同一份 24 小時調查檔應該算出
  同一組數字。實測三份 24 小時檔的「全日時段 PCU／輛」與「全日尖峰
  時段／PCU／輛」共 15 項，兩邊完全一致。

  ⚠️ 但有一項兩套系統目前不一致：全日交通量**沒有**「每一格先四捨五入」
     這一條。同一份帶小數的檔案餵給兩套系統，全日交通量那邊仍會算出
     帶小數的那一組。要不要一併補上，請使用者決定。

■ v2.1.30-8：車種完全沒有動

  使用者特別交代「範例檔只有 4 種車，但系統要保留可以匯入多車種」。
  新增的計算一律走 recordVehicleIds()（＝資料裡實際出現過的所有車種，
  含自訂車種），沒有任何一處寫死那四種。守門測試釘住這一點。

■ v2.1.30-9：新增守門測試 17 項，其中 8 項行為檢查對未修正版實測 7 項紅字

  因為新的函式在 v2.1.29 根本不存在，直接拿新測試去跑只會「模組沒有這個
  匯出」——那不算證明。另外寫了一支只用兩版都讀得到的欄位的行為檢查，
  對 v2.1.29 與 v2.1.30 各跑一次：v2.1.29 是 1 通過／7 紅字，v2.1.30 是 8 通過。
  唯一「通過」的那一項是空的通過（舊版根本沒有全日尖峰這個概念），已標註。

-----------------------------------------------------------------
以下是前幾版的說明
-----------------------------------------------------------------

■ v2.1.29-1：修好 v2.1.28 新增的「被更新版本鎖定」警示（我自己複查抓到的）

  v2.1.28 新增了一條分支，用來提醒「這筆成果是被比目前更新的版本鎖定的」。
  它少了一件事：沒有先確認兩個版號都解析得出來。

  isVersionAtLeast() 認不得格式時一律回傳 false。用在「鎖定版本早不早於
  某個常數」是對的（不知道就當作不早於，不報警），但用在這條新分支會反過來
  ——只要 VERSION 帶了 -final、-rc1 這種尾巴，或前後多一個空白，
  就解析不出來，於是**每一筆 v2.1.21 以後鎖定的資料全部亮紅字**，
  正好是 v2.1.28 要修掉的那個症狀。實測 19×19 的版號矩陣會多出 344 筆紅字，
  而且訊息還說反了（「由較新的版本鎖定」）。

  改成兩個版號都解析得出來才判斷。版號認不得是「不知道」，不是「有問題」。

■ v2.1.29-2：四大車種的名稱，兩張表對齊

  app/traffic-app.tsx 的 VEHICLE_LABELS 寫「大型／大客車」「特種／聯結車」，
  lib/traffic.ts 的 CORE_VEHICLE_LABELS 寫「大型車」「特種車」，
  而匯入時寫進紀錄的是後者。vehicleLabel() 優先讀紀錄裡的名稱，
  所以 v2.1.19 以前沒有 vehicleLabels 欄位的舊備份會落到前者——
  跨季比較與結論草稿因此同一個車種前後兩種寫法。

  以匯入時實際寫入的那一組為準（也就是「大型車」「特種車」）。
  「車種轉向當量」那一頁用的是另一組較長的名稱，那是不同的畫面，刻意不動。

  新增第 12 項發布結構檢查釘住兩張表一致，已實測會在舊寫法下紅字。

■ v2.1.29-3：重新打包，中文檔名標記 UTF-8

  v2.1.28 交付的 ZIP 沒有標記 UTF-8，解壓後中文檔名全部變成 #Uxxxx 亂碼，
  包內因此找不到程式連得到的手冊檔名——發布結構檢查第 4 項紅字
  （44 項中 2 項失敗）。**那是打包的問題，不是程式的問題**：
  檔名還原後 44/44 全過。

  ⚠️ 線上網站不受影響（已由使用者實測手冊下載正常）。
     受影響的只有交付出去的備存 ZIP：拿它還原 repository 會少掉手冊檔。

■ v2.1.28-1：修正「鎖定衝突」誤報（使用者回報）

  畫面上出現紅字「偵測到鎖定衝突：鎖定版本與目前系統版本不同」。
  舊版是 record.resultLock.version !== VERSION——字串完全相等比較，
  所以**升過一次版，所有已鎖定的資料就永遠亮紅字**，下一版又全部重來。
  使用者有 20 幾季資料，那就是 20 幾筆永遠消不掉的警告。

  更糟的是它會淹掉真正該看的那一個：「鎖定後的資料內容已變更」。
  兩種訊息長得一模一樣紅，看久了連真的那一次也會被略過。

  新增 LAST_CALC_CHANGE_VERSION（目前 v2.1.21，最後一次變更尖峰小時口徑
  的版本）與 lockStatus()：只有鎖定當時的版本**早於**它才算衝突；
  升版但沒動計算改為一行灰字說明。真正的衝突（資料被動過）維持紅字。

  ⚠️ 使用者不需要、也不應該為了那行紅字去解除並重新鎖定——
     重新鎖定會蓋掉原本的鎖定時間與版本，那是稽核紀錄。

  ⚠️ 日後若真的改了計算口徑（PCU 當量、尖峰視窗、駛入推導、車種歸類…），
     LAST_CALC_CHANGE_VERSION 要跟著改成新版號，否則舊鎖定會被誤判為仍然有效。
     只改介面、文件、測試或發布流程則不要動它。常數的註解裡也寫了這一點。

  新增單元測試 6 項（含「版號要照數字比大小，不是照字串」——
  字串比較會說 v2.1.9 > v2.1.10）與端對端檢查 2 項。

■ v2.1.28-2：把測試示範資料裡的實際名稱換掉

  scripts/seed-state.json 與 seed-wide.json 是自動測試用的示範資料，
  裡面寫著實際的委託案名稱、計畫編號、站號與路口名稱，而這兩個檔案就跟
  程式一起放在公開的 repository 裡，也能從 GitHub Pages 直接下載。
  （裡面的流量數字本來就是 seed-state.mjs 用固定種子產生的假資料，
   不是實際調查值；但名稱是真的。）

  已全部換成「示範捷運延伸線示範標」「示範1－示範交流道路口」與
  A0000／S01-01 這類假識別碼，依名稱選取的 e2e 腳本也同步改掉。

  ⚠️ lib/ 與 app/ 裡的路口名稱**刻意沒有動**：referenceMovementForOd 是靠
     路口名稱同時含「中山北路」與「岡山路」來決定要不要套用七叉參考轉向表，
     那是產品行為，改掉會影響實際匯入。要不要改是產品決策，不是清理工作。

■ v2.1.28-3：新增 .gitattributes，換行字元不再被自動改寫

  上一版的備份 ZIP 裡每一個文字檔都被轉成 CRLF，連建置產物也一樣，
  結果 assets/index-<雜湊>.js 的內容不再雜湊成它自己的檔名，
  備份包與線上內容無法逐位元核對——而網站照樣跑得動，完全看不出來。
  成因是在 Windows 上取出（core.autocrlf）。
  本版新增 .gitattributes（`* -text`），包內所有文字檔也已正規化為 LF。

  ★ 打包時請用 git archive 從 commit 直接產生，不要用會改寫換行的工具。

■ v2.1.28-4：npm test 會先跑 lint；發布結構檢查 9 → 11 項

  程式碼檢查本來沒有被任何自動流程執行，壞了也不會有人知道
  （姊妹系統「全日交通量」就這樣紅了很久沒被發現）。
  現在 npm test 改成先跑 npm run lint，CI 從此會把關。

  第 10 項：測試種子與選取腳本不得出現實際的計畫、站號或路口名稱。
  第 11 項：必須有 .gitattributes 且關閉換行轉換。
  兩項都已實測會在對應的錯誤狀態下失敗，還原後全部通過。

■ 沒有變更任何計算規則

  交通量、PCU、駛入／駛出、尖峰搜尋、車種組成全部未動。
  鎖定衝突的判斷屬於「要不要顯示警告」，不影響任何數字。

■ v2.1.27：把上一版修好的發布結構釘進自動檢查

  v2.1.26 修好了四件發布層面的問題（單一部署路徑、補 .nojekyll、
  清掉殘留資產與舊手冊、統一套件管理），但**沒有任何東西擋著它們再壞一次**
  ——這四件事壞掉的時候畫面完全正常，版號、數字、按鈕全部看起來都對，
  只有實際打開網站的人會拿到舊版本。

  新增 tests/release-structure.test.mjs（8 項），釘住：
    1. 版號在每一個寫著它的檔案裡都一致（漏改就是線上手冊 404）
    2. CHANGELOG 最新一則等於程式版號
    3. 手冊封面戳記與每一頁頁尾的版號、日期一致
    4. 手冊有本版更新說明，且沒有留著舊版手冊檔
    5. .nojekyll／.gitignore／.github／.openai/hosting.json 都在
    6. workflow 不含任何發布步驟（只能有一條部署路徑）
    7. 套件管理只有 npm 一套
    8. 根目錄沒有殘留上一版的雜湊資產

  這 8 項已逐一實測會在對應的錯誤狀態下失敗，共 11 種情境，
  還原後全部通過。只加會通過的檢查等於沒加檢查。

  另外四件：
  ・重寫 DEPLOYMENT.md。舊版仍寫著「兩種相容部署路徑」、已退役的
    GPT Site 網址，以及一組 pnpm 指令——照著做會把剛移除的 pnpm 裝回來。
  ・README 與 DEPLOYMENT 補上：npm ci 會連 cdn.sheetjs.com，
    連不到時的 403 是網路問題不是程式問題。
  ・修正 scripts/stress-drag.mjs 寫死了某一台機器上的絕對路徑，
    在別台電腦上一執行就丟 ENOENT。
  ・CHANGELOG.md 改為指向 lib/traffic.ts 的 VERSION_HISTORY 這個唯一來源
    （本檔曾漏掉 v2.1.5～v2.1.25 共 21 個版本而沒有人發現）。

■ v2.1.26：發布、依賴安全與交付一致性

  1. GitHub Pages 固定由 main 分支根目錄發布；Actions 只負責建置與測試，
     不再由第二條 workflow 重複發布。
  2. 補入 .nojekyll，並清除舊雜湊資產與 v2.1.21～v2.1.25 舊手冊。
  3. 套件管理統一使用 npm；正式網站相依套件安全掃描為 0 項弱點。
  4. 程式檢查已修正為 0 錯誤、0 警告。
  5. 交通量計算、車種當量、駛入／駛出、尖峰搜尋及報表公式均未改動。
  6. 人工上傳 GitHub 時，務必確認 .github、.nojekyll、.gitignore 等
     點號開頭項目沒有被檔案選擇器略過。
  7. 驗證線上版本時，只信帶版號手冊、?v= 參數與新版專屬雜湊資產；
     不以固定檔名判斷，以免 CDN 快取造成誤判。

■ v2.1.25：佔比拆成兩個方向

  使用者問：「駛入 422.4／駛出 417.8／佔駛入 10.6% —— 這個 10.6%
  是方向無關的值，還是只是駛入的？」

  是駛入的，而且**舊版永遠只寫得出駛入那一組**。兩個百分比其實都算了，
  但輸出是「if (inShare) … else if (outShare) …」，只有整筆完全沒有駛入
  資料時才輪得到駛出；而 inflowPcu 由 destinationFlowTotal() 產生、
  那支函式永遠回傳數字，所以「佔駛出」是**執行不到的死碼**，
  一次都沒有被寫出來過。選項名稱又只寫「佔路口總量百分比」，
  沒說是哪個方向，標籤還會隨資料在兩者之間悄悄變動。

  兩個方向的分母相同（都是路口總量，因為駛入合計必然等於駛出合計），
  但分子不同，數字差很多——實測某一筆：
    路口B 駛入 10.4%／駛出 27.6%
    路口F 駛入 42.8%／駛出 14.7%

  拆成「各支線佔駛入路口總量百分比」與「各支線佔駛出路口總量百分比」，
  各勾各的；兩個都勾時駛入排在駛出前面。

  **沒有動任何計算**：舊的 share 鍵只換成 shareIn，**不是**展開成兩項
  ——展開會讓既有範本多出一段從來沒有過的「佔駛出」，那就不是輸出不變了。
  已用整份字串比對的迴歸測試釘住。

  新增單元測試 5 項（含「兩個方向用同一個分母、各自加總都是 100%」的
  防線——日後有人把駛出的分母換掉，每一格看起來仍是合理的百分比，
  畫面上看不出來）與端對端檢查 3 項。

■ v2.1.24-1：範本現在專屬於各自的計畫

  結論草稿的「條件範本」與報表的「匯出項目範本」，舊版都存成一份
  扁平清單、所有計畫共用——在甲計畫存的範本，切到乙計畫照樣列出來。

  這不只是看了礙眼：結論條件裡存著 intersectionKeys 與 branchNames，
  那是該計畫專屬的識別字，套到別的計畫會篩出 0 筆而找不出原因。
  這支程式其實已經知道這件事（換計畫時本來就會重設「正在編的條件」
  與草稿，理由就寫在那段註解裡），漏掉的只有「已存起來的範本清單」。

  改法與 pceByProject／catalogByProject／mappingsByProject 完全一致，
  連 setter 都沿用同一個 scopedSetter。

  **既有範本不會不見**：升版時舊的扁平清單會分給每一個計畫各一份，
  請使用者到各計畫把用不到的刪掉一次，之後新存的就只屬於當下那個計畫。
  （全部歸給某一個計畫的話，其他計畫就再也找不到過去存的範本。）
  備份與還原同樣依計畫分開帶，舊版備份的扁平清單也讀得懂。

■ v2.1.24-2：「各支線各車種駛入／駛出車輛數」拆成兩個勾選項

  舊版一勾就是兩個方向都寫，只需要其中一個方向時只能產生完再自己刪。
  拆成「各支線各車種駛入車輛數」與「各支線各車種駛出車輛數」兩項。

  只勾一個方向時「呈現方式」那一區會收起來——「雙向合計」是把兩個
  方向加起來，對單一方向來說那是**錯的數字**（會把另一個方向的車也
  算進去），而且它一樣會印出一行看起來合理的文字，畫面上看不出來。

  **沒有動任何計算**：舊的 branchComposition 鍵在 normalizeCondition
  裡原地展開成新的兩項，既有範本與備份產生的草稿與 v2.1.23 逐字相同，
  已用「整份字串比對」的迴歸測試釘住（只比幾個 match 驗不出多寫或
  少寫一段）。

■ 新增測試

  單元測試 6 項、備份完整性測試 6 項（新檔 tests/backup-completeness.test.mjs）、
  端對端檢查 6 項（切計畫看不到別的計畫的範本、單一方向的輸出）。
  備份完整性那一支已驗證在 v2.1.23 的程式上會失敗 5 項、在本版全數通過。

■ 上一版（v2.1.23）：結論草稿的「時段」可以兩個都不勾

  需求

  只想要某個路口「全調查時段」的車種組成那一行，但舊版強制
  「時段至少要留一個」——取消最後一個尖峰時會被自動加回去，
  草稿一定夾帶不要的尖峰段落，只能產生完再自己刪。

  兩個都不勾 ＝ 不敘述尖峰時段，只寫全調查時段的數值。
  這時記得在「要寫哪些數字」勾起「車種組成」。

  配套三件事：統計範圍那一行會寫明「不敘述尖峰時段」；
  沒有尖峰時不再印「PCU/hr 是該尖峰一小時的流率」那句說明；
  兩個都不勾卻只選了寫在尖峰底下的項目時，草稿會直接說明產生不出內容，
  而不是交出一份只有標題的空草稿。

  **沒有動任何計算**：有勾時段時的輸出與 v2.1.22 完全相同，已用測試釘住。

  另外修正一個相關的小陷阱：舊範本沒有 peaks 欄位時仍要補上預設的
  上午＋下午，但「有欄位、是空陣列」是使用者刻意的選擇，不可以被補回預設。
  這兩種情況以前被當成同一件事。


發布方式（重要，和以往不一樣）
-----------------------------------------------------------------

本包是原始碼修正包，不含建置後的網站。
根目錄的 index.html 與 assets/ 已刻意移除，必須由發布者自己重新建置。

原因：本版由 Claude 在一個連不到 cdn.sheetjs.com 的環境完成。
package.json 的 xlsx 指向 SheetJS 官方 0.20.3 發布包（安全警示的修正版），
該環境抓不到，所以在那裡產生的網站成品會把 SheetJS 悄悄降回舊版
（實測：該環境建置出來的 bundle 帶的是 0.18.5），而且從畫面上完全看不出來。
與其交一份可能有問題的成品，不如不交。

發布步驟：

  1. 把本包的檔案覆蓋到 repository（https://github.com/baotoast/Turning-Traffic）。
  2. npm ci          ← 需要連得到 cdn.sheetjs.com
  3. npm run lint    ← 應為 0 錯誤 0 警告
  4. npm test        ← 先跑 lint，再跑 189 項測試（模組 45 ＋ TypeScript 144）
  5. npm run e2e     ← 11 支瀏覽器腳本、308 項檢查全過
  6. npm run build:github，把產出的 index.html 與 assets/ 放回根目錄，
     手冊 PDF／DOCX 也會一併帶到根目錄
  7. 建置完成後請確認產出的 bundle 帶的是 SheetJS 0.20.3，不是 0.18.5
  8. commit + push

上傳只覆蓋同名檔案、不會刪除，所以一定要手動刪掉：
  ・所有不是 v2.1.30 的 Turning-Traffic-*-新手操作手冊.pdf 與 .docx
    （根目錄與 public/ 都要看）
  ・assets/ 底下舊版的 .js／.css：檔名帶內容雜湊，新版檔名不同，
    覆蓋不到舊檔。第 8 項檢查就是在擋這件事，
    沒刪乾淨的話 npm test 會直接紅字告訴你。
    （本包沒有 assets/，實際要刪哪幾個檔請在第 6 步建置完之後，
     拿產出的檔名跟 repository 現有的比對。）

若用 GitHub 網頁手動上傳，點號開頭的項目會被靜默濾掉；
請另外確認 .github/workflows/pages.yml、.nojekyll、.gitignore 與
.openai/hosting.json 都還在（第 5 項檢查也會擋）。
⚠️ .openai/hosting.json 不可刪除：vite.config.ts 會匯入它。

部署完成後請確認：
  ・左側欄下方顯示「v2.1.43」
  ・https://baotoast.github.io/Turning-Traffic/?v=2.1.43 開得起來
  ・Turning-Traffic-v2.1.43-新手操作手冊.pdf 下載得到（不是 404）
  ・手冊封面戳記與頁尾都是 v2.1.43 ｜ 2026-09-01
  ・路口轉向圖的「時段」下拉有四個選項；只做幾小時的路口，
    後兩個會變灰並寫出原因
  ・「顯示」下拉有「車輛數」這一個選項
  ・頁首有「期別顯示」與「年份顯示」兩顆切換鈕；
    按「年份顯示」後全站季度改成 2026Q1 這種寫法，數字一個都不變
