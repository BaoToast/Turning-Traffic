路口轉向 — 原始碼修正包 v2.1.29
=================================================

⚠️ 這一包不是可以直接整包上傳的網站包。請務必先看最後一節。

本版沒有變更任何計算規則，也沒有變更任何操作方式。
-----------------------------------------------------------------

■ v2.1.29-1：修好 v2.1.28 新增的「被更新版本鎖定」警示（我自己複查抓到的）

  v2.1.28 新增了一條分支，用來提醒「這筆成果是被比目前更新的版本鎖定的」。
  它少了一件事：沒有先確認兩個版號都解析得出來。

  isVersionAtLeast() 認不得格式時一律回傳 false。用在「鎖定版本早不早於
  某個常數」是對的（不知道就當作不早於，不報警），但用在這條新分支會反過來
  ——只要 VERSION 帶了 -final、-rc1 這種尾巴，或前後多一個空白，
  就解析不出來，於是**每一筆 v2.1.21 以後鎖定的資料全部亮紅字**，
  正好是 v2.1.28 要修掉的那個症狀。實測 19×19 的版號矩陣會多出 344 筆紅字，
  而且訊息還說反了（「由較新的版本鎖定」）。

  改成兩個版號都解析得出來才判斷。版號認不得是「不知道」，不是「有問題」。

■ v2.1.29-2：四大車種的名稱，兩張表對齊

  app/traffic-app.tsx 的 VEHICLE_LABELS 寫「大型／大客車」「特種／聯結車」，
  lib/traffic.ts 的 CORE_VEHICLE_LABELS 寫「大型車」「特種車」，
  而匯入時寫進紀錄的是後者。vehicleLabel() 優先讀紀錄裡的名稱，
  所以 v2.1.19 以前沒有 vehicleLabels 欄位的舊備份會落到前者——
  跨季比較與結論草稿因此同一個車種前後兩種寫法。

  以匯入時實際寫入的那一組為準（也就是「大型車」「特種車」）。
  「車種轉向當量」那一頁用的是另一組較長的名稱，那是不同的畫面，刻意不動。

  新增第 12 項發布結構檢查釘住兩張表一致，已實測會在舊寫法下紅字。

■ v2.1.29-3：重新打包，中文檔名標記 UTF-8

  v2.1.28 交付的 ZIP 沒有標記 UTF-8，解壓後中文檔名全部變成 #Uxxxx 亂碼，
  包內因此找不到程式連得到的手冊檔名——發布結構檢查第 4 項紅字
  （44 項中 2 項失敗）。**那是打包的問題，不是程式的問題**：
  檔名還原後 44/44 全過。

  ⚠️ 線上網站不受影響（已由使用者實測手冊下載正常）。
     受影響的只有交付出去的備存 ZIP：拿它還原 repository 會少掉手冊檔。

■ v2.1.28-1：修正「鎖定衝突」誤報（使用者回報）

  畫面上出現紅字「偵測到鎖定衝突：鎖定版本與目前系統版本不同」。
  舊版是 record.resultLock.version !== VERSION——字串完全相等比較，
  所以**升過一次版，所有已鎖定的資料就永遠亮紅字**，下一版又全部重來。
  使用者有 20 幾季資料，那就是 20 幾筆永遠消不掉的警告。

  更糟的是它會淹掉真正該看的那一個：「鎖定後的資料內容已變更」。
  兩種訊息長得一模一樣紅，看久了連真的那一次也會被略過。

  新增 LAST_CALC_CHANGE_VERSION（目前 v2.1.21，最後一次變更尖峰小時口徑
  的版本）與 lockStatus()：只有鎖定當時的版本**早於**它才算衝突；
  升版但沒動計算改為一行灰字說明。真正的衝突（資料被動過）維持紅字。

  ⚠️ 使用者不需要、也不應該為了那行紅字去解除並重新鎖定——
     重新鎖定會蓋掉原本的鎖定時間與版本，那是稽核紀錄。

  ⚠️ 日後若真的改了計算口徑（PCU 當量、尖峰視窗、駛入推導、車種歸類…），
     LAST_CALC_CHANGE_VERSION 要跟著改成新版號，否則舊鎖定會被誤判為仍然有效。
     只改介面、文件、測試或發布流程則不要動它。常數的註解裡也寫了這一點。

  新增單元測試 6 項（含「版號要照數字比大小，不是照字串」——
  字串比較會說 v2.1.9 > v2.1.10）與端對端檢查 2 項。

■ v2.1.28-2：把測試示範資料裡的實際名稱換掉

  scripts/seed-state.json 與 seed-wide.json 是自動測試用的示範資料，
  裡面寫著實際的委託案名稱、計畫編號、站號與路口名稱，而這兩個檔案就跟
  程式一起放在公開的 repository 裡，也能從 GitHub Pages 直接下載。
  （裡面的流量數字本來就是 seed-state.mjs 用固定種子產生的假資料，
   不是實際調查值；但名稱是真的。）

  已全部換成「示範捷運延伸線示範標」「示範1－示範交流道路口」與
  A0000／S01-01 這類假識別碼，依名稱選取的 e2e 腳本也同步改掉。

  ⚠️ lib/ 與 app/ 裡的路口名稱**刻意沒有動**：referenceMovementForOd 是靠
     路口名稱同時含「中山北路」與「岡山路」來決定要不要套用七叉參考轉向表，
     那是產品行為，改掉會影響實際匯入。要不要改是產品決策，不是清理工作。

■ v2.1.28-3：新增 .gitattributes，換行字元不再被自動改寫

  上一版的備份 ZIP 裡每一個文字檔都被轉成 CRLF，連建置產物也一樣，
  結果 assets/index-<雜湊>.js 的內容不再雜湊成它自己的檔名，
  備份包與線上內容無法逐位元核對——而網站照樣跑得動，完全看不出來。
  成因是在 Windows 上取出（core.autocrlf）。
  本版新增 .gitattributes（`* -text`），包內所有文字檔也已正規化為 LF。

  ★ 打包時請用 git archive 從 commit 直接產生，不要用會改寫換行的工具。

■ v2.1.28-4：npm test 會先跑 lint；發布結構檢查 9 → 11 項

  程式碼檢查本來沒有被任何自動流程執行，壞了也不會有人知道
  （姊妹系統「全日交通量」就這樣紅了很久沒被發現）。
  現在 npm test 改成先跑 npm run lint，CI 從此會把關。

  第 10 項：測試種子與選取腳本不得出現實際的計畫、站號或路口名稱。
  第 11 項：必須有 .gitattributes 且關閉換行轉換。
  兩項都已實測會在對應的錯誤狀態下失敗，還原後全部通過。

■ 沒有變更任何計算規則

  交通量、PCU、駛入／駛出、尖峰搜尋、車種組成全部未動。
  鎖定衝突的判斷屬於「要不要顯示警告」，不影響任何數字。

■ v2.1.27：把上一版修好的發布結構釘進自動檢查

  v2.1.26 修好了四件發布層面的問題（單一部署路徑、補 .nojekyll、
  清掉殘留資產與舊手冊、統一套件管理），但**沒有任何東西擋著它們再壞一次**
  ——這四件事壞掉的時候畫面完全正常，版號、數字、按鈕全部看起來都對，
  只有實際打開網站的人會拿到舊版本。

  新增 tests/release-structure.test.mjs（8 項），釘住：
    1. 版號在每一個寫著它的檔案裡都一致（漏改就是線上手冊 404）
    2. CHANGELOG 最新一則等於程式版號
    3. 手冊封面戳記與每一頁頁尾的版號、日期一致
    4. 手冊有本版更新說明，且沒有留著舊版手冊檔
    5. .nojekyll／.gitignore／.github／.openai/hosting.json 都在
    6. workflow 不含任何發布步驟（只能有一條部署路徑）
    7. 套件管理只有 npm 一套
    8. 根目錄沒有殘留上一版的雜湊資產

  這 8 項已逐一實測會在對應的錯誤狀態下失敗，共 11 種情境，
  還原後全部通過。只加會通過的檢查等於沒加檢查。

  另外四件：
  ・重寫 DEPLOYMENT.md。舊版仍寫著「兩種相容部署路徑」、已退役的
    GPT Site 網址，以及一組 pnpm 指令——照著做會把剛移除的 pnpm 裝回來。
  ・README 與 DEPLOYMENT 補上：npm ci 會連 cdn.sheetjs.com，
    連不到時的 403 是網路問題不是程式問題。
  ・修正 scripts/stress-drag.mjs 寫死了某一台機器上的絕對路徑，
    在別台電腦上一執行就丟 ENOENT。
  ・CHANGELOG.md 改為指向 lib/traffic.ts 的 VERSION_HISTORY 這個唯一來源
    （本檔曾漏掉 v2.1.5～v2.1.25 共 21 個版本而沒有人發現）。

■ v2.1.26：發布、依賴安全與交付一致性

  1. GitHub Pages 固定由 main 分支根目錄發布；Actions 只負責建置與測試，
     不再由第二條 workflow 重複發布。
  2. 補入 .nojekyll，並清除舊雜湊資產與 v2.1.21～v2.1.25 舊手冊。
  3. 套件管理統一使用 npm；正式網站相依套件安全掃描為 0 項弱點。
  4. 程式檢查已修正為 0 錯誤、0 警告。
  5. 交通量計算、車種當量、駛入／駛出、尖峰搜尋及報表公式均未改動。
  6. 人工上傳 GitHub 時，務必確認 .github、.nojekyll、.gitignore 等
     點號開頭項目沒有被檔案選擇器略過。
  7. 驗證線上版本時，只信帶版號手冊、?v= 參數與新版專屬雜湊資產；
     不以固定檔名判斷，以免 CDN 快取造成誤判。

■ v2.1.25：佔比拆成兩個方向

  使用者問：「駛入 422.4／駛出 417.8／佔駛入 10.6% —— 這個 10.6%
  是方向無關的值，還是只是駛入的？」

  是駛入的，而且**舊版永遠只寫得出駛入那一組**。兩個百分比其實都算了，
  但輸出是「if (inShare) … else if (outShare) …」，只有整筆完全沒有駛入
  資料時才輪得到駛出；而 inflowPcu 由 destinationFlowTotal() 產生、
  那支函式永遠回傳數字，所以「佔駛出」是**執行不到的死碼**，
  一次都沒有被寫出來過。選項名稱又只寫「佔路口總量百分比」，
  沒說是哪個方向，標籤還會隨資料在兩者之間悄悄變動。

  兩個方向的分母相同（都是路口總量，因為駛入合計必然等於駛出合計），
  但分子不同，數字差很多——實測某一筆：
    路口B 駛入 10.4%／駛出 27.6%
    路口F 駛入 42.8%／駛出 14.7%

  拆成「各支線佔駛入路口總量百分比」與「各支線佔駛出路口總量百分比」，
  各勾各的；兩個都勾時駛入排在駛出前面。

  **沒有動任何計算**：舊的 share 鍵只換成 shareIn，**不是**展開成兩項
  ——展開會讓既有範本多出一段從來沒有過的「佔駛出」，那就不是輸出不變了。
  已用整份字串比對的迴歸測試釘住。

  新增單元測試 5 項（含「兩個方向用同一個分母、各自加總都是 100%」的
  防線——日後有人把駛出的分母換掉，每一格看起來仍是合理的百分比，
  畫面上看不出來）與端對端檢查 3 項。

■ v2.1.24-1：範本現在專屬於各自的計畫

  結論草稿的「條件範本」與報表的「匯出項目範本」，舊版都存成一份
  扁平清單、所有計畫共用——在甲計畫存的範本，切到乙計畫照樣列出來。

  這不只是看了礙眼：結論條件裡存著 intersectionKeys 與 branchNames，
  那是該計畫專屬的識別字，套到別的計畫會篩出 0 筆而找不出原因。
  這支程式其實已經知道這件事（換計畫時本來就會重設「正在編的條件」
  與草稿，理由就寫在那段註解裡），漏掉的只有「已存起來的範本清單」。

  改法與 pceByProject／catalogByProject／mappingsByProject 完全一致，
  連 setter 都沿用同一個 scopedSetter。

  **既有範本不會不見**：升版時舊的扁平清單會分給每一個計畫各一份，
  請使用者到各計畫把用不到的刪掉一次，之後新存的就只屬於當下那個計畫。
  （全部歸給某一個計畫的話，其他計畫就再也找不到過去存的範本。）
  備份與還原同樣依計畫分開帶，舊版備份的扁平清單也讀得懂。

■ v2.1.24-2：「各支線各車種駛入／駛出車輛數」拆成兩個勾選項

  舊版一勾就是兩個方向都寫，只需要其中一個方向時只能產生完再自己刪。
  拆成「各支線各車種駛入車輛數」與「各支線各車種駛出車輛數」兩項。

  只勾一個方向時「呈現方式」那一區會收起來——「雙向合計」是把兩個
  方向加起來，對單一方向來說那是**錯的數字**（會把另一個方向的車也
  算進去），而且它一樣會印出一行看起來合理的文字，畫面上看不出來。

  **沒有動任何計算**：舊的 branchComposition 鍵在 normalizeCondition
  裡原地展開成新的兩項，既有範本與備份產生的草稿與 v2.1.23 逐字相同，
  已用「整份字串比對」的迴歸測試釘住（只比幾個 match 驗不出多寫或
  少寫一段）。

■ 新增測試

  單元測試 6 項、備份完整性測試 6 項（新檔 tests/backup-completeness.test.mjs）、
  端對端檢查 6 項（切計畫看不到別的計畫的範本、單一方向的輸出）。
  備份完整性那一支已驗證在 v2.1.23 的程式上會失敗 5 項、在本版全數通過。

■ 上一版（v2.1.23）：結論草稿的「時段」可以兩個都不勾

  需求

  只想要某個路口「全調查時段」的車種組成那一行，但舊版強制
  「時段至少要留一個」——取消最後一個尖峰時會被自動加回去，
  草稿一定夾帶不要的尖峰段落，只能產生完再自己刪。

  兩個都不勾 ＝ 不敘述尖峰時段，只寫全調查時段的數值。
  這時記得在「要寫哪些數字」勾起「車種組成」。

  配套三件事：統計範圍那一行會寫明「不敘述尖峰時段」；
  沒有尖峰時不再印「PCU/hr 是該尖峰一小時的流率」那句說明；
  兩個都不勾卻只選了寫在尖峰底下的項目時，草稿會直接說明產生不出內容，
  而不是交出一份只有標題的空草稿。

  **沒有動任何計算**：有勾時段時的輸出與 v2.1.22 完全相同，已用測試釘住。

  另外修正一個相關的小陷阱：舊範本沒有 peaks 欄位時仍要補上預設的
  上午＋下午，但「有欄位、是空陣列」是使用者刻意的選擇，不可以被補回預設。
  這兩種情況以前被當成同一件事。


發布方式（重要，和以往不一樣）
-----------------------------------------------------------------

本包是原始碼修正包，不含建置後的網站。
根目錄的 index.html 與 assets/ 已刻意移除，必須由發布者自己重新建置。

原因：本版由 Claude 在一個連不到 cdn.sheetjs.com 的環境完成。
package.json 的 xlsx 指向 SheetJS 官方 0.20.3 發布包（安全警示的修正版），
該環境抓不到，所以在那裡產生的網站成品會把 SheetJS 悄悄降回舊版——
而且從畫面上完全看不出來。與其交一份可能有問題的成品，不如不交。

發布步驟：

  1. 把本包的檔案覆蓋到 repository（https://github.com/baotoast/Turning-Traffic）。
  2. npm ci          ← 需要連得到 cdn.sheetjs.com
  3. npm run lint    ← 應為 0 錯誤 0 警告
  4. npm test        ← 先跑 lint，再跑 172 項測試（模組 45 ＋ TypeScript 127）
  5. npm run e2e     ← 11 支瀏覽器腳本、308 項檢查全過
  6. npm run build:github，把產出的 index.html 與 assets/ 放回根目錄，
     手冊 PDF／DOCX 也會一併帶到根目錄
  7. commit + push

上傳只覆蓋同名檔案、不會刪除，所以一定要手動刪掉：
  ・所有不是 v2.1.29 的 Turning-Traffic-*-新手操作手冊.pdf 與 .docx
    （根目錄與 public/ 都要看）
  ・assets/ 底下舊版的 .js／.css：檔名帶內容雜湊，新版檔名不同，
    覆蓋不到舊檔。第 8 項檢查就是在擋這件事，
    沒刪乾淨的話 npm test 會直接紅字告訴你。
    （本包沒有 assets/，實際要刪哪幾個檔請在第 6 步建置完之後，
     拿產出的檔名跟 repository 現有的比對。）

若用 GitHub 網頁手動上傳，點號開頭的項目會被靜默濾掉；
請另外確認 .github/workflows/pages.yml、.nojekyll、.gitignore 與
.openai/hosting.json 都還在（第 5 項檢查也會擋）。

部署完成後請確認：
  ・左側欄下方顯示「v2.1.29」
  ・https://baotoast.github.io/Turning-Traffic/?v=2.1.29 開得起來
  ・Turning-Traffic-v2.1.29-新手操作手冊.pdf 下載得到（不是 404）
  ・手冊封面戳記與頁尾都是 v2.1.29 ｜ 2026-08-27
  ・隨便開一筆已鎖定的成果，不應該出現「由較新的系統版本鎖定」紅字
